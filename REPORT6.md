# REPORT6 — Ref2VA「参考视频」能力实测（含真人动作参考）

> 环境：`connect.westc.seetacloud.com:39494`（RTX 4090 24G，ComfyUI 0.34 + MiniMax H3 Optimizations）
> 权重：`minimax_h3_ref2va_pruned_int8_convrot.safetensors` + `minimax_h3_ref2v_turbo_8step_v1.0_768p` LoRA
> 时间：2026-09-11 16:00–17:20　脚本：`test_r2v.py`（新增 `--ref-video` / `--ref-video-audio` / `--sparse-budget` / `--sparse-off`）
> ⚠️ 该实例测试结束后已释放，`/tmp/board_*.png` 看板未回传；下列矩阵均为当时终端原始输出。

---

## 1. 一句话结论

**Ref2VA 的「参考视频」输入在本机实现下对生成结果没有任何可测影响。**

换参考视频的人、场景、时间方向、甚至换成一张静止帧，输出与「完全不接视频」的差异 **全部落在"同一素材改名重跑"的复现噪声之内**（音轨 PCM 逐位相同）。而同一批实验里，改动**稀疏注意力档位**这一真正的模型级参数，会让输出变化 10–23/255 并改变音轨 —— 说明实验装置本身是灵敏的。

→ **生产结论：不要依赖 Ref2VA 的参考视频做动作迁移。** 动作迁移请走已实测有效的路径（FL2VA 首尾帧 + 中间锚点，见 REPORT2/3）。

---

## 2. 节点机制（先读源码再动手）

`MiniMaxH3ReferenceToVideo` 的 `ref_videos` 是 **AUTOGROW** 输入，槽位名 `ref_video_0..N`（0 基）。
它吃的是 **IMAGE 帧序列**，不是 VIDEO 对象，所以接线必须是：

```
LoadVideo(file=rv_a.mp4) → GetVideoComponents.images → MiniMaxH3ReferenceToVideo.ref_videos["ref_video_0"]
                         ↘ .audio → （可选）ref_video_audios
```

节点内部按 **24fps 取帧、截断到生成帧数**（不足 5 帧报错），VAE 编码成 `kind:"video"` 的 ref block；
Qwen 文本编码器侧则以 **2fps + 时间戳** 看这段视频。提示词里用 `<Video k>`（k 从 1 起）引用。

接线已用 `/history/{prompt_id}` 反查提交图核验通过（LoadVideo(400) → GetVideoComponents(401) → `ref_videos`），**不是没接上**。

---

## 3. 素材

| 代号 | 来源 | 内容 | 规格 |
|---|---|---|---|
| RV-A | `fylina_v2_natural_4seg.mp4` 0–5.2s | 真人女性用精华滴管上脸（真人实拍 UGC） | 768×1344 原生 9:16 / 24fps / 124 帧 |
| RV-B | `ditto_真实素材_15秒测试.mp4` 0–5.2s | 真人女性办公室口播 | 裁 9:16 / 24fps / 124 帧 |
| RV-A′ | `rv_a_rev.mp4` | RV-A **时间反转**（外观相同、动作相反） | 124 帧 |
| RV-A″ | `rv_a_still.mp4` | RV-A **首帧重复 124 次**（外观相同、零动作） | 124 帧 |
| RV-A‴ / RV-B′ | `rva2.mp4` / `rvb2.mp4` | RV-A / RV-B 的**字节副本改名**（强制绕开缓存） | 同上 |

参考图沿用 REPORT5：`ref_model.png`（粉底气垫 × 灰西装模特）+ `ref_product.png`（产品特写）。

---

## 4. 实验矩阵

⚠️ ComfyUI 对 `LoadVideo` 的缓存指纹**只取文件 mtime**，而 rv_a/rv_b 是同秒上传（mtime 相同）→ 曾整批命中缓存。
**修法：换文件名强制绕开缓存**，并以「同素材改名重跑」作为噪声基线。

| 批次 | 用例 | 变量 | 帧数 |
|---|---|---|---|
| 1 | V0 | 无视频 + 提示词不提视频 | 124 |
| 1 | V1 | **+RV-A** | 124 |
| 1 | V2 | 同 V1，只把参考视频换成 **RV-B** | 124 |
| 1 | V3 | 同 V1，**去掉模特参考图** | 124 |
| 1 | V5 | +RV-B 且接其原声 | 124 |
| 2 | W1b | **RV-A 字节副本改名**（噪声基线） | 124 |
| 2 | W2 | RV-B 副本改名 | 124 |
| 2 | W4 | **RV-A′ 时间反转** | 124 |
| 2 | W5 | **RV-A″ 静态帧** | 124 |
| 2 | W3 | 换 seed（88888） | 124 |
| 3 | X1 | 提示词有 `<Video 1>` 但**完全不接视频** | 124 |
| 3 | X2 | **零参考图** + RV-A | 124 |
| 3 | X3 | 无视频提示词 + 接 RV-B | 124 |
| 4 | Y1/Y2 | 稀疏预算 0.3：无视频 / +RV-A | 56 |
| 4 | Y3/Y4 | 稀疏预算 1.0（≈全注意力）：无视频 / +RV-A | 56 |
| 4 | Y5/Y6 | **稀疏注意力整体旁路**：无视频 / +RV-A | 56 |

同一批次内 **seed 固定 20260911、提示词固定**，只动一个变量。

---

## 5. 结果

### 5.1 音轨（PCM 逐位比较）

| 对比对 | 相关系数 r | 最大差 | 判定 |
|---|---|---|---|
| V1 ↔ V2（换参考视频的人/场景） | +1.0000 | 0.00000 | **逐位相同** |
| V1 ↔ X2（去掉全部参考图） | +1.0000 | 0.00000 | **逐位相同** |
| W1b ↔ W2 ↔ W4 ↔ W5（含时间反转、静态帧） | +1.0000 | 0.00000 | **逐位相同** |
| Y1 ↔ Y2（稀疏 0.3，有无视频） | +1.0000 | 0.00000 | **逐位相同** |
| Y3 ↔ Y4（稀疏 1.0，有无视频） | +1.0000 | 0.00000 | **逐位相同** |
| Y5 ↔ Y6（**稀疏关闭**，有无视频） | +1.0000 | 0.00000 | **逐位相同** |
| Y1 ↔ Y3（**只改稀疏档位** 0.3→1.0） | +0.5771 | 2.26458 | 明显不同 |
| Y1 ↔ Y5（0.3→off） | +0.5657 | 2.22724 | 明显不同 |

采样在「同提示词 + 同 seed」下是**完全确定**的（音轨可逐位复现）→ 只要音轨变了，就一定是条件真的进了模型。

### 5.2 视频帧（解码后逐帧像素差，0–255）

| 对比对 | 帧差 | 性质 |
|---|---|---|
| Y1 ↔ Y2（有无参考视频） | **0.96** | ≈ 噪声 |
| Y3 ↔ Y4（同上，全注意力） | **1.02** | ≈ 噪声 |
| Y5 ↔ Y6（同上，稀疏关闭） | **0.94** | ≈ 噪声 |
| **W1b ↔ W2**（同素材改名重跑，噪声基线） | **1.08** | ← 基线 |
| W1b ↔ W4（时间反转） / W1b ↔ W5（静态帧） | 1.09 / 1.08 | ≈ 噪声 |
| Y1 ↔ Y3（**只改稀疏档位**） | **22.91** | 真实差异 |
| Y1 ↔ Y5 / Y3 ↔ Y5 | 21.50 / 10.32 | 真实差异 |

**换整条参考视频造成的差异（0.94–1.09）比"同一素材改名重跑"（1.08）还小。**

### 5.3 配色相似度矩阵（双向最近邻加权，越小越像）

56 帧批（Y）：

|  | Y1 | Y2 | Y3 | Y4 | Y5 | Y6 |
|---|---|---|---|---|---|---|
| Y1 | 0.0 | **0.7** | 30.7 | 34.6 | 20.2 | 22.3 |
| Y3 | 30.7 | 30.6 | 0.0 | **4.4** | 29.7 | 31.0 |
| Y5 | 20.2 | 20.0 | 29.7 | 30.0 | 0.0 | **2.3** |

124 帧批（V/W/X）：

|  | V1 | V2 | W4 | W5 | X2 | W3 |
|---|---|---|---|---|---|---|
| V1 | 0.0 | **0.9** | **0.1** | **1.5** | **1.4** | 14.0 |
| （参考视频 RV-A / RV-B 与所有输出） | 51.5 | 51.1 | 51.5 | 51.5 | 51.5 | 58.6 |

组内（有无视频 / 换视频 / 时间反转 / 静态帧 / 零参考图）全部 ≤2.3；组间（换稀疏档位 20–35、换 seed 14）差一个数量级。**输出与参考视频本身的相似度（51.5）远大于任何组内变化。**

### 5.4 耗时

| 用例 | 耗时 |
|---|---|
| Y1（无视频，稀疏 0.3） | 65.1s |
| Y2（**+参考视频**，稀疏 0.3） | **60.0s** |
| Y3 / Y4 | 65.1s / 60.0s |
| Y5 / Y6 | 60.1s / 60.1s |
| （对照）REPORT5：参考**音频**让耗时 135→265s | **+96%** |

加参考视频的边际耗时 ≈ **0**（首跑编译造成的 5s 差异），与参考音频「翻倍算力但同样零效果」形成鲜明对照 —— 视频帧根本没有进入任何重计算。

---

## 6. 结论

1. **参考视频无效（已证）**：4 类扰动（换人/换场景、时间反转、静态帧、完全不给）全部无差别。
2. **不是稀疏注意力丢段**：关掉 `H3SparseAttentionAdvanced`（Y5/Y6）结论不变。同时稀疏档位本身能造成 10–23/255 的差异 → 装置灵敏。
3. **参考图影响同样微弱**：去掉模特参考图（V1↔V3）1.05、零参考图（V1↔X2）1.4 → 本实验条件下输出由 **提示词 + seed** 主导。方向与 REPORT5「Ref2VA 锁不住脸」一致，但需专门实验才能对参考图下最终结论。
4. **成本悖论**：参考音频零效果却让耗时翻倍；参考视频零效果且耗时不变 —— 二者都指向「ref 条件未进入 DiT 重计算」。
5. **推断根因**（待验）：`MiniMaxH3ReferenceToVideo` 很可能没有把 ref block 真正接进 DiT 的 `cond_video_latents`，或在 2fps/时间戳预处理阶段被丢弃。
   下一步可用**低秩探针**确认：打印 `PackedLayout` 中 `ref_img`/`ref_video` 段的实际 token 数，或把参考视频换成极端内容（纯色/噪点）看 cond 张量是否变化。

---

## 7. 可复用的实验纪律（本次踩到的坑）

1. **`LoadVideo` 的缓存指纹只取 mtime** → 同秒上传的不同文件会互相命中缓存。测视频条件必须**换文件名**，并用「同素材改名重跑」当噪声基线。
2. **mp4 的 md5 不可作内容判据**（内嵌文件名/参数元数据）→ 必须比较**解码后的帧**与**解码后的 PCM**。
3. **音轨 PCM 是比帧更灵敏的判据**：同提示词同 seed 下采样完全确定，PCM 逐位相同即「条件没进模型」的铁证（fp16 视频 VAE 解码的非确定性只污染帧、不污染音频）。
4. 换参考音频/视频 A/B 时，seed 与文件名必须同时控制。
5. AutoDL 实例不持久 —— **实验结果（数值矩阵、看板图）要在会话内立即回传本地**，否则实例释放后只剩终端日志。

---

## 8. 与 REPORT5 的衔接（修正后的 Ref2VA 能力表）

| 能力 | 实测结论 |
|---|---|
| 画质 | ✅ 明显优于 FL2VA（肤质/妆效/锐度） |
| 单段长镜头 | ✅ 单段 5.9s 跑完整个叙事，无拼接漂移 |
| 提示词级运镜 | ✅ `camera pushes in slowly` 生效 |
| 台词（`<d>[English]…</d>`） | ✅ 逐字准确，走 T2VA 路径 |
| 锁脸（参考图） | ❌ 属性级，非身份级；`ref_image_size=max` 无效 |
| 音色参考（`<Audio j>`） | ❌ 零效果，且耗时 +96% |
| **动作迁移（`<Video k>`）** | ❌ **零效果（本报告）** |

→ **Ref2VA 在本机实现的实用价值 = 「高画质单段长镜头生成器」，不是「参考驱动的可控生成器」。**
