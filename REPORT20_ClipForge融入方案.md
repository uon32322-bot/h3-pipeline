# REPORT20 · ClipForge 融入方案（文案脚本层）

> 日期：2026-09-17
> 起因：辉哥要求「把 ClipForge 融入项目」，「**注意融入项目不要影响视频的质量和生成的速度**」
> 性质：**融入实施文档**——含形态选型、隔离红线、接口契约、部署清单、风险边界
> 配套：`REPORT19`（顺序审计 + 选型依据）、`shotlist_schema.json`（接口契约样例）

---

## 0. 一句话方案

> **ClipForge 只做「文字层」，跑在本地 Mac，接口是「分镜表 JSON」。**
> 它不装 3090、不碰 GPU、不参与画面生成 —— 所以**对视频质量与生成速度的影响，设计上为零**。

```
     文字层（CPU · 本地 Mac）                    画面层（GPU · 3090）
┌──────────────────────────────┐          ┌──────────────────────────────┐
│  ClipForge（Docker，:3000）   │          │  H3 流水线                    │
│  ① 商品链接入库               │  分镜表   │  ⑤ 闸门② 动作审查             │
│  ② 生成口播脚本 + 分镜节拍     │ ──JSON──→│  ⑥ 三张独立锚定照             │
│  ③ script-judges 四判官审稿   │          │  ⑦ H3 生成（含抽卡）          │
│  ④ 输出分镜表 JSON            │          │  ⑧ 判官团 L0/L1/L2（出 GPU）  │
└──────────────────────────────┘          └──────────────────────────────┘
        ▲ 纯 CPU / SQLite                          ▲ 唯一吃 GPU 的地方
        ✗ 零 GPU 依赖                              ✗ 不跑任何判官模型
```

---

## 1. 融入形态：三选一（推荐 A + B，明确不做 C）

| 形态 | 内容 | 资源占用 | 对生成的影响 | 建议 |
|---|---|---|---|---|
| **A · 文案 Skill** | 装 `script-judges`（四位判官：节奏/口语/创意/结构）到 WorkBuddy | **零**（纯文本 prompt，无二进制） | **无** | ✅ **先上** |
| **B · ClipForge 服务** | Docker 起服务，只调「脚本生成 / 商品入库 / 热点」 | Node + SQLite，CPU ≈ 300–500 MB 内存 | **无**（前提：跑在 Mac，见 §2） | ✅ **主用** |
| **C · 它的出片链路** | 免费快剪 / AI 生成成片 | 会调远端视频模型（花钱） | 与 H3 重复 | ❌ **不启用** |

**为什么不做 C**：它自己也能出片（免费素材 + Edge TTS + FFmpeg 合成），但那是**素材拼接/图文混剪**路线，**画质上限低于 H3 真实生成**。让它出片等于放弃了 H3 的核心价值，还多一条要维护的链路。

**为什么 A 先上**：`script-judges` 是**纯文本 skill、无二进制依赖、任何 agent 宿主都能跑**，装进来零成本、零风险，立刻能给文案把关。

---

## 2. 🚨 三条隔离红线（"不影响质量和速度"的全部依据）

> 这三条是硬约束，**任何一条破了，"不影响"就不再成立**。

### 红线 1 · ClipForge 不装 3090 服务器

**理由**：它虽然不占 GPU，但 **FFmpeg 合成与 Node 进程会吃满 CPU**，而 ComfyUI 的**数据加载/VAE 解码/图像预处理全依赖 CPU**。实测同一实例上已跑着 `tts-gateway`，CPU 争抢会直接拉长单段生成耗时。

⇒ **部署位置**：本地 Mac（推荐，M 系列足够）或独立小机器。
⇒ **验收动作**：部署后在 3090 上执行 `docker ps`，**不得出现 clipforge 容器**。

### 红线 2 · 它的 LLM Key 不许指向 3090 上的 qwen3:14b

**理由**：辉哥在 3090 上跑着 qwen3:14b（漫剧分镜用）。若 ClipForge 的「写脚本」LLM 指向它，**会占用 3090 显存**，与 H3 生成（≈20 GB）直接抢卡。

⇒ **两条替代路**：
| 方案 | 成本 | 说明 |
|---|---|---|
| **远端 API**（推荐） | **≈ ¥0.001/次** | DeepSeek 等 OpenAI 兼容接口。一条视频的脚本成本可忽略，且零资源占用 |
| 本地 Mac 小模型 | 零 API 成本 | Mac 上跑 ollama + 小参数模型；缺点是慢、且要占 Mac 内存 |

⇒ 3090 的显存**只留给 H3 生成**——这条要当纪律执行。

### 红线 3 · 只用它的文字产出，不搬它的分镜图方案

**理由**：ClipForge 用 **3×3 九宫格**（一图多镜 → 裁成各分镜关键帧）；而 **H3 官方 SOP 明确要求 3 张独立锚定照、禁用网格/拼贴**（我方闸门③第 6 条：「锚定照是 3 张独立照片，无网格/拼贴」）。

⇒ **铁律**：**九宫格整图绝不进 H3**。
⇒ **但裁出的单帧可以用**（见 §4.2）。

---

## 3. 它提供什么 / 我们怎么调

### 3.1 能力清单（只取这几项）

| 能力 | 接口 | 用途 |
|---|---|---|
| **脚本生成** | MCP `clipforge_generate_script` | ⭐ **核心**：输出「去商品化旁白 + 分镜节拍」脚本 |
| **商品入库** | MCP `clipforge_ingest_product` / CLI `product` | 贴链接抓标题/价格/图片 → 商品事实 |
| **文案审稿** | Skill `script-judges` | ⭐ 四位判官撕台词 + 等长重写 |
| **热点选题** | CLI `trends` | 抖音热搜 → 选题参考 |
| CLI 出口 | `create` / `compose` / `gate` / `qc` / `export` | 仅作参考，**不出片**（`gate` 有 exit code 语义，其门禁思路可借） |

### 3.2 它的最佳资产（可借鉴进我们自己的规范）

| # | 资产 | 我们已有的对应 | 处理 |
|---|---|---|---|
| 1 | **钩子库 16 款**（含自曝反转/认缺点开场/淘汰式测评/成本拆账…） | 无 | ✅ **采纳**（原文层） |
| 2 | **留人硬规则**（商品前 3 秒入画 / 超 20 秒必有 40–60% 处续命钩 / CTA 双落位 / 紧迫感分档） | 无 | ✅ 采纳 |
| 3 | **动作可生成性四类禁写**（双主体精确交互 / 内部心理 / 否定式动作 / 一拍三步动作链） | 闸门②动作白名单 | ✅ **同源印证**，互补写入闸门② |
| 4 | **分镜三硬规则**（关键动作唯一主落实镜 / 相邻两镜至少变一项 / 景别由信息尺度定） | 闸门③第 3、4 条 | ✅ 采纳 |
| 5 | **情绪写法铁律**（禁终态词，「触发→身体先反应→面部克制」三拍） | 无 | ✅ 采纳 |
| 6 | **废片六症状单变量重投**（脸崩/皮肤蜡感/背景死/光不对/模糊/商品变形） | §3.2「每次只改一个变量」 | ✅ 同源印证 |
| 7 | **爆款复刻：ffmpeg 解析镜头切点出节奏骨架** | REPORT19 结论「标准视频只提取规格」 | ✅ **完全一致**，可直接用它的实现 |

> **注意 3 与 6 的价值**：这两条说明**两家独立收敛到了同一结论**——不是拼凑，是共识。可作为我方规范的**外部交叉验证**。

---

## 4. 接口契约：分镜表 JSON 交接

### 4.1 映射关系（ClipForge 输出 → 我方 11 列节拍表）

我方 11 列（官方口径）：`时间段 / 镜头 / 目的 / 视觉主导 / 运镜 / 款式 / 文案 / 颜色 / 文字效果 / 转场 / 节奏`

| # | 我方列 | ClipForge 来源 | 桥接动作 |
|---|---|---|---|
| 1 | 时间段 | 按台词/节拍时长推算 | 转 `00:00.000–00:02.500` 格式，**贴 17k+5 帧网格 @24fps** |
| 2 | 镜头 | 分镜序号 | 合并成「**一条片内多镜**」写法 `[Shot 1]…[Shot 2] At 00:03.500` |
| 3 | 目的 | 镜头意图 | 直接保留 |
| 4 | 视觉主导 | 画面描述 | 保留，**补白名单动作词** |
| 5 | 运镜 | 它的 18 款命名运镜预设 | ⚠️ **映射到动作白名单**（举起/并排/推近/旋转/开合/滑入/光影流动/静置/特写平移） |
| 6 | 款式 | 商品款式 | 保留 |
| 7 | **文案** | **口播台词** | ⚠️ **必须三分流**，见 §4.3 —— **这是融入最关键的一步** |
| 8 | 颜色 | 文字配色 | 套官方两段配色规则（白底科技风前半用深色 / 黑底轮廓光后半用商品色） |
| 9 | 文字效果 | — | 按官方规范补 |
| 10 | 转场 | — | 一条片内多镜走**原生切镜**，不用外挂转场 |
| 11 | 节奏 | 节拍时长 | 保留 |

样例见 **`shotlist_schema.json`**（可直接作为两端联调的契约样例）。

### 4.2 九宫格的正确用法（有条件的 reuse）

**九宫格整图 ❌ 不进 H3**（违反闸门③）。

**但裁出的单帧 ✅ 有两个正当用途**：

| 用途 | 怎么用 | 条件 |
|---|---|---|
| **分镜关键帧** | 裁出的单帧 → 接 `MiniMaxH3AddGuide` 作**中间锚点**（一段 1 个），或作镜头的起点锚定 | 产品**形态级保真**（无 logo/中文/价格） |
| **跨镜一致性锚** | 作为「同源派生」的基准图 | 同上 |

> ⚠️ **前提条件必须满足**：九宫格里的产品是 **AI 画的**。若产品含 **logo / 中文标签 / 价格 / 复杂印刷** → 必糊，且每次糊法不同 → **走闸门①的「像素级保真」路线（混合合成）**，不要用它。

### 4.3 ⭐ 最关键的分叉：文案三分流（融入的成败点）

**问题**：ClipForge 的脚本是**中文口播文案**；而 H3 的 prompt 内文案要求是**画面内的短英文（3–5 词/句、单行、可读）**。**两者不能直接互换。**

⇒ 必须走我方原有的「**文案三分流**」：

| 分流 | 去向 | 处理 |
|---|---|---|
| **① 画面内英文** | 进 H3 prompt，**逐字包含** | 把中文卖点**重写成 3–5 词英文**；闸门③第 1 条校验「prompt 逐字包含所有画面英文文案」 |
| **② 中文旁白** | 后期 TTS 配音（或 H3 原生音频） | 中文口型在 H3 上不稳 ⇒ 优先**后期配音**；若用 H3 原生，须过 `lipsync2.py` 口型检测 |
| **③ 后期图文**（价格/CTA/合规标识） | 后期叠加 | 绝不交给模型画（必崩） |

> **一句话**：**ClipForge 出「说什么」，我们决定「哪些字进画面、哪些字进配音、哪些字进后期」。**
> 跳过这一步直接把中文台词塞进 H3 prompt ⇒ 画面文字必糊 + 触发闸门③否决。

---

## 5. 部署清单（7 步，可逐步验证）

| 步 | 动作 | 位置 | 验收 | 影响 |
|---|---|---|---|---|
| ① | 装 `script-judges` skill | WorkBuddy（Mac） | skill 可加载；零资源占用 | 无 |
| ② | Docker 起 ClipForge（`-p 3000:3000 -v clipforge-data:/data`） | **本地 Mac / 独立机** | `curl localhost:3000` 返回 200 | 无 |
| ③ | 配 LLM Key（**远端 API**） | ClipForge 设置页 | 生成一条测试脚本成功 | 无 |
| ④ | 建商品库 / 主播库（贴链接导入） | ClipForge | 抓取标题/价格/图成功 | 无 |
| ⑤ | 定义并冻结分镜表 JSON schema | `h3-pipeline/shotlist_schema.json` | 两端都能解析 | 无 |
| ⑥ | **端到端连通测试** | 两侧 | 一条完整链路跑通（脚本 → 11 列 → 闸门② → 生成） | 无 |
| ⑦ | **隔离验收** | 3090 服务器 | `docker ps` 无 clipforge；`nvidia-smi` 无 ClipForge 相关进程 | 无 |

**环境要求（实测自官方 Dockerfile / package.json）**：

| 项 | 要求 |
|---|---|
| Node | **≥ 20**（推荐 22） |
| 包管理 | pnpm ≥ 10 |
| 数据库 | **better-sqlite3（本地 SQLite 文件）** —— **无需 Postgres** |
| 系统依赖 | FFmpeg（含 `drawtext` 滤镜，中文字体内置） |
| 数据卷 | `/data`（sqlite + uploads + output） |
| GPU | **无** |
| 部署三选一 | ① Docker 一行 ② Electron 桌面版（双击） ③ 源码 `pnpm dev` |
| 当前版本 | **v0.9.7** |

---

## 6. 融入后的整体链路（与部署指南对齐）

```
【文字层】ClipForge（Mac）                          【画面层】H3（3090）
 商品链接 → 产品事实
      ↓
 生成口播脚本（钩子库 16 款 + 留人硬规则）
      ↓
 script-judges 四判官审稿 → 等长重写        ← 文案闸门（免费、零资源）
      ↓
 输出分镜表 JSON  ─────────── 接口物 ────────→  转 11 列节拍表
                                                ↓
                                          ⭐ 闸门② 动作审查
                                          （白名单 + 四类禁写 + 因果链否决）
                                                ↓
                                           三张独立锚定照（非网格）
                                                ↓
                                           H3 生成 + 抽卡（N 由 p 决定）
                                                ↓
                                           判官团 L0/L1/L2（**全部出 GPU**）
                                                ↓
                                           拼接 / 卡点 / 后期合规
```

**分工边界（一句话）**：
- **ClipForge 管「说什么」**（卖点、钩子、节奏、台词）
- **H3 流水线管「怎么画」**（动作可行性、锚定、生成质量、画面判官）
- **中间的契约是分镜表 JSON**，不是像素

---

## 7. 风险与边界（诚实清单）

| # | 风险 | 级别 | 处理 |
|---|---|---|---|
| 1 | **AGPL-3.0 传染性** | 🔴 若做对外 SaaS | 自部署**自用**没问题；**若改造成对外提供的服务，必须按 AGPL 开源修改版**（网络服务条款）。**它生成的视频内容不受影响**。辉哥的一键视频网站若要内置它，这条必须先定 |
| 2 | **九宫格与 H3 SOP 冲突** | 🟠 已规避 | 见 §2 红线 3 |
| 3 | **中文台词直接进 prompt** | 🟠 已规避 | 见 §4.3 文案三分流 |
| 4 | **它的文案质量是否真优于我们自研 prompt** | ⚠️ **未测** | 需拿 3–5 个真实商品做**盲测对比**（同 brief 两路出稿，人工/LLM 双盲评） |
| 5 | **接口稳定性** | 🟠 中等 | 它是活跃项目（v0.9.7，昨日仍在提交），**API/schema 可能变** ⇒ 契约层要自己做适配层，不要硬编码进流水线 |
| 6 | **它在快速迭代** | 🟡 机会 | 昨天（09-16）仍在提交 ⇒ 会有新能力，但也意味着**版本要锁定**（与我们的环境冻结原则一致） |
| 7 | 数据在本地 | ✅ 优点 | SQLite 落 `/data` 卷，不上云 —— 与"项目自持"原则一致 |

---

## 8. 落地优先级建议

| 顺序 | 动作 | 为什么排这个位置 |
|---|---|---|
| 1 | 装 `script-judges` skill | 零成本零风险，**立刻有收益**（文案闸门） |
| 2 | Docker 起服务 + 配远端 API Key | 拿「脚本生成」主能力 |
| 3 | 定义分镜表 JSON 契约 | 让两端能对接 |
| 4 | 端到端连通测试 | 验证闭环 |
| 5 | 与 H3 部署 Phase 2（闸门②）对齐 | **闸门②要把它的「四类禁写」并进去** |
| — | 盲测两路文案质量 | 决定长期是否依赖它 |

> ⚠️ **时序注意**：ClipForge 的**部署与 H3 的 Phase 顺序解耦** —— 它不参与 H3 的生成链，所以**可以在 Phase 0 之前或并行部署**，不影响 Phase 0–6 的施工顺序（REPORT19 Part B）。唯一需要对齐的是：**Phase 2 闸门②要并入它的「四类禁写」判据**。
