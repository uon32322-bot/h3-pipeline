# REPORT31 · 补 L：L 镜落点、中文口型标定、音轨单源原则

> 日期：2026-09-18　｜　触发：辉哥拍板「**补 L**」（对 REPORT30 §八 待决策项 2 的回答）
> 上游：`REPORT30`（音频层三模式 + 30–50 s 重构）
> 产物：`deliverables/shoe_40s/`（分镜表 **v3.0** / 决策表 **v2.0** / `anchor_L/` 标定包）
> 状态：**标定已出数 ⇒ 按判定线不通过 ⇒ 镜 7 退回 V（文案改画外音，零损失）**（见 §九）

---

## 一、这一轮要解决的是什么

辉哥原话（REPORT30 记录）：**「TTS 可以用 H3 自身带的音画同步。两种方式，一种是画外音，一种是口型同步，根据产品以及视频的实际情况进行自动区分。」**

REPORT30 把**三模式 + 三层路由**建好了，但结论落在「运动鞋走全 V 片」——**L 这条路一次都没真跑过**。
「补 L」就是**把 L 从纸面做成实测**。

---

## 二、⭐ 落点：镜 7（信任背书段）

### 候选评估

| 候选 | 判定 | 理由 |
|---|---|---|
| 镜 6（真人上脚） | ❌ | 要变 L 必须改成中近景露脸 ⇒ **整个 G2 段（362 帧，已卡段长上限）取景全重做**，且段内混 L/V |
| 镜 8 + 9（CTA） | ❌ | 句⑤（6.567 s）**跨 G4/G5 两段** ⇒ L 必然跨段；且两段仅 4.46 / 3.04 s，口型信号不足 |
| **镜 7（信任背书）** | ✅ | 4 条判据全过，见下 |

### 四条判据

| # | 判据 | 镜 7 实测 |
|---|---|---|
| 1 | **L 镜不跨生成段** | ✅ G3 是**单镜独立段**（175 帧 / 7.292 s）⇒ 改 L 不污染任何其他段 |
| 2 | **口播句完整落在段内** | ✅ 句④ 25.500–32.287 vs 段 25.208–32.500 ⇒ **头 0.292 + 尾 0.213 全是留白**，语音不贴段边 |
| 3 | **语义需要一张可信的脸** | ✅「**说实话**它不算轻…」是全片**唯一对镜坦白的语气**（承认缺点 = 信任背书最强节点） |
| 4 | **不改段长、不增段数** | ✅ 175 帧不变；只多挂 1 个 audio guide ⇒ **五段方案与 86.4 min 成本全不动** |

> ⚠️ **段数不能靠拆段解决**：`Σ(17k_i+5)=960 ⇒ 5n ≡ 8 (mod 17) ⇒ n ≡ 5` ⇒ 40 s **只有 5 段解**。
> 所以「把镜 7 拆成两段、只让后半说话」这条路**数学上不存在**。

### 产品曝光补偿

镜 7 的 7.292 s 产品不入画 ⇒ **后期左下角 PIP 产品小图 + 规格卡**补上。
**为什么不让模特手持**：手持 = 手部运动；实测「动作与语音时间重叠」会把口型信号 r 从 0.44 拉到 **0.16–0.36**（REPORT3 §4.2）。
⇒ **L 镜必须「静止说话」**，这是硬约束不是审美偏好。

---

## 三、L 镜资产（已产出）

| 文件 | 规格 | 关键点 |
|---|---|---|
| `anchor_L/anchor_L_first.png` | 768×1344（= H3 9:16 @0.98MP 画布） | 中近景胸以上 · 正面对镜 · 嘴微张 · 混凝土背景 · 两侧留白 |
| `anchor_L/anchor_L_last.png` | 同上（**同图**） | **首帧 = 尾帧** ⇒ 静止脸，排除人体位移干扰 |
| `anchor_L/s4_trust_full.wav` | 7.292 s / 32 kHz / mono | = **G3 段整段音轨**（25.208–32.500），与段长 175 帧逐位一致 |
| `anchor_L/silence_control.wav` | 7.292 s | 静音对照（标定 B 轨） |
| `anchor_L/L_talk.txt` | 官方 4 段结构 | 显式写「头肩不动 / 不做手势 / 不换姿势 / 只对镜说话」 |
| `anchor_L/roi.txt` | `319 417 115 80` | 口部 ROI（目视定位，**不含眼睛**）——⚠️ **实测作废**，见 §九 修正 |

### 踩到的两个坑（都会静默毁片）

| # | 坑 | 现象 | 处置 |
|---|---|---|---|
| 1 | **图像模型必改产品** | 第一版首帧把鞋的生成了「银白主调 + 白大底」，与真实产品（黑主调 + 黑中底）不符 —— 与 REPORT23 的 TT Image2 结论同源 | ⇒ **L 镜产品不入画**，改后期 PIP。**绕开而不是修** |
| 2 | **生成水印 + 画布比例** | 出图 1024×1536（比例 0.6667）≠ 画布 768×1344（0.5714），会被首帧守卫拦；右下角还有生成水印 | ⇒ 按**画布比例**（不是 9:16 = 0.5625）左对齐裁 878×1536 再缩放：**比例偏差 0.033%，水印（x≥890）恰好被裁掉** |

> 📌 **比例口径修正**：H3 的 9:16 @0.98MP 经 `resolve_px` 得到的是 **768×1344（0.5714）**，
> 不是数学上的 9/16 = 0.5625。**首帧要贴的是画布比例，不是宽高比标签。**

---

## 四、⭐ 标定实验：中文口型同步（首次）

### 为什么必须先标定

`judge_audio.py` 的 A-03（口型同步）当前状态是 **"仅测量、无否决权"** —— 因为实测过误判：
全身运动镜 r = 0.52 / 0.79（**人在动 ≠ 嘴在动**），两种补救闸门均无法分离。
而**中文**口型更是**零数据**（REPORT3/5 全程英文）。

### 实验设计（单变量 + 静止脸）

| 轨 | 首帧/尾帧 | 音频 | Prompt | 段长 | Seed |
|---|---|---|---|---|---|
| **A** | 同图（静止脸） | `s4_trust_full.wav`（真实句④） | L_talk | 175 帧 | 2026091831 |
| **B** | 同图（静止脸） | `silence_control.wav`（静音） | **同 L_talk** | 175 帧 | **同 seed** |

> ⭐ **只换音频这一个变量**（同 seed、同首尾帧、同提示词）⇒ A 与 B 的差异**只可能**来自音频。
> 这是 SOP「只改一个变量」原则在标定上的落地。

### 判据

| 判项 | 方法 | 通过线 |
|---|---|---|
| **嘴动确由语音驱动** | `r(A) 显著 > r(B)` | A 明显高于 B（B 应接近 0） |
| **同步性** | 口部 ROI 运动 vs 音频包络互相关峰值 | **r ≥ 0.30 且 \|滞后 ≤ 2 帧\|**（≤83 ms @24fps） |
| 交叉验证（零额外算力） | 同一 A 产物 vs **句①（异内容）** 的 r | r(A↔句④) **≫** r(A↔句①) |

> 💡 **第 3 条是本轮省下的算力**：不必再跑第三条对照组 —— 用**同一 A 产物**去和另一句音频算相关即可，
> 零 GPU 成本就能证明「口型跟的是内容，不是随便动」。

### 执行

- 服务器脚本：`scripts/run_L_calib.sh`（**自带显存等待器**，见 §六）
- 输出：`/root/autodl-tmp/h3p/output/H3L/CAL_A*.mp4` / `CAL_B*.mp4`
- 测量：`scripts/liproi.py <mp4> 319 417 115 80 --fps 24`（固定 ROI，避开自动搜索的干扰）
- 成本：2 × 12.8 min ≈ **26 min GPU**

---

## 五、⭐ 理论修正：「禁 L+V 混用」→「**音轨单源**」

### v1.0 的表述有问题

REPORT30 写的是「**有任一 L 镜 ⇒ 全部含口播的段都锚同一条音轨**」（= 全片升 L）。
它的隐含前提是「L 与 V 混用＝音轨也混用」，**把两条独立约束捆在一起**。
后果：为了一个 7 s 的镜头，**整片音色都变成 H3 重渲染版（波形相关仅 0.27）**。

### 正确拆开成两条

| # | 约束 | 管什么 | 违反后果 |
|---|---|---|---|
| **① 音轨单源**（真·铁律） | 成片音轨**唯一来源**：全片 H3 渲染，或全片贴 TTS 原音 | 音色**一致性** | 观众听到「换人说话了」 |
| **② V 镜禁露脸说话** | 标 `V` 的镜**不得**出现「脸可辨 + 说话构图」 | 口型**穿帮** | 观众看到嘴型对不上声音 |

> ⭐ **L / V 描述的是「画面驱动方式」，音色断层来自「音轨来源拼接」—— 两者是不同维度。**
> 只要音轨单源，同一条片子里 L 镜与 V 镜共存**不会**产生音色断层。

### 本片采用的路线（β）

| 项 | 做法 |
|---|---|
| 音轨来源 | **唯一：`vo/track_full.wav`（TTS 原音）** ⇒ 音色 **100% 保留**、全片一致 |
| 画面驱动 | 镜 7 走 L（`AddGuide(audio)` 牵引口型）；其余 8 镜走 V |
| H3 渲染出的音轨 | **丢弃不用**（只取画面） |
| 成立前提 | ⚠️ **标定 §四 必须通过**：须证明「audio guide 牵引出的口型与**输入音频**同步」 |

**回退顺序**（若标定不通过）：**① 镜 7 退回 V**（文案改画外音，零损失）→ ② 才考虑全片锚 H3（牺牲音色）。
**不采用路线 α（全片升 L）**：为一个 7 s 镜头牺牲整片音色，不划算。

---

## 六、脚本能力升级：`--audio`

`test_fl2v_official.py` 原本只有 `--mid`（**画面**锚点）。本轮加上**语音锚点**：

```bash
./test_fl2v_official.py first.png last.png prompt.txt 7.292 0.98 8 H3L/CAL_A 2026091831 \
    --audio "s4_trust_full.wav@0" --aspect 9:16 --port 6011
```

**实现要点（源码级）**：`MiniMaxH3AddGuide` 支持 `image` **或** `audio`，两者都走
`keyframe["audio_latent"] / keyframe["latent"]` 写进 conditioning 的 `minimax_keyframes`，
**每一去噪步重新注入、不参与去噪** ⇒ 输出音轨保留内容、画面口型被牵引对齐。

> ⚠️ **多个锚点必须串联，不能并联**：每个 `AddGuide` 都是
> `keyframes = list(positive[0][1].get("minimax_keyframes", []))` + append
> ⇒ **并联会让后一个从旧 positive 出发，把前一个的 keyframe 丢掉**。
> 脚本已改为链式（`tail` 传递）。

---

## 七、成本

| 项 | 值 |
|---|---|
| L 镜本身增量 | **≈ 0** —— 段长不变（175 帧），只多挂 1 个 audio guide（REPORT3 实测 100.6 s vs 同长无锚点同级） |
| 首帧出图 | 1 次图像生成（2 张，含废掉的第一版） |
| 标定 | 2 × 12.8 min ≈ **26 min GPU**（一次性，入资产库后可复用） |
| 全片总计 | **≈ 86.4 min**（五段方案不变，G3 = 12.8 min） |

---

## 八、🚧 阻塞：GPU 被外部业务占满

| 项 | 实测（2026-09-18 10:20） |
|---|---|
| 6011（**本项目** ComfyUI） | 13,680 MiB —— 空闲（队列空，日志停在 05:08） |
| 6006（`/root/ComfyUI`，**非本项目**） | 9,384 MiB + **GPU 利用率 100%** —— 在跑 `digital-human` 批量任务（队列 24 单） |
| tts-gateway | 466 MiB |
| **⇒ free** | **仅 271–525 MiB** |

**为什么不直接提交**：H3 推理需 ~20 G staged + ~6 G 激活，此刻提交必 OOM；
更危险的是**与 6006 抢显存可能把对方正在跑的任务搞崩**。
⇒ **不擅动外部实例**（D6 项目独立原则）。

**处置**：`scripts/run_L_calib.sh` 内置**显存等待器** ——
每分钟查一次 `memory.free`，**连续 3 次 ≥ 9 G** 才启动（连续判据用于避开 6006 批量任务的间隙误判），
然后自动跑 A 轨 → B 轨 → 落日志 `/root/autodl-tmp/h3p/logs/L_calib.log`。
**已在服务器后台运行**（PID 801108 起），无需人工盯守。
**✅ 已解除**：等待器 10:50:37 放行（free=9537 MiB），A/B 两轨 11:38:39 全部跑完，见 §九。

> ⚠️ 注意「判据不是总显存 21.5 G」：6011 自己常驻 13.7 G（模型 staged），
> 推理真正额外需要的是**激活内存**。free 回到 9 G 以上只可能是 6006 释放了它那份。

---

## 九、⭐ 标定结果（实测 2026-09-18 上午）

### 执行回执

| 项 | 值 |
|---|---|
| GPU 等待 | 21 分钟（`free` 连续 3 次 ≥ 9 G，10:50:37 启动；6006 释放后） |
| A 轨（句④语音） | rc=0，1511.1 s ≈ **25.2 min**，`CAL_A_00001_.mp4`（768×1344 @24fps，175 帧） |
| B 轨（静音，同 seed） | rc=0，1371.0 s ≈ **22.9 min**，`CAL_B_00001_.mp4`，11:38:39 `L-CALIB-ALL-DONE` |
| 实际成本 | ≈ **48 min GPU** + 21 min 等待（预估 26 min，偏保守 1.8×） |

### ⚠️ 坑 3：处方 ROI 目视定位偏了（测量先行作废）

把 `319 417 115 80` 叠回锚点首帧与 A 轨逐帧：**框压在左脸颊/下颌，嘴唇在框右上外**。
用该框测得 r(A)=+0.062（≈0，失效）。**修正 ROI = `400 418 120 88`**（叠框目视复核：完整罩唇、不含鼻眼），以下全部数据用修正 ROI。

### 主测量（修正 ROI，liproi.py --fps 24，±8 帧时滞扫描）

| 轨 | 峰值 r | 峰值滞后 | 零时滞 r | 嘴动均值 | 音频包络 peak |
|---|---|---|---|---|---|
| **A**（句④语音） | **+0.310** | **-1 帧（-42 ms）** | +0.261 | 12.71 | 31126 |
| **B**（静音对照） | **+0.311** | **-1 帧（-42 ms）** | +0.303 | 12.11 | 1749 |

### 交叉验证（同一 A 产物 vs 异句音频，零额外 GPU）

| 嘴动 ↔ 音频 | A 轨 | B 轨 |
|---|---|---|
| ↔ 句④（A 实际听到的） | **+0.292 @ -1** | +0.200 @ -1 |
| ↔ 句①（0.400–2.906 s） | +0.109 @ -5 | +0.105 @ -1 |
| ↔ 句③（10.279 s 起，等长 7.292 s） | +0.122 @ +5 | +0.177 @ -7 |

置换检验（400 次音频循环移位 ≥20 帧）：A 全段峰值 z=+2.26 / p=0.002，但**去掉前 12 帧唤醒瞬态后 p=0.127（不显著）**；B 同法 p=0.117。

### 三个机制发现（判据为什么失效）

1. **静音不是「嘴不动」的开关**：B 轨嘴动均值 12.11 ≈ A 的 12.71 —— prompt 写了「说话」，嘴就全程说，audio guide 只对齐**时机**、不门控**有无**。
2. **H3 音视频联合生成，B 轨输出音频 ≠ 输入静音**：输入 peak=8（数字静音）→ 输出 peak=1749 ⇒ 模型自己生成了微弱口型音，与自己的口型内洽（B 峰值 z=+4.95）。⇒ **r(B) 不是机会水平基线，「r(A)≫r(B)」这条判据设计上不成立**。
3. **A 对「自己那句」有特异性增益**：A↔句④ 0.292 vs A↔句①/③ 0.11–0.12、B↔句④ 0.200 —— audio guide 确实把口型拉向输入语音，但效应弱（Δr≈0.09，解释方差 ~8%），远达不到「真人级口型」。

### 判定：❌ 不通过 ⇒ 镜 7 退回 V

| 判项 | 结果 |
|---|---|
| r(A) 明显高于 r(B) | ❌ 0.310 vs 0.311 —— 无差异（且该判据被机制发现 1/2 证伪） |
| \|滞后\| ≤ 2 帧 | ✅ -1 帧（-42 ms），但失去意义 |
| 交叉验证 r(A↔句④) ≫ r(A↔句①) | ⚠️ 方向成立（0.292 vs 0.109）但效应弱、去瞬态后不显著 |

**按预设回退顺序**：镜 7 退回 **V**（文案改画外音，零 GPU 损失）；音轨单源路线 β 不变（全片贴 `vo/track_full.wav` TTS 原音）。A-03 维持「仅测量、无否决权」。

> 📌 **给下一轮的遗产**：对 H3 做口型标定，**静音对照无效**（会自生成音频+口型）。有效对照是**错位音频**（换句/时移）——本轮交叉验证实际已经就是正确方法。L 路线若要复活，需要口型编辑后处理（如 latentsync 类）或换口型驱动模型，H3 原生 audio guide 的牵引力不够。

---

### ⚠️ 补遗（同日 11:50 复测）：**上一轮所有 r 值受脚本 bug 污染，须重读**

#### 元凶：ffmpeg `crop` 对 yuv **强制偶数**，奇数尺寸被静默减 1

实测：`crop=115:80:319:417`（本片定稿的嘴部 ROI）实际输出 **114×80**，即 9120 B/帧而非 9200 B。
而两个口型脚本都按**请求尺寸** reshape：

```python
n = len(b) // (fw * fh)                 # 用请求尺寸
f = b[:n*fw*fh].reshape(n, fh, fw)      # ← 实际每行短 1 像素 ⇒ 逐行错位
```

⇒ **图像被斜切**，相关性沦为噪声。**这不是精度问题，是数据损坏。**

| 脚本 | 违规 ROI | 受影响 |
|---|---|---|
| `liproi.py` | x=319 y=417 **w=115** h=80 | 是 |
| `lipsync2.py` | **fx=215** fy=120 **fw=353** fh=430（768×1344 下的取值） | 是 |

**⇒ 第九节全部数值（A 0.310 / B 0.311 / 交叉验证 0.292 / 0.109 / 0.122）作废，不可作判据。**

已修（两脚本均已偶数化 + 字节整除硬断言 + 改走临时文件，避免大块 rawvideo 走管道）：

```
liproi.py   319 417 115 80  ->  318 416 114 80
lipsync2.py 214 120 352 430 （搜索区自动取偶）
```

#### 修正后复测（A/B，同 seed 2026091831，严格单变量）

| 测量 | A 轨（真实语音） | B 轨（静音） | 读法 |
|---|---|---|---|
| `liproi.py` 固定 ROI，峰值 r | +0.249 | +0.395 | ❌ **该指标失效**：它测 ROI 内**全部**运动，被头部微动/压缩噪声淹没，A 反而更低 |
| `lipsync2.py` 最佳窗口**位置** | **嘴部 x=414 y=456** ✅ | **眼眉 x=422 y=336** ❌ | ⭐ **A 能在嘴部找到耦合，B 只能抓到眼眉的伪相关** |
| 最佳窗口峰值 r @ 时滞 | +0.291 @ **0 帧** | +0.363 @ 0 帧 | A 零时滞；B 的 r 虽高但窗口是错的，数值无意义 |
| 逐帧嘴部 **MAD** | — | — | **10.30**（复现噪声基线 **1.08** ⇒ **9.5×**） |
| 差异的垂直分布 | — | — | **单峰，峰在 y 440–464 = 嘴唇带**（顶部 8.35 / 峰 20.43 / 底部 5.19） |
| A/B 口型序列互相关 | — | — | **r = −0.020**（几乎完全无关 ⇒ 音频改写了口型的时间结构） |

视觉复核（`calib_face_AB.png`）：**A 轨 f130(5.42s) 是清晰的 O 形张嘴，B 轨同帧仅轻微张开**；差异集中在嘴、眼周、脸部轮廓。

#### 修正后的结论：不是「零效果」，是「**弱耦合**」

| 旧表述 | 修正后 |
|---|---|
| A/B 无差异（0.310 vs 0.311） | ❌ 那是错位图像的产物。**真实差异是嘴部 MAD 9.5× 噪声** |
| audio guide 完全无效 | ⚠️ **有效但弱**：A 的耦合精确定位在嘴部且零时滞；B 无法在嘴部建立耦合 |
| — | 强度：r≈0.29（解释方差 ~8%），**远低于真人级**，也低于 REPORT3/5 的英文值（0.44–0.53，⚠️ 但那些值同样出自带 bug 的 lipsync2，**需用修正脚本复测才可比较**） |

**⇒ 工程决定不变：镜 7 退回 V。** 理由变了 —— 不是"完全没口型"，而是"**口型存在但强度不足**"：弱同步在成片里会被观众读成"嘴型对不上"，比全片画外音更糟；而 V 路线零损失。

**⇒ 第九节的「静音对照无效」结论仍成立，但机理要修正**：B 轨不是"没有口型"，而是"**口型与音频在嘴部不耦合**"（B 的最佳窗口跑到眼眉去了）。所以**错位音频仍是最佳对照**，且"r(A)≫r(B)"这条判据**在同一窗口下才成立**（跨窗口比 r 值是错的）。

---

## 十、验收清单

- [x] L 镜落点定稿（镜 7 / G3）且**四条判据全过**
- [x] L 镜首帧产出（768×1344 画布比例、脸可辨、嘴可见、无水印）
- [x] 音轨锚点切片（7.292 s = 段长 175 帧，逐位一致）
- [x] 口部 ROI 定稿（`319 417 115 80`，目视定位、不含眼睛）
- [x] 标定实验设计（A/B 同 seed 单变量 + 静止脸 + 交叉验证）
- [x] **`--audio` 能力落地**（脚本 + 链式锚点修正）
- [x] 「音轨单源」替代「禁混用」（含回退顺序）
- [x] 分镜表 v3.0 / 决策表 v2.0 同步
- [x] 显存等待器部署并运行
- [x] **标定出数**（A：r=+0.310 @ -1 帧；B：r=+0.311 @ -1 帧；交叉验证 0.292 / 0.109 / 0.122）
- [ ] ~~标定通过 ⇒ G3 正式生成（12.8 min）⇒ A-03 实测复核~~
- [x] 标定**不通过** ⇒ 镜 7 退回 V（文案改画外音，零损失）——分镜表 v3.0 镜 7 标记需同步改回 V

> **未验证项**（继续显式标注）：H3 中文语音质量（`<d>` 自生成）｜多音频锚点叠加｜
> A-03 的否决权（标定通过后才给）｜A-05 跨段音色一致（本轮不涉及，音轨单源下恒为一致）。

---

## 十一、⭐ 服务器 GPU 共享实况与 arbiter（2026-09-18 11:45 实测）

### 显存不是"被外部占满"，是**三家用一间房**

| PID | 归属 | 显存 | 性质 |
|---|---|---|---|
| 118976 | **数字人视频流水线**（`/root/ComfyUI` @6006，跑 `dh:va`） | **14 728 MiB** | 唯一大户；**用的是同一套 H3 权重** |
| 489655 | **我们的 H3**（6011，`/root/autodl-tmp/h3p`） | **8 368 MiB** | ⚠️ **idle 常驻，纯占位** |
| 130844 | `tts-gateway`（配音网关） | 466 MiB | 常驻小户 |

合计 **23.6 / 24.6 GB，free 仅 479 MiB**。

### ⚠️ 我们自己的标定把辉哥另一个业务的吞吐打掉一半

数字人流水线今日逐条 `Prompt executed` 耗时：

| 时段 | 单条耗时 | 备注 |
|---|---|---|
| 09:41 – 10:48（8 条） | **509–522 s（≈8.5 min）** | 基线 |
| 11:08 起（3 条） | **13:47 / 17:24 / 17:49** | ⚠️ **正好是我们 A/B 轨在跑的时间段（10:50–11:38）** |

⇒ 两个 ComfyUI 实例各自做动态 VRAM 调入调出、互相驱逐，**双方都变慢**。我们的标定本身也从公式预测的 12.9 min 拖到 **22.9 min**。
**结论：`24 GB 跑不下两个 H3 实例的满载任务`，必须串行。**

### ⭐ 正解：这台机器**已有正式的 GPU 仲裁服务**，而我们没接入

```
/root/autodl-tmp/relocate/shared/gpu_arbiter.py   →  127.0.0.1:6200
  POST /acquire   {project, stage, vram_mb, priority, label, timeout}  → 阻塞直到授予，返回 lease_id
  POST /heartbeat {lease_id}                                            → 续租
  POST /release   {lease_id}
  GET  /status
```

| 机制 | 值 | 含义 |
|---|---|---|
| `vram_reserve_mb` | 2048 | 单次申请上限 = 24576−2048 = **22528 MiB** |
| 排队序 | `(priority, seq)` | 同级 FIFO ⇒ **公平队列，不必抢** |
| 租约 | `timeout` + 心跳 + `LEASE_TTL` 过期释放 | 进程死了不会永久占坑 |
| 现状 | `active = dh:va（21000 MiB, prio 1）`，**`queue = []`** | 无积压 |

**数字人业务自己在用它**（active.label = `dh:va`）；我们的 6011 却是靠轮询 `nvidia-smi` 抢显存 —— **土办法**。
⇒ **行动项：给 H3 生成也接 arbiter**（`project="h3-pipeline"`, `vram_mb=21000`, `priority=1`）。收益：
① 正式排队，不再竞态，两家都不必互相拖慢；② 双方吞吐可预测；③ 彻底消除"提交时才发现显存不够"。

> ⚠️ **纪律**：arbiter 是共用基础设施，**只 acquire/release 自己的租约**，不得去 release/抢占 `dh:*` 的租约，也不得擅自停 `6006` 那个实例。

### 等待器门槛勘误

`run_L_calib.sh` 原设 `NEED=3000 MiB`（3 次连续达标）—— 实测在 free=9537 MiB 时触发并**成功跑完**。
⇒ 结论：6011 已有 8.4 GB 常驻的前提下，**额外 9.5 GB 即可开跑**（合计 ≈18 GB），**不需要 20 GB free**。此数可直接用作 arbiter 申请量的下限校验。
