# H3 带货出片管线 · 首尾帧路线实测报告

**测试日期**：2026-09-11
**测试目标**：验证「图片模型生成首尾帧 → FL2VA 插值出片」这条路线，动作到底可控到什么程度；
并对比两条关键帧生成路线：**宫格一次性生成** vs **同源编辑链**。

**测试主题**：护肤精华瓶的「拿起 → 拧开」动作（含手部与产品接触，属最难的那一类）。

---

## 一、路线设计

| | A 路线 · 宫格 | B 路线 · 同源编辑链 |
|---|---|---|
| 做法 | 一次生成 2×2 分镜图，再裁成 4 张单帧 | 先生成底图 p1，再逐帧编辑派生 p2 / p3 |
| 调用次数 | **1 次** | **3 次**（1 次文生图 + 2 次编辑） |
| 单帧尺寸 | 2731×1536 ÷ 4 ≈ 1365×768 | 1920×1088 |
| 状态数 | 4 个（3 个片段） | 3 个（2 个片段） |

两条路线的关键帧随后**全部裁到 1280×720（16:9）**，配对喂给 FL2VA：

- 段①「拿起」：A_p1→A_p2 ／ B_p1→B_p2
- 段②「拧开」：A_p2→A_p3 ／ B_p2→B_p3

---

## 二、成本与耗时（实测）

| 环节 | 次数 | 单次耗时 | 花费 |
|---|---|---|---|
| TT Image 2 文生图（16:9） | 4 | 37–46s | ¥0.0444 / 次 |
| TT Image 2 图片编辑（同源派生） | 2 | 54–66s | ¥0.0444 / 次 |
| H3 FL2VA 插值（5.17s / 0.4MP / 8 步） | 3 | **140–150s** | 自有算力 |

**整条链路首轮出片成本 ≈ ¥0.27 + 约 11 分钟 GPU**（含 3 段视频）。

> 注：接口文档警告 size「只稳定决定宽高比档位」。实测 1024×1024 与 1920×1088 **精确命中**；
> 但请求 2560×1440 实际返回 2731×1536（比例正确、像素略有出入），与文档描述一致。

---

## 三、核心指标

### 1. 锚点质量 —— 产品本体对齐度

用竖向边缘剖面估计产品左右边界，比较两帧之间产品的**尺度**与**位置**漂移：

| 片段 | 尺度漂移 Δ宽度 | 位置漂移 Δ中心 | 判定 |
|---|---|---|---|
| A 段① 拿起 | +2.0% | −3.5 px（0.3% 画幅） | ✅ |
| A 段② 拧开 | −0.7% | +1.5 px | ✅ |
| A 段③ 倒出 | −1.3% | 0.0 px | ✅ |
| B 段① 拿起 | +0.6% | +1.5 px | ✅ |
| B 段② 拧开 | −0.6% | +0.5 px | ✅ |

**结论：两条路线的产品对齐度都很好，没有出现尺度/位置漂移。**

### 2. 锚点质量 —— 背景与机位一致性（纯背景区 MAE，越小越好）

只统计画面顶部 15% 行 × 左侧 65% 列（该区域无手臂、无产品，纯背景）：

| 片段 | A 宫格 | B 同源编辑链 |
|---|---|---|
| 段① 拿起 | **1.11** | 3.64 |
| 段② 拧开 | **6.10** | 10.87 |
| 段③ 倒出 | **4.99** | — |

**结论：这一项 A 路线（宫格）反而更好。** 原因是宫格四格在同一次前向传播里渲染，
背景与光照共用同一份上下文；而编辑链每次都要整图重绘，背景会被重新合成、产生轻微漂移。

### 3. 落地精度 —— 生成的末帧是否真落在指定尾帧上 ⭐

这是「状态可控」是否成立的**决定性证据**：

| 检查项 | 整体 MAE | 变化像素占比 | 判定 |
|---|---|---|---|
| 段① 生成的**首帧** vs B_p1 | 3.27 | 0.16% | ✅ 几乎像素级一致 |
| 段① 生成的**末帧** vs B_p2 | 6.79 | 3.71% | ✅ 高度吻合 |
| 段② 生成的**首帧** vs B_p2 | 3.95 | 0.27% | ✅ |
| 段② 生成的**末帧** vs B_p3 | 6.95 | 3.94% | ✅ 高度吻合 |
| 宫格段① 生成的**末帧** vs A_p2 | 4.58 | 1.54% | ✅ 高度吻合 |

**结论：✅ 状态可控——实测成立。**
首帧几乎像素级还原（MAE 2.9–4.0），末帧高度吻合（MAE 4.6–7.0，变化像素仅 1.5–4%）。
模型确实把画面**送到了你指定的那个状态**，而不是自己发挥。

### 4. 动作的时间分布（逐 0.5s 运动量，@6fps）

| 片段 | 逐 0.5s 运动量 | 动作实际区间 |
|---|---|---|
| 段① 拿起 | 4.2 · 8.0 · 6.0 · 4.1 · 1.3 · 1.0 · 0.3 · 0.2 · 0.2 · 0.1 | **0 → 2.0s**，之后 3.2s 近乎静止 |
| 段② 拧开 | 0.6 · 5.3 · 3.6 · 4.1 · 3.2 · 6.1 · 6.0 · 0.5 · 0.2 · 0.1 | **0.5 → 3.5s**，两段式 |
| 宫格段① | 5.0 · 6.6 · 4.4 · 2.4 · 1.2 · 0.7 · 0.3 · 0.2 · 0.1 · 0.1 | **0 → 2.0s** |

**结论：❌ 时序不可控——被量化确认。**
5.17s 的片段里，动作只占 2.0–3.5s，**剩下的时间是空转**。而且动作总被顶到片段前部，
你不能指定它「慢一点」或「卡在第 3 秒」。段② 出现了两段式发力，说明模型的节奏是自发的。

### 5. 接触面检查（末帧放大 3× 目视）

手掌握持玻璃瓶身这一最难的一类接触，**三个末帧均未出现明显穿插、粘连或多指/少指**。
与前期调研一致：H3 在手部交互上是强项。
（注意：成片仅 864×480，细微穿模在此分辨率下不可见；且本次是**静态刚性握持**，
不等于「涂开」「拧紧」这类有持续形变与摩擦的接触。）

---

## 四、对先前判断的三处修正

| 我此前的说法 | 实测修正 |
|---|---|
| 「宫格裁帧会造成尺度漂移，两帧不齐会撕裂」 | ❌ **不成立**。产品尺度漂移仅 +2.0%/−0.7%/−1.3%，位置漂移 ≤3.5px |
| 「同源编辑链在一致性上严格优于宫格」 | ⚠️ **部分不成立**。背景/光照一致性上**宫格更优**（1.11 vs 3.64）；产品对齐两者相当 |
| 「宫格必须裁成单帧，且不能整张喂」 | ✅ **成立**。整张宫格是错误格式，必须裁帧 |
| 「FL2VA 会自作主张」 | ❌ **本次未发生**。给了首尾两帧后，落点精度极高，没有切镜头 |

**宫格路线真正的短板，不在像素，而在语义控制：**
宫格生成的 p4「擅自」把瓶子放回了桌面（我们要求的是手举着瓶子拧开盖），
即**状态跃迁不单调**——这是编辑链不会犯的错。另外宫格**无法单独重出某一格**，
改一格要重跑整图，导致迭代成本高、且已对齐的其余格全部作废。

---

## 五、生产建议

1. **两条路线组合用，不要二选一**
   - 宫格负责「世界观」：一次定下人物、产品、光照、色调，跨片段统一
   - 编辑链负责「逐对同源」：沿链对相邻帧做局部编辑微调，确保喂给 FL2VA 的每一对是同一底图派生
2. **段长压到 2–3 秒**（重要）
   实测 5.17s 段里动作只占 2–3.5s，后段空转。把段长压到 2–3s，动作密度立刻上去，
   同时每段耗时也从 140–150s 降到约 60–80s，出片效率翻倍。
3. **锚点按「出错频率」加，不按「动作数量」加**
   先跑 2 锚点，哪段糊就给那一段补中间锚点（用 `MiniMaxH3AddGuide` 按 `frame_idx` 注入）。
4. **提示词只写「变化」，不要把分镜脚本整段丢进去**
   锚点图已承载画面内容；脚本应拆成逐段的「变化描述」再喂。
5. **一对首尾帧只承载一次状态跃迁**
   本次一次跃迁的落点精度 MAE 4.6–7.0；若塞进多个动作（如「拿起+拧开+倒出」做成一对），
   成功率会明显下降。

---

## 六、本次产出的可复用脚本

| 文件 | 作用 |
|---|---|
| `ttimg.py` | TT Image 2 接入封装（文生图 / 图片编辑，异步任务+轮询，纯标准库无依赖） |
| `test_fl2v.py` | H3 FL2VA 首尾帧插值，走 ComfyUI API，含耗时统计 |
| `align.py` | **产品本体对齐度检测**（尺度漂移 / 位置漂移）——零成本预检 |
| `frames.py` | 宫格裁帧、首尾帧一致性自检、差异热力图、接触表 |
| `pA_grid.txt` / `pB_p1~p3.txt` | 可复用的提示词模板（含「保持不变」的强约束写法） |
| `q_seg1.txt` / `q_seg2.txt` | 片段级「只写变化」的提示词模板 |

**上线前的零成本预检流程**：`align.py` 查产品对齐 → `frames.py metrics` 查背景一致性与变化幅度 →
两项都过关再花 GPU 跑 FL2VA。
