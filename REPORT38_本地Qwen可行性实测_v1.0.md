# REPORT38 · 「用 3090 上的 Qwen 替代 DeepSeek」可行性实测

> 日期：2026-09-19
> 起因：辉哥问「能不能用 3090 服务器上的 Qwen？」
> 性质：实测核查 + 成本对比 → 结论：**不推荐**
> 上游：REPORT36 §3.1（LLM 指向红线）、REPORT37（ClipForge × H3 部署）

---

## 0. 结论

> **不推荐。** 技术上有条件可行，但为省 ≈¥3/月，换来"可能 OOM 毁掉 30 分钟 H3 生成"的风险，不划算。
> 且 REPORT36 §3.1 记录的「3090 上有 qwen3:14b（漫剧业务）」**实测已不存在**。

---

## 1. 实测事实（nmb2, 2026-09-19 16:4x）

| 检查项 | 实测 |
|---|---|
| `ollama` 二进制 | ✅ `/usr/local/bin/ollama` 已装 |
| **ollama 模型** | ❌ **空**（`~/.ollama` = 0 KB；`ollama list` 报 "could not connect"） |
| ollama 服务 | ❌ 未运行 |
| `11434` / `8000` / `8080` / `1234` 端口 | ❌ 全部无响应 |
| `qwen3-tts`（`/root/autodl-tmp/qwen3-tts/`） | ⚠️ **语音合成**，非 LLM |
| 实际监听的其他端口 | `6107` h3-gateway · `6108` heygem-gateway · `6109` tts-gateway · `6200` **gpu_arbiter** · `8001` app · `5202` ai-analysis |

⇒ **3090 上当前没有任何可用的 Qwen LLM**。
REPORT36 §3.1 写的「别人的 qwen3:14b（漫剧业务）」已不存在，"不碰别人项目"这条理由**自动失效**。

---

## 2. 真正的阻碍：显存

**实测当前 GPU 占用**：
```
PID 349145 (ComfyUI/H3)   23536 MiB
PID 849125 (ComfyUI/6006)   256 MiB
总卡 24 GB → 几乎占满
```

| 项 | 显存 |
|---|---|
| H3（INT8 + 4 步 Turbo，完整 resident） | **23.5 GB** |
| Qwen3:14b（Q4_K_M + KV cache） | ~11–12 GB |
| **并发需求** | **~35 GB** ❌ |

**时序错开理论可行**（写脚本在生成之前），但有两个坑：

1. **Ollama 默认 `keep_alive=5m`** —— 5 分钟内不卸载，H3 一启动就 OOM
2. **`gpu_arbiter.py` 不覆盖 ollama** —— ollama 绕过租约直接抢显存，
   而仲裁器的核心价值正是「同一时刻只授予一个租约，防 OOM」
   （`gpu_arbiter.py` docstring：*24GB 单卡多项目共享, 租约制 + 优先级队列, 防 OOM*）

> 用户已投入建设仲裁器治理多项目共享；引入一个**不参与仲裁**的常驻 LLM 服务
> 是与其设计目标背道而驰的。

---

## 3. 成本对比

| 方案 | 单条成本 | 100 条/天 | 额外风险 |
|---|---|---|---|
| **DeepSeek API**（已集成） | ~¥0.001 | **~¥3/月** | 无 |
| 本地 Qwen3:14b | ¥0 | ¥0 | 下 9 GB 模型 + 串行调度改造 + 每次 OOM 浪费 30 min H3 生成 |

---

## 4. 何时才该用本地 Qwen

| 场景 | 结论 |
|---|---|
| 纯省钱 | ❌ DeepSeek 更划算（¥3/月） |
| **不想把商品信息发给外部 API**（隐私/涉密/竞品敏感） | ✅ 本地是唯一解 |
| 断网/内网环境 | ✅ 本地 |
| 实验/学习 | ✅ 本地 |

**若将来确实需要**，安全做法（避免踩显存坑）：
1. 选小模型（`qwen3:4b` ≈ 2.6 GB，显存压力小）
2. 设 `OLLAMA_KEEP_ALIVE=0`（请求结束立刻卸载）
3. 在 Bridge 层加"生成前确保 LLM 已卸载"的串行保障
4. 把 ollama 纳入 `gpu_arbiter` 的租约体系（否则它仍是 OOM 隐患）

---

## 5. 决策

辉哥拍板：**用 DeepSeek**。
配套改造见 REPORT37 §6（DEEPSEEK_* env 兜底 + 交互式 Key 注入）。
