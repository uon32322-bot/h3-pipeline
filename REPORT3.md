# MiniMax H3 FL2VA —— 完整「模特使用产品 + 音画同步」带货视频 实测报告

日期：2026-09-11
服务器：`connect.westc.seetacloud.com:39494`（RTX 4090 24G / ComfyUI 6006）
模型：`minimax_h3_fl2va_pruned_int8_convrot` + Turbo LoRA v4 + Sparse BF16 Triton
素材：粉色气垫粉底产品图 + 灰西装模特图（TT Image 2 同源编辑链派生状态帧）

---

## 一、核心结论

**跑通了一条 15 秒的完整带货视频：模特从举起产品 → 开盖 → 取粉扑 → 上妆 → 合盖展示，全程口播语音与画面口型同步。**

三条关键结论：

| # | 结论 | 证据强度 |
|---|---|---|
| 1 | **FL2VA 支持"音频锚点"** —— 可以把一段真实配音锚定到指定帧，输出音轨保留该配音内容，画面口型随之对齐 | 强（DTW 内容距离仅为基线的 1/6） |
| 2 | **音画同步是"内容级"的，不是"氛围级"的** —— 不是模型随便哼哼，而是精确复现了你给的那句话 | 强 |
| 3 | **动作过程可用但需规避** —— 举起/开盖/取粉扑/上妆/合盖 5 个动作全部完成；手部精细动作仍是弱项 | 中 |

---

## 二、音画同步的实现机制（本轮最大发现）

### 2.1 官方提示词规范：H3 本就是音视频一体生成

服务器上的《Video Prompt Writing Guide》明确了 H3 的任务定义：**T2VA / I2VA / FL2VA / L2VA —— 后缀 "VA" = Video + Audio**。

提示词有严格的三段式结构（此前我们用的是自由散文体，这大概是之前只出环境音的原因）：

```
How the reference pictures align with the target video — Picture 1 (from Shot 1) aligns with
the 0.00-second mark of the target video; Picture 2 (from Shot 1) aligns with the 3.04-second mark.

integrated_multimodal_description: [Shot 1] ... 说话人用 (S1) 编号，台词用 <d>[English] ...</d>
overall_soundscape: 环境音 + 物理动作音 + 非语言人声
non_diegetic_music: 只有观众能听到的配乐
```

### 2.2 更强的做法：把配音"锚"进去（AddGuide 的 audio 输入）

`MiniMaxH3AddGuide` 节点除了图片锚点，还支持 **audio 输入**：

```python
io.Audio.Input("audio", optional=True,
    tooltip="Soundtrack to anchor starting at the same frame index,
             cropped to the video's remaining duration.")
```

其实现（`nodes_minimax_h3.py:223-233`）把音频编码成 `audio_latent` 并写入
`keyframe["audio_latent"]`。**关键点是 keyframe 每一去噪步都被重新注入、不参与去噪** →
输出音轨保留我们给的音频，同时画面在同一个去噪过程里被"牵引"着对齐它。

于是音画同步从"祈祷模型自己对齐"变成了**可控工程**：

```
写文案 → TTS 出配音 → 转 32kHz → 作为 audio guide 锚定到段首 → H3 生成与它同步的画面
```

**成本几乎为零**：多注入 1 个音频锚点，单段耗时 100.6s（与无锚点的 3.0s 段同级）。

---

## 三、链路设计

### 3.1 分镜（5 段 × 3.0s = 15.2s）

| 段 | 起点帧 | 终点帧 | 画面动作 | 口播（S1） |
|---|---|---|---|---|
| ① | R_p1 | R_p2 | 右手把合盖气垫从胸前举到脸旁 | This cushion compact is my everyday base. |
| ② | R_p2 | R_p3 | 拇指按开盖子，镜面与米色粉芯露出 | One press, and the mirror does the rest. |
| ③ | R_p3 | R_p4 | 左手捏起粉色粉扑，举到胸前 | The puff picks up just the right amount. |
| ④ | R_p4 | R_p5 | 粉扑举到脸颊并轻贴 | A soft press, and it melts right in. |
| ⑤ | R_p5 | R_p6 | 移开粉扑、合盖、手持产品看向镜头微笑 | Light, fresh, and it lasts all day. |

### 3.2 状态帧链（6 个状态，含 3 张新生成）

```
R_p1 ──edit──> R_p2 ──edit──> R_p3 ──edit──> R_p4 ──edit──> R_p5 ──edit──> R_p6
(已有)         (已有)         (已有)         (取粉扑)       (粉扑贴脸)     (合盖收尾)
```

每段从自身起点派生。段③④⑤ 属新生成的"使用产品"环节，均由 TT Image 2 同源编辑完成。

### 3.3 出片参数

```
--length 73 (3.04s)  --mp 0.4  --steps 8  --aspect "9:16 (Portrait Widescreen)"
--audio vo_sN.wav@0        # 音频锚定在段首
```

段长 3.0s 的选择依据：口播句 2.0–2.5s，动作自然时长 1.2–2.0s，比值 ≈2×。
**未加中间锚点**（按"锚点按出错频率加"原则，先跑干净的，不预先堆锚点）。

---

## 四、验证结果

### 4.1 音频锚点确实生效 —— DTW 内容一致性

用 log-mel + DTW 比较"输出音轨"与"本句配音"的内容距离，并以"另一句配音"为对照：

| 段 | 输出 vs 本句 | 输出 vs 他句 | 本句 vs 他句（基线） | 判定 |
|---|---|---|---|---|
| ① | **0.0360** | 0.2203 | 0.2044 | ✅ |
| ② | **0.0435** | 0.1942 | 0.1933 | ✅ |
| ③ | **0.0512** | 0.1844 | 0.1653 | ✅ |
| ④ | **0.0514** | 0.1668 | 0.1350 | ✅ |
| ⑤ | **0.0498** | 0.2286 | 0.2313 | ✅ |

**5/5 段全部通过。** 输出与本句的距离只有与"另一句"距离的 1/4~1/5，甚至低于"两句话本身"的基线距离。
说明模型输出的不是"相似氛围的语音"，而是**内容上确实是你锚进去的那句话**。

> 佐证：短时频谱图上，输出音轨的音节分段与基频谐波结构与输入配音高度一致；
> 语音在 2.3s 左右结束，之后音轨转入静音 —— 与配音时长精确吻合。

### 4.2 口型与语音同步

**视觉证据**：`faces_5x12.png`（5 段 × 每段 12 帧）显示每段口型依次出现闭唇、微张、圆唇、大张等
多种发音口型，且都在语音结束后收回为闭唇微笑。

**量化证据**（嘴部 ROI 逐帧运动量 vs 音频响度包络，互相关）：

| 段 | 峰值相关 | 时滞 | 备注 |
|---|---|---|---|
| ① | **+0.529** | 0 帧（0 ms） | 信号干净 |
| ② | +0.361 | 5 帧（208 ms） | 开盖动作干扰 |
| ③ | **+0.483** | 0 帧（0 ms） | 信号干净 |
| ④ | +0.276 | 0 帧 | 粉扑遮脸干扰 |
| ⑤ | +0.161 | 13 帧 | 动作最大（放粉扑+合盖+展示） |

**解读**：动作幅度小的段（①③）给出零时滞、r≈0.5 的清晰同步信号；
动作大的段（②④⑤）由于"手/产品的运动与语音在时间上重叠"，测量被干扰 —— 这是**测量方法的局限，不是生成质量的下降**。

#### 对照实验（干净标定）

为排除上表中的干扰，额外跑了一个对照镜头：**首尾帧设为同一张图**（模特姿势完全不动）+ 同一个音频锚点。

| 指标 | 结果 |
|---|---|
| 画面是否静止 | 是 —— 嘴部 ROI 的帧差基线从 6.6 降到 0.1~0.8，无整体运动 |
| 音频内容一致性 | DTW **0.0357**（对照 0.2175）✅ |
| 口型-音频互相关 | **r = +0.437 @ 0 帧滞后（0 ms）** |
| 帧带观感 | 口型依次出现闭唇 → 圆唇 → 大张 → 微张 → 闭合，与音节节奏吻合 |

**在画面完全静止的条件下，仍有零时滞的显著相关** —— 说明运动只可能来自说话，
**坐实了口型确实在跟随语音**，而不是手部动作与语音的巧合相关。

产物：`vo/calib.mp4`、`vo/calib_face.png`

### 4.3 画面动作全部完成

5 段动作均按设计发生：举起 → 开盖（镜面与粉芯可见）→ 取粉扑 → **粉扑贴上脸颊** → 合盖展示。
其中"粉扑按压取粉"按此前建议做成"捏起悬于胸前"的构图，避开了"粉芯形变"这个 H3 弱项。

### 4.4 状态帧链一致性（累积漂移控制）

| 对比 | 纯背景区 MAE | 判定 |
|---|---|---|
| R_p3 → R_p4 | 1.93 | ✅ 好 |
| R_p4 → R_p5 | 2.31 | ✅ 好 |
| R_p5 → R_p6 | 2.07 | ✅ 好 |
| R_p1 → R_p4（跨 3 步） | **6.31** | ⚠️ 一般（累积漂移可见） |

**单步编辑背景一致性都很好**，但跨 3 步累积到 6.31（临界值 6）—— 与此前记录的"同源编辑链累积漂移"一致。
本次每步增量很小（约 2），实际观感未受影响。

---

## 五、局限与下一步

### 5.1 本轮的局限

1. **口型同步的量化方法在复杂镜头下不鲁棒** —— 手部动作与语音时间重叠时，互相关会把"手在动"误判为"嘴在动"。
   本轮已用"画面静止 + 只说话"的对照镜头完成干净标定（见 4.2），但**逐段自动化验证**仍需要更鲁棒的嘴部定位
   （当前靠"搜索相关性最高的窗口"，会被手部/粉扑动作抢占）。
2. **音频是"内容保留 + 音色重渲染"** —— 波形相关只有 0.27，DTW 内容距离 0.04。
   说明模型保留了语音内容/节奏，但音色与波形由它重新合成。要 1:1 复用音色需 Ref2VA。
3. **手部精细动作仍是弱项** —— 手怎么抓、接触面是否穿模、粉扑是否精确贴合，仍是"半合理"。
4. 每段尾部约 0.7s 是静音空转（动作与语音都结束），可后期裁掉做更紧凑的版本。

### 5.2 下一步可选

1. **裁尾紧凑版**：每段裁到"语音结束 + 0.25s"，成片从 15.2s 压到约 12.8s，语句衔接更连贯
2. **叠 BGM + 字幕**：H3 原生音轨已含环境音与拟音，后期叠观众向 BGM 即可（`non_diegetic_music` 也可直接写进提示词让模型生成）
3. **中文版**：把 `<d>[English] ...</d>` 与 TTS 换成中文，验证中文口型同步
4. **修累积漂移**：段④⑤ 改为从 R_p3 直接派生（不跨段链式），把背景 MAE 压回 3 以内
5. **上 Ref2VA**：若要跨条视频保持同一主播音色/脸

---

## 六、产物清单

### 成片
- `real/out/带货成片_完整_音画同步.mp4` —— 15.2s / 9:16 / 32kHz 立体声，5 段完整叙事 + 音画同步

### 分段
- `vo/seg1.mp4` ~ `vo/seg5.mp4`

### 状态帧链
- `real/R_p1.png` ~ `real/R_p6.png`（其中 R_p4/R_p5/R_p6 为本轮新生成）
- `vo/chain_grid.png` —— 6 状态总览

### 证据图
- `vo/faces_5x12.png` —— **5 段口型序列（音画同步的视觉证据）**
- `vo/calib_face.png` —— **对照实验帧带（画面静止、只有口型在动）**
- `vo/calib.mp4` —— 对照实验视频（首尾同帧 + 音频锚点）
- `vo/strip_face.png` / `vo/strip_mouth.png` —— 探针阶段口型序列
- `vo/spec_cmp.png` —— 探针：输入配音 vs 输出音轨 频谱对比
- `vo/s1_spec_cmp.png` —— 第①段：输入配音 vs 输出音轨 频谱对比
- `vo/seg1_face.png` —— 第①段 18 帧口型细节

### 工具（本轮新增，可复用）
| 脚本 | 作用 |
|---|---|
| `remote.sh` | 远程执行封装（自动重试 SSH 偶发 Permission denied） |
| `lipsync2.py` | **口型-音频同步量化**（自动搜索嘴部 ROI + 互相关） |
| `audio_match.py` | **音频内容一致性验证**（log-mel + DTW，判断输出是不是你锚的那句话） |
| `vo/storyboard.md` | 分镜与口播文案 |
| `vo/q_s1.txt` ~ `q_s5.txt` | 各段官方结构化提示词 |
