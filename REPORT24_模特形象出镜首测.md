# REPORT24 · 模特形象出镜首测（运动鞋 + 男模特）

**日期**：2026-09-18 00:35–00:52
**素材**：产品 `/Users/admin/Desktop/工具/测试产品/K1_服装鞋_运动鞋/主图.png`（已测）
　　　　模特 `/Users/admin/Desktop/工具/测试产品/模特形象/男模特.png`（1024×1536）
**产出**：`deliverables/model_real/MODEL_REAL_TEST.mp4`（768×1344 / 24fps / 124 帧 / 5.167s / **426.2s**）
**结论**：✅ **成功。模特出镜链路首次跑通，且是三条片子中信息量最大的一条。**

---

## 一、结论速览

| 验证项 | 结果 | 判定 |
|---|---|---|
| 合成「模特穿产品鞋」 | TT Image2 双参考图 → 鞋与产品**同款可辨**（黑网面/银白 rollout 弧线/黑包边鞋头/厚中底逐项对应） | ✅ |
| 模特身份保持 | 脸型五官发型笑容全对（脸部差异 6.44，属整图重采样） | ✅ |
| 画布适配 | 1024×1536 → **768×1344**，比例偏差 0.00000，接缝未进 top6 跳变 | ✅ |
| **推近运镜** | 全身 → 腰下 → **脚部特写**，平滑过渡 | ✅ |
| **H3 是否执行多镜指令** | 推近精确发生在 **m02→m03 之间（≈1.3–1.7 s）**，与提示词 `At 00:02.500` 一致 | ✅⭐ |
| **产品是否变色**（坑 1 复测） | 五帧 R−B 偏移最大仅 **+4.2**（上次运动鞋 **+43.7**） | ✅ **修法有效** |
| 产品是否被重绘 | 输入尾帧→成片尾帧逐级一致，**零重绘** | ✅ |
| 模特是否有异常动作 | 五帧姿势一致（右手插兜 / 左手垂放），无走动挥手 | ✅ |
| 未影响他人 | 共享实例 6006 → 200 ✅｜数字人 8383 → 200 ✅ | ✅ |

---

## 二、素材处理三步（模特片专用，已固化为流程）

| 步 | 动作 | 工具 | 实测 |
|---|---|---|---|
| ① 去水印 | 右下角「AI生成 / WORKBUDDY」 | `buddy-image-processing` skill `erase` | 全图平均 1.64，仅水印区 4.07 ⇒ 干净 |
| ② **双图合成** | 模特图（底图）+ 鞋产品图（参考）→「模特穿着这双鞋」 | **`ttimg.py edit`（多参考图）** | 脚部差异 **49.41**、背景 2.75、白T 4.43 |
| ③ 画布适配 | 1024×1536(2:3) → 768×1344(9:16) | **`canvas_fit.py`** | 按宽度缩放至 768×1152 + 上下各补 96 px 背景，**模特全身零裁切** |
| ④ 造尾帧 | 纯几何推近（焦点下移） | **`canvas_motion.py --focus`** | zoom 1.60 / focus 0.5,0.62 / light 0.04 |

---

## 三、⭐ 核心发现 1：多参考图合成可行 —— 但要分清「它擅长什么」

`ttimg.py` 的 `edit` **支持多张参考图**（`refs = sys.argv[5:]`）。实测「男模特换鞋」：

| 检查区 | 差异 | 判定 |
|---|---|---|
| 脚部鞋区 | **49.41** | ✅ 换鞋发生，改动聚焦 |
| 脸部 | 6.44 | ⚠️ 整图被重采样（**不是"只改局部"**） |
| 牛仔裤 | 8.30 | ⚠️ 附带改动（**裤脚被改短，露出脚踝**） |
| 背景 / 白 T | 2.75 / 4.43 | ✅ 基本未动 |

### ⇒ 关键判据（与 REPORT23 的结论并不矛盾，是**分工**）

| 任务类型 | 例子 | AI 图片编辑能否胜任 |
|---|---|---|
| **几何姿态改变** | 鞋身旋转 25°、产品翻转、开合 | ❌ **做不出**（REPORT23 实测）—— 它只会重绘材质 |
| **语义部件替换** | 把脚上的白鞋换成另一双鞋 | ✅ **能做出**，且产品的**语义特征能保住**（配色/图案/材质/鞋型大类） |

**生产含义**：模特片可以走「**TT 合成 → 几何推近 → H3**」链路。
⚠️ 但产品细节一致性仍需复核：本次是「**同款可辨**」，不是「逐像素一致」。
**这是"看起来是同一款鞋"，不是"产品图的数字复刻"** —— 对外商用前需人工过目。

---

## 四、⭐ 核心发现 2：H3 精确执行 `[Shot 2] At …` 的多镜指令

提示词写的是：

```
[Shot 1] … stands still … The camera begins a smooth slow push-in toward his sneakers.
[Shot 2] At 00:02.500 The camera has moved much closer: the sneakers now fill the lower frame …
```

**帧间差异的实测分布**：

| 帧间 | 平均差异 | 画面 |
|---|---|---|
| m01→m02（0 → 1.3 s） | 16.44 | 全身 → 全身（轻微推近） |
| **m02→m03（1.3 → 2.5 s）** | **51.86** | **全身 → 腰下（发生镜头切换）** |
| m03→m04（2.5 → 3.8 s） | 26.15 | 腰下 → 更近 |
| m04→m05（3.8 → 5.2 s） | 16.66 | 更近 → 脚部特写 |

⇒ **镜头切换精确落在提示词指定的 2.5 s 处**，而 2.5 s 对应帧号 60，m02=30 / m03=61 —— **逐帧对齐** ✅
⇒ **一条片内多镜（`[Shot 1]…[Shot 2] At …`）在本项目首次实测生效**，这是降生成次数杠杆（D3 三档杠杆排序第 1 位）的直接落地。

---

## 五、✅ 坑 1 的修法验证有效（REPORT23 遗留问题）

REPORT23 实测：提示词含 `warm light sweep` + 图上另加光带 ⇒ 两层信号叠加，H3 把**黑白鞋刷成金色**（R−B 偏移 **+43.7**）。

本次严格执行新规则（**提示词禁用一切颜色词 + `light` 参数为无色亮度渐变**）：

| 帧 | 鞋身 R−B | 相对首帧偏移 |
|---|---|---|
| m01 | −19.8 | 基准 |
| m02 | −19.4 | +0.4 |
| m03 | −15.6 | +4.2 |
| m04 | −17.3 | +2.5 |
| m05 | −17.5 | +2.3 |

⇒ **最大偏移 +4.2，相比上次的 +43.7 降低约 90%，已回到"色调稳定"区间** ✅
**规则**：色彩敏感品类（服装鞋帽 / 美妆 / 家居布艺 / 食品）**提示词禁用 warm / golden / amber / 暖 / 金 等一切颜色词**，改用 `a soft neutral light sweep`。

---

## 六、⚠️ 新发现：人物构图的硬约束（会决定推近幅度）

**推近会切头。** 全身站姿图里模特头顶距画布顶部往往只剩 ~100–120 px：
实测适配到 768×1344 后**头顶落在 y=118** ⇒ **zoom 超过约 1.18 就切掉头顶**。
⇒ 想"保留全身"就只能轻微推近（鞋几乎不变大）；想"看清鞋"必须接受**脸出画**。
⇒ **带货片的正解是"推近运镜"**：脸在前 2 s 出现建立信任，随后进入产品特写（即本轮的 Shot 1→Shot 2）。

| 尾帧方案 | zoom / focus | 首尾差异 | 画面 |
|---|---|---|---|
| 温和推近 | 1.25 / 中心 | 25.49 | ✅ 全身完整、脸清楚，鞋小 |
| **推近到脚部（采用）** | **1.60 / 0.5,0.62** | **52.44** | ✅✅ 鞋清晰、脸出画 |
| 大幅特写 | 1.95 / 0.5,0.86 | 69.71 | ✅ 鞋极大，跨度偏大 |

**顺带修掉一个工具缺陷**：`canvas_motion.py` 的 `pan` 原用 `np.roll`，
实测 `zoom 1.85 --pan 0,300` 时把溢出内容**环绕**回来，边缘行回填后**画面顶部出现一整条拉伸的肤色带**（模特头被切 + 脖子以上是纯色块）。
⇒ **已改为 `--focus fx,fy` 焦点裁切窗口**（放大图层面上自由选窗口 + clip 到合法范围），既不露黑边也无环绕伪影。
⇒ **伪影检测法**：看**顶行/底行的横向 std**，若 ≈ 0（整行一个色）就是被拉伸的色带伪影。

---

## 七、保真链路逐级验证（产品零失真）

| 环节 | 与上一环的差异 | 判定 |
|---|---|---|
| 产品原图 → TT 合成图脚部 | 同款可辨（黑网面/银白弧线/黑包边鞋头/厚中底） | ✅ 语义保真 |
| TT 合成图 → 输入首帧（canvas_fit） | 零重绘几何缩放 + 补背景 | ✅ 逐像素可控 |
| 输入首帧 → 成片首帧 | 平均 **4.67**（产品区 4.97） | ✅ 忠实（复现噪声基线 1.08，换 seed 差异 14.0） |
| 输入尾帧 → 成片尾帧 | 鞋型/图案/配色全对，**零重绘** | ✅ |

四方对比图见 `deliverables/model_real/09_鞋保真四方对比.png`。

---

## 八、耗时数据（产线公式第三次复现）

ComfyUI 日志 tqdm 逐行原文（**权威**）：

```
 12%|█▎        | 1/8 [00:47<05:35, 47.97s/it, Model Initialization complete!]
 25%|██▌       | 2/8 [01:31<04:33, 45.63s/it]
 50%|█████     | 4/8 [03:03<03:04, 46.22s/it]
100%|██████████| 8/8 [06:08<00:00, 46.28s/it]
[INFO] Comfy model compiler graph breaks: 3, rogues: 0
[INFO] Prompt executed in 424.49 seconds
```

| 阶段 | 耗时 |
|---|---|
| ① 冷启动（权重按需载入 + TorchCompile + 第 1 步） | **47 s** |
| ② 第 2–8 步（7 个间隔） | **321 s ⇒ 稳态步速 45.86 s/步** |
| ③ VAE 解码 + 编码保存 + 收尾 | **56.5 s** |
| **合计（ComfyUI 权威）** | **424.49 s**（客户端 elapsed 426.2 s） |

### 三条片子横向对比

| 片子 | 产物 | 大小 | 耗时 | 公式 `64 + 45.9×N` 预测 |
|---|---|---|---|---|
| TEST（真空杯） | `TEST_00001_.mp4` | 661 KB | 430.75 s | 431.2（偏差 −0.1%）|
| SHOE（鞋特写） | `SHOE_00001_.mp4` | 1,482 KB | 429.4 s | 431.2（偏差 −0.4%）|
| **MODEL（模特片）** | `MODEL_00001_.mp4` | 1,100 KB | **424.49 s** | 431.2（偏差 **−1.6%**）|

⇒ **公式三次命中（最大偏差 1.6%）**，可作产线容量规划的可靠依据。
⇒ `Comfy model compiler graph breaks: 3` **三次稳定复现** —— 那 3 处图断裂是确定存在的 T1 优化点。

---

## 九、交付物

```
deliverables/model_real/
├── MODEL_REAL_TEST.mp4              成片（768×1344 / 24fps / 5.167s / 自带 AAC）
├── m01.png … m05.png                 成片五帧
├── 01_模特去水印_1024x1536.png        素材净化后
├── 02_合成_模特穿产品鞋_1024x1536.png   TT 双图合成结果
├── 03_首帧_768x1344.png               画布适配后
├── 04_尾帧_推近到脚部.png              几何焦点推近
├── 05_产品鞋对比.png                   产品原图 vs 合成鞋
├── 06_脸部保真对比.png                 模特身份核验
├── 07_推近方案对比.png                 三档推近方案
├── 08_成片五帧序列.png                 运镜全序列
└── 09_鞋保真四方对比.png               产品零失真链证
```

---

## 十、下一步建议

| 优先级 | 事项 | 说明 |
|---|---|---|
| **P0** | **14 个产品批量前置** | 全为 1024×1024 方图，**全部需画布适配**；水印检测规则**会漏检**（运动鞋实测漏判），必须人工过目 |
| **P0** | **模特片纳入产品线标准形态** | 与"产品特写片"配对交付：特写片讲产品，模特片讲"真人穿着效果" |
| P1 | 女模特同流程验证 | `女模特.png` 同规格（1024×1536），流程可复用 |
| P1 | 多镜提示词标准化 | `At 00:02.500` 已证实生效 ⇒ 可推广为"一条片内多镜"的标准写法 |
| P2 | T1 提速档（VAE Speedup） | 消除那 3 处图断裂 + 批量 tile 解码 |
