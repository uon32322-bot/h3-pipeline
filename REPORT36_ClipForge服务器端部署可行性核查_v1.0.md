# REPORT36 · ClipForge 服务器端部署可行性核查

> 日期：2026-09-19
> 起因：辉哥问「它可以装在服务器端吗」
> 性质：**可行性核查**——回答"能不能装 / 该装哪 / 要改什么"，并对 `REPORT20` 做两处修正
> 上游：`REPORT19 §C.2`（选型）、`REPORT20`（融入方案）、`REPORT21 §S1`（编排）

---

## 0. 结论（一句话）

> **能装，官方就是为服务器自托管设计的。**
> 但「装在一台服务器」≠「装 3090 那台生成机」——后者仍然否决，
> **理由从「性能不够」改成「共享机器纪律 + 项目隔离」**（见 §3）。

---

## 1. 实测证据（本次核查，非引用文档）

### 1.1 官方容器镜像（实测自 GHCR registry API）

| 项 | 实测值 |
|---|---|
| 镜像地址 | `ghcr.io/xixihhhh/clipforge` ✅ 存在 |
| 部署命令 | `docker run -d -p 3000:3000 -v clipforge-data:/data ghcr.io/xixihhhh/clipforge` |
| 标签总数 | **222**（含 113 个版本标签 + 109 个 `sha-` 提交标签） |
| 版本区间 | `0.6.0` → **`0.9.7`**（与桌面版 `ClipForge-0.9.7-arm64.dmg` **同版本**） |
| **平台架构** | 🔴 **仅 `linux/amd64`** —— 实测 `0.9.7` / `0.9.5` / `latest` 三个标签的 manifest 均为**单一 amd64**，**无 arm64** |
| 发布节奏 | 随 Release 自动构建（100+ 次推送，最高到 0.9.7） |

### 1.2 运行依赖（实测自官方 README）

| 项 | 要求 |
|---|---|
| 技术栈 | Next.js 16 + React 19 + TypeScript 5 |
| 数据库 | SQLite + Drizzle ORM（启动自动 migrate） |
| 系统依赖 | **镜像内置 ffmpeg + 中文字体** ⇒ 容器内零额外安装 |
| 持久化 | `-v clipforge-data:/data`（项目 / 商品图 / 成片） |
| **GPU** | **无需求**（AI 生成走云端 API） |
| 包管理器 | pnpm（源码路线必须用 pnpm，**不能用 npm**） |

---

## 2. 🔴 由此暴露的两个坑（REPORT20 需要修正）

### 坑 1 · 官方镜像**没有 arm64** ⇒ REPORT20 §5 步骤②在 Mac 上跑不起来

- REPORT20 部署清单第②步写的是「Docker 起 ClipForge（`:3000`）→ 本地 Mac」。
- **实测：该镜像只有 `linux/amd64`。** 在 Apple Silicon Mac 上 `docker run` 会报
  `no matching manifest for linux/arm64/v8`。
- ⇒ **Mac 上的正确路径只剩两条**（Docker 只能靠 Rosetta 模拟跑，慢且有兼容风险）：
  | 路径 | 命令 / 载体 | 备注 |
  |---|---|---|
  | **Electron 桌面版** | `ClipForge-0.9.7-arm64.dmg`（辉哥已下载到 Downloads） | ✅ **Mac 上首选**，版本最新 |
  | 源码 dev | `pnpm install && pnpm dev` | 需 Node + FFmpeg + pnpm ≥ 10 |

### 坑 2 · 「CPU 争抢」这条理由被我写宽了

- REPORT20 红线 1 的理由是「FFmpeg 合成与 Node 进程会吃满 CPU」。
- **精确化**：FFmpeg 合成只在**使用它自己的出片链路**（`create` / `compose`）时才发生 ——
  而 REPORT20 §1 已明确**不启用形态 C（不出片）**。
- ⇒ 我们只用文字接口时，常驻的只是一个**空闲 Node 进程**（脚本生成是 LLM API 网络往返，本地 CPU ≈ 0）。
  **这条理由在我们自己的使用方式下基本不成立。**
- ⚠️ 保留它的唯一场景：**哪天要启用它的出片链路，这条立刻复活。**

---

## 3. 该装哪台？三处判定

### 3.1 ❌ 3090 生成机（`nmb2`）—— 仍然否决，但换理由

原「内存不够」这条经实测**不成立**：

| 判据 | 实测（REPORT22 §2） | 结论 |
|---|---|---|
| 容器内存上限 | `memory.max = 90.0 GiB` | — |
| 当前占用 | `memory.current = 88.0 GiB`（97.8%） | 看着爆满 |
| 但 anon（真实进程内存） | **28.7 GiB** | 只占上限 **31.9%** |
| 其余 | `file` page cache **58.5 GiB** | ✅ **可回收弹性缓冲** |
| 压力 / OOM | `some avg10=0.00` / **历史 0 次 OOM** | **零内存压力** |
| 出片活体实证 | anon 涨 +14.8 GiB，file 同步被丢 −14.9 GiB，总量不动 | **内核自己腾地方** |

⇒ 加一个 300–500 MB 的 Node 服务，只是让内核少缓存 0.5 GB。**内存不是理由。**

**真正成立的否决理由（按硬度排序）**：

| # | 理由 | 级别 |
|---|---|---|
| 1 | **共享机器纪律** —— 这台机器跑着**别人的项目**（heygem 数字人 + tts-gateway，辉哥红线「不碰、不停、不改」）。反向同理：不该往里加常驻服务 | 🔴 |
| 2 | **重启不回生** —— AutoDL 容器重启后靠 `restore_services_nmb2.sh` 才拉起服务；多一个自建常驻服务 = 多一份要维护的恢复脚本 | 🔴 |
| 3 | **LLM 指向** —— 绝不能让它用 3090 上的 qwen3:14b（漫剧业务），必须远端 API | 🔴 |
| 4 | **端口可达性** —— AutoDL 只暴露 SSH 端口，访问 `:3000` 还得开隧道，**那还不如直接跑本地** | 🟠 |
| 5 | **隔离与可复现** —— 项目规范 D6「完全独立、零软链」+ 环境冻结原则，常驻服务是新增变量 | 🟠 |

### 3.2 ✅ 独立 x86 Linux 服务器 —— **可行，而且这才是它的主场**

- 1–2 核 / 2 GB 内存即可（CPU 常驻占用 ≈ 0，内存 300–500 MB）。
- 前提：**amd64** 架构（arm 不行，见坑 1）。
- 优点：**7×24 常驻**、不依赖辉哥 Mac 开机。
- 待核：`a800`(36.137.238.251) / `hao`(43.136.35.203) 两台的**资源余量与 Docker 可用性** —— **本次未测**。

### 3.3 ⚠️ 一键视频网站后端 —— **必须服务器端，但被 AGPL 卡着**

- 网站不能依赖辉哥本机 ⇒ 要内置就必须上服务器。
- **但 AGPL-3.0**：改造成对外提供的 SaaS **必须按 AGPL 开源修改版**。
- ⇒ 这条是**先决条件，不是部署细节**：要么开源，要么自研文案层替代。

---

## 4. 与「装在哪」无关的一条成本提醒

它**自己出片**那条链（`create` / `compose` / `carousel` 等）会调**云端付费模型**
（Atlas / fal.ai / 火山引擎）—— 这才是它真正烧钱的地方。
⇒ 我们只取文字接口，这条链不开，**与部署位置无关**。

---

## 5. 修正后的部署清单（替换 REPORT20 §5 步骤①②）

| 步 | 动作 | 位置 | 验收 |
|---|---|---|---|
| ① | 装 `script-judges` skill | WorkBuddy（Mac） | skill 可加载；零资源占用 |
| ② | **Mac 路线**：装 `ClipForge-0.9.7-arm64.dmg`（桌面版） | 本地 Mac | 双击可启动，`:3000` 可访问 |
| ②' | **服务器路线**：`docker run -d -p 3000:3000 -v clipforge-data:/data ghcr.io/xixihhhh/clipforge` | **独立 amd64 机器** | `curl localhost:3000` 返回 200 |
| ③ | 配 LLM Key（**远端 API**，严禁指向 3090 的 qwen3:14b） | 设置页 | 生成一条测试脚本成功 |
| ④ | 建商品库（贴链接导入） | ClipForge | 抓取标题/价格/图成功 |
| ⑤ | 确认分镜表 JSON schema 两端可解析 | `shotlist_schema.json` | 双方都能解析 |
| ⑥ | 端到端连通测试 | 两侧 | 脚本 → 15 列 → 闸门 → 生成 跑通 |
| ⑦ | 隔离验收 | 3090 服务器 | `docker ps` **无 clipforge**；`nvidia-smi` 无相关进程 |

---

## 6. 诚实边界

- ✅ **本次实测**：GHCR 镜像存在 / 222 个标签 / 版本到 0.9.7 / **仅 amd64 无 arm64** / `latest` 与 `0.9.7` 同平台 / README 依赖清单。
- ❓ **未测**：`a800`、`hao` 两台服务器的资源余量与 Docker 可用性；Mac 上 Docker 走 Rosetta 跑 amd64 镜像的实际表现；服务器端部署后的真实内存/CPU 占用（300–500 MB 是官方文档值，非我方实测）。
- ⚠️ **仍未测**：ClipForge 的文案质量是否真优于我方自研 prompt（需 3–5 个真实商品盲测，见 REPORT19 §E）。
- ⚠️ 镜像 tag 会随其发版继续前移 ⇒ **部署时锁版本**（与"环境冻结"原则一致），不要用 `latest`。
