# REPORT7 — 电商/带货短视频：用 MiniMax H3 做到「可靠 + 不降质」的方案分析

> 依据：本机 4090 / ComfyUI 上 H3 FL2VA + Ref2VA 的**全部实测数据**（REPORT2 / 3 / 5 / 6），
> 叠加 H3 官方能力口径（2026-09）与国内平台 AI 内容合规要求（2025-09 ~ 2026-05）。
> 结论区分「实测事实」与「推断」两类，均标注。

---

## 0. 先破题：「崩坏」不是一件事，是七件事

这是整份分析的前提。用户说的"崩坏"实际混了七种性质完全不同的失效，它们的可控性差 **10 倍以上**。
不分清就会得出错误结论（"AI 视频不可控"或"换个模型就好了"）。

| # | 崩坏类型 | 典型表现 | 可控性 | 解决手段 | 依据 |
|---|---|---|---|---|---|
| A | **画质崩坏** | 糊、噪、颗粒、塑料感 | ★★★★★ 最可控 | 分辨率/步数/量化档位 | 实测 |
| B | **状态不守恒** | 液量不变、盖子自合、用量不减 | ★ 最难 | **无法用提示词解决 → 只能实拍或改动作** | 实测 |
| C | **接触物理崩坏** | 手指穿插、握持点粘连、多指 | ★★ | 避免持续形变接触（涂抹/按压） | 实测 |
| D | **主体一致性漂移** | 产品外观变、脸变、logo 变 | ★★★★ | **首尾帧同源**（同源编辑链） | 实测 |
| E | **时序结构崩坏** | 自作主张切镜头、动作提前结束、动-停-动 | ★★★★ | 段长 = 动作自然时长 × 1.3–1.5；锚点 ≤1 个 | 实测 |
| F | **文字/商标崩坏** | 中文、价格、logo 画不准 | ★★★★★ | **移出模型 → 后期叠** | 实测 |
| G | **指令失效** | 参考图/参考视频/参考音频不生效 | — | 换链路或不依赖（见 §3） | 实测 |

**一条判据回答 90% 的"这个动作能不能 AI 做"：**
> **「这个动作有没有可验证的物理结果？」**
> 有（涂开后皮肤变了、倒了之后瓶里少了、拧开后盖子分离）→ **必实拍**。
> 无（举起、并排、推近、光影流动、气泡升腾）→ **AI 够用**。

⚠️ 扩散模型学的是**帧间视觉统计**，没有因果链。所以它**局部像、全局不像** ——
这是 B/C 类崩坏的根本原因，不是提示词写得不好，也不是换个模型能解决的。

---

## 1. 直接回答 Q2：纯卖点讲解（不做演示）是不是更容易？

**方向对，但需要再往下拆一层，否则会踩坑。**

"不做演示"混了两个独立的轴：

- **轴 X：有没有操作因果链**（要不要展示"用了之后发生什么"）→ 决定 B 类崩坏
- **轴 Y：人在不在画面里**（要不要露脸/口播/手部）→ 决定 C 类崩坏 + 口型同步风险

按这两轴把形态排开，难度阶梯是：

| 档 | 画面形态 | 崩坏风险 | 剩余风险 | 推荐度 |
|---|---|---|---|---|
| **①** | **产品动态化 + 画外音**（产品特写、运镜/光影/气泡/雾化，无人） | **最低** | 只剩 A 类画质 | ★★★★★ **主力** |
| ② | 口播讲解（人说话 + 产品相对静止） | 低-中 | 手部自由生成 + 口型 | ★★★☆ |
| ③ | 单步操作演示（局部刚体动作） | 中 | B/C 类局部 | ★★★ |
| ④ | 多步流程演示（涂抹、拆盒、穿戴、连续上妆） | **最高** | B+C+E 全中 | ★ **不建议** |

### 关键结论：最稳的不是 ②（真人讲解），是 ①（画外音 + 产品动态化）

因为 ② 里的"人"带来三个不可控变量：**口型**（要跟音频对）、**手**（自由生成）、**表情**。
① 把这三个全去掉，只留"产品"一个主体 —— 产品是刚体、没有表情、没有因果链。

① 还有一个**隐藏优势，对带货是决定性的**：
> **用户上传的真实产品图可以直接当首帧 → 产品真身 100% 保真。**

而 ②③ 里，产品要先跟人合成到一张图里，多一道漂移风险。
对带货而言，"产品像不像真的"直接决定合规与转化，这一条比任何画质指标都重要。

### ⚠️ 两个必须避开的陷阱

1. **"卖点讲解"很容易滑回 ④。** 一旦文案写成"看我怎么用/我教你怎么做"，
   画面就不得不出现操作动作 → 立刻掉进 B/C 类崩坏。
   → 文案必须写成 **陈述 / 成分 / 对比 / 价格 / 场景 / 结果**，不写"演示流程"。
2. **口播文案要和画面动作解耦。** 一句话 ≈2.5s、段长 3.0s。
   别写"现在我把它拧开"这种**需要画面同步**的句子（两套约束会打架，这是最难组合）。

---

## 2. 直接回答 Q3：找对标视频 + 产品替换 / 模特替换 / 动作迁移，能降低崩坏吗？

**不能。这条路会同时踩上技术、原理、合规三个坑，其中两个是硬的。**

### 2.1 先给对照表

| 子任务 | 本机 ComfyUI | 官方 API | 合规 |
|---|---|---|---|
| **动作迁移**（`<Video k>` / `reference_video`） | ❌ **实测零效果** | ⚠️ 支持，但"解释后重生成"，非逐帧复制 | 中（用他人视频 → 著作权） |
| **产品替换** | ❌ 需图像编辑/重绘模型（**本机未装**） | ⚠️ In-context Regeneration | 中 |
| **模特替换 / 换脸** | ⚠️ 无专用模型；只有"同源编辑链"能锁脸 | ⚠️ | **❌ 红线（明令禁止）** |

### 2.2 坑一（实测事实）：本机的动作迁移 = 零效果

刚做完的决定性实验（REPORT6）：`LoadVideo → GetVideoComponents.images → ref_videos["ref_video_0"]` 接线已用 `/history` 反查核验**是通的**，但输出对参考视频**完全不响应**：

| 扰动 | 帧差（0–255） | 音轨 PCM |
|---|---|---|
| 换参考视频（换人 / 换场景） | 0.9 | **逐位相同** |
| 时间反转 | 1.09 | **逐位相同** |
| 静止帧 | 1.08 | **逐位相同** |
| 完全不接视频 | 0.96 | **逐位相同** |
| （噪声基线：同素材改名重跑） | **1.08** | 逐位相同 |
| （灵敏度对照：只改稀疏注意力档位） | **22.91** | r=0.58 明显不同 |

→ 所有扰动都落在复现噪声内，而真正的模型级改动留下 **20 倍大**的痕迹 ⇒ **装置灵敏，结论成立**。
关掉稀疏注意力（`--sparse-off`）结论不变 ⇒ 不是被稀疏注意力丢段。
加参考视频的**边际耗时 ≈ 0**（对照：参考音频同样零效果，却让耗时 +96%）⇒ 视频帧没进任何重计算。

**但必须区分清楚（重要）：**
> 这是**本地 ComfyUI 适配层**的问题，**不等于模型没有这个能力**。
> 官方 API 明确支持 V2V Motion Transfer（`role: "reference_video"`）。
> → **要走路，只能走官方 API，不能走本地。**

### 2.3 坑二（原理）：H3 没有"替换"这个操作

直觉之所以觉得"抄对标视频更简单"，是因为把视频模型当成了**剪辑软件** ——
以为能像图层一样"把产品换掉、把模特换掉、动作留着"。

**但 H3 是生成模型，它的编辑是"重生成"，不是"局部手术"。**（官方口径原文：
*"H3 is not a conventional editor where you select a layer, replace a word, and leave every unaffected pixel untouched... H3 edits by regeneration, not by surgically modifying the original file."*）

后果：
- 替换产品 → **手、遮挡关系、光照、阴影、背景全都会跟着被重新生成** → 这些都是**新的崩坏源**，不是你省下的
- 动作迁移无法把"动作"和"镜头运动 / 构图 / 遮挡"解耦 → 官方自己提示：对需要精确复刻的编排，
  **要拿输出和源时间轴逐帧比对，不要假设 reference = 精确复制**
- 参考塞得越多，指令冲突越多：**"9 张参考不是自动比 3 张好"**，两张产品图包装不一致 → 模型只能猜

换句话说：**这条路把崩坏从"模型的随机性"换成了"不可控的大范围重生成"，并没有降低总量。**

### 2.4 坑三（硬红线）：合规

这一条不是"概率"问题，是**"做出来能不能投放"**的问题。

- **《人工智能生成合成内容标识办法》**（2025-09-01 施行）：AI 生成的音视频必须带**显式 + 隐式**标识；
  用户发布时**必须主动选择**标识。2026-05 起，中央网信办已把内容标注做成**发布必经环节**（6 类必选标签，
  含"含有 AI 生成内容""含有营销信息"），未规范标注的存量内容要**分批补标**。
- **北京消协 + 京东/美团/拼多多/唯品会/抖音/快手/小红书/微信直播 8 平台《促进 AI 技术规范应用承诺书》**（2025-12）：
  **"严禁利用 AI 合成换脸技术仿冒名人明星带货"**；未标识的疑似 AI 数字人 / AI 虚拟场景 → **屏蔽链接**。
- **著作权**："找对标视频"意味着以他人的**视听作品**为素材基底，涉及著作权 + 表演者权。
  直接拿商用竞品的投放素材，风险最高。

**"找对标视频 + 模特替换"最危险的形态，正是"把一个有辨识度的脸换进我的视频"——
直接撞在明令禁止的红线上。** 即便换成普通素人，用他人视频作基底仍有版权问题。

→ 结论：**作为量产方案，这条路的 ROI 是负的** —— 技术收益为 0 或负，法律风险为正。

---

## 3. 那什么才是"最可靠且不降质"？

核心思路一句话：

> **把可控性从"模型能力"转移到"素材与结构"。**
> 不和模型的弱点对抗，而是**绕开它** —— 让它只做它擅长的那一小段。

三根支柱：

1. **把动作难度降级** —— 只做白名单内的动作 + 段长匹配动作自然时长
2. **首尾帧同源** —— 这是画面一致性的生命线（两次独立生成比不做首尾帧**更糟**）
3. **把不可控模块移出模型** —— 文字/商标/音色/多步流程全交给后期

### 3.1 三条生产线（按可靠性排序）

#### S1 — 产品动态化（**最稳，首发推荐**）

```
用户上传产品实拍图（直接当首帧，真身 100% 保真）
  → I2V / Ref2VA 让"环境动、产品静"
     安全运镜：缓慢推近 / 拉远、光影色温流动、环境元素轻动、轻微手持感
     产品动态：蒸汽 / 气泡 / 雾化 / 液面反光 / 精华晕开（"结果状态"动态化）
  → 画外音解说（TTS）
  → 后期叠字幕 / 价格 / logo
```

- 可靠性：**最高**（无人、无手、无口型、无因果链；只剩 A 类风险）
- 画质选型：用 **Ref2VA** 出画（肤质/妆效/锐度明显更好 + 支持提示词级运镜 `camera pushes in slowly`），音轨后置贴
- 单段 5–6s 一段到底，无需拼接

#### S2 — 单步操作演示（动作白名单）

```
产品图 + "操作后状态图"（用图像编辑从产品图派生，或直接实拍）
  → FL2VA 首尾帧 + 中间锚点（AddGuide）
  → 短段 2–3s
```

**动作白名单**（特征：局部、刚体、小位移、结果确定）
拧开瓶盖 ｜ 揭盖 ｜ 拉环 ｜ 抽出纸巾 ｜ 挤出膏体 ｜ 按下泵头 ｜ 倒出液体 ｜ 并排对比 ｜ 举到镜头前

**动作黑名单**（不建议自动化）
涂抹（大面积形变 + 手部接触）｜ 拆盒（多步骤）｜ 穿戴 ｜ 复杂手部交互 ｜ 任何需要"来回"的动作

- 可靠性：中高
- 铁律：**首尾帧必须同源**。底图只生成一次，后续状态用**图像编辑**从前面派生
  （⚠️ 每段从**自己的起点**派生，不要跨段链式派生 —— 实测 4 步累积漂移 27% 画幅）

#### S3 — 口播带货（音画同步）

```
真人形象底图 → FL2VA + AddGuide(audio) 锚定 TTS 配音 → 每句一段，段长 3.0s
```

- 可靠性：中（口播可控，但**手部仍自由生成** → 镜头上尽量别放手部动作）
- 实测：输出音轨 vs 本句配音 DTW 内容距离 **0.036**（= 输出就是你锚的那句）；口型 r=**+0.437**@0 帧
- 音色是"内容保留 + 音色重渲染"（波形相关仅 0.27）→ 要 1:1 复用音色需后置换轨

#### S4 — 素材来源：自建「动作原子库」（**替代"对标视频"的正解**）

不找别人的视频，而是**自己/同事拍一次动作，终身复用**：

- 约 **20 个原子动作**即可覆盖绝大部分带货场景（拧、揭、拉、按、倒、涂、举、比、放、开盒…）
- 拍**无声空镜**，只提供"动作数据"，不出现别人的脸和品牌
- 一次性解决三个问题：**版权（自拍自有）｜ 检索精度（自己标签化，不靠文本搜）｜ 预处理（按 2–5s / 24fps 直接拍）**

### 3.2 不降质的硬约束清单

| 项 | 规格 | 理由 |
|---|---|---|
| 分辨率 | 本地天花板 **768p**；试镜档 0.4MP | 2K 依赖**闭源**的 H3-Regenerate-2K 模块 |
| 步数 | **8 步**（不要 4 步 turbo） | 低步数在参考类任务上会失效 |
| 段长 | 2–4.5s，动作窗落在**片段前 40–60%** | 模型实动时间恒定约 1.4–2.3s，与段长无关 |
| 中间锚点 | **≤1 个**，且选"运动中"的一帧 | 2 个以上出现"动-停-动" |
| 文字 / 商标 | **一律后期叠** | 模型画中文/logo 一律不准 |
| 音轨 | FL2VA 锚定 或 Ref2VA 出画后置贴 | — |
| 编排 | 一条 30s 成片 ≈5 段 | **单段 5s 是优势不是劣势** → 按"一步一镜"拆 |

### 3.3 双链路分工（画质 vs 锁脸，必须二选一）

| 想要 | 用 | 代价 |
|---|---|---|
| **画质 + 单段长镜头 + 提示词级运镜** | **Ref2VA** | ❌ 锁不住脸、参考图/视频/音频全部无效 |
| **跨镜头保持同一张脸** | **FL2VA 同源编辑链** | ⚠️ 偏磨皮、需多段拼接 |
| 折中 | Ref2VA 出画 + 后置贴音轨；脸的一致性靠"底图只生成一次 + 同源派生" | — |

### 3.4 算力与产能（4090 单卡 / 0.4MP / 8 步）

| 链路 | 耗时 |
|---|---|
| FL2VA 2.0s 段 | 81–86s |
| FL2VA 3.0s 段 | 100–105s |
| FL2VA 4.5s 段 | 130–135s |
| Ref2VA | 1.10–1.28 s/帧（141 帧 ≈135–180s） |

一条 30s 成片（5 段）≈ **10–15 分钟**；24h 理论产出 ≈70–100 条/卡（按 70% 出片率）。
→ **建议两档渲染**：试镜档（低分辨率 / 短段）先确认，再出成片档，避免算力浪费。

### 3.5 品类可行度分档

| 档 | 品类 | 做法 |
|---|---|---|
| **直接出片** | 美妆护肤、食品饮品、服饰鞋包、家居香氛、珠宝首饰 | S1 产品动态化为主 |
| **生成 + 后期** | 3C 数码、家电厨具、大件汽车、母婴玩具、工具仪器 | S1 + 局部 S2，重后期 |
| **不建议纯生成** | 医疗保健、药品酒类、精确 SKU 展示、**AI 生成真人代言**、功效宣称 | 必须实拍 |

---

## 4. 落地路线图（建议执行顺序）

| 步 | 动作 | 目的 |
|---|---|---|
| ① | 先做 **S1 产品动态化** 4–6 条（不同品类） | 验证"可靠性 + 转化"是否够用，风险最低见效最快 |
| ② | 同步开拍 **「动作原子库」**（20 个动作） | 为 S2 铺路，一次投入终身复用，规避版权 |
| ③ | S2 按白名单**逐个上**，每个动作出一份自检报告 | 用 `frames.py` / `align.py` / `density_roi.py` 把"感觉"变成数字 |
| ④ | S3 口播片用于**人设/复购**内容，不作主力转化素材 | 规避手部+口型的双约束打架 |
| ⑤ | 若确需"对标视频的动作" → **走官方 API 的 `reference_video`** | 本地已证无效；先跑 3 条验证是否真迁移 + 做版权评估 |

### 零成本自检（花 GPU 前必做，能拦掉大部分废片）

```bash
python3 scripts/frames.py metrics p1.png p2.png   # 纯背景区 MAE < 6 为佳
python3 scripts/align.py   p1.png p2.png          # 尺度漂移 <2%、位置漂移 <5px
```

三条判据：① 除目标区域外有大面积差异 → 返工；② 产品边缘线不重合 → **一定会 morph**；
③ 避开"首帧被遮挡、尾帧才露出"的镜头设计（那块区域是纯凭空生成的）。

出片后：`density_roi.py`（动作是否真发生）→ `state_track.py`（状态是否落在正确帧号）
→ `audio_match.py` + `lipsync2.py`（做了口播才跑）。

---

## 5. 一页总结

| 你的问题 | 结论 |
|---|---|
| 怎么做最可靠、不降质？ | **把可控性从"模型能力"转移到"素材与结构"**：动作降级到白名单 + 首尾帧同源 + 文字/音色/多步流程移出模型交给后期 |
| 纯卖点讲解是否更容易？ | **是，但最稳的不是"真人讲解"，是"画外音 + 产品动态化"（无人）** —— 用户产品图直接当首帧，真身 100% 保真。文案必须写成陈述/成分/对比，**不能写成"演示流程"** |
| 对标视频 + 替换 + 动作迁移能降崩坏吗？ | **不能。** ①本机动作迁移**实测零效果**（要走路只能走官方 API）②H3 的编辑是"重生成"不是"替换图层"，替换产品会连带改掉手/遮挡/光照 → **新崩坏源** ③**合规红线**：AI 换脸仿冒明星带货明令禁止，用他人视频涉著作权 |
| 崩坏能不能根治？ | **不能根治，但能分层管理。** A/D/E/F 类可控；**B 类（状态不守恒）目前无解 → 只能实拍或改动作**。判据：「这个动作有没有可验证的物理结果？」 |

**一句话：H3 的正确用法不是"让它演完整套流程"，而是"让它只补它擅长的那一小段"——
其余的交给实拍素材、结构约束和后期。**
