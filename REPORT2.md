# MiniMax H3 FL2VA —— 段长 × 锚点密度实测 & 真实带货视频跑通报告

日期：2026-09-11
服务器：`connect.westc.seetacloud.com:39494`（RTX 4090 24G / ComfyUI 6006）
模型：`minimax_h3_fl2va_pruned_int8_convrot` + Turbo LoRA v4 + Sparse BF16 Triton
图片模型：TT Image 2（`tt-image-2`，灵炫国际站）

---

## 一、结论速览

### ① 段长不改变"动作能持续多久"，只改变"动作占多少比例"

实测同一对首尾锚点、同一 seed、同一 8 步，只改段长：

| 段长 | 帧数 | 动作窗口 | 动作占比 | 单段耗时 | 落到尾帧的 MAE |
|---|---|---|---|---|---|
| 2.0s | 56 | 0.00–1.38s（1.38s） | **58%** | 85.7s | 6.49 |
| 3.0s | 73 | 0.00–1.62s（1.62s） | 54% | 100.1s | 6.71 |
| 4.5s | 107 | 0.25–2.50s（2.25s） | 50% | 130.1s | 6.35 |
| 5.2s | 124 | 0.12–2.00s（1.88s） | 37% | ~145s | 6.83 |

**关键：无论段长多少，模型实际"在动"的时间都只有约 1.4–2.3 秒。** 多出来的时长全部变成空转。
落位精度四档完全一致（MAE 6.35–6.83），说明短段长没有牺牲可控性；视觉上也未见画质退化。

单位算力产出动作最多的档位是 **4.5s**（58s / 每秒可用动作），5.2s 最差（77s / 秒），2s 与 3s 并列（62s / 秒）。

### ② AddGuide 多锚点在 FL2VA 上可用，而且是**帧级精确**的时序控制

官方 `multiframe_reference` 模板用的是 Ref2VA 底座，我们只有 FL2VA。实测结果：**FL2VA + AddGuide 完全可用，官方没提供模板但机制是通的。**

连续三组独立验证，指定帧号 → 状态出现帧号：

| 测试 | 指定锚点帧 | 状态实际最相似帧 | 中间态 MAE（无锚点 → 有锚点） |
|---|---|---|---|
| 合成 · 单动作 5.2s | @62 | **62** | 10.23 → **4.84**（−53%） |
| 合成 · 单动作 5.2s | @42 / @84 | **42 / 84** | 11.04 → **4.97**（−55%） |
| 合成 · 单动作 3.0s | @36 | **36** | — |
| 合成 · 单动作 4.5s | @53 | **54** | — |
| 合成 · 双动作 5.2s | @31 / @62 | **32 / 60** | 10.30 → **6.59** |
| 真实 · 段① 5.2s | @62 | 命中 | — |
| 真实 · 段② 5.2s | @62 | 命中 | — |

误差 0–2 帧（≤0.08s）。**成本几乎为零**：多注入 2 个锚点只增加约 5s（145.1s → 150.2s）。

### ③ 但锚点不是越多越好 —— 1 个中间锚点才是甜点

合成测试（同一动作弧，只改中间锚点数）：

| 中间锚点数 | 动作段数 | 覆盖率 | 抖动度 |
|---|---|---|---|
| 0 | 1 | 37% | 0.150 |
| **1** | 1 | **76%** | **0.050** |
| 2 | 2 | 61% | 0.099 |

- **0 个**：动作全挤在前 1.8s，后面 3.2s 空转
- **1 个**：动作被"牵引"着铺满整段，抖动最低，最顺滑
- **2 个**：出现"动—停—动"（模型在每个锚点"停靠"），覆盖率和顺滑度双双回落

---

## 二、真实带货视频链路（产品图 + 模特图）

### 素材
- 产品：粉色气垫粉底（玫瑰浮雕盖 + 金色环 + 镜面 + 粉扑），1536×1024
- 模特：灰西装 + 米色高领坐姿女性，1088×1920
- 输出：9:16 竖屏 480×864，带 32kHz 立体声

### 管线（4 步）
```
① 双参考合成底图      R_p1   （产品图 + 模特图 一次融合，定"世界观"）
② 同源编辑链派生状态   R_p1 → R_p1m → R_p2 → R_p2m → R_p3
③ 逐段 FL2VA 首尾帧+中间锚点出片
④ 拼接（+ 后期叠字幕/价格/logo）
```

### 状态设计（3 状态 / 2 段）
| 状态 | 内容 |
|---|---|
| R_p1 | 右手持合盖气垫于胸前 |
| R_p1m | 举到下巴高度（段① 中间锚点） |
| R_p2 | 举到脸旁齐眼（段① 终点 / 段② 起点） |
| R_p2m | 盖子掀开约一半（段② 中间锚点） |
| R_p3 | 完全打开，镜面朝镜头，露出米色粉芯 |

### 出片结果
| 版本 | 单段耗时 | 动作段数 | 覆盖率 | 抖动度 |
|---|---|---|---|---|
| A：5.2s/段 ×2（共 10.4s） | 150.3 / 150.1s | 2 / 2 | 35% / 29% | 0.678 / 0.952 |
| B：3.0s/段 ×2（共 6.2s） | 105.0 / 105.3s | 2 / 1 | 46% / 38% | 1.603 / 1.151 |
| C：2.0s/段 ×2（共 4.7s） | 80.6 / 85.1s | **1 / 1** | 43% / 46% | 0.945 / 1.719 |

落位校验（全部通过）：首帧 MAE 3.60–3.79、中间锚点 5.30–5.66、尾帧 5.18–5.69，变化像素占比 0.23–2.40%。
**动作确实发生了**：段② 在 frame 62→82 之间能看到盖子从闭合→掀开→镜面与粉芯完整露出。

---

## 三、本轮新发现的三个坑

### 坑 1：中间锚点会制造"到点停靠"
合成测试里 1 个中间锚点给出 95% 覆盖率，真实素材里同样的配置却出现"动—停—动"（段① 5.2s、3.0s 都是 2 个动作段）。

**原因**：合成测试的中间锚点是"手正在伸向瓶子"（运动中的一帧），模型自然顺势继续；真实素材的中间锚点是"产品已举到下巴"（一个稳定姿态），模型会停在那儿"摆好姿势"再继续。

**对策**：
- 中间锚点尽量选**运动中**的一帧（带轻微运动痕迹），而不是一个静态摆姿
- 或干脆不加锚点，把段长压到接近动作自然时长（见下）

### 坑 2：同源编辑链存在**累积**漂移
逐级编辑（p1→p1m→p2→p2m→p3）时，产品横向位置一路右移：**8% → 22% → 30% → 33% → 35%**，并伴随轻微缩小。每步只偏 3–5%，但 4 步累积下来就明显了。

**对策**：**每一段都从它自己的起点帧直接派生，不要跨段链式派生。**
```
段① 锚点：p1 → edit(p1)@mid → edit(p1)
段② 锚点：p2 → edit(p2)@mid → edit(p2)
```
这样漂移不跨段累积。更彻底的办法是回到"宫格一次性生成"（一次前向传播，无累积）。

### 坑 3：全画面平均会把"局部小动作"完全淹没
最初用全画面帧差算运动量，段②（开盖）得出"0 个动作段、覆盖率 0%"，看起来像完全没动。实际上盖子开合只占画面 0.5% 的面积，被平均掉了。

**对策**：改用 **ROI 感知**度量 —— 先用首尾锚点的差异区域自动求出 ROI，再只在 ROI 内算运动量（`density_roi.py`）。这一步是所有"动作有没有真的发生"判断的前提。

---

## 四、修正后的选型规则

> **段长应该由"动作的自然时长"决定，而不是拍脑袋。**

记动作自然时长为 `T`（实测：把气垫从胸前举到脸旁 ≈ 1.0s；掀开盖子 ≈ 1.1s；手伸向瓶子并握住 ≈ 2.0s）：

| 段长 / 动作时长 | 建议 | 效果 |
|---|---|---|
| ≈ 1.3–1.5 倍 | **不加中间锚点** | 动作自然铺满，单段无停顿 —— 最干净 |
| ≈ 2 倍 | 加 1 个中间锚点 | 动作被拉长铺满，轻微放缓 |
| ≥ 2.5 倍 | 必须加 1 个中间锚点 | 否则前半演完、后半空转 |

补充规则：
1. **中间锚点最多 1 个**（2 个以上会出现"到点停靠"）
2. **锚点按"出错频率"加**，不按动作数量加：先跑 2 锚点，看哪一段糊了再给那一段补
3. **生成后按动作窗口裁剪**：动作永远落在片段前 40–60%，尾部是死时间，剪掉
4. 追求最低延迟选 2s 段（80–85s）；追求最顺滑选 5.2s 段 + 1 锚点（抖动 0.678）

---

## 五、产物清单

### 成片
- `real/out/带货成片_A_5.2s段x2_共10.4s.mp4` —— 最顺滑（抖动 0.678 / 0.952）
- `real/out/带货成片_B_3.0s段x2_共6.2s.mp4` —— 通用甜点参数（3s + 1 锚点）
- `real/out/带货成片_C_2.0s段x2_共4.7s.mp4` —— 动作最干净（单段无停顿）+ 最省算力

### 证据图
- `real/grid_chain.png` —— 同源编辑链 5 状态定位网格（看漂移）
- `real/strip_t2.png` —— 段② 开盖过程逐帧带（盖子确实在开）
- `real/check_real.png` —— 成片逐帧检验
- `chart_sweetspot.png` —— 动作覆盖对照曲线
- `table_result.png` —— 段长 × 锚点数实测对照表
- `cmp_duration.png` / `cmp_anchor.png` —— 段长 / 锚点数视觉对照表

### 工具（可复用）
| 脚本 | 作用 |
|---|---|
| `test_h3.py` | FL2VA 出片，支持任意段长 / 宽高比 / 任意多个 AddGuide 中间锚点 |
| `ttimg.py` | TT Image 2 接入（gen / edit，支持多参考图） |
| `frames.py` | 首尾帧一致性自检（背景 MAE / 变化集中度） |
| `align.py` | 产品轮廓对齐度（尺度漂移 / 位置漂移） |
| `density.py` / `density2.py` | 动作结构分析（单峰 / 多峰 + 抖动度） |
| `density_roi.py` | **ROI 感知**动作密度（自动聚焦动作区域，推荐） |
| `state_track.py` | 状态轨迹追踪（验证锚点是否按时落位） |
| `viz.py` | 统一中文字体与对照表 / 曲线图版式 |

---

## 六、下一轮建议

1. **修掉累积漂移**：按段独立派生（见坑 2），并对派生结果做产品轮廓对齐自检，超阈值就重出
2. **补 ASR 打轴**：口播文案 → 逐句时间轴 → 反过来决定每段的起止，让画面动作卡在旁白的重音上
3. **补第 3 段**：粉扑按压取粉（粉芯形变是 H3 的弱项，建议用"粉扑悬于粉芯上方"这种没有接触的构图规避）
4. **上 Ref2VA**：如果要"同一个主播跨条视频保持同一张脸"，Ref2VA 的身份一致性是 FL2VA 给不了的
