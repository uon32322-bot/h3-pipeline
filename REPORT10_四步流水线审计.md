# REPORT10 · 四步流水线审计：识别产品 → 文案 → 分镜 → H3 生成

> 审计对象（用户提案）：
> ① 识别产品 ② 生成相对完整的带货文案 ③ 生成产品分镜脚本
> ④ 由 H3 按分镜生成视频（文生 / 参考图生，可做三张参考图符合官方要求）
>
> 对照基准：官方 `minimalist-product-ad-generator` SOP（10 步）、官方提示词规范 `base-en.txt`、
> 以及本项目 REPORT2/3/5/6/7/8/9 的全部实测结论。

---

## 0. 结论先行

**方向完全正确，而且你已经无意中复刻了官方 SOP 的骨架。但「会更好」是有条件的：**

| 问题 | 回答 |
|---|---|
| 这个流程会不会更好？ | **会**，但提升的不是「崩坏率」，而是**崩坏的迭代效率与可诊断性** |
| 提升有多大？ | 试错成本从 **~100–135s GPU/次** 降到 **~0 成本/次**（前 3 步全是文本） |
| 它能解决 B 类崩坏（状态守恒）吗？ | **不能**。无解项在任何流程下都无解 |
| 缺什么才会「反而更差」？ | **3 个闸门**。没有闸门的规范流程，只会把「随机崩坏」变成「稳定复现的崩坏」 |

一句话：**你这 4 步是「把大创作拆成可审阅的小决策」，这是对的方向；但真正决定成败的是拆开之后在每个接缝上装的那道闸门，而闸门不在你的 4 步里。**

---

## 1. 你的 4 步 vs 官方 10 步：映射与覆盖度

官方 SOP 的真实结构（含 1 个强制前置 Gate）：

| 官方步骤 | 作用 | 你的 4 步覆盖了吗 |
|---|---|---|
| **启动问询（强制 Gate）** | 一次性确认素材/款式/时长/比例/风格/文案方式 | ❌ 缺 |
| 1. 素材检查与产品事实摘要 | 品类 · 主色 · 质量 · **可展示结构** | ✅ 你的①（但漏了 2 项） |
| 2. 确认制作简报 | 把问询结果固化成可执行参数 | ⚠️ 隐含在你的①里，未显式 |
| 3. 选择产品叙事脊柱 | 发布型 / 功能触感型 / 色彩家族型 | ❌ 缺（你的③会退化成套模板） |
| 4. 导演动效语言 | 转场逻辑 + 强弱节奏 + 安全空间 | ❌ 缺 |
| 5. 生成/确认英文文案 | 3–5 词单行、两段配色、去促销词 | ✅ 你的②（但形态不对） |
| 6. 生成三张独立锚定照片 | 主视觉 / 材质细节 / 结尾构图 | ✅ 你的④（理解需修正） |
| 7. 精确节拍与文字分镜表 | 11 列执行表，含时间/运镜/文案/转场 | ✅ 你的③（但缺格式约束） |
| 8. 视频生成 | 一条片、全画幅、传锚定照不传原图 | ✅ 你的④ |
| 9. music-2.6 科技配乐 | H3 原生音频不行才换 | ❌ 缺 |
| 10. 音乐分析卡点 + 交付验证 | 按音乐重剪，不是铺上去 | ❌ 缺 |

**覆盖度：你的 4 步 ≈ 官方 6 步。漏的 4 步（叙事脊柱 / 动效语言 / 配乐 / 卡点验证）恰好都是「决定成片像不像广告」的部分。**

---

## 2. 逐步审计

### ① 识别产品 —— 方向对，但漏了两件最关键的事

官方要求产出的「产品事实摘要」有 6 项：产品品类、主体位置与画面占比、**图片质量**、
商品主色与主推款、**可展示结构**、可用于分镜的真实外观特征。

你写「识别产品」通常会得到：品类 + 颜色 + 卖点。**漏掉的两项是致命的：**

**漏项 1：可展示结构清单**（官方举例：开合、旋转、升起、扣合、折叠、弹出、发光、屏幕、纹理）

> 这是第 ③ 步分镜的**唯一合法素材来源**。没有这份清单，分镜只能套模板 ——
> 而官方《失败处理》明确把「分镜套模板」列为失败项，处置方式是「回到精确节拍表，基于产品形态重写」。
> 也就是说：**没有结构清单 → 分镜必然套模板 → 必输。**

**漏项 2（最贵的一个）：保真分层判断**

官方 SOP 把「产品本体颜色是**硬保真约束**」单列为运行原则第 3 条，原话：
> 必须在所有锚定照片和视频中保留用户原图里的产品本体颜色、点缀色、材质色相和可见表面质感。
> 只有背景、光线环境和构图可以转向所选模板；**绝不能重染产品本体颜色**。

但必须在你这一步就做出**更进一步的判断** —— 这个产品要求的是哪个级别的保真：

| 产品形态 | 要求级别 | 处置 |
|---|---|---|
| 纯色/无标/无中文/无价格 | **形态级** | ✅ 可以让 H3 亲自画，三张锚定照压漂移 |
| 有 logo / 中文标签 / 价格 / 复杂包装印刷 | **像素级** | ❌ **H3 不许画产品** → 走混合合成（真实产品像素直接合成） |

**不做这个判断的后果**：第 ④ 步会**稳定地把 logo 和中文画糊**，而且每次糊法不一样，
你会误以为是「提示词没写好」而反复调 prompt —— 白烧 GPU。而这一判断在文本阶段是**零成本**的。

⚠️ 官方 SOP 里其实埋了同一条红线，但它表达为「产品本体颜色是硬保真约束」，
说的是「不能重染」；**我们在 REPORT9 里从跨模型实证推出的结论更强**：SSIM 0.96–0.97 的纯生成，
退货率 15–30%，而混合合成（真实产品像素）保真 100%、退货率 3–5%。**标签/logo 纯生成必崩。**

---

### ② 生成「相对比较完整的」带货文案 —— 形态错了，这是全流程最大的风险点

**「完整」是这里最危险的词。** 官方对文案有四条互相咬合的硬约束：

1. **画面内文案强制英文**，中文必须英文化
2. **必须是 3–5 个英文词**，尽量 ≤32 字符含空格；**不要写 1–2 个词的孤立功能标签**
3. **必须单行**，不换行、不堆叠；同一时间画面里只允许一条
4. **10 秒片通常只保留 1 次中段文案 + 1 次结尾文案**，不要每个镜头都有文字

> 如果你把一整段「完整带货文案」直接喂给 H3，结果必然是：
> 中文渲染成乱码字形 / 多行文字崩坏 / 文案挤满画面。
> 官方《失败处理》对应两条：「文案太长 → 压缩到 3–5 词英文」「视频文字变形 → 重做文字锚点」。

**正确的第 ② 步产出不是「一段文案」，而是分流到三个桶：**

| 桶 | 内容 | 形态约束 | 由谁执行 |
|---|---|---|---|
| **A. 画面内文案** | 中段 1 句 + 结尾 1 句 | 3–5 英文词 · 单行 · 两段配色 | **H3 内生成**（官方明确禁止擅自退化成后期字幕） |
| **B. 旁白 / 口播** | 讲解句 | 走 `<d>[English] ...</d>`；画外音必须紧跟 `lips remain completely closed` | H3 内生成 |
| **C. 后期文案** | 中文说明 / 价格 / 参数 / CTA / logo | 无模型约束 | **一律后期叠** |

**另一条内容层面的红线**：文案必须写成**陈述、卖点、成分、对比、价格、场景**。
一旦写成「看我怎么用 / 教你三步搞定」→ 立刻把分镜推入**多步因果链** → 正撞 B 类崩坏（无解）。
这是 REPORT7 已经证过的结论，在文案阶段就要拦住。

**还有：文案长度决定时长预算。** 官方配比 —— 5s→3–4 节拍 / 10s→5–7 节拍 / 15s→6–9 节拍。
写文案时就要知道它要摊到几个节拍上，否则第 ③ 步会超时。

---

### ③ 生成产品分镜脚本 —— 这一步最有价值，但必须换掉「分镜」的理解

**首先，官方这里有一个反直觉规定，是本步成败的关键：**

> 官方原话：**「锚定照片只是商品锚定，绝不能把锚定照片当成分镜本身，也不能把三张照片按顺序直接拍成三段视频。」**
> 「精确节拍分镜表才决定真正的镜头顺序、动作和文字出现方式。」

也就是说：**分镜是「文字执行表」，不是「图」。** 你必须输出的是官方那张 **11 列节拍表**：

| 时间段 | 镜头 | 镜头目的 | 视觉主导 | 画面 / 运镜 | 款式状态 | 文案内容 | 文字颜色 | 文字效果 | 转场 / 连贯 | 节奏意图 |
|---|---|---|---|---|---|---|---|---|---|---|

配套硬规则（全部来自官方原文）：
- 时间段用秒（如 5 秒 3–4 节拍 / 10 秒 5–7 节拍 / 15 秒 6–9 节拍）
- **每个节拍只保留一个主要动作**，次级动作延迟出现
- **文案拆分只能用自然语言描述「前半 / 后半」，禁止写箭头、斜杠、加号或连接符**
  —— 否则模型会把符号当成画面文案
- 文字颜色按风格明确拆两段：白底风「前半黑色或深灰，后半具体商品色」；黑底风才可「前半白色，后半商品色」
- 节奏意图枚举：铺垫 / 建立 / 预备 / 冲击 / 制动 / 稳定
- 结尾必须是「单一全画幅产品收束 + 完整单行文案」，**严禁结尾出现网格、分屏、分镜板**

**⚠️ 第二个必须换掉的认知：不是「5 个镜头 = 5 段视频」。**

官方明确：**「默认生成一条 H3 全画幅产品片，而不是逐镜生成多个首帧视频。」**
H3 在**一次生成内部自带多镜切**，官方规范原文：

```text
[Shot 1] Live-action, cinematic, a medium-wide shot frames...
[Shot 2] At 00:03.500, the camera cuts to...
```

这一条直接解决了我此前那套「生成 5 段再拼」方案的三大副作用：**接缝、色调漂移、身份漂移**。
所以第 ③ 步的正确产出，是让分镜表**天然落进「一条片多镜」结构**，而不是「一段一个文件」。

再补两条运镜规则（否则分镜表会写废）：
- **切镜必须引入新信息**（主体 / 空间 / 状态 / 视角 / 时间之一）。
  **如果只是改景别或轻微改角度 → 必须改用运镜，不许切镜。**
- 运镜写成**「类型 + 幅度 + 速度」的句中动作句**，不能堆成句尾标签：
  ✅ `The camera pushes in with small amplitude at slow speed toward the folded letter in her hands.`
  ❌ `... , push in, small amplitude, slow.`

**⭐ 第 ③ 步要装的是全场最硬的一道闸门：动作白名单 + 因果链否决。**
分镜里每一个动作必须连过两问：

1. **在白名单内吗？** 举起 / 并排 / 推近 / 旋转 / 光影流动 / 开合 / 滑入 / 静置 / 特写平移 …
2. **有没有「可验证的物理结果」？** 有 → **否决**。

> 判据一句话：**「这个动作有没有可验证的物理结果？」
> 有（涂开后皮肤变了、倒了之后瓶里少了、用了几次之后余量下降）→ 必实拍或后期；
> 无（举起、并排、推近、光影流动）→ AI 够用。**

B 类崩坏（状态守恒：液量不变、盖子自合、用量不减）是**扩散模型无因果链导致的架构属性**，
**换任何模型都一样**（REPORT9 已跨模型验证）。**唯一防线就是在第 ③ 步把这类动作挡在分镜表外面。**

---

### ④ 由 H3 按分镜生成 —— 大方向对，「三张参考图」的理解需要修正

你说「也可以考虑做出三张参考图，符合 H3 官方的生成要求」—— **这个直觉完全正确，
三张锚定照就是官方为「多镜头产品形态对不上」给出的官方解法。** 但有 6 个细节不能错：

**细节 1：必须是三张「独立」照片，不是三宫格/四宫格/拼贴板。**
官方理由（原话）：**「本 Skill 不再默认使用四宫格锚定图，因为视频模型可能把宫格版式带进最终画面。」**
禁止生成：网格、分屏、拼贴板、画框、多面板、产品墙、分镜板。

**细节 2：三张的角色是固定的，不是随便三张。**

| 锚定照 | 锁什么 |
|---|---|
| 照片 1 | 主视觉 / 刁钻主视角 —— 最有吸引力的产品视角、轮廓、开场视觉方向 |
| 照片 2 | 材质 / 功能细节 —— 表面材质、颗粒、反光、触感 + **最重要的可见结构/机制**（开合/按钮/盖子/屏幕线索合并进这张） |
| 照片 3 | 结尾文案锚点 —— 最终构图、字体气质、文字位置、单行排版、两段配色 |

- 三张必须**统一光、影、调色、背景语言**，且每张都能作为**独立产品照片**成立
- 结构动作若与主视角重叠，**不要单独生成结构动作锚定照**，合并进照片 2
- 照片 3 里只放那一条格式参考文案，不得有其他文字
- 比例必须与视频尺寸一致；比例一经选定，锚定图 / 视频 / 成片**全部严格沿用**

**细节 3：⭐ 生视频阶段不要把原始商品图传给模型。**
官方原话：「生视频阶段不要把用户上传的原商品图传给视频模型。原商品图只用于前置产品分析和锚定图生成。」
这条容易违反，而且违反了会让产品身份出现两个互相冲突的参考源。

**细节 4：prompt 里必须逐字写出所有画面英文文案。**
官方《失败处理》+ 步骤 8 硬校验：
> **「如果最终视频生成 prompt 里没有逐字包含所有计划出现的英文文案，本次任务还不能派发，必须先重写 prompt。」**
必须写出：每一句原文 + 出现时间段 + 前半/后半进入时间 + 颜色 + 单行行为。
不能概括成「文字卡点」或「给文案留白」。

**细节 5：关键帧对齐指令必须是 prompt 第一行，两位小数，后接一个空行。**
- I2VA：`For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.`
- FL2VA：`How the reference pictures align with the target video — Picture 1 (from [Shot 1]) aligns with the 0.00-second mark of the target video; Picture 2 (from [Shot 1]) aligns with the 8.00-second mark of the target video.`

**细节 6：三条字段一个都不能少，且不能自相矛盾。**
`integrated_multimodal_description` / `overall_soundscape` / `non_diegetic_music`。
没 BGM 也要写 `N/A`，但**不能和画面里出现的声音矛盾**（角色能听到的音乐属 diegetic，必须写在 multimodal 里）。

**模型选型（对应你说的「文生或参考图生」）：**

| 场景 | 选择 | 依据 |
|---|---|---|
| 纯产品、无标、要控产品形态 | **参考图生（FL2VA）**，3 张锚定照当锚点 | 官方 SOP 默认路径 |
| 画面里有模特、要跨镜头锁脸 | **FL2VA 同源编辑链** | 实测唯一能锁脸的路径（REPORT5） |
| 纯环境 / 氛围 / 无产品主体 | 文生（T2VA） | 无锚定需求 |
| ❌ 别用 | Ref2VA 的 `ref_videos` 参考视频 | **本地实测零效果**（REPORT6） |

---

## 3. ⭐ 必须补上的 3 个闸门

这是本次审计的核心结论。**闸门不在你的 4 步里，但决定成败的就是它们。**

### 闸门 ①（装在第 ① 步）—— 保真分层判断

```
识别产品时，必须同时回答：
  这个产品有没有 logo / 中文标签 / 价格 / 复杂包装印刷？
    ├─ 有 → 判为「像素级保真」→ H3 只画环境，产品走混合合成
    └─ 无 → 判为「形态级保真」→ 可以交给 H3，用 3 张锚定照压漂移
```
**不装的代价**：第 ④ 步稳定画糊 logo，你会反复调 prompt 白烧 GPU。

### 闸门 ②（装在第 ②③ 步交界）—— 动作白名单 + 因果链否决

```
分镜表里每个动作连过两问：
  1) 在白名单内吗？（举起/并排/推近/旋转/开合/滑入/光影流动…）
  2) 有没有可验证的物理结果？
       有 → 否决（B 类无解），改为实拍或后期
       无 → 放行
```
**不装的代价**：崩坏从「随机」变成「稳定复现」——每次都在同一秒崩同一个地方，且无法通过改 prompt 修好。

### 闸门 ③（装在第 ④ 步派发前）—— 官方硬校验清单

派发前逐条打勾，任一条不过就**不许派发**：

- [ ] prompt **逐字包含**所有计划出现的英文文案（含时间段、前后半、颜色）
- [ ] 关键帧对齐指令在**第一行**，两位小数，后接一空行
- [ ] 每个节拍**只有一个主要动作**
- [ ] 每个切镜都**引入了新信息**（只改景别的已改为运镜）
- [ ] `overall_soundscape` 与 `non_diegetic_music` 都写了，且相互不矛盾
- [ ] 锚定照是 **3 张独立照片**，无任何网格/拼贴版式
- [ ] **没有**把原始商品图传给视频模型
- [ ] 比例与锚定图、简报**完全一致**
- [ ] 结尾是单一全画幅收束，无网格/分屏/小窗
- [ ] 文案无箭头/斜杠/加号/连接符

---

## 4. 修正后的完整流程（7 步 + 3 闸门）

```
【Gate】一次性确认：素材 / 款式 / 时长 / 比例 / 风格 / 文案方式
   ↓
① 识别产品 ──────────────────────── 文本，成本≈0
   ├ 产品事实摘要（品类·主色·质量·可展示结构·外观特征）
   └ ★闸门① 保真分层判断 → 决定第④步 H3 能不能画产品
   ↓
② 文案三分流 ────────────────────── 文本，成本≈0
   ├ A 画面内文案（3–5 英文词 · 单行 · 两段配色 · 10s 只 1 中段+1 结尾）
   ├ B 旁白/口播（<d> 块 + 画外音要写 lips remain closed）
   └ C 后期文案（中文/价格/CTA/logo）
   ↓
③ 分镜表 ───────────────────────── 文本，成本≈0
   ├ 选叙事脊柱（发布型 / 功能触感型 / 色彩家族型）
   ├ 定动效语言（转场驱动 + 强弱节奏 + 安全空间）
   ├ 出 11 列精确节拍表（时间段/镜头/目的/视觉主导/运镜/款式/文案/颜色/文字效果/转场/节奏）
   ├ 结构落成「一条片内多镜 [Shot 1]…[Shot 2] At 00:03.500…」
   └ ★闸门② 动作白名单 + 因果链否决
   ↓
④ 三张独立锚定照（主视觉 / 材质结构 / 结尾文案构图）──── 图像，成本低
   ↓
⑤ H3 生成 ─────────────────────── GPU，~100–135s/条
   └ ★闸门③ 派发前 10 项硬校验
   ↓
⑥ 配乐与卡点剪辑 ───────────────── 按音乐重剪，不是铺上去
   ↓
⑦ 后期合成 + 合规 ─────────────── 字幕/价格/logo/CTA 叠加
   └ AI 标识（显式 + 隐式，发布必选标签）
```

**贯穿全流程的一条铁律**：**迭代时一次只改一个变量。**
官方规范里「一次只改一个变量」是反复强调的原则，也是实际最容易违反的一条 ——
同时改 prompt 和 seed，你就永远不知道是哪个起了作用。

---

## 5. 预期效果量化：哪些崩坏会被真的压掉

诚实清单。**7 类崩坏里，这个流程能压掉 5 类，压不掉 2 类。**

| 崩坏类型 | 4 步流程能否压住 | 靠什么 |
|---|---|---|
| A 产品身份漂移 / 形态变 | ✅ **能** | 3 张独立锚定照 + 闸门① 分层 |
| D 参考不生效 | ✅ **能** | 锚定照写明用途（「素材不写用途是参考失效第一原因」）+ 闸门③ 逐字校验 |
| E 文字/商标崩 | ✅ **能**（英文短句）| 3–5 词单行 + 两段配色；中文/价格仍走后期 |
| F 镜头语言平庸 / 接缝漂移 | ✅ **能** | 一条片内多镜自带切（替代 5 段拼接） |
| G 音画不对齐 | ✅ **能** | `overall_soundscape` + `non_diegetic_music` 必须写；原生音频 |
| **B 状态不守恒**（液量/用量/开合） | ❌ **不能** | 只能靠闸门② **挡在分镜表外面** |
| **C 接触物理**（手-物交互） | ⚠️ **部分** | 锚定照减少漂移，但手仍会自发生成；靠「产品静止 + 环境动」规避 |

**关键推论**：这个流程**不提升模型能力上限**，它做的是两件事 ——
1. 把「能不能生成」的问题提前到文本阶段**否决掉**（闸门②）
2. 把「生成得好不好」的问题变成**可审阅、零成本迭代**的文本产物

**所以正确的期望值是：出片**良率↑、**返工成本↓、**废片**不再浪费 GPU。而不是「崩坏消失了」。

---

## 6. 直接回答你的问题

**Q：这样的流程做下来，会不会效果会好些？**

**A：会。你提的流程本质上是正确的，而且和官方 10 步 SOP 骨架一致。但「更好」的来源不是你想的那个 —— 不是「分步了就少崩坏」，而是：**

1. **前 3 步全是文本** → 迭代成本从 ~100–135 秒 GPU/次，降到接近 0。**这是最大的收益。**
2. **三张锚定照确实是官方解** → 产品形态漂移（A 类）被显著压住。你的直觉对。
3. **分镜表天然落进「一条片多镜」** → 顺手解决了我以前那套 5 段拼接的接缝/色调/身份漂移。

**但要让「更好」真的发生，必须补 3 件事：**

1. **第 ① 步加「保真分层判断」** —— 有 logo/中文的产品，H3 不许画，走混合合成。（否则稳定画糊）
2. **第 ②③ 步加「动作白名单 + 因果链否决」** —— 有物理结果的动作直接踢出分镜。（B 类无解，只能挡在外面）
3. **第 ④ 步加「派发前 10 项硬校验」** —— 尤其「prompt 未逐字包含所有文案 → 不许派发」。

**没这 3 个闸门，这个流程会把「随机崩坏」升级成「稳定复现的崩坏」—— 看起来更规范，实际一样不可用，而且更难诊断。**

---

## 7. 参考：本项目累计实测基准（供算力与判据使用）

| 项目 | 数值 |
|---|---|
| FL2VA 耗时 | 2s→81–86s / 3s→100–105s / 4.5s→130–135s |
| Ref2VA 耗时 | ≈1.1–1.3 s/帧 |
| 本地分辨率天花板 | **768p**（2K 依赖闭源 Regenerate-2K） |
| 推荐步数 | **8 步**（不用 4 步） |
| 推荐段长 | 2–4.5s，动作落在前 40–60% |
| 中间锚点 | **≤1 个**，且选「运动中」的一帧 |
| 复现噪声基线（同素材改名重跑） | 帧差 **1.08/255** |
| 真模型参数改变（稀疏注意力档位） | 帧差 **22.91/255** |
| 参考视频扰动（换人/换场景/时间反转/静态） | 帧差 0.94–1.09/255 ⇒ **全部落在噪声内** |
| 混合合成 vs 纯生成 | 产品保真 100% vs SSIM 0.96–0.97；退货率 3–5% vs 15–30% |
