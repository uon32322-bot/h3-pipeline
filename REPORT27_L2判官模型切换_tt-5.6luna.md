# REPORT27 · L2 判官模型切换：`doubao-seed-2-1-pro` → `tt-5.6-luna`

> 指令（辉哥原话）：**「把 TT-5.6luna 作为替换『豆包seed2.1pro』的模型。不能再调用除了 TT-5.6luna 以外的同类模型。」**
> 日期：2026-09-18　｜　执行：金刚
> **结论：换模型已完成并端到端验证通过；规范落成代码级硬约束（白名单断言 + 启动自检 + 违规即退出）；顺带用新判官抓出并修掉 C1 的一处真缺陷（声明残留）。**

---

## 一、为什么要换（背景与合规依据）

| 事实 | 说明 |
|---|---|
| 原 L2 模型 | `doubao-seed-2-1-pro-260628`，走灵炫 `api.lk888.ai` |
| 问题 | 它与生图模型 `tt-image-2` **同网关但是两个不同模型** ⇒ 越出部署指南 §2.0b **D6 模型白名单**「调用清单只允许出现单一供应商模型」的边界 |
| 佐证 | 灵炫 `/v1/models` 实测列出 **67 个模型**（`doubao-*` / `gem-*` / `omni-*` / `qwen*` / `kimi*` / `glm*` / `deepseek-*` / `MiniMax-*` …）⇒ **「接口支持」≠「允许使用」** |
| 辉哥处置 | 指定 `tt-5.6-luna` 作为**唯一**允许的同类模型，其余一律禁用 |

---

## 二、改了什么（4 处代码 + 3 处文档 + 技能包同步）

### 2.1 代码级硬约束（写进代码，不靠自觉）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `judge_shot.py` | `VLM_MODEL` 默认值 `doubao-seed-2-1-pro-260628` → **`tt-5.6-luna`** |
| 2 | `judge_shot.py` | 新增 **`ALLOWED_VLM_MODELS = ("tt-5.6-luna",)` 白名单 + 断言**：即使有人用 `JUDGE_VLM_MODEL` 环境变量改写模型名，不在白名单内立即 `sys.exit` 报违规 |
| 3 | `orchestrator.py` | 新增 `JUDGE = _resolve("judge_shot.py")` 常量；`MODEL_POLICY` 新增 **`judge` 段**（allow / banned / note）；`_check_model_policy()` 新增**判官白名单自检** |
| 4 | `judge_shot.py` | `_vlm_call` 加固：`timeout` 120 s → **200 s**；新增 **`rtok > 1200` 丢弃重抽**（应对思考链被上游忽略）；返回体记录 `_reasoning_tokens` 便于审计成本 |

### 2.2 文档与配置

| 文件 | 改动 |
|---|---|
| `judge_config.yaml` | `l2:` 段新增 `model: tt-5.6-luna` / `provider` / **`banned_models` 清单** |
| `部署指南_判官团抽卡生产流水线.md` | **D6 白名单**追加判官项；§2.0b 表格新增「判官/审片」行；执行机制 **3 处 → 4 处**；验收清单同步 |
| `分镜脚本_动态卖点版.md` | 升至 **v1.2**，新增修订摘要第 5 条（见 §五） |
| `prompt/C1_run_v2.txt` | 删去不成立的「落地／中底压缩」声明（生成期原版备份为 `C1_run_v2.txt.bak_生成期原版`） |

### 2.3 技能包同步（三份副本必须一致）

`~/.workbuddy/skills/h3-fl2va-video-pipeline/` 下同步：`scripts/judge_shot.py`、`scripts/judge_config.yaml`、`scripts/orchestrator.py`、`reference/部署指南_判官团抽卡生产流水线.md`。

> 三份 `judge_shot.py`（项目根 / action 工作目录 / 技能包）md5 实测一致：`8d0617369e6d24d59c8f6441cdb5318b`

---

## 三、`tt-5.6-luna` 能力探针（换模型前必做）

| 探针项 | 实测结果 |
|---|---|
| **多模态支持** | ✅ 支持（64 px 纯色图识别正确） |
| **底层真实模型** | 网关别名实际打到 **`grok-4.6` / `grok-4.6-build`**（响应体 `model` 字段） |
| **是否推理模型** | ✅ 是（有 `reasoning_content`，不关思考时小图即吐 684 reasoning tokens） |
| **关思考是否生效** | ⚠️ **只部分生效** —— 同一 payload 两种结果交替（见 §四） |
| **单次延迟** | 生效档 **2.1–2.4 s**；被忽略档 **6.7–53.6 s** |
| **返回结构兼容性** | ✅ 完全兼容现有解析（`{"verdict","evidence","confidence"}`），`judge_shot.py` 无需改解析逻辑 |
| **余额／限流** | ✅ 本轮全程 200，未复现此前 `doubao` 的 `402 billing_error` |

---

## 四、关键坑：思考链关不干净（已防护）

### 4.1 实测数据

同一判项（P-06）、同一批帧，传 `thinking:{"type":"disabled"}` 后连测：

| 轮次 | 尺寸 | 耗时 | reasoning_tokens | 结论 |
|---|---|---|---|---|
| 压测 #1 | 320 px × 6 帧 | 9.4 s | 391 | 被忽略 |
| 压测 #2 | 320 px × 6 帧 | 13.3 s | 597 | 被忽略 |
| 压测 #3 | 320 px × 6 帧 | 6.7 s | 276 | 被忽略 |
| 压测 #4 | 320 px × 6 帧 | 11.6 s | 505 | 被忽略 |
| 探针 B | 320 px × 6 帧 | **2.4 s** | **0** | **生效** |
| 判项 R-03 | 448 px × 6 帧 | **53.6 s** | **2646** | 被忽略（最坏档） |

### 4.2 试过的替代写法（全部无效）

| 写法 | 结果 |
|---|---|
| `thinking:{"type":"disabled"}` | 部分生效（保留） |
| `+ reasoning_effort="none"` | 2.1 s / rtok=0 —— 与 baseline 无差异 |
| `+ reasoning_effort="minimal"` | 9.3 s / rtok=0 —— 不更稳 |
| `+ enable_thinking=false` | 2.3 s / rtok=0 —— 与 baseline 无差异 |
| `+ chat_template_kwargs` | 5.1 s / rtok=0 —— 不更稳 |

⇒ **保留 `thinking:{"type":"disabled"}`**，并在脚本层加防护：**`timeout` 定 90 s**（覆盖实测最坏 53.6 s + 余量，让卡死请求尽快失败走退避重试）。

### 4.3 ⛔ 一次翻车：用「重抽」对抗慢响应会雪崩（本轮最重要的工程教训）

**做过的错事**：对 `rtok > 1200` 的响应**丢弃重抽**，想以此把被忽略的慢响应换成 2 s 档。

| 场景 | 结果 |
|---|---|
| 单发 / 小并发 | "看着有效" —— C1 首跑触发 2 次、修正后复跑触发 4 次 |
| **生产并发（C2+D 连跑）** | **彻底失控** —— **25 次重抽、0 个判项完成**，伴随 200 s 超时，跑 **16 分钟仍未收敛** ⇒ **逼近单镜生成 9.4–11.4 min，隔离红线告急** |

**根因**：快／慢是**上游路由**决定的（不是请求本身有问题），重抽大概率仍落慢后端；而并发重抽又进一步压垮上游 ⇒ **正反馈雪崩**。

**正确做法**：**慢就接受** —— 能解析出 verdict 就用，`rtok` 只记录供成本审计。判官的要求是「**藏在生成后面**」，**不是「单次快」**。已改回并写入 `judge_shot.py` 的 docstring 作为反面教材。

---

## 五、端到端验证：C1 抓出一处真缺陷并修复

### 5.1 第一次判官（原声明）→ REJECT

| 层级 | 结果 |
|---|---|
| L0 技术校验 | 8/8 全绿 |
| L1 本地检测 | 全绿（L1-11 出框余量 **0.125** = 12.5 %，出框问题已修好） |
| **L2 语义判官** | **REJECT** —— R-03 `2/3 no`、W-03 `3/3 no`、W-07 `3/3 no` |

三条否决的**证据高度统一**：

| 判项 | 票型 | 判官给出的区域描述 |
|---|---|---|
| R-03 | 2/3 (no,no,yes) | *both feet off the floor throughout all frames* |
| W-03 | 3/3 (no,no,no) | *both sneakers fully airborne with no floor contact or shadow* |
| W-07 | 3/3 (no,no,no) | *entire body airborne, both feet off the floor* |

### 5.2 根因定位：**声明残留**，不是画面缺陷

- 抽 f0/f14/f36/f60 全分辨率核验：**四帧全部双脚离地**，判官判得**完全正确**。
- 但画面本身是对的 —— 锚点对 `runA3/runB3` 是我为**避开尾部冻结**而**故意选的"双腾空大步幅相位对"**（v2 落地相锚点实测触发 0.50 s 尾部冻结，见 REPORT26）。
- 真正的问题是：`prompt/C1_run_v2.txt` 写于 **v2（落地相）锚点时代**，里面声明了 *"the front foot comes down and lands flat and firmly on the studio floor, the sculpted white foam midsole visibly compressing under its weight"* —— **换 v3 锚点时忘了同步改声明**。

⇒ 属**「声明滞后于设计决策」**，修法是**改声明**（不是改画面、也不是重抽）。

### 5.3 修复动作 + 复判结果

| 步骤 | 内容 |
|---|---|
| 改声明 | 落地/中底压缩 → 如实描述为「双膝抬起、双脚同时腾空的大步幅原地跑」；音景同步 |
| 备份 | `prompt/C1_run_v2.txt.bak_生成期原版` 保留生成期原文 |
| 分镜脚本 | 升至 v1.2，C1 描述标注「**本镜无落地相**：缓震表达由 D 片 [Shot 4] 承载」 |
| **复判** | **PASS** ✅ —— L0/L1 全绿 + L2 **17 项全部通过**（R-03/W-03/W-07 由 `no` 翻正为 `yes`，均 3/3 票），耗时 2 m 58 s |

**判官单镜耗时（17 项 × 3 票 = 51 次调用）**：1 m 53 s ～ 2 m 58 s，**仍远小于单镜生成 9.4–11.4 min** ⇒ 判官隔离红线未破（判官依旧"藏在生成后面"）。

---

## 六、副作用与待决策项

### 6.1 ⚠️ L2 上线后判据显著变严 —— 旧片需复检

C1 这处缺陷，**只用 L0+L1 是永远抓不到的**（出框、活动量、断裂、闪烁全部合格）。此前 C1v3/C2/D 三镜的"PASS"是**仅按 L0+L1 出厂判据**得出的。

⇒ **凡是在 L2 缺席期间通过验收的片子，都应重跑一次完整判官。** 本轮已对 C2 / D 启动复检（见 §七）。

### 6.2 待辉哥拍板

| # | 事项 | 选项 |
|---|---|---|
| 1 | **C1 要不要也表达缓震？** | (a) 保持现状：C1 只做动作镜，缓震全交 D 片（零 GPU 成本，当前方案）(b) 给 C1 加**中间落地锚点**（3 锚点结构，约 +215 s GPU，且需声明为片内多镜以避免硬切） |
| 2 | **`tt-5.6-luna` 的推理开销** | 关不干净意味着单次可能多花 276–2646 个 reasoning token。是否接受该成本，或后续换更"听话"的模型（需重新走白名单授权） |
| 3 | **白名单是否要扩到第三项** | 当前锁死两项：`tt-image-2`（生图）+ `tt-5.6-luna`（判官）。若未来需要文字模型，须单独授权 |

---

## 七、验收清单

- [x] `judge_shot.py` 默认模型 = `tt-5.6-luna`
- [x] 白名单断言存在且**实测拦截有效**（传入 `doubao-seed-2-1-pro-260628` → 报违规退出）
- [x] `orchestrator.py` 启动自检新增判官项且**实测全绿**
- [x] 三份 `judge_shot.py` 副本 md5 一致
- [x] `judge_config.yaml` YAML 可解析，`l2.model` 正确
- [x] 部署指南 D6 / §2.0b / 验收清单三处同步
- [x] 端到端实跑：C1 **PASS**（L0+L1+L2 全部通过）
- [ ] C2 复检（进行中）
- [ ] D 复检（进行中）
- [ ] 成片是否需要按 v1.2 分镜脚本重新装配（C1 画面未变，故理论上无需重装）

---

## 附：一句话总结

**换模型不只是换个字符串** —— 它一次性带来三件事：① 合规边界收回；② 规范从"注释"升级为"断言"（违规即退出）；③ **新判官上线后立刻抓出了旧判官抓不到的缺陷**，而这正说明 L2 这一层不是可选项。
