# REPORT13 · 判官团续论：抽卡（rejection sampling）的真实收益边界

> 日期：2026-09-17　承接 `REPORT11_判官团多采样择优审计.md`
> 用户命题：**「让判官评价 H3 生成的视频，没达标就重新生成，相当于抽卡——因为 H3 还是会生成出质量很好的视频的。」**
> 结论先行：**命题成立，而且有实测地基。但抽卡是"第三顺位"的杠杆，不是第一顺位。**

---

## 1. 抽卡的地基：已实测（这不是假设）

REPORT6 的配色相似度矩阵（双向最近邻加权，越小越像）里，同一批实验、同一提示词、**只改 seed**：

| 变量 | 差异 | 性质 |
|---|---|---|
| 换参考视频的人 / 场景 | 0.9 | ≈ 噪声 |
| 时间反转 | 0.1 | ≈ 噪声 |
| 静态帧 | 1.5 | ≈ 噪声 |
| 去掉全部参考图 | 1.4 | ≈ 噪声 |
| 「同一素材改名重跑」噪声基线 | 1.08（帧差口径） | ← 基线 |
| ⭐ **换 seed（20260911 → 88888）** | **14.0** | **真实差异** |
| 只改稀疏注意力档位（真模型参数） | 20 – 35 | 真实差异 |

**换个随机数带来的画面差异（14.0），是「辛苦换参考素材」（0.1–1.5）的 10–140 倍。**

⇒ 在所有可拧的旋钮里，**seed 是仅次于模型参数的第二强杠杆**。抽卡空间真实存在。
⇒ 也解释了用户的观察：**「H3 有时出很好的片」= seed 方差**（已证同 seed 同 prompt 输出逐位相同，所以变化必然来自 seed 或你改过的其他变量）。

---

## 2. 抽卡收益 = 多样性 D × 通过率 p × 判官准确率 J

| 乘数 | 含义 | 现状 |
|---|---|---|
| **D 多样性** | 每次抽出来的片子有多不一样 | ✅ **已实测 = 14.0**（噪声 2.3）→ 空间真实 |
| **p 通过率** | 单次抽中「可交付」的概率 | ❓ **未测** → 决定抽卡成本（期望抽次 = 1/p） |
| **J 判官准确率** | 判官判定与人工判定的一致率 | ❓ **未测** → 最大风险项（判错 = 白抽 + 放过废片） |

⚠️ **三者是乘法**：任一项接近 0，抽卡整体归零。
- D≈0 → 每次抽都一样 → 判官给你 N 次相同否决（零收益）
- p≈0 → 抽再多也没用（结构性失败）
- J≈0 → 判官随机放行/否决 → 系统退化成纯噪声

**关键澄清：D=14 是「画面差异」，不是「质量差异」。** 差异大只说明抽卡有空间，**不保证里面存在「更好的那一份」**。

---

## 3. ⭐ 最重要的发现：整片良率按「生成次数」指数惩罚

这是本次分析的**核心增量**，也是抽卡定位的改变。

**整片良率 = [1 − (1−p)^N]^S**，S = **生成次数**（不是镜头数），N = 每段抽卡次数。

### p = 0.5 时

| 方案 | 生成次数 S | N=1 | N=3 | N=5 | N=5 总耗时 |
|---|---|---|---|---|---|
| 每镜一次生成（2.5s/次） | 12 | **0.02%** | 20.1% | 68.3% | 34.9 分钟 |
| 每次含 3 镜（7.5s/次） | 4 | 6.3% | 58.6% | **88.1%** | 23.3 分钟 |
| 每次含 6 镜（15s/次） | 2 | 25.0% | 76.6% | **93.8%** | **21.3 分钟** |

### 同一算力下，「少生成」完胜「多抽卡」

| 手段 | 良率变化 | 耗时变化 |
|---|---|---|
| **减少生成次数**（S: 12→2，N 不变） | 68.3% → **93.8%**（+25.5pt） | 34.9 → **21.3 分钟（还更快）** |
| **多抽卡**（S=12，N: 1→5） | 0.02% → 68.3% | 18.0 → **34.9 分钟（成本翻倍）** |

**⇒ 抽卡的优先级低于「降低生成次数」。** 因为：
- 生成次数是**指数惩罚**（乘法链，每段都必须过）
- 抽卡次数是**边际递减**（N=5→8 只从 68.3% 涨到 95.4%，但成本再涨）

**降低生成次数的唯一手段就是官方 base 的「一条片内自带多镜切」**（`[Shot 1] ... [Shot 2] At 00:03.500 ...`）——这条官方硬规则在 REPORT8 里只是「博主为什么不崩坏」的手法，**在这里它变成了良率的乘法级杠杆**。

### 三档杠杆的正确排序

1. **降生成次数**（片内多镜，S→1–2）→ 指数级收益，成本还降 ⭐ **最高优先**
2. **提 p**（闸门② 动作白名单 + 官方提示词规范）→ 决定抽卡是否可负担
3. **抽卡**（增 N）→ 边际递减，成本 ∝ N ← 用户当前在问的这一档

---

## 4. 抽卡成立的前提：失败必须「随 draw 改变」

这是决定抽卡是**真抽卡**还是**假装抽卡**的唯一判据。

| 失败类型 | 是否随换 seed 改变 | 抽卡有效吗 |
|---|---|---|
| 手部畸形 / 手指数量 | ✅ 会 | ✅ 有效 |
| 构图偏移 / 主体出框 | ✅ 会 | ✅ 有效 |
| 纹理闪烁 / 材质不一致 | ✅ 会 | ✅ 有效 |
| 短英文文字糊 | ✅ 部分会 | ⚠️ 有限有效 |
| **状态不守恒**（液量不减、盖子自合） | ❌ **不会** | ❌ **零效果** |
| **接触物理**（抓握、按压、形变） | ❌ **不会** | ❌ **零效果** |
| **多步因果链**（拧→倒→涂） | ❌ **不会** | ❌ **零效果** |

**机制**：扩散模型逐帧生成、无物理引擎 ⇒ 没有「因果链」和「守恒量」概念。这类失败**每次都在同一秒、以同一方式复现**，抽 N 次只是花 N 倍算力拿到 N 份同样错的片子。

⇒ **抽卡不是无条件的抽卡，它前面必须先有一道「可重抽性」判定。** 没有这道判定，抽卡没有地板，会无限烧卡。

---

## 5. 判官输出规格必须升级：不是「通过 / 不通过」

REPORT11 定的「原子是/否清单」还不够。因为**判官的结论决定下游动作，而不同失败类型要的动作完全不同**。

判官必须输出**三项**，而非一项：

| 输出项 | 取值 | 下游动作 |
|---|---|---|
| ① 是否达标 | 是 / 否 | 是 → 进种子资产库 |
| ② **失败类型** | 随机型 / 结构型 | 随机 → 重抽；结构 → 停 |
| ③ **可重抽性** | 会变 / 不会变 | 不会变 → **禁止重抽** |

**为什么必须分类**：判官只看到结果，看不到「这是第几次抽」。它天然会建议「这个不行，重抽」——**但对结构型失败，重抽 N 次仍然不行**。分类字段是把判官的「否决」翻译成正确动作的唯一接口。

⚠️ 单点铁律：**判官说「选出最好的」≠「达标」。** N 份全不合格时它会给你最不难看的那份 → 必须**绝对阈值 + 明确失败出口**，不许相对择优。

---

## 6. 保底机制：抽卡的灵魂，也是唯一防烧卡装置

真实抽卡游戏有「保底」（pity）。视频生成的保底**不是再送一抽，而是强制换策略**：

```
抽第 1 次 → 判官否决（随机型）→ 换新 seed
抽第 2 次 → 否决 → 换新 seed
抽第 3 次 → 否决 → 触发保底
   ↓
❌ 不再抽
✅ 强制走下列之一：改动作（回闸门②） / 改提示词 / 改段长 / 换工具 / 实拍补
```

**硬性封顶 N = 3–5**（p≥0.5 取 3；p<0.3 取 5 就别取了，直接改方案）。
**到顶必须报明确失败，绝不静默续跑** —— 对一键成站尤其重要：无上限重试 = 无上限账单。

⚠️ 反面模式（必须避免）：判官否决 → 直接重跑同一个任务。**同 prompt + 同 seed = 逐位相同输出（已实测）**，你会花 N 倍算力拿到 N 份视觉相同的片子和 N 次相同的否决。

---

## 7. 抽卡的单位经济学

抽卡成本 ∝ 段长（实测：2s→83s / 3s→102s / 4.5s→132s）：

| 段长 | 抽 1 次 | 抽 3 次 |
|---|---|---|
| 2s | 83s | 4.2 分钟 |
| 3s | 102s | 5.1 分钟 |
| 4.5s | 132s | 6.6 分钟 |

**规则：抽卡应作用在「最小的、包含该失败的单元」上。**
- ✅ 失败集中在 1 秒内 → 把该镜设计成短单元重抽
- ❌ 不要为了一段失败重跑整条长片（浪费已通过的段）
- ⚠️ 但**不要为省抽卡成本无限拆段**——段数是整片良率的指数惩罚项（第 3 节），拆多了整片良率崩得更快

**判定落在「段」不落在「整片」**（REPORT11 已有结论，此处进一步量化）。

---

## 8. 「多抽卡」vs「加步数」：等算力对照（未测，可当天验证）

当前用 8 步（蒸馏/加速档）。同样算力下，另一条路是**单次加步数**：

- 8 步 × 抽 2 次 = 204s，单段良率 **75.0%**（p=0.5）
- 16 步 × 抽 1 次 = 204s（若耗时翻倍）→ **单次通过率必须 ≥ 75.0% 才打平**

⇒ **可判定命题：加步数只有在能把单次通过率提升 25pt 以上时，才优于多抽一次。**
这是一个干净的 A/B，成本极低（同总算力），但从未测过。**它对抽卡决策的影响可能大于调 N。**

---

## 9. 三个决定性实验（都可当天跑完，一台 GPU）

### 实验 A · 抽卡真实增益 + 失败归因（≈13 分钟）
同一镜头（无因果链的 S1 类型，如产品静置+光影）**跑 8 个不同 seed**，逐次判官判定与人工复核，记录：
- 通过几个 → **p**
- 失败是否**落在同一处** → 区分随机型 / 结构型
- 8 次两两画面差异 → **D 的独立复测**

**判读**：有通过 + 失败位置分散 → 抽卡有效，用 §3 表定 N｜全部失败且失败位置相同 → **结构型，抽卡无效，立刻回闸门②**

### 实验 B · 2 因子方差分解（≈13 分钟）⭐ 决定钱花在哪
**3 个提示词版本 × 3 个 seed = 9 次**，比较：
- 组内方差（同提示词的 seed 间差异）
- 组间方差（不同提示词间的差异）

**判读**：组间 > 组内 → **优化提示词的收益大于抽卡**（假设 3:1，则提示词杠杆是抽卡的 3 倍）｜组内 > 组间 → 抽卡才是主要杠杆

> 这个实验回答的是**最值钱的问题**：你的钱该投在「把提示词写对」还是「多抽几次」。

### 实验 C · 等算力对照（≈7 分钟）
同一镜头：8 步 × 2 seed vs 16 步 × 1 seed，总耗时对齐，比通过率。

---

## 10. 与已有闸门体系的关系（不能互相替代）

```
闸门②（动作白名单）→ 挡「结构型失败」进门     ← 唯一防线，不可卸
    ↓
闸门③（可重抽性判定）→ 抽卡前的止损闸门       ← 本次新增
    ↓
判官团（三级级联 + 分类输出）→ 筛随机型失败
    ↓
保底（N 次到顶 → 强制换策略）
```

**抽卡只能替代「人工看片」，不能替代闸门②。** 卸掉闸门② 只装判官团，是最坏的组合：你会用 N 倍算力稳定复现同一个不可修的错误。

---

## 11. 诚实边界

- ✅ **实测**：D = 14.0（seed 差异）｜噪声 2.3｜模型参数 20–35｜同 seed 同 prompt 逐位复现｜段长耗时曲线
- ❓ **未测**：p（通过率）｜J（判官准确率）｜失败是否随 seed 变化（推论有机制支撑，但未直接测）｜16 步增益
- 🧮 **外推（非实测）**：7.5s / 15s 单次耗时按 124 帧 ≈115s 线性外推，实际有非线性开销，**别当基准用**
- ⚠️ 15s 段 + 片内多镜的高收益方案需要**官方 API**（本地 768p base 段长受限），与 REPORT12 结论一致

---

## 12. 一句话结论

**抽卡不是错觉，它建立在实测的 seed 方差（14.0）之上；但它排在第三顺位。**
比它更值钱的两件事是：**① 降低生成次数（片内多镜，指数级收益）② 先把 p 提上去（闸门② + 提示词规范）**。
而且抽卡前面必须先装一道**「可重抽性」判定**——否则对结构型失败，你只是用 N 倍算力稳定复现同一个不可修的错误。
