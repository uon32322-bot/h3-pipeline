# 带货视频生成 SOP · 从产品信息到成片（九步）

> 版本：**v2.0**（v1.0 → 本版：**音频层上线 + 时长 30–50 s + 生成分组 + 15 列 + 三闸**）　｜　日期：2026-09-18
> 定位：带货内容层的主干流程。插在「拿到产品信息」与「写 H3 提示词」之间 —— 补上原先缺失的**结构 → 文案 → 音频 → 脚本**四层。
> 依据：行业结构调研（REPORT29 §一）× 本项目实测能力边界（REPORT29 §二）× **音频层实测（REPORT30 / REPORT3 / REPORT5）**
> 配套模板：`templates/商品档案模板.md`、`templates/带货文案模板.md`、`templates/音频层决策表.md`、`templates/分镜脚本模板.md`
> 接口契约：`shotlist_schema.json` **v2.1**　｜　音频判官：`scripts/judge_audio.py`

---

## 0. 一句话

> **先定「这条片怎么说服人」，再定「观众听到什么」，最后定「每个镜头画什么」。**
> 三道顺序都不可颠倒 —— 颠倒的产物分别叫「卖点陈列片」「哑巴片」「返工片」。

```
产品信息
   ↓ S0 商品档案      ← 卖点分级：谁是主角、谁是配角、谁只配进后期
   ↓ S1 结构选型      ← 选骨架 + 定目标时长档（32 / 40 / 50 s）
   ↓ S2 带货文案      ← 写出「说服链」：钩子/痛点/证明/信任/CTA 各说一句
   ↓ S3 文案四分流    ← 决定每个字去哪：音轨（口播）/ 画面英文 / 后期图文 / H3 音效
   ↓ S3.5 ⭐ TTS 出整条音轨   ← 【音频先行】段长从此由音轨决定，不再拍脑袋
   ↓ S4 分镜脚本      ← 一段文案 = 一个镜头，标注带货功能 + 音频模式（15 列）
   ↓ S1.5 生成分组    ← 相邻镜合并成生成段（≤15.08 s），段长贴音轨
   ↓ S5 闸门②        ← 三闸：动作可行性 + 文案合规 + **音频可行性**
   ↓ S6 生成与判官    ← 锚定 → H3 生成 → 判官团 L0/L1/L2 + **A 段音频**
   ↓ S7 装配与投放层  ← 音轨（锚定或直贴）+ 叠字 + 合规标识 + 卡点
成片
```

**S0–S4、S1.5 全部零 GPU 成本**（纯文字 + TTS，Mac 上完成）。算力只在 S6 花。

> ⭐ **v2.0 最重要的次序改动：音频先行（S3.5 提前到生成之前）**。
> 理由：H3 的音频锚点是「锚在**帧索引**上、裁到视频剩余时长」⇒ **音轨时长必须先定**。
> 若反过来（先出画面再配 TTS），一旦口播装不下就是**整段重跑**（40 s 片的单段 ≈ 22–38 min）。

---

## S0 · 商品档案（Product Brief）

**输入**：产品信息原文（厂家给的，**不增不改**）
**输出**：`商品档案.md`（用 `templates/商品档案模板.md`）
**校验**：每个卖点都能追溯到原文的一行，没有凭空发明的卖点

| 动作 | 说明 |
|---|---|
| ① 卖点抽取 | 逐条抄原文，**禁止改写、禁止补充**，保留原编号 |
| ② ⭐ 卖点分级 | 主卖点 1 个 / 辅助卖点 ≤2 / 规格走后期 / 其余删 —— **决定结构，最容易被跳过** |
| ③ 人群定位 | 谁买（年龄/身份/场景），决定钩子用哪一款 |
| ④ 场景清单 | 产品在哪些真实场景里被使用，决定「场景代入」段拍什么 |
| ⑤ 客单价档位 | 决定信任段长度（低价冲动品可压缩，高价必做满） |
| ⑥ 合规定级 | 有没有功效宣称 / 极限词风险（鞋服低风险，食品美妆高风险） |
| ⑦ ⭐ **品类路由** | 产品**必须靠人脸才成立吗**？→ 决定这条片走 **L 片**还是 **V 片**（见 S3） |

> ⚠️ **行业共识：一条视频只讲 1–2 个核心卖点。卖点堆砌 = 没有卖点。**
> ⚠️ **首因效应**：第一个出现的卖点必须是最核心的那个。

---

## S1 · 结构选型（Structure）

**输入**：商品档案
**输出**：`结构选型卡`（写进文案模板头部）
**校验**：骨架能覆盖主卖点；五段时长相加 = 目标时长；>20 s 已补续命钩

### 1.1 先选时长档

| 档 | 目标时长 | 口播字数 | 语速 | 单条片期望 GPU（模型①可重抽） | 适用 |
|---|---|---|---|---|---|
| 精简 | **32 s** | 128–160 | 4.0–5.0 | ≈ 58 min | 钩子强、单卖点、投流测试量 |
| **标准（推荐）** | **40 s** | 160–200 | 4.0–5.0 | ≈ 97 min | 五段完整展开，行业主流 |
| 长片 | **50 s** | 200–250 | 4.0–5.0 | ≈ 123 min | 高客单价、需多卖点铺陈 |

> 现状 16.2 s / 97 字 = **5.99 字/秒（超速）** —— 这是"15 s 太短"的量化证据。

### 1.2 五段比例表（可缩放到任意目标时长）

| 段 | 只干一件事 | 比例 | 下限 | 上限 | 32 s | 40 s | 50 s |
|---|---|---|---|---|---|---|---|
| **① 钩子** | 让他**停下** | 8% | 2.5 | 4.0 | 2.56 | 3.20 | 4.00 |
| **② 痛点 / 场景代入** | 让他**代入** | 17% | 4.0 | 8.5 | 5.44 | 6.80 | 8.50 |
| **③ 价值证明** | 让他**相信有用** | **37%** | 10.0 | 18.5 | 11.84 | 14.80 | 18.50 |
| **④ 信任背书** | 让他**放下顾虑** | 20% | 5.0 | 10.0 | 6.40 | 8.00 | 10.00 |
| **⑤ CTA 促单** | 让他**现在点** | 18% | 4.0 | 9.0 | 5.76 | 7.20 | 9.00 |
| | | 100% | | | 32.00 | 40.00 | 50.00 |

### 1.3 ⭐ 续命钩（>20 s 必填，v1.0 提过但从未落地）

| 目标 | 续命钩位置（40–60% 处） | ③ 段内部结构 |
|---|---|---|
| 32 s | 12.8–19.2 s | 前 60% 承接主卖点（慢说明） |
| **40 s** | **16.0–24.0 s** | **后 40% 制造第二高潮**（换景别 / 换场景 / 揭晓新证据） |
| 50 s | 20.0–30.0 s | |

### 1.4 四套骨架（按产品类型选一套）

| 骨架 | 逻辑 | 适配 | 画面能不能支撑 |
|---|---|---|---|
| **PAS**（痛点爆破） | 痛点 → 激化 → 解决 | 功能型（缓震/防水/清洁） | ✅ 能（痛点是"脚累"，画面给「落地受压」） |
| **场景代入**（BAB 变体） | 不拍"用前多惨"，改拍"用的时候是这样" | 鞋服/家居 | ✅ 能 —— **我们的 BAB 只能做后半段**（AI 生成不出同一主体的真实 Before） |
| **FABE**（特性→优势→利益→证据） | 逐层递进 | 高客单价 / 决策周期长 | ✅ 40 s 起**能装下完整 4 层**（16 s 时只能压成 2 层） |
| **反差悬念** | 反常识开场 → 揭晓 | 视觉差异化强 | ✅ 最能发挥 AI 生成优势（悬浮/极近特写/反常视角） |

**选型决策树**

```
产品有可量化的强功能卖点吗？（缓震/防水/续航）
├─ 有 → PAS
└─ 无 → 客单价 > 300 元？
        ├─ 是 → FABE（40 s 起可展开完整 4 层）
        └─ 否 → 视觉差异化强吗？
                ├─ 强 → 反差悬念
                └─ 弱 → 场景代入
```

---

## S2 · 带货文案（Copy）

**输入**：结构选型卡
**输出**：四段文案（口播 / 画面英文 / 后期图文 / H3 音效），用 `templates/带货文案模板.md`
**校验**：口播字数 ≤ 目标时长 × 5 字/秒，且 ≥ 目标时长 × 4 字/秒

### 2.1 四段并写（不是先后）

| # | 文案 | 载体 | 长度铁律 | 说什么 |
|---|---|---|---|---|
| **① 中文口播** | 说服链主体 | **音轨**（TTS 出，见 S3.5） | **目标时长 × 4–5 字/秒** | 完整的 5 段逻辑 |
| **② 画面内英文** | 视觉锚点 | 进 H3 prompt 逐字 | **3–5 词 / 句，单行** | 每镜 ≤1 句，感受型/功能型 |
| **③ 后期图文** | 参数与指令 | 后期叠加 | 屏上 ≤ 12 字 | 价格 / 尺码 / CTA / 合规标识 |
| **④ H3 音效** | 氛围与真实感 | `overall_soundscape` / `non_diegetic_music` | 短句描述 | 环境音 + 拟音 + 配乐 |

> ⭐ **「带货逻辑」的主要载体是 ① 中文口播**。画面英文只能装 3–5 词，后期图文用户不读 —— **只有口播能讲完一套说服链**。
> ⭐ **④ 是 v2.0 新增的分流**：H3 是音视频联合生成，`overall_soundscape` 写好了它自带环境音与拟音（我们之前只出环境音，是因为提示词用的是自由散文体）。

### 2.2 五段式口播填空（照填）

| 段 | 模板句式 | 举例（运动鞋·40 s 版） |
|---|---|---|
| **① 钩子** | 痛点点名：「总在【场景】踩坑的人，这条别划」／反差／反常识／结果前置 | 「跑了三年步，膝盖最先抗议的，是不是你？」 |
| **② 痛点/场景** | 具体场景 + 不解决的代价（**用「你」，不用「我们」**） | 「跑完膝盖发酸，第二天上下楼都得扶着栏杆。」 |
| **③ 价值证明** | 1–2 个买点 **翻成使用画面**（不念参数）；**40 s 起可在后 40% 加第二高潮** | 「踩下去先是软，然后马上回弹，落地那一下不砸膝盖。」+ 第二拍 |
| **④ 信任背书** | 给**能拍出来的证据**，配一个**小缺点**（零缺点=不可信） | 「中底是 EVA 高弹发泡，厚切分层你看得见。鞋码偏正常，脚宽的按平时码来。」 |
| **⑤ CTA** | **理由 + 指令**（不是只喊便宜） | 「129 起，36 到 45 码，链接我放下面了。」 |

> ⭐ **④ 必须配一个「小缺点」** —— 零缺点反而降低可信度。
> ⭐ **用「你」**：第二人称脚本完播率平均高出约 40%（行业经验值，未在我们身上实测）。
> ⭐ **数字 > 形容词**：「3 天卖 10 万单」比「很多人买」有力；「250g」比「很轻」有力。

### 2.3 合规红线（写文案时逐词替换）

| ❌ 别说（极限词 / 功效承诺） | ✅ 换成（可考证 / 安全） |
|---|---|
| 最好用 / 第一 / 全网最低 / 顶级 / 国家级 | 我自己用下来最顺手的 / 这个价位挺能打 |
| 销量第一 / 断货王 / 全网爆卖 | 最近回购的人是真不少 |
| 根治 / 治愈 / 彻底解决 / 药到病除 | 连用两周，真实变化是…… |
| 瘦 X 斤 / 永不复发 / 立竿见影 | 配合作息，我自己的体感是…… |
| 唯一 / 独家（无专利支撑时） | 我目前见过做得比较细的 |

> 依据：《广告法》第九条禁止「国家级」「最高级」「最佳」等绝对化用语；保健食品、医疗器械另有专门规定。
> ⇒ **此表进闸门② 闸 B**（见 S5）。

---

## S3 · 文案四分流（Split）

**输入**：四段文案
**输出**：每镜的 `copy_split` 字段（进 schema）
**校验**：**没有任何一行中文进入 H3 prompt**

| 分流 | 去向 | 处理规则 |
|---|---|---|
| **① 画面内英文** | **进 H3 prompt，逐字包含** | 中文卖点 → 重写成 3–5 词英文；闸 B 校验「prompt 逐字包含所有画面英文」 |
| **② 中文口播** | **音轨**（TTS 出整条） | 送达方式二选一：**L 模式锚进 H3** / **V 模式后期直贴** —— 见下面 3.3 |
| **③ 后期图文** | 后期叠加 | 价格 / 尺码 / CTA / 合规标识 **绝不交给模型画**（必糊） |
| **④ H3 音效** | `overall_soundscape` / `non_diegetic_music` | 短句描述环境音 + 拟音 + 配乐 |

### 3.1 画面内英文的写法铁律

| 规则 | 说明 | 反例 |
|---|---|---|
| 3–5 词、单行 | 超过就拆成两镜，或**别进画面** | ❌ `Air Flows Right Through The Mesh Upper` |
| 只写**可读**的短词 | 长词在竖屏小字号下糊 | ❌ `PERSPIRATION` |
| **落位必须一起声明** | 说清站在哪个面、是否须避开产品 | ❌ 只写文字不写位置（→ D 片真实事故，REPORT28） |
| 不与产品主体重叠 | 压在产品上必糊（对比度不够） | ❌ 字压在黑网面上 |
| 一镜 ≤ 1 句 | 两句同时出现在一镜里会互相干扰 | — |

### 3.2 ⭐ 音频层：三模式（辉哥要的「自动区分」）

| 模式 | 代号 | 画面特征 | 音轨怎么来 | 口型 | 音色 |
|---|---|---|---|---|---|
| **口型同步** | **L** | 有**可辨识人脸**且在**说话构图**（中近景 / 正侧脸 / 嘴可见） | **H3 锚定**（`AddGuide(audio)` 锚在生成段首） | 嘴动跟随（实测零时滞 r≈0.44–0.53） | H3 重渲染（波形相关 0.27） |
| **画外音** | **V** | 无脸 / 脸不可辨（**全身远景、运动中**）/ 产品特写 / 空镜 | **后期直贴**（H3 只出环境音） | 无 | **TTS 音色 100% 保留** |
| **纯环境音** | **N** | 需 ASMR / 拟音 / 氛围（钩子镜、纯视觉镜） | 不贴语音，保留 H3 原生 `overall_soundscape` | 无 | — |

> ⚠️ **概念澄清（重要）**：H3 在这一环是**音画同步器**，不是**配音员**。
> 实测：FL2VA `AddGuide(audio)` 能把外部配音**内容级保留**并让口型跟随（DTW 0.036）；而 Ref2VA 的 `ref_audios`音色参考**实测零效果**且算力 +95~115%。
> ⇒ **配音必须由外部 TTS 出**（音色可控），H3 只负责"同步进画面"。

### 3.3 三层路由（自动区分规则）

```
第一层 · 品类（立项时定，S0 第 ⑦ 项）
├─ 产品必须靠「人的脸/身体」才成立？
│   美妆上妆 / 个护上脸 / 服饰上身 / 食品入口 / 健身示范 / 母婴使用
│   ├─ 是 → 必须有至少一镜 L ⇒ 全片走「H3 锚定」路线
│   └─ 否 → 推荐全 V（鞋类 / 3C / 家电 / 家居 / 玩具 / 工具）
↓
第二层 · 逐镜
├─ 有可辨识人脸吗？
│   ├─ 无 → V
│   └─ 有 → 处于说话构图吗？（中近景 / 正侧脸 / 嘴可见）
│       ├─ 是 → L
│       └─ 否（全身远景、运动中、背影、侧脸遮挡）→ V ⭐
└─ 需 ASMR / 拟音 → N
↓
第三层 · 全片一致性（铁律：只能走一条路）
├─ 一旦有任一 L 镜 ⇒ **全片所有含口播的生成段都锚同一条音轨**（同输入音色+同模型 ⇒ 漂移最小）
└─ 全 V 片 ⇒ **全片后期直贴**（音色完美 + 可随时改文案 + 零生成成本）
```

**为什么「全身远景 + 运动」必须判 V**：REPORT3 §4.2 实测，口型-音频互相关在动作小的段为零时滞 r≈0.5；
在动作与语音重叠的段掉到 0.16–0.36 —— 那是测量被干扰，但同时也说明**人在跑/在走时嘴部信号本就不可辨**。
⇒ 我们的 **C1 棚拍跑步 / C2 户外行走（全身远景）判 V**，运动鞋这条片天然是**全 V 片**。

### 3.4 音频层两条硬规则

| # | 规则 | 依据 |
|---|---|---|
| 1 | **说话时别做别的**：L 镜不得同时安排大幅手部/产品动作 | REPORT3 §4.2 实测（动作与语音时间重叠 ⇒ 口型测量被干扰） |
| 2 | **L 镜不得跨生成段**：一个口型镜必须整镜落在同一生成段内 | 跨段 ⇒ 语音被两次独立重渲染 ⇒ 边界音色 + 口型双断 |

### 3.5 ⭐ TTS 出整条音轨（音频先行，本步是硬前置）

| 项 | 要求 |
|---|---|
| 输出 | **整片一条 32 kHz 单/立体声 wav**（贯穿全片，句间留呼吸空隙） |
| 音色 | 全片同一音色（跨片统一主播声音） |
| 语速 | 目标时长 × 4–5 字/秒（40 s ⇒ 160–200 字） |
| 运行位置 | ⚠️ **必须 CPU / 本地 Mac** —— TTS 与 ASR 都**不得占 3090 的 CUDA**（GPU 隔离红线延伸） |
| 交付物 | `vo/track_full.wav` + `vo/track_manifest.json`（**每句的起止时间**） |

> `track_manifest.json` 是下游的硬输入：段长由它定，`--cuts` 由它定，A-04 判项也读它。

---

## S4 · 分镜脚本（Shotlist）

**输入**：四分流后的文案 + 音轨清单
**输出**：`分镜脚本.md`（**15 列**节拍表）
**校验**：每镜带货功能唯一；相邻两镜至少变一项；每镜动作数 = 1；**每镜音频模式已标注**

### 4.1 分镜表 **13 列 → 15 列**（v2.0 新增两列）

| 新列 | 取值 | 作用 |
|---|---|---|
| **音频模式** | `L` 口型同步 / `V` 画外音 / `N` 纯环境音 | 决定该镜音轨怎么送、要不要口型 |
| **音轨锚点** | 该镜口播句在音轨上的**起止时间**（`00:03.200–00:06.850`） | 定段长、定 `--cuts`、喂 A-03/A-04 判项 |

> ⚠️ **「音频模式」列是约束②的落地处** —— 标 `V` 的镜**不得出现「脸可辨 + 说话构图」**（否则口型穿帮）。
> 音色一致性由**约束①音轨单源**保证（全片同一条 `vo/track_full.wav`），**与 L/V 怎么分布无关**（v2.0 修正，见 S3.3 第三层）。

### 4.2 分镜三硬规则

1. **关键动作唯一主落实镜** —— 一个卖点动态只能由一镜承担，不许两镜各拍一半
2. **相邻两镜至少变一项**（景别 / 动作 / 场景 / 款式），否则合并
3. **景别由信息尺度定** —— 要看清材质就给特写，要传达"轻"就给大留白全景

### 4.3 时长必须贴帧网格

| 规则 | 值 |
|---|---|
| 时长网格 | **17k+5 帧 @ 24 fps**（禁用任意秒数） |
| 常用档 | 39 帧 1.625 s｜56 帧 2.333 s｜73 帧 3.042 s｜107 帧 4.458 s｜124 帧 5.167 s｜141 帧 5.875 s｜192 帧 8.000 s｜226 帧 9.417 s｜243 帧 10.125 s｜294 帧 12.250 s｜328 帧 13.667 s｜362 帧 15.083 s |
| 单镜上限 | ≤ 2.5 s/节拍（超了拆镜） |
| **单段上限** | **362 帧 = 15.083 s**（本地权重实际可用上限） |

---

## S1.5 · 生成分组（Grouping）—— v2.0 新增步骤

**输入**：15 列节拍表 + 音轨清单
**输出**：`生成分组表`（G1…Gn）
**校验**：每段 ≤ 362 帧；**L 镜不跨段**；段边界落在转场/句末

### 分组三原则

| # | 原则 | 说明 |
|---|---|---|
| 1 | **段边界贴音轨句末** | 口播句在 6.85 s 结束 ⇒ 段切在 6.85 s 附近（不留半句话） |
| 2 | **段边界落在转场点** | 优先硬切/场景变更处，不切在动作中段 |
| 3 | **每段 2–4 镜** | 超 4 镜 ⇒ 单镜被压到 <2 s，动作做不完（H3 每段真实"在动"时间恒定 1.4–2.3 s） |

### 分段数 ≠ 成本决定项（⭐ 反直觉，必须知道）

| 成本模型 | 结论 |
|---|---|
| **① 允许逐段重抽**（我们的流程） | 期望成本 = `Σ t_i / 0.875` ⇒ **段越短越省**（超线性 `t ∝ f^1.7`） |
| **② 要求一次过** | 期望成本 = `Σ t_i / 0.875^S` ⇒ **段越少越省**（S 是指数项） |

**两个模型结论相反 ⇒ 分段数不该由成本决定，应由内容决定**（哪几镜必须同段才连贯）。
唯一确定的是**总量**：16.2 s ≈ 32 min → **32 s ≈ 58 min / 40 s ≈ 97 min / 50 s ≈ 123 min**（模型①口径）。

> 单段耗时标度（768×1344 / 8 步 / 3090 显存受限，**>243 帧为外推**）：
> `t(f) ≈ 431 × (f/124)^1.70` 秒　｜　实测锚点：124 帧 = 431 s、243 帧 = 1325.5 s

---

## S5 · 闸门②（三闸：动作 + 文案 + **音频**）

**输入**：分镜脚本 + 生成分组表 + 音轨
**输出**：放行 / 退回改写
**校验**：逐行过三张表，任一行触发否决 ⇒ **整行退回上层重做，不得进入生成**

### 闸 A · 动作可行性（原有）

| 判定 | 内容 |
|---|---|
| **白名单**（只能写这 9 个） | 举起 / 并排 / 推近 / 旋转 / 开合 / 滑入 / 光影流动 / 静置 / 特写平移 |
| **物理不可生成** | 涂开 / 倒出 / 拧开 / 涂抹 / 挤压出液 |
| **生成性禁写** | 双主体精确交互 / 内部心理活动 / 否定式动作 / 一拍三步动作链 |
| **硬规则** | 每镜动作数 = 1；含可验证物理结果的动作 → 否决 |

### 闸 B · 文案合规（v1.0 新增）

| 检查项 | 不通过则 |
|---|---|
| 极限词表命中（最好/第一/全网最低/根治/治愈…） | 退回换词 |
| 功效承诺无依据（"瘦 X 斤""永不复发"） | 删除或降级为体感描述 |
| 价格/尺码/CTA 出现在画面英文里 | 移至后期图文 |
| 画面英文字数 > 5 词或非单行 | 拆句或移出口播 |
| 画面英文**未声明落位** | 补落位声明（否则必出 D 片事故） |

### 闸 C · 音频可行性（**v2.0 新增**）

| 检查项 | 不通过则 |
|---|---|
| 口播字数 > 目标时长 × 5 字/秒 | 删句（**先删 ③ 的形容词，再删 ② 的修饰**） |
| 整片音轨路线**不唯一**（既有 L 又贴后期） | 收敛到单一路线（S3.3 第三层） |
| 标了 `L` 的镜**没有可辨识人脸 / 不在说话构图** | 降为 `V` |
| 标了 `L` 的镜**同时有大幅手部/产品动作** | 拆镜（说话交给另一镜），或降为 `V` |
| `L` 镜**跨了生成段** | 重排分组（S1.5） |
| 音轨总长 ≠ 成片总长（缝隙 > 0.3 s/句） | 重做 TTS 轨 |
| 走 L 且是**中文配音**但**未做标定** | 先跑标定（中文口型同步**零实测数据**） |

---

## S6 · 生成与判官（Generate & Judge）

**输入**：放行的分镜脚本 + 生成分组表 + **已定稿的音轨**
**输出**：通过判官团的生成段

| 步 | 动作 | 关键约束 |
|---|---|---|
| ① | 三张独立锚定照（**禁网格/拼贴**） | 有 logo/中文/价格 ⇒ 走像素级保真（混合合成） |
| ② | 锚点构图**留 ≥13% 四边余量** | 插值会外插到锚点之外（C1 实测） |
| ③ | 锚点跨幅目标 **下半身 MAD ≈ 17–19** | 超上界 ⇒ 尾部冻结（结构型，禁重抽） |
| ④ | **L 片：整条音轨作单锚点挂段首**（`AddGuide(audio)`）／**V 片：不锚音频** | 一端锚点即可；**别做多音频锚点叠加**（未验证） |
| ⑤ | H3 生成（多镜一条片内） | 8 步 Turbo，768×1344 @0.98 MP；`--cuts` 声明多镜切点 |
| ⑥ | 判官团 **L0 / L1 / L2 + A 段**（**全部出 GPU？不 —— A 段与 L0/L1 全在 CPU**） | L2 锁定 `tt-5.6-luna`；A 段 CPU int8 |
| ⑦ | **分段判官**：一段生成完**立即**判，不等全片 | 早发现早重来（40 s 片一段就 22–38 min） |
| ⑧ | 不达标 → **随机型**换 seed 重抽（N≤3）｜**结构型禁重抽**，走规避/转嫁/拆解 | 同 seed 同 prompt 逐位相同 |

### ⭐ 判官团 v2.0 阵容

| 层 | 覆盖 | 成本 | 否决权 |
|---|---|---|---|
| **L0**（纯算术） | 解码/帧数/分辨率/时长/亮度/切点 + **A-01 音轨非静音** + **A-04 语音段内结束** | <1 s | 有 |
| **L1**（Mac CPU / numpy） | 活动度 / 断裂 / 闪烁 / 出框余量 / 产品保真 / 色调 | 数秒 | 有 |
| **L2**（远端 VLM `tt-5.6-luna`） | R/W/P 段 18 项视觉判据（3 票投票） | 1.5–3 min | 有 |
| **A 段**（**新增，CPU**） | A-01 音轨非静音｜**A-02 音轨 vs TTS 参考（log-mel + DTW）**｜A-03 口型同步（**仅测量，无否决权**）｜A-04 语音段内结束｜A-05 跨段音色一致（**待标定**） | 数秒 | ✅ A-01/A-02/A-04 已标定；⚠️ A-03/A-05 只报数值 |

> ⚠️ **A 段四条纪律**：
> ① 能用算术判的**不要问 VLM**；
> ② **A-02 用「与自持 TTS 参考做 DTW」，不用 ASR** —— `faster-whisper` 权重下载被 HF CAS 401 拦死（实测），而我们**本来就持有喂进去的那条 TTS**，log-mel + DTW 零网络、约 1 s 出结果（与 REPORT3 同源方法）；
> ③ **A-03 降级为「只测量」** —— 全身运动镜实测 r=0.52 / 0.79，互相关无法区分"嘴在动"和"人在动"；两种补救闸门（整幅活动度、局部集中度）**均无法分离**（详见 REPORT30 §7），⇒ 默认 `verdict=na`，仅 `--force-a03` 出结论；
> ④ **新增判项必须两头标定才能上线**（R-08 的教训）—— `judge_audio.py` 在标定前**只报数值、不参与否决**（`--enforce-uncalibrated` 可强制启用做实验）。
>
> 📌 **A-02 标定证据（4 点，全部通过）**：同内容异音色 0.41 ✅｜异内容 2.17 ✅｜纯环境音 0.86 ✅｜真实 H3 输出 0.18 ✅（阈值 `max_dtw_ratio=0.75`、`max_dtw_abs=0.10`）。

---

## S7 · 装配与投放层

**输入**：通过的生成段 + 四段文案 + 音轨
**输出**：成片 + 投放版

| 层 | 内容 | 载体 |
|---|---|---|
| **中文口播** | S2 的口播稿 | **L 片**：已在 H3 输出里 ⇒ 只搬不用贴<br>**V 片**：`ffmpeg` 直贴 `track_full.wav`（音色 100% 保留） |
| **中文字幕** | 口播逐句字幕（可选） | 后期叠加，读 `track_manifest.json` 对齐 |
| **信息条** | 卖点标签（≤12 字/屏） | 后期叠加 |
| **CTA 层** | 价格 / 尺码 / 「点击下方链接」 | 后期叠加，**末尾 3 s 必须出现** |
| **合规标识** | AIGC 显式标识 | 后期叠加 |
| **音轨混音** | 口播 + BGM | BGM 压到 −18 dB 左右，口播优先；`loudnorm` 归一 |
| **卡点** | 段落切换踩 BGM 重拍 | 段落边界 = S1 的五段时间点 |

### 双版本输出（沿用现有约定）

| 版本 | 内容 | 用途 |
|---|---|---|
| **纯净版** | 无中文层（零后期文字） | 严格合官方 SOP，素材复用 |
| **投放版** | 口播 + 字幕 + 信息条 + CTA | 实际投流 / 带货 |

---

## 附：全流程验收清单（v2.0）

- [ ] **S0** 每个卖点可追溯到产品信息原文；主卖点唯一；规格信息已归入后期；**品类路由已判（L 片 / V 片）**
- [ ] **S1** 骨架选定并写明理由；五段时长相加 = 目标时长；**>20 s 已补续命钩**
- [ ] **S2** 口播字数 = 目标时长 × 4–5 字/秒；④ 有「小缺点」；⑤ 有「理由 + 指令」；极限词零命中
- [ ] **S3** 画面英文每镜 ≤ 5 词、单行、**已声明落位**；无一行中文进 prompt
- [ ] **S3** 每镜**音频模式已标注**（L/V/N）；**音轨单源**（全片只用一条音轨）；**`V` 镜无「脸可辨 + 说话构图」**
- [ ] **S3.5** 整条音轨已出 + `track_manifest.json` 已出；**TTS 跑在 CPU**
- [ ] **S4** 每镜有带货功能 + 卖点承载 + 音频模式 + 音轨锚点；相邻两镜至少变一项；动作数 = 1
- [ ] **S4** 全部时长贴 17k+5 帧网格
- [ ] **S1.5** 每段 ≤ 362 帧；L 镜不跨段；段边界贴句末/转场
- [ ] **S5** 闸 A / 闸 B / **闸 C** 全绿
- [ ] **S6** 锚点 ≥13% 余量；跨幅在 17–19 区间；**分段判官**通过（L0/L1/L2 + A 段）
- [ ] **S7** 末尾 3 s 有 CTA；AIGC 标识在；口播与画面段落对齐
- [ ] **覆盖核对**：主卖点 + 辅助卖点**每一条**都能在 S4 表里找到承载镜（回填 `卖点覆盖表.md`）

---

## 附：常见坑（行业经验 + 本项目实测）

| # | 坑 | 后果 | 规避 |
|---|---|---|---|
| 1 | 跳过文案直接写分镜 | 卖点陈列片，无转化逻辑 | S2 必须先于 S4 |
| 2 | 卖点堆砌 | 用户记不住任何一条 | 1–2 个卖点，其余进后期 |
| 3 | 钩子超过 4 s | 流量流失 | 钩子段 ≤ 96 帧 |
| 4 | 零缺点 | 不可信 | ④ 必配一个小缺点 |
| 5 | 只喊便宜不给理由 | CTA 无力 | ⑤ = 理由 + 指令 |
| 6 | 画面英文不声明落位 | 文字压产品上（**D 片实测事故**） | 闸 B |
| 7 | 把 Before/After 写进 prompt | AI 生成不出真实前后 | 改「场景代入」 |
| 8 | 拿提示词驱动动作 | 100% 不动作（实测） | 帧驱动（锚点相位） |
| 9 | 结构改了却沿用旧锚点 | 声明与画面不符（C1 实测事故） | 结构变更 ⇒ 锚点重审 |
| 10 | 只做画面不做口播 | 没有说服链 | ① 口播是带货逻辑的主载体 |
| 11 | **先出画面再配 TTS** | 口播装不下 ⇒ **整段重跑**（22–38 min） | **S3.5 音频先行** |
| 12 | **L 片与 V 片混贴音轨** | 音色断层（听着像换人） | 全片路线唯一 |
| 13 | **在全身远景运动镜上做口型同步** | 白付成本 + 口型不可辨 | 判 V |
| 14 | **说话的同时安排大幅动作** | 口型测量被干扰（实测 r 掉到 0.16） | 「说话时别做别的」 |
| 15 | **一个口型镜跨了两个生成段** | 音色 + 口型在段边界双断 | L 镜不跨段 |
| 16 | **想靠 H3 自己念中文台词** | 音色不可控；中文语音质量**未验证** | 外部 TTS + 锚定/直贴 |
