# REPORT8 — 官方 skill、博主手法、以及"本地参考不生效"的根因

> 调研日期：2026-09-16 ｜ 资料来源：MiniMax 官方仓库 `github.com/MiniMax-AI/MiniMax-H3`（已拉取到本地 `official/`）、官方 README 中文版、官方可复现脚本、H3 提示词规范原文（`base-en.txt` / `ref-en.txt`）、官方 9 个技能包、社区实战复盘
>
> 本文修正 REPORT7 中的两条结论，见 §5。

---

## 0. 三个问题的直接答案

| 你问的 | 答案 | 一句话依据 |
|---|---|---|
| 博主们是怎么控制不崩坏的？ | **不是模型不同，是控制结构不同**。他们用官方那套"结构化规格书"提示词 + **一条片内自带多镜切** + 每镜一个动作 | 官方 `base-en.txt` 把每个规则都写死成硬约束 |
| 官方有专门的 skill 吗？ | **有，9 个**。其中 1 个通用提示词引擎 + 2 个电商/品牌专用 | `npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --skill h3-prompt-writing` |
| 官方电商案例为什么效果好？ | 因为走了**完整三层架构**（Context-IR + Base + 2K），我们本地只有中间那层 | 官方原话："Context-IR 对最终输出质量至关重要，强烈建议纳入流水线" |

---

## 1. 官方 skill 清单（真实存在，已拉取验证）

仓库根目录 `skills/`，共 **9 个**：

### 1.1 唯一可移植的那个（最重要）

| 技能 | 作用 | 依赖 |
|---|---|---|
| `h3-prompt-writing` | 把随口说的需求改写成符合 H3 规范的**结构化提示词**（规格书） | **纯 Markdown，无外部 API，任何 agent 都能用** |

它带两份核心参考文档，是全部规则的源头：

- `references/base-en.txt`（15.7 KB）— T2VA / I2VA / FL2VA / L2VA 的完整规范 + 4 个官方 case
- `references/ref-en.txt`（23.5 KB）— Ref2VA 全参考模式的**六段式**规范 + 4 类标签 + 4 级保留强度

我已把这两份规范原文和 8 个风格技能的 SKILL 全文拉到本地：
```
h3-pipeline/official/
├─ h3-prompt-writing/references/{base-en.txt, ref-en.txt}
├─ style-skills/*.SKILL.md + *.SKILL.cn.md   （16 个文件）
└─ scripts/  （官方可复现脚本 8 个 + README 中文版）
```

### 1.2 另外 8 个风格化技能（电商相关只有 2 个）

| 技能 | 定位 | 和你的关系 |
|---|---|---|
| **`minimalist-product-ad-generator`** | 一张产品图 → Apple 风高级产品广告片 | ⭐ **这就是官方的"电商视频方案"**，见 §3 |
| **`brand-promo-video-generator`** | 品牌/产品/官网宣传片，15–30s，含旁白 + CTA + 合规核验 | ⭐ 带货长片可参考它的素材核验与合规流程 |
| `music-video-subtitle-generator` | MV / 歌词贴字 / 卡点剪辑 | 字幕与节拍方案可借用 |
| `3d-animation-short-generator` | 故事型 3D 动画短片（角色卡 → 分镜 → 拼接） | 唯一明确"全片拼接"的流程 |
| `papercraft-stop-motion-explainer` | 纸艺定格科普 | 风格参考 |
| `paper-collage-explainer-generator` | 纸拼贴讲解动画 | 风格参考 |
| `co-op-game-intro-generator` | 双人游戏开场 | — |
| `handdrawn-live-video-generator` | 手绘实拍融合 | — |

⚠️ **关键限制**：这 8 个风格技能依赖 **MiniMax Hub 画布工具**（`hub_generate_video`、`hub_canvas_get_node`、`hub_image_search` 等）。**把它们的文件夹拷进通用 agent 不会复现那套环境**——只有 `h3-prompt-writing` 是真正可移植的。

⚠️ **缺口**：**没有"真人 KOC 口播带货"的官方 skill**。`minimalist-product-ad-generator` 的适用范围里明确写了"**不适合 KOC 口播**"。也就是说：纯产品卖点片官方有成熟 SOP，真人讲解带货片官方没给方案——这块得自建。

### 1.3 怎么装

```bash
# 只装提示词引擎（推荐先装这个）
npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --skill h3-prompt-writing

# 看仓库里有哪些技能
npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --list

# 在 MiniMax Agent 平台内，也可以直接对它说：
#   "安装官方H3技能包：https://github.com/MiniMax-AI/MiniMax-H3，启用h3-prompt-writing技能"
```

装完落在 `.skills/h3-prompt-writing/`。**注意：技能本身不生成视频**，它只产出提示词；渲染仍走海螺 App / API / 本地部署。

---

## 2. 博主们"不崩坏"的真正原因（8 条可复制手法）

核心事实：**他们没用什么隐藏技巧，用的是官方 `base-en.txt` 里的硬规则。** 这些规则普通人写提示词时会漏，所以看起来像"玄学"。

### 2.1 一条片内自带多镜切 —— 这是最大的认知差

H3 **在一次生成内部就支持切镜**，语法是：

```
[Shot 1] ... 描述第一镜 ...
[Shot 2] At 00:03.500, the camera cuts to ...
```

- 第一镜**不加时间戳**，后续镜用**严格递增**的切点时间（`00:03.500` 这种两位小数格式）
- 对白跨切镜时，两端都要标 `<scenetrans>`，并说明音频连续

→ **博主是"一条片里自带切镜"，不是"生成 5 段再拼接"**。多段拼接的接缝、色调漂移、身份漂移问题，官方路线从根上绕开了。

### 2.2 一个镜头只留一个主要动作

英文原话最能说明问题：**"形容词支撑画面，动词创造视频。"**（*Adjectives can support the image, but verbs create the video.*）

- `"A glamorous concert scene"` ❌ 弱
- `"the singer steps into a warm spotlight, raises the microphone, and turns toward camera"` ✅ 强

副作用规则：**次级元素要延迟**，不能和主元素抢画面（先产品进入，再文字出现；先主款稳定，再辅助款滑入）。

### 2.3 切镜必须引入"新信息"，否则用运镜

官方判据：切镜要带来**主体 / 空间 / 状态 / 视角 / 时间**中至少一项的新信息。
**如果只是改变景别或轻微角度 → 必须用运镜，不许切镜。** 这条直接砍掉一大类"乱切导致崩坏"的失败。

### 2.4 运镜写成"三要素自然语句"

完整运镜 = **运动类型 + 幅度 + 速度**，且必须写成句中的英语动作，不能末尾堆标签：

```
The camera pushes in with small amplitude at slow speed toward the folded letter in her hands.
```

运动类型官方给了完整词表：`Zoom / Push / Pull / Pan / Truck / Tilt / Pedestal / Arc / Tracking / Static / Shake / POV / Roll`。**中幅 + 常速可省略不写**（默认值）。

⚠️ 手册特别指出：写 `truck left + pan right` 这种具体动作，远胜笼统说"环绕运镜"。

### 2.5 每个素材必须声明用途 —— 参考被忽略的头号原因

> **"上传了素材却没写用途"是官方列出的参考失效第一原因。**

官方用途分类（这实际上就是 H3 的自然语言 API 界面）：

| 用途 | 锁什么 |
|---|---|
| 人物参考 | 脸 / 形象 |
| 物体参考 | 产品 / 道具 |
| 场景参考 | 场景 |
| 关键帧 | 明确是首帧还是尾帧 |
| 音色参考 | 音色 |
| 故事版 | 分镜内容 |
| 风格参考 | 图片风格 |
| 构图参考 | 取景与排布 |
| **动作参考** | **视频中的动作** |
| **运镜参考** | **运镜** |
| 视频编辑 | 要被修改的那段视频 |

写法示例（官方原文口径）：
> `@图片1 是人物参考（锁这位女子的脸），@视频1 是动作参考（用里面的舞剑动作），@音频1 是情绪参考。`

**"参考冲突"是第二大失效源**——两份素材对同一件事给出不兼容指令（两张图定义不同的脸 / 两条视频给不兼容的动作 / 提示词要静态但视频1是手持）。官方给了冲突自检表：身份、外观、动作、运镜、场景、音色，各由谁定义？两个素材答同一题而且答案不同 → **先在提示词里裁决**。

### 2.6 关键帧对齐指令必须放第一行

FL2VA / I2VA / L2VA 的第一行是固定的对齐声明，格式严格：

```
How the reference pictures align with the target video — Picture 1 (from Shot 1) aligns with the 0.00-second mark of the target video; Picture 2 (from Shot 1) aligns with the 8.00-second mark of the target video.
```

要点：`N` 是**实际末镜序号**；时长**必须两位小数**；后面要空一行再接 `integrated_multimodal_description`。

⚠️ FL2VA 官方明确**倾向单镜**（好让模型从首帧连续插值到尾帧），**只有明确要求才用多镜**。

### 2.7 音频必须写，且不能自相矛盾

H3 是**音视频联合生成**。不写声音方向 → 音频可能变成"奇怪的语音杂音"。

- 对白：`<d>[English] I get off at the next station.</d>`，**原文逐字保留，不翻译**
- **画外音**必须紧跟 "**while his lips remain completely closed**"（否则模型会去对口型）
- 画内文字用**英文双引号写原文**：`A red neon sign reading "营业中" glows above the doorway.`
- 不要 BGM → 末尾写 `non_diegetic_music: N/A`；**绝不能一处要配乐、一处禁 BGM**
- `non_diegetic_music` 只写**乐器/速度/节奏/动态**，**不许写抽象情绪词**（手册点名的失败模式）

### 2.8 迭代：一次只改一个变量

官方推荐阶梯（和工程调试逻辑一致）：

```
① 先证明主体、场景、光、风格  → ② 加主动作，查运动连续性
→ ③ 加一个主运镜             → ④ 加短对白 / 声音
→ ⑤ 一次只引入一个新参考      → ⑥ 最后出目标分辨率
```

配套：**只在其中一项出问题时改那一项**（身份漂移→改参考图；动作混乱→简化动词；画面平→调光或机位）。**"全部重写"会掩盖原因。**

剪辑层面的两条经验（来自实战复盘）：**在不稳定的首帧之后进，在手或脸开始漂移之前出**；**匹配运动方向接点**（向右的动作接向右的动作更顺）。

---

## 3. 官方电商案例是怎么做的（`minimalist-product-ad-generator` 拆解）

这是官方唯一直接对电商产品的完整 SOP。10 个步骤，其中 **5 个反直觉点**最值钱：

### 3.1 ⭐ 用三张独立锚定照片，不用四宫格

> **官方原话：不再默认使用四宫格锚定图，因为视频模型可能把宫格版式带进最终画面。**

这几乎肯定是官方踩过坑之后回填的规则——**"多镜头产品形态对不上"的另一种解法，是给模型三张独立、干净、同光同调的产品照**。

三张照片的固定分工：

| 照片 | 职责 |
|---|---|
| 照片 1 | 主视觉 / 刁钻主视角，锁最有吸引力的视角、轮廓、开场方向 |
| 照片 2 | 材质 / 功能细节，锁材质颗粒反光 + **合并进开合、按钮、接口、屏幕等结构线索** |
| 照片 3 | 结尾文案构图，锁字体气质、文字位置、单行排版、两段配色 |

硬约束：**每张都必须是可独立成立的完整产品照**；禁止网格、分屏、拼贴板、画框、多面板、产品墙、分镜板。

### 3.2 ⭐ 锚定照片 ≠ 分镜

> **锚定照片只是商品锚定，防止产品身份/颜色/材质/版式出错；绝不能把锚定照片当成分镜本身，也不能把三张照片按顺序直接拍成三段视频。**

镜头顺序、动作、文字出现时机，**全部来自"精确节拍文字分镜表"**——一份给视频模型看的**执行表**，不是美术图。

节拍数是量化的：**5s→3-4 个节拍；10s→5-7 个；15s→6-9 个**。每个节拍只保留一个主要动作。

### 3.3 ⭐ 生视频阶段**不传原商品图**

> 原商品图只用于前置产品分析和锚定图生成。**生视频阶段不要把用户上传的原商品图传给视频模型。**

### 3.4 ⭐ 文字是"视频内一体化动效"，明确禁止退化成后期字幕

`minimalist-product-ad-generator` 对文字的规定是**全套里最细的**，而且**和 REPORT7 的结论相反**：

| 维度 | 官方要求 |
|---|---|
| 语言 | 画面文案**强制英文** |
| 长度 | 3–5 个英文词，≤32 字符含空格，**单行** |
| 次数 | 10s 片**通常只留 1 次中段 + 1 次结尾**；易错就再减，**不要求每镜都有** |
| 颜色 | 单镜不超两色；白色科技风前半**黑/深灰**（禁白），黑底轮廓光前半才可白，后半**始终用具体商品色** |
| 位置 | **不放下方字幕位**；放上下居中区，靠左/右参与构图，字号略大 |
| 动效 | **默认两段式**：前半淡入 → 后半淡入，后半出现时前半在**同一行内**轻位移让位（10–15px） |
| 禁止 | 上下两行、双行标题、主副标题并列、两处文字同时出现；**也禁止"为了避两行而做成完全静态"** |
| 兜底 | 只有用户明确要求后期叠字，或内生成失败且用户接受，才用后期文字 |

**派发前硬校验**：最终 prompt 里**没有逐字包含所有计划文案 → 不许派发，必须先重写 prompt**。

### 3.5 ⭐ 默认"一条 H3 全画幅片"，不是逐镜多段

> 默认生成一条 H3 全画幅产品片，而不是逐镜生成多个首帧视频。

最后一条尤其重要：**结尾不得回到网格、分镜板、小窗合集或产品格子墙**（模型会把锚定图布局拍进结尾）。

### 3.6 附带的：失败处理清单（可直接抄成自检表）

| 症状 | 处置 |
|---|---|
| 产品图太弱 | **暂停流程**，给重拍建议，等更好的图 |
| 文案太长 | 压到 3–5 词英文，让用户确认 |
| 文字锚点错误 | **先重做文字锚点，不要直接进视频** |
| 锚定图无法保真产品 | 加强产品保真要求后重生成 |
| 分镜套模板 | 回到节拍表，按产品形态与叙事脊柱重写 |
| 视频里没文案 | 重跑 prompt，明确文案至少出现在中段和结尾 |
| 视频文字变形 | 重做文字锚点，或把文字镜退化为锚定图的轻微动效 |
| 比例错误 | **立即重跑**，明确使用用户选择比例 |
| 音乐太差 | 换独立配乐模型重试，从长音乐里剪好片段，不强控时长 |
| 合成后后半段没声 | 重新分析音乐，选连续有声片段，直接替换音轨 |

### 3.7 顺带：`brand-promo-video-generator` 的合规流程值得单独抄

它有一套"**品牌事实表**"机制，值得直接搬到带货生产线上：

1. 素材按可信度排序：**用户原始导出文件 > 官网/官方资料包 > 公司控制的媒体库 > 授权图库**
2. 提取：LOGO 精确版本 + 安全空间、官方字体、主辅色、品牌语气、产品名称/功能/口号/CTA/免责声明
3. **"如果 LOGO、产品界面、人物、吉祥物、包装、字体或色彩系统无法认证 → 停止并索要授权原件，不要生成看起来相似的替代品"**

→ 这条和第 8 平台《承诺书》"严禁 AI 换脸仿冒"是同一条红线的落地写法。**把它写成流程里的一个 Gate，而不是靠人记得。**

---

## 4. ⭐ 根因确认：官方是三层架构，我们本地只有中间一层

这是本次调研最有价值的发现，它解释了我之前所有"参考条件不生效"的异常。

### 4.1 完整 H3 系统

| 模块 | 职责 | 开源？ |
|---|---|---|
| **H3-Context-IR** | 托管预处理 + 编排：解析指令、跨模态关联、时间理解、复杂逻辑推理，把自由形式多模态输入序列化成结构化表示 | ❌ **未开源**（提供 API） |
| **H3-Base** | 按 IR 输出生成音视频，**768p** | ✅ 开源（社区有量化/剪枝版） |
| **H3-Regenerate-2K** | 把 768p 结果**连同原始上下文**送回 H3 重生成 2K（**是 in-context 重生成，不是超分**） | ❌ 未开源（提供 API） |

### 4.2 官方原话（README 中文版）

> **"H3-Context-IR 对最终输出质量至关重要，因此我们强烈建议将其纳入你的生成流水线，或按照'提示词指导'构建自己的上下文处理系统。"**

社区总结更直白：**"接 H3-Context-IR 虽额外按 token 计费，但官方强烈建议——它直接决定输出质量，别省。"**

### 4.3 所以之前那些"零效果"该怎么解释

我之前的实测（REPORT6）：本地 Ref2VA 换参考视频 → 音轨 PCM **逐位相同**、帧差 0.94–1.09/255（噪声基线 1.08）；而只改稀疏注意力档位 → 帧差 **22.91**。

现在有三个候选解释，**必须做三组对照才能定论**：

| 假设 | 判据 | 怎么验 |
|---|---|---|
| **A. 缺 Context-IR 编排层** | 官方点名"决定质量"的层缺失，参考的语义角色没被正确编排进 IR | 用官方 API 跑同素材 Ref2VA，看参考是否生效 |
| **B. ComfyUI 适配层接线丢失** | PCM 逐位相同 → 更像是条件根本没进采样器（管道问题，而非质量衰减） | 换全注意力**非剪枝**官方权重，同 graph 重跑 |
| **C. 剪枝 int8 权重丢了参考通路** | 用的是社区 `*_pruned_int8_convrot` 版，剪枝可能切掉 ref-attention 分支 | 官方 BF16 权重本地跑同一 case |

**倾向判断**：PCM 逐位相同这个现象更支持 **B/C**（条件压根没参与），而不是 A（A 应该表现为"参考生效但不够准"）。但 A 是官方亲口承认的质量层，**无论如何都该补上**。→ 需要重开实例复测（原实例已释放）。

### 4.4 而官方确实给了 Ref2VA 的"视频编辑"可复现样例

官方脚本 `reproducible-768p-ref2va-request.sh` 的 prompt 任务类型前缀是：

```
[video editing + audio reference + audio reuse]
The target video is an edited version of <Video 1>.
```

结构：
- `<Video 1>` = **源视频（编辑对象）**，`fully_preserved` — 保留原始运镜、黄金时刻光、草地场景、背景羊群，只编辑主体
- `<Audio 1>` = `partially_copy` — 原视频 BGM 部分复用
- `<Audio 2>` = `reference` — 参考男声**音色**生成新台词
- 效果：**给源视频里的角色"加上说话"**（口型动画 + 新台词），其余全部保留

API 形态：
```json
{ "task": "ref2va",
  "conditions": [ {"type":"video","uri":"...","role":"reference"},
                  {"type":"audio","uri":"...","role":"reference"} ],
  "target": {"short_edge":768,"aspect_ratio":"auto","duration_seconds":5}, "seed": 0 }
```

→ **这就是你上一轮问的"拿对标视频做替换/编辑"路线，官方有跑通的样例。** 关键差别：走的是 **API + Context-IR**，不是本地 ComfyUI。

---

## 5. ⚠️ 对 REPORT7 的两条修正

对接官方规范后，我上次的两条结论需要更正：

| REPORT7 原结论 | 修正后 | 依据 |
|---|---|---|
| "文字/商标**一律**后期叠" | **默认让 H3 内生成**（官方有完整规范：3–5 词、单行、两段式动效）；**控制次数**（10s 只 1 中段 + 1 结尾），文字易错就**减次数**而不是放弃。只有在本地 768p 量化版确实糊、且用户接受时才退回后期 | `minimalist-product-ad-generator` §文字规范 |
| "一步一镜、5 段生成再拼" | **优先"一条片内多镜自带切"**（`[Shot 1]` / `[Shot 2] At 00:03.500`），这能从根上绕开拼接缝、色调漂移、身份漂移。多段拼接降为"单条时长不够时的补充手段" | `base-en.txt` §4.2 |

**没有变的结论**（仍然成立）：B 类状态守恒 + C 类接触物理仍然无解；动作白名单 + 首尾帧同源 + 把多步流程移出模型这三根支柱不变；8 平台合规红线不变。

---

## 6. 落地方案：三条线，明确分工

不要再问"本地还是官方"——**它们本来就是分工关系**。

```
                    ┌─────────────────────────────────┐
                    │  线 C：官方 skill 的 SOP（免费）  │
                    │  不管走哪条渲染线，提示词都该用它  │
                    └───────────────┬─────────────────┘
                                    │
          ┌─────────────────────────┴─────────────────────────┐
          ▼                                                   ▼
┌───────────────────────┐                       ┌───────────────────────┐
│ 线 B：本地 ComfyUI     │                       │ 线 A：官方 API         │
│ 【量产线 / 试错线】     │  ── 草稿过了再出终版 ──▶│ 【质量线 / 收尾线】    │
│ • 迭代到满意为止        │                       │ • Context-IR 编排      │
│ • 边际成本≈电费        │                       │ • Regenerate-2K        │
│ • 768p 天花板          │                       │ • Ref2VA 视频编辑       │
│ • 适合 S1 产品动态化    │                       │ • 0.8 元/秒 + token    │
└───────────────────────┘                       └───────────────────────┘
```

这**正是官方 "Full 2K Workflow" 的设计意图**：本地 SGLang 跑 Base，官方 API 补 Context-IR + Regenerate-2K。

### 6.1 立即可做的三件事

| # | 动作 | 为什么先做这个 |
|---|---|---|
| 1 | **装官方技能**：`npx skills add https://github.com/MiniMax-AI/MiniMax-H3 --skill h3-prompt-writing` | 零成本，立刻把提示词从"散文"升级成"规格书" |
| 2 | **按官方 SOP 重做一条 S1 产品动态化**：三张独立锚定照 + 精确节拍表 + **一条片内多镜** | 用最低风险品类验证官方路线是否真的更稳 |
| 3 | **用官方 API 跑一条 Ref2VA 视频编辑样例** | 闭掉 §4.3 那个悬而未决的假设，同时实测"换产品/加特效"到底行不行 |

### 6.2 生产线的最终形态（建议）

```
① 官方 skill 写规格书  →  ② 本地出草稿（多轮迭代，成本≈0）
   →  ③ 人工确认  →  ④ 官方 API 出终版 2K
   →  ⑤ 后期（音乐卡点 / 必要时叠字 / 平台版本裁切）
```

**真人 KOC 口播带货片**这一块官方没有 skill，仍按 REPORT7 的方案自建（动作原子库 + 白名单 + 少量口播段落）。

---

## 7. 一页速查：要做什么 → 用哪套

| 需求 | 模式 | 关键控制点 |
|---|---|---|
| 产品动态化 / 氛围片（无操作因果链） | T2VA 或 I2VA，**一条片多镜** | 三张独立锚定照；一镜一动作；文案 2 条 |
| 精确首尾一致的变化 | FL2VA | 对齐指令第一行；两位小数；**倾向单镜** |
| LOGO 揭示 / 产品定版收尾 | L2VA | 推断前序状态 → 收敛到给定尾帧 |
| 专业运镜 | 提示词内写 `类型+幅度+速度` | 具体到 `truck left + pan right` |
| 动作迁移 | Ref2VA `<Video N>` 作动作参考 | **走官方 API**（本地已证无效） |
| 换产品 / 加特效 / 改台词 | Ref2VA 视频编辑 | 点名目标 + 枚举"其他全保留"；走官方 API |
| 需要 2K | 官方 Regenerate-2K | 是 in-context 重生成，不是超分；**救不了错的运动计划** |
| 音色克隆 | `<Audio N>` 音色参考 | 官方口径支持；**本地实测零效果**，走官方 API |

---

## 8. 参考文件位置

```
h3-pipeline/official/
├─ h3-prompt-writing/references/base-en.txt    官方基础模式规范（含 4 个官方 case）
├─ h3-prompt-writing/references/ref-en.txt     官方 Ref2VA 六段式规范
├─ style-skills/minimalist-product-ad-generator.SKILL.cn.md   ⭐ 电商产品广告 SOP（中文全文）
├─ style-skills/brand-promo-video-generator.SKILL.cn.md       品牌片 SOP（含合规 Gate）
├─ style-skills/*.SKILL.md / *.SKILL.cn.md      其余 6 个风格技能
└─ scripts/
   ├─ README.zh-CN.md                          官方中文 README（三层架构说明）
   ├─ reproducible-768p-ref2va-request.sh      ⭐ 官方可复现 Ref2VA 视频编辑样例
   ├─ reproducible-768p-{t2va,fl2va}-request.sh
   └─ full-2k-*.sh                              完整 2K 工作流脚本（本地+官方 API 混合）
```
