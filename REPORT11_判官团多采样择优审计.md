# REPORT11 · 「判官团」多采样择优架构审计

> 问题：让判官团评判，符合要求就通过，不符合重新生成 —— 这样能不能解决 H3「能出好片但不稳定」的问题？
>
> 结论：**方向完全正确，而且是行业标准做法。但直接照这个描述实现，会有很大概率变成「空转烧卡」——因为"不符合就重新生成"缺了一个关键动作，而我们有实测证据证明它是空转。**

---

## 0. 结论先行

| 判断 | 结论 |
|---|---|
| 这个想法本身 | ✅ **正确**，就是行业在用的 best-of-N / rejection sampling |
| 按"不符合就重新生成"字面实现 | ❌ **会空转**：同 seed 同 prompt 重提 → 输出与上一次**逐位相同**（我们实测过） |
| 判官能否替代闸门②（动作白名单） | ❌ **不能**。判官只能**筛**，不能**修** |
| 判官对随机性崩坏 | ✅ **非常有效**，5 类崩坏能被筛出来 |
| 判官对架构性崩坏（状态不守恒/接触物理） | ❌ **无效**，只会变成"昂贵的确认失败" |
| 真正的收益来源 | ⭐ **不是"筛出好片"，而是把随机过程变成可复现资产（种子资产库）** |
| 成本 | p=0.5 时一条 30s 约 16.5 分钟（可接受）；p=0.1 时即使 N=8 也要 48 分钟、整片良率仅 6%（必须止损） |

---

## 1. 先正名：这就是 best-of-N，业界已经在用

你的描述对应三项成熟技术，都有可查的实现：

| 系统 | 实现 | 关键参数 |
|---|---|---|
| **NVIDIA Cosmos-Predict2** | `video2world_bestofn.py` | `--num_generations`(默认2) · `--num_critic_trials`(5次重复评估求均值) · 接受阈值 0.8 · 最大拒绝次数 5 |
| **Cosmos-Reason1（专职物理判官）** | 逐元素分析，多次评估取"无缺陷比例"作分数 | 判据含**重力、物体恒存性、碰撞动力学、因果关系** |
| **FIRM-Video-8B / VideoScore2** | Best-of-8 采样择优 | 在三代生成模型上，Best-of-8 的 VBench 总分/质量分/语义分**全部高于随机采样** |

Wan / LaVie / CogVideoX 上的验证结论：Best-of-N 一致优于随机采样，**语义分提升最明显（+1.24 ~ +2.11）**。

⇒ 所以「加评判环节」不是激进，而是**标准工程实践**。你说"关键是加一个评判环节"——这个直觉是对的。

### 判官的正确形态：不是"一个裁判"，是"级联 + 清单"

两份权威来源给出了同一套架构（下面第 3 节展开）：

1. **三级级联，按成本排序**（Fora Soft 的 quality gate 工程实践）：
   - L1 技术校验（免费算术）→ L2 专用小模型（廉价）→ L3 VLM 判官（贵）
   - 原话：*"You only reach this layer for clips that passed the two cheaper ones."*
2. **check-before-score 原子清单**（FIRM-Video, arXiv 2608.21839）：
   - 禁止开放式推理打分；必须**把提示词拆成加权的原子要求**，逐条对时间证据核验，只聚合已核验的判定。

---

## 2. ⚠️ 四个陷阱（第 1 个是致命的，而且我们有实测证据）

### 陷阱 1（致命）：同 seed 重跑 = 零收益，判官会拒绝 N 份完全相同的片子

**我们的实测事实**（REPORT6，可复查）：

> 同提示词 + 同 seed 下，无论参考视频换成谁、换成时间反转、换成静态帧、甚至完全不接视频 —— **输出音轨 PCM 逐位相同**，帧间差仅 0.94–1.09/255（而这正是"同一素材改名完全重跑"的复现噪声基线 1.08）。

也就是说：**H3 的采样在给定 (prompt, seed, 条件) 下是确定的**；你观察到的"不稳定"，100% 来自你**两次之间改动了某个变量**（换 seed、改措辞、缓存状态），而不是模型每次自己乱跳。

⇒ 后果：如果你实现成「不满意 → 重提同一个 job」，你会：
1. 花 N 倍算力
2. 拿到 N 份**视觉上完全相同**的东西
3. 判官对 N 份给出**完全一样的否决理由**

**这是最烧钱、且 100% 确定的失败模式。** 修正：重生成必须**换 seed**（或改措辞），否则循环无意义。

### 陷阱 2：判官只能「筛」，不能「修」

Rejection sampling 的作用是把**随机失败**变成**可搜索的多样性**。
但如果某个失败是**架构性、确定性**的，那么 N 次里每一次都会以同样方式失败 —— **p ≈ 0，N 再大也没用**。

我们已确证的两类架构性崩坏：
- **状态不守恒**（液量不减、盖子自合、用量不变）
- **接触物理**（手与物体穿插、悬空无支撑、抓握失败）

原因不是提示词，是**扩散模型逐帧生成、没有物理引擎**，它没有"因果链"和"守恒量"这两个概念。Cosmos-Reason1 甚至能**判**出这类问题（"gripper wraps around it in an awkward manner… suggesting a glitch in the interaction physics"）—— **但判出来≠修得回来**。

⇒ **判官和闸门②（动作白名单）不是替代关系，是互补：**
- 闸门② = **预防**（把 AI 做不了的动作挡在分镜之外）→ 唯一能处理 B 类
- 判官团 = **过滤**（在能做的动作里挑出这一轮运气好的）→ 只对 A 类随机性有效

**卸掉闸门②只装判官团，是最坏的组合**：你会花 N 倍算力，反复确认同一件做不到的事。

### 陷阱 3：判官自己也会崩（三类失效 + 物理能力弱 + 版本漂移）

FIRM-Video 论文明确列出「整体式判官」的三个系统性缺陷 —— 这些在你搭判官团时会原样复现：

| 失效模式 | 表现 | 后果 |
|---|---|---|
| **检查不完整** | 只看显著的全局属性，忽略提示词特有属性、短暂动作、时序顺序、局部缺陷 | 漏检 |
| **理由不忠实** | 先有分数，再编理由（post-hoc 合理化） | 假阳性，无法审计 |
| **归因纠缠** | 同一个问题被同时算作语义不符 + 物理不一致 + 画质缺陷，重复扣分 | 分数不可解释，阈值无法标定 |

另外三条要记住的边界：
- **通用 VLM 判官在物理/常识上很弱**。VideoScore2 作者自己承认：*"model still struggle in evaluating physics and common senses… highlighting the importance of a world model for video evaluator."*
- **判官版本漂移是未解决风险**（IVEBench 指出：依赖特定 MLLM，模型版本变化影响复现性）→ 必须**锁定判官版本**。
- **裁判-运动员冲突**（FiVE-Bench 的 FlowEdit 由基准作者提出）→ 自建判官时，标定集必须独立于你用来调参的样本。

**还有一个隐藏错误：「选出最好的那一个」≠「达标」。**
如果 N 份**全部不合格**，best-of-N 会给你**最不难看的那一份**，然后你误以为通过了。→ 必须有**绝对阈值 + 明确失败出口**，而不是"取最高分"。

### 陷阱 4：成本非线性，必须硬性封顶

期望尝试次数（几何分布）：

```
E[尝试次数] = 1 / p            （p = 单次通过率，无上限）
E[尝试次数] = (1-(1-p)^N) / p  （封顶 N 次）
```

2026 年的实测记录：一条预期 $1.50 的片子，经 5 次重生成后涨到 **$5**。
行业给出的止损规则原话：

> *"Always cap retries at a small number — three is a common choice… A clip that needs five tries is telling you the prompt is wrong or the model is the wrong tool; spending more money will not fix it."*

⇒ **重试必须封顶（3–5 次），到顶就报明确失败，绝不静默续跑。** 这条对你的「一键成站」尤其重要：一个用户提交了模型根本做不了的产品动作，无上限重试 = 无上限账单。

---

## 3. 修正后的架构：4 项改造

### 改造 ①：重生成必须换 seed —— 而且要建「种子资产库」

这是**最大的实际收益**，比判官本身重要。

因为「同 seed + 同 prompt + 同输入 = 输出逐位相同」是**已证实的**，所以一旦某个 seed 被判定通过：

```
(产品图, 提示词, seed=77213)  → 一个可复现的成片
```

它就从"一次抽奖结果"变成了**可重复调用的资产**：

| 之前（无种子库） | 之后（有种子库） |
|---|---|
| 想复刻一条好片 → 只能再抽一次，可能变差 | **同 seed 重跑 → 逐位复现** |
| 想提高分辨率/延长时长 → 全部重推倒 | 在冻结的 seed 上做**定向重渲染** |
| 出片好坏归因不了 | 日志记录 (prompt, seed, 全部参数) → 可归因 |

⇒ **别把它当"判官团的副产品"，把它当第一交付物。** 很多团队搭了一堆判官，最后发现真正省下时间的是一份参数日志。

### 改造 ②：三级级联，按成本排序（并且你已经有一半了）

| 级 | 判什么 | 成本 | 我们能抓到哪类崩坏 | 现有工具 |
|---|---|---|---|---|
| **L0 技术校验** | 解码成功 / 帧数 / 分辨率 / 时长 / 亮度范围（黑屏白屏）/ 音轨存在 / 长静音段 / 切点数量 | **<1s，纯算术** | 崩坏到无法成片的那批 | 需新写（很简单） |
| **L1 专用检测器** | 人脸检测计数 + 人脸嵌入距离 / 光流幅度 / 调色板相似度 / OCR 逐字比对 / 唇音频互相关 r | **5–20s/段，本地 CPU** | 身份漂移、参考不生效、文字崩塌、音画不对齐、接缝色调 | **`v6diff.py` `v6board.py` `asr.py` `lipsync2.py` `refcheck.py` `v6motion.py` —— 已经写好了** |
| **L2 VLM 判官** | 原子清单核验（指令遵循 / 时序顺序 / 状态变化 / 物理合理性） | **20–60s/段（本地 Qwen3-VL-8B）或 5–15s + 费用（API）** | 语义漂移、状态不守恒、接触物理 | 需新搭 |
| **L3 人工** | 最终成片 | 分钟级 | 全部（金标准） | — |

**关键工程点**：
- L0/L1 先用**算术和本地 CV** 砍掉绝大部分，L2 只跑幸存者 → *只在活下来的片子上花贵钱*。
- **早停（early-stop）是最大省钱手段**：一旦某份通过就立刻停，期望尝试是 `(1-(1-p)^N)/p`，不是 N。
- L1 的检测器**必须各自独立标定阈值**（用你历史的人工判定样本）。L1 的假阴性比 L2 的更重要——它会把好片误杀，白烧生成算力。

### 改造 ③：判据必须是「原子是/否清单」，不是「整体打分」

这是 FIRM-Video 的核心贡献，直接解决陷阱 3 的三个失效模式。

**错误写法**（整体式）：
```
请评估这条视频的质量，1-10 分。
```
→ 检查不完整 + 理由不忠实 + 归因纠缠。

**正确写法**（原子清单，逐条对时间证据核验，只聚合已核验的）：

```
【指令遵循】逐条核验（每条只能答 是/否，附时间戳证据）
R1  画面内出现的文案是否逐字等于："Silk Camellia"        → 是/否 + 出现时间
R2  文案是否单行、≤32 字符                               → 是/否
R3  主体人物是否为亚裔女性、长发、深灰西装（属性级）      → 是/否
R4  产品是否在第 2–4s 出现在画面中                       → 是/否 + 时间

【世界一致性】逐条核验
W1  容器内液量/膏体余量在整段内是否单调不减              → 是/否
W2  手持产品的过程中，手指是否与瓶身发生穿插             → 是/否
W3  产品是否有任何一帧脱离手部支撑而悬空                 → 是/否
W4  盖子/泵头是否在无操作的情况下自行开合                → 是/否

【感知质量】只查缺陷，不用提示词
P1  是否存在帧间闪烁 / 颜色跳变                          → 是/否
P2  是否有明显的结构畸变（多指、融脸、物体粘连）          → 是/否
P3  文字是否可辨且无乱码                                 → 是/否
```

**判定规则**：R/W 类**任一条为"否" → 否决**；P 类按缺陷计数设阈值。
不允许判官输出"整体感觉还行"——**只允许聚合已核验的原子判定**。

### 改造 ④：判定落在「段」，通过的段立刻冻结

一条 30s = 5 段。如果整片判定，一段不过就要全部重跑（1 段坏 → 5 段全废）。
→ **按段判定、按段重生成、通过即冻结**。整片良率交给"冻结段 + 段间一致性校验"来保。

---

## 4. 良率 / 成本模型（可直接代入）

设：段数 S=5，单段 3s → FL2VA 实测 ≈102s GPU/次。

**单段出片概率** = `1-(1-p)^N`　·　**整片良率** = `(1-(1-p)^N)^S`　·　**期望尝试** = `(1-(1-p)^N)/p`

| 单次通过率 p | N=1 单段 | N=3 单段 | N=5 单段 | N=8 单段 | **整片良率(N=5)** | 期望尝试(N=5) | **一条 30s 总耗时(N=5)** |
|---|---|---|---|---|---|---|---|
| **0.70** | 0.700 | 0.973 | 0.998 | 1.000 | **98.8%** | 1.43 | **12.1 分钟** |
| **0.50** | 0.500 | 0.875 | 0.969 | 0.996 | **85.3%** | 1.94 | **16.5 分钟** |
| **0.30** | 0.300 | 0.657 | 0.832 | 0.942 | **39.9%** | 2.77 | **23.6 分钟** |
| **0.20** | 0.200 | 0.488 | 0.672 | 0.832 | **13.7%** | 3.36 | **28.6 分钟** |
| **0.10** | 0.100 | 0.271 | 0.410 | 0.570 | **1.2%** | 4.10 | **34.8 分钟（N=8 时 48.4 分钟，整片仍只有 6.0%）** |
| **0.05** | 0.050 | 0.143 | 0.226 | 0.337 | **0.1%** | 4.52 | **38.5 分钟（N=8 时 57.2 分钟，整片 0.4%）** |

> 基线参照：N=1 且无判官时，一条 30s = 5 段 × 102s = **8.5 分钟**。

**读法（三条决策线）**：

- **p ≥ 0.5**：判官团**非常划算**。N=3~5，一条 30s 只从 8.5 分钟涨到 12–17 分钟。**这就是该上判官团的区间。**
- **0.2 ≤ p < 0.5**：还能用，但 N=5 整片良率只有 13.7–39.9% → 必须靠「分段冻结」把坏段单独重跑，不能整片重跑。
- **p < 0.2**：❌ **止损区**。这时判官团的边际收益已低于边际成本，说明**不是运气问题，是方案问题** → 回到闸门②改动作、改提示词、或换工具。

> ⚠️ 注意 p 的定义是「**单次生成**通过判官的概率」，不是"一条好片的概率"。搭好判官后**第一步就是测 p**：同一个提示词连跑 8 个 seed，数几条过。这个数字决定后面所有决策。

---

## 5. 判官团分诊表：7 类崩坏，哪些判官真能救

| # | 崩坏类型 | 判官能自动检出吗 | 重生成能救吗 | 净结论 |
|---|---|---|---|---|
| 1 | **身份漂移**（脸变了） | ✅ 能（人脸嵌入距离，L1） | ✅ **能**（换 seed 出好脸） | ⭐ **判官团主力场景** |
| 2 | **参考不生效**（颜色/材质/形态不符） | ✅ 能（调色板相似度 / CLIP 相似，L1） | ✅ **能** | ⭐ **主力场景** |
| 3 | **英文文字商标崩** | ✅ 能（OCR 逐字比对，L1） | ⚠️ **部分**：短英文 3–5 词可救；中文/价格/复杂 logo 救不了 | 混合：分流到后期 |
| 4 | **接缝与色调漂移**（段间） | ✅ 能（边界帧差 / 直方图，L1） | ⚠️ 部分；**根治靠"一条片内多镜"** | 优先修结构，不是修运气 |
| 5 | **音画不对齐** | ✅ 能（唇音互相关 r + ASR 逐字比对，L1） | ✅ **能** | ⭐ **主力场景** |
| 6 | **状态不守恒**（液量不减/盖子自合） | ✅ 能检出（L2 原子问句 W1/W4） | ❌ **救不了**（p≈0，N 倍算力换 0） | ❌ **只判不救 = 昂贵确认** → 回闸门② |
| 7 | **接触物理**（穿插/悬空/抓握失败） | ⚠️ 弱（通用 VLM 差；需 Cosmos-Reason1 类专职物理 critic） | ❌ **救不了** | ❌ **同上** |

**总结成一句**：
> **判官团对「5 类」是主力武器，对「2 类」是昂贵的确认器。**
> 5 类能救的共同点：失败是**随机的**（换个 seed 就变好）。
> 2 类救不了的共同点：失败是**架构决定的**（换 100 个 seed 都一样，只是崩的位置不同）。

---

## 6. ⭐ 第三条路：让 B 类「不可验证化」——比修它更聪明

前面说 B 类"只能挡在分镜外"。但还有一条更好的路：**不让画面里出现可验证的物理结果**。

判据是「**这个动作有没有可验证的物理结果？**」。反向利用它：

| 想表达 | ❌ 会崩的拍法 | ✅ 换成"无可验证结果"的拍法 |
|---|---|---|
| 产品用量足 | 涂开后皮肤变化 | 产品静置特写 + 光影扫过 + 模特微笑（**没有任何一帧能验证消耗量**） |
| 质地轻薄 | 挤膏体在手上抹开 | 产品泵头静止特写 + 逆光质地流动（只拍产品内部，不拍手） |
| 使用方便 | 旋盖→按泵→涂抹全过程 | 手持产品静止 + 单手拇指轻按（**手无位移，不产生因果链**） |

**三点必须注意**：
1. **判官的清单要同步调整** —— 如果画面里本来就不存在那个可验证结果，W1/W4 就应判 **N/A（不适用）而不是"否"**。否则你会用判官误杀一批完全合格的片子。
2. **这不是造假，但受广告法约束** —— "把过程移出画面"可以；"暗示涂开后就有 X 功效"不行。功效宣称是另一条独立的合规线。
3. **这是设计问题，不是生成问题** —— 它应该发生在第③步分镜表（闸门②），成本几乎为 0。

---

## 7. 落地：三步走

### 第 1 步：先把「可复现性」建起来（半天，零 GPU）

| 要做的事 | 产出 |
|---|---|
| 每次生成**必然记录**：prompt 全文、seed、所有参数、参考素材 md5、输出路径 | `runs.jsonl` |
| 重生成时**必然换 seed**，并在日志里标 `attempt_index` | 防止陷阱 1 的空转 |
| 通过的 (prompt, seed) 对**标记为资产**，可一键复现 | **种子资产库** |

### 第 2 步：搭 L0 + L1 判官（1–2 天，本地 CPU，零额外成本）

L1 的六个检测器**我们已经写好并实测过**（`v6diff.py` / `v6board.py` / `asr.py` / `lipsync2.py` / `refcheck.py` / `v6motion.py`），只需：
1. 包成统一的 `judge_l1.py`，输入 mp4 + 清单，输出逐项 是/否 + 数值 + 证据（时间戳）
2. 用**历史人工判定样本**标定每个检测器的阈值
3. 输出一份自检报告（可给用户看，也可入库）

### 第 3 步：只在 L1 存活后接 L2，并把清单固化

- L2 用本地 Qwen3-VL-8B（**锁定版本号**）或 API
- prompt 直接用第 3 节的原子清单
- 测出 p（同提示词 8 个 seed，数几条过）→ 用第 4 节的表决策 N 与是否止损

---

## 8. 一句话总结

> **判官团不是"更激进"，它是标准做法 —— 但它的作用是「把随机性变成可搜索的多样性」，不是「把做不到的变成做得到」。**
>
> 装它的正确顺序是：**先修可复现性（换 seed + 种子库）→ 再装廉价判官级联 → 最后才装 VLM 判官**。
> 闸门②（动作白名单）永远不能卸 —— 判官只能筛，不能修。
> 而且最省钱的一招不在判官里，在分镜里：**别让画面出现可验证的物理结果。**
