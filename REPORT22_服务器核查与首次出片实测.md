# REPORT22 · 服务器核查 + 独立部署验收 + 首次真机出片实测

> 日期：2026-09-17 23:55（CST）｜执行：金刚｜授权：辉哥离家前全权授权
> 服务器：`connect.nmb2.seetacloud.com:36229`（RTX 3090 24G / Ubuntu 22.04.5 / kernel 5.15.0-136）
> 原则：**实测 ✅ / 推算 ❓ / 未测 ⚠️ 分开标注**；每一步都有可回退出口。

---

## 0. 摘要（三问答卷）

| 辉哥的问题 | 结论 | 关键证据 |
|---|---|---|
| **数字人项目还有任务在跑吗？** | **❌ 没有。但进程常驻不释放** | GPU 利用率 **0%**、显存 8705 MiB **三次采样纹丝不动**、**累计 CPU 仅 10–39 秒**（已跑 4h06m）、日志只有 **68 秒一次的探活**、最后产物 **18:44**（4.5 小时前） |
| **内存为什么满了？要重启吗？** | **不需要重启**（详见 §2） | 容器上限 90 GiB、当前 88.0 GiB（97.8%），但**真实进程只占 28.7 GiB**，另一半 58.5 GiB 是**可回收的 page cache**；`pressure/memory = 0.00`、历史 OOM **0 次**。**出片过程中实测内核自动回收了 14.9 GiB 缓存**（§2.3）|
| **部署推进到哪了？** | **✅ 已跑通端到端真实出片** | 模型 5/5 就位（官方原版）→ 独立实例起在 6011 → **首次真机生成成功：430.75 秒 / 768×1344 / 24fps / 带音轨**（§5）|

---

## 1. 数字人（heygem）任务核查 —— "有进程 ≠ 在干活"

### 1.1 判据链（五条独立证据，全部指向"空转"）

| # | 判据 | 实测值 | 含义 |
|---|---|---|---|
| 1 | **GPU 利用率** | **0%** | 无推理活动 |
| 2 | **显存波动** | 三次采样（间隔 3s）**均为 8705 MiB，一位数不变** | 有推理必然波动 ⇒ 静态常驻 |
| 3 | **累计 CPU 时间** ⭐ | 跑 **4h06m**，累计仅 **10 / 22 / 26 / 39 秒** | **决定性判据**：一次数字人合成至少几十秒 CPU |
| 4 | **HTTP 请求类型** | 日志**只有** `GET /easy/query?code=probe`，**每 68 秒一次**（23:18→23:19→23:20→23:21→23:22→23:23 间隔恒定） | 哨兵探活，**零业务请求** |
| 5 | **产物新鲜度** | `/code/data/temp/` 最后产物 **18:44**；网络**无外网连接** | 4.5 小时无新任务 |

### 1.2 探活来源（已定位）

`/opt/dh-watch/dh_health_check.sh` → 由 `/root/start_services.sh`、`/root/restore_services_nmb2.sh` 拉起。
另有 `/var/log/heygem-watchdog.log` 记录 01:54 的 `8383 down, maintenance flag set, skip`（当时处于维护态，现已恢复，8383 正常 LISTEN 且 probe 返回 200）。

### 1.3 但它占着资源（这是真问题）

| 资源 | 占用 | 明细 |
|---|---|---|
| **显存** | **8.7 GB** | heygem 3× worker：2044 + 3666 + 2098 MiB |
| **内存 (anon)** | **27.6 GB** | tts-gateway 11.0 G ＋ heygem 3× worker 16.2 G ＋ 主进程 0.4 G —— **占全机真实内存的 96%** |

> 🚨 **归属他人项目**（`/code → /root/autodl-tmp/heygem-official/rootfs/code`）—— **不碰、不停、不改**（辉哥红线：不碰其他项目）。
> ⚠️ **后果**：3090 可用显存被压到 **15.4 GB**，而 H3 需求 ≈20 GB ⇒ 端到端必须走 ComfyUI 动态 VRAM 加载（§5.2 已实测跑通）。

---

## 2. 内存"爆满"诊断 —— 为什么不需要重启

### 2.1 第一层：容器视角 vs 宿主机视角（最易误判）

| 视角 | 读数 | 说明 |
|---|---|---|
| `free -h`（**宿主机全局**） | total **755 Gi**，used 60 Gi，buff/cache 443 Gi，**available 687 Gi** | 完全宽裕 —— 与"爆满"矛盾 ⇒ 说明看错了对象 |
| **`/sys/fs/cgroup/memory.*`（容器视角）** | **`memory.max = 90.0 GiB`**，**`memory.current = 88.0 GiB` → 97.8%** | **这才是 AutoDL 控制台显示的那个"内存"** |

### 2.2 第二层：97.8% 的构成拆解（关键）

| 项 | 大小 | 性质 |
|---|---|---|
| **`anon`（真实进程内存）** | **28.7 GiB** | ❌ 不可回收 —— **真实硬占用，仅占上限 31.9%** |
| **`file`（page cache）** | **58.5 GiB** | ✅ **可回收** —— 模型权重 / 镜像层被读进缓存 |
| `slab` / `shmem` / `sock` | 0.6 / 0.3 / 0.0 GiB | 可忽略 |
| **`/proc/pressure/memory`** | **`some avg10=0.00`** | **零内存压力** ⇒ 内核回收完全跟得上 |
| 历史 OOM 事件 | **0 次** | `dmesg` 无 `oom-kill` 记录 |

### 2.3 第三层：出片过程中抓到的**活体实证**（这是最有说服力的一条）

生成任务运行时，`anon` 因加载模型暴涨，**内核当场回收 page cache 顶上**：

| 时刻 | anon | file | 合计 | 压力 |
|---|---|---|---|---|
| 生成前 | 29.4 GiB | 57.2 GiB | 87.4 / 90.0 (97.1%) | 0.00 |
| **生成中**（模型已加载） | **44.2 GiB**（+14.8） | **42.3 GiB**（**−14.9**） | **87.3 / 90.0 (97.0%)** | some avg60=**0.01** |

> ⭐ **结论**：`anon` 涨了 14.8 GiB，**`file` 同步被丢掉了 14.9 GiB**，总量几乎不动。
> 这就是"内存显示 97% 却毫发无伤"的机制 —— **page cache 是内核的弹性缓冲，不是泄漏**。
> **重启的代价**：中断他人项目 + 缓存重新填充要重新读 40 GB 权重（反而变慢）。**⇒ 否决重启。**

### 2.4 给辉哥的一句话

> 控制台那个 97% 是 **Alpine 式的读数错觉**：它把"内核借来当缓存用的空闲内存"也算成"已用"。**真实占用只有 32%**。以后看到 9x% 不用慌 —— 只要看两个数：`anon` 是否接近上限、`pressure` 是否 >0。

---

## 3. 权重部署：从"白烧 3.6 小时"到"6 分钟物理独立"

### 3.1 失败现场（hf-mirror 大文件传输损坏）

`deploy_independent.sh download` 跑了 **3 小时 36 分**，结果：

| 文件 | 体积 | 下载耗时 | 结局 |
|---|---|---|---|
| diffusion（主干） | 19.53 G | 2431 s | ❌ "下载完成"但 **sha256 不符**，自删 |
| text_encoder | 14.61 G | 1749 s | ❌ sha256 不符（实得 `768b7bbe…`） |
| video_vae | 4.85 G | **8645 s（2.4 h）** | ❌ sha256 不符，自删 |
| audio_vae | 0.56 G | 快 | ✅ 成功 |
| 8 步 LoRA | 1.82 G | 快 | ✅ 成功 |

> **规律**：**小文件全成功、大文件全失败** ⇒ 指向 `curl -fL --retry 3 -C -` 的**断点续传拼接**：重试时从断点续传，而 hf-mirror 的 CDN 节点/分片不一致 ⇒ **拼接损坏**（小文件不触发续传，故幸免）。

### 3.2 关键排障：不是脚本写错

拉 hf-mirror API 的 `lfs.sha256`（官方权威值）与脚本期望值逐件比对 → **5/5 完全一致 ✅**
⇒ **脚本是对的，传输是坏的**。（同时纠正了我此前"数据盘共享库全空"的误判 —— 实际 8-14 就存有全套官方原版。）

### 3.3 解法：两阶段（含一次自我纠正）

| 阶段 | 做法 | 结果 | 判定 |
|---|---|---|---|
| ① | `cp --reflink=auto`（XFS 支持） | **0.01 秒**完成 | ⚠️ `filefrag -v` 显示源/目标 `physical_offset` **完全相同 + `shared` 标记** ⇒ **物理块共享**，不满足"不共享"口径（COW 已实测：改目标不影响源） |
| ② | **`dd bs=16M iflag=direct oflag=direct`** | **约 6 分钟**（107 MB/s） | ✅ `filefrag` 复核：目标 extent `1097645767…` vs 源 `329431787…`，**完全不同且无 `shared`** ⇒ **真物理独立** |

> ⭐ **O_DIRECT 的额外价值**：复制 39 GB 全程 **`file` cache 稳在 58.5 GiB 不变** —— 在 97.8% 的危险水位下**零新增缓存压力**（普通 `cp` 会灌 39 GB dirty page）。

### 3.4 最终验收

| 项 | 结果 |
|---|---|
| 权重就位 | **5/5 件 = 官方原版**（逐件 sha256 对 API 权威值），合计 **41.4 GB** |
| 独立性 | **零软链**（`find -type l` = 0）＋ **物理独立**（extent 无 shared）＋ 独立 inode（`links=1`） |
| 清单 | `$ROOT/models_manifest.txt`（含 size + sha256 + 获取方式） |

---

## 4. 独立 ComfyUI 实例部署验收

| 项 | 结果 |
|---|---|
| 代码 | ComfyUI **0.36.0**，commit `d39cdfdb…`（与共享实例**同配方**，49 M） |
| 依赖 | ✅ 22 项全绿（venv 继承 miniconda base：torch 2.9.1+cu130 / triton 3.5.1 / transformers 5.16.1 / comfy_kitchen 0.2.34） |
| 启动 | ✅ 端口 **6011**，18 秒起，PID 489655 |
| 目录纪律 | base / input / output / temp **全落数据盘**；系统盘零本项目产物 |
| **踩坑修复** | ❌ 首次启动崩溃：`FileNotFoundError: .../comfy/custom_nodes`（ComfyUI prestartup 阶段 `os.listdir`）⇒ 预建 `custom_nodes` / `models` / `user` 后解决，**已回写进 `start_comfyui.sh`** |

### 4.1 一个重要的"干净度"发现

| 实例 | 节点数 | custom_nodes |
|---|---|---|
| **本项目 6011** | **957** | **空 = 纯官方** ✅ |
| 共享实例 6006 | 1008 | 3 个第三方包 |

差集 **51 个节点全部来自**：
- `ComfyUI-ALLinONE-MinimaxH3`（H3OneNode / H3StitchFrames / H3PaintedRegion …）
- `ComfyUI-H3-Motion-Context`（MiniMaxH3MotionContext* 系列）
- `ComfyUI-VideoHelperSuite`（VHS_* 系列）

> ✅ **本项目节点是共享实例的真子集（独有 0 个）**，且**官方 H3 链路 17 项必需节点 100% 齐备**。
> ⚠️ 前两个包属**社区魔改类**（与此前造成"模型选错"误判的社区节点同源）⇒ **按 D6"只照抄配方、不复制文件"原则，坚决不装**。

### 4.2 节点真名修正（文档勘误）

| 此前文档写法 | **实测真名** |
|---|---|
| `AddGuide` | **`MiniMaxH3AddGuide`** |
| `SigmaShift` | **`MiniMaxH3SigmaShift`** |
| （其余） | `MiniMaxH3ImageToVideo` / `MiniMaxH3ReferenceToVideo` ✅ 一致 |

---

## 5. ⭐ 首次真机出片实测（本次部署的核心成果）

### 5.1 任务参数

| 项 | 值 |
|---|---|
| 首尾帧 | `first916.png` / `last916.png`（**TT Image2 生成，768×1344**，同源派生满足 Gate-P P-05） |
| 画布 | **9:16 (Portrait Widescreen)** @ 0.98 MP，multiple=32 → **768×1344** |
| 步数 / 采样器 | 8 步 / `res_multistep` + `simple` + 8 步 Turbo LoRA |
| 时长参数 | 4.5 s（实际 snap 到 **124 帧 = 5.167 s**，符合 `17k+5` 网格） |
| 提示词 | 一条片内多镜写法（`[Shot 1]` 手伸入举起 → `[Shot 2]` 旋转/光影流动） |
| seed | 123456789 |
| 比例守卫 | ✅ 通过（画布 768×1344，已检 2 张） |

### 5.2 实测耗时（**首次真机数据，此前均为推算**）

ComfyUI 日志的 tqdm 逐行原文（**权威、可复核**）：

```
  0%|          | 0/8 [00:00<?, ?it/s]
  0%|          | 0/8 [00:00<?, ?it/s,   Model Initializing ...  ]
  0%|          | 0/8 [00:48<?, ?it/s,  Model Initialization complete!  ]
 12%|█▎        | 1/8 [00:48<05:39, 48.45s/it,  Model Initialization complete!]
 25%|██▌       | 2/8 [01:31<04:35, 45.96s/it]
 38%|███▊      | 3/8 [02:18<03:51, 46.20s/it]
 50%|█████     | 4/8 [03:04<03:05, 46.27s/it]
 62%|██████▎   | 5/8 [03:50<02:18, 46.28s/it]
 75%|███████▌  | 6/8 [04:37<01:32, 46.35s/it]
 88%|████████▊ | 7/8 [05:23<00:46, 46.45s/it]
100%|██████████| 8/8 [06:09<00:00, 46.40s/it]
  [INFO] Comfy model compiler graph breaks: 3, rogues: 0
  [INFO] Prompt executed in 430.75 seconds
```

**逐行反算真实节奏**（tqdm 累计值相减）：

| 步 | 累计 | 相邻间隔 |
|---|---|---|
| 1/8 | 48 s | —（**含冷启动**） |
| 2/8 | 91 s | **43 s** |
| 3/8 | 138 s | 47 s |
| 4/8 | 184 s | 46 s |
| 5/8 | 230 s | 46 s |
| 6/8 | 277 s | 47 s |
| 7/8 | 323 s | 46 s |
| 8/8 | 369 s | 46 s |

⇒ **稳态步速 = 321 s ÷ 7 间隔 = 45.9 s/步**，与 tqdm 显示值（46.4）**一致** ✅

| 阶段 | 耗时 | 依据 |
|---|---|---|
| **① 冷启动**（权重按需载入 + TorchCompile + 第 1 步） | **48 s** | tqdm `1/8 [00:48, 48.45s/it]` |
| **② 第 2–8 步** | **321 s** ⇒ **45.9 s/步** | 369 − 48 |
| **③ VAE 解码 + 编码保存 + 收尾** | **61.75 s** | 430.75 − 369，含 AudioVAE 576 MB + VideoVAE 4965 MB 按需载入 |
| **合计（ComfyUI 权威值 `prompt executed`）** | **430.75 s ≈ 7 分 11 秒** | 日志原值 |

> ✅ 校验：48 + 321 + 61.75 = 430.75 —— **逐位自洽**。

**⭐ 由此得到的产线估算公式**（本机当前配置，实测拟合）：

```
单段耗时(s) ≈ 64 + 45.9 × N      N = 去噪步数
```

| 步数 N | 推算耗时 | 与实测对照 |
|---|---|---|
| 8 | **431 s（≈7.2 min）** | ✅ 实测 430.75 s —— **公式命中** |
| 16 | 798 s（≈13.3 min） | ❓未测 |
| 20 | 982 s（≈16.4 min） | ❓未测 |

> 公式含义：**固定开销 64 s**（冷启动 48 s + VAE 解码保存 62 s，其中冷启动与第 1 步重叠 −46 s）+ **45.9 s × 步数**。
> ⚠️ **冷启动 48 s 是否能被摊掉 ❓未测** —— 若模型常驻不卸载，多片连出可省下这 48 s/片（产能 +11%）。

| 资源 | 峰值 / 表现 |
|---|---|
| **显存** | 峰值 **23582 MiB**，最低仅剩 **545 MiB** —— **未 OOM** ✅ |
| 策略 | ComfyUI **动态 VRAM 加载**：text_encoder 14956 MB / video VAE 4965 MB / 主干 19995 MB `Staged`，`fast_disk=True` |
| 量化 | `Native ops: int8_tensorwise, convrot_w4a4, asym_w4a8_int8` ✅ INT8 生效 |
| 温度 / 功率 | 68 °C / 339 W（正常满载） |

> ⚠️ **两条重要保留**：
> ① **能在 15.4 GB 可用显存下跑通，靠的是 ComfyUI 动态 VRAM（权重按需换入换出）** —— 代价就是速度（45.9 s/步）。这是显存被他人项目占用 8.7 GB 的**直接后果**：若独占 24 GB，速度应显著更快（❓**未测**，需 heygem 释放后复测）。
> ② **`Comfy model compiler graph breaks: 3, rogues: 0`** —— `TorchCompileModel` 已生效但图被断成 **3 段**（未 fuse 的一次性开销）。这是个**白捡的 T1 优化点**：若能降到 0，每步还能再省一点（❓未测，成本极低）。

### 5.3 输出规格（逐项核对）

| 项 | 实测 | 判定 |
|---|---|---|
| 分辨率 | **768×1344** | ✅ 与画布**逐像素一致**（零拉伸/零裁切） |
| 帧率 | 24 fps | ✅ |
| 帧数 / 时长 | 124 帧 / **5.167 s** | ✅ 贴 `17k+5` 网格 |
| 视频编码 | h264 High / yuv420p / bt709 | ✅ 通用兼容 |
| **音频轨** | **AAC LC / 32 kHz / stereo / 131 kb/s** | ✅ **H3 原生音视频联合生成**（附带产出） |
| 文件 | `H3PROD/TEST_00001_.mp4`，**661,251 B** | ✅ |

### 5.4 画面质量核验（三帧人工判读）

| 帧 | 内容 | 判定 |
|---|---|---|
| **首帧**（n=0） | 黑色保温杯静置白底，构图干净 | ✅ 产品保真，无变形 |
| **中间帧**（n=50） | **手从右侧伸入、手指正握住杯身**，光影在杯身流动 | ✅ **动作真实发生**；手部解剖结构正确（AI 生成难点） |
| **尾帧**（n=106） | 手已离开，产品回到静置，**光影明显转暖** | ✅ 状态连贯，无崩坏 |

**质检小结**：产品保真 ✅ ｜ 构图稳定（无漂移/跳变）✅ ｜ 白底无文字水印 ✅ ｜ 手部自然 ✅ ｜ 光影流动自然 ✅

### 5.5 ⭐ 一条重要的工程洞察（实测得出，修正既有假设）

**现象**：提示词写了 `[Shot 2] The flask rotates slowly...`（旋转 45°），但**成片里产品角度基本没变**（只有光影变化 + 手部进出）。

**归因**：不是模型失败 —— 是**我上传的尾帧图本身就没有真正旋转**（TT Image2 的 edit 只改了光影，没改角度）。
⇒ **H3 严格忠实于首尾帧锚定**：画面变化幅度 **完全由首尾帧的状态差决定**。

**推论（写进生产规范）**：

| 想要的画面效果 | 必须在哪一步实现 |
|---|---|
| 大动作（旋转/开合/位移） | **必须在图片层（TT Image2）就把状态差做出来** |
| 小动作（光影流动/微推近） | 提示词即可 |
| ⚠️ 但 TT Image2 的**"状态跃迁"能力弱**（实测：会"擅自"改状态，也无法稳定做出精确旋转） | ⇒ 与既有"**禁2：TT Image2 只做状态切片、不做状态跃迁**"完全吻合 ✅ |

> **生产含义**：**动作设计的瓶颈在图片层，不在 H3**。要么用多张真实产品图（不同角度）作锚定，要么接受"静置 + 光影 + 手部交互"这类动作幅度。

---

## 6. 环境冻结清单（Phase 0.5 部分交付）

| # | 项 | 冻结值 |
|---|---|---|
| 1 | **内核版本** | ComfyUI **0.36.0**，commit `d39cdfdb03932f14f390dda27891b3e546efb746` |
| 2 | **加速档** | **T0 官方纯净档**（未装 SageAttention；INT8 注意力 + 动态 VRAM 生效；未启 TorchCompileModel） |
| 3 | **节点包 commit** | `custom_nodes` = **空**（纯官方 957 节点）｜`comfy_kitchen 0.2.34` |
| 4 | **启动参数** | `--port 6011 --listen 0.0.0.0 --enable-manager --extra-model-paths-config $ROOT/comfy/extra_model_paths.yaml --base-directory $ROOT/comfy --input-directory $ROOT/input --output-directory $ROOT/output --temp-directory $ROOT/temp --preview-method none --disable-auto-launch`（vram = auto） |
| 5 | **权重指纹** | 5 件 sha256（见 `models_manifest.txt`）—— 与官方 API `lfs.sha256` 逐件一致 |

---

## 7. 诚实边界（❓ 未测 / ⚠️ 风险）

| # | 项 | 状态 |
|---|---|---|
| 1 | **独占 24 GB 显存下的速度** | ❓ 未测（当前 46.4 s/步是 15.4 GB 受限值，**不能作为产线基准**） |
| 2 | **多段拼接成整片** | ❓ 未测（本次只验证单段 5 s） |
| 3 | **判官团实测** | ❓ 未上线（阈值仍需 Phase 3.0 人工标定集） |
| 4 | **图片层状态跃迁能力** | ⚠️ TT Image2 无法稳定做出精确角度变化（§5.5）⇒ 大动作需另想图片层方案 |
| 5 | **TT Image2 云端版本漂移** | ⚠️ 供应商静默更新无法感知，需金标准样本比对监控 |
| 6 | **hf-mirror 大文件下载** | ⚠️ 已证实不可靠 ⇒ **后续一律走"数据盘查重 + O_DIRECT 复制"**，避免重蹈 3.6 h |
| 7 | **他人项目占用** | ⚠️ heygem 常驻 8.7 GB 显存 + 27.6 GB 内存；若其有任务将直接与 H3 抢显存 |

---

## 8. 本轮产出清单

| 类别 | 文件 |
|---|---|
| 服务器 | `$ROOT/models/`（5 件官方权重，41.4 GB）｜`$ROOT/models_manifest.txt`｜`$ROOT/comfy/ComfyUI`（0.36.0）｜`$ROOT/venv`｜实例 6011 运行中 |
| 脚本（已同步到服务器） | `test_fl2v_official.py`（含 `--aspect` + 比例守卫）｜`ttimg.py`（含指数退避重试）｜`orchestrator.py`（`img_size=auto` + Gate-P P-07）｜`start_comfyui.sh`（已修 custom_nodes 崩溃）｜`env_datadisk.sh` / `check_datadisk.sh` |
| 出片证据 | `h3-pipeline/deliverables/H3_FIRST_TEST.mp4`（661 KB）＋ `f01/f02/f03.png` 关键帧 |
| 报告 | 本文 `REPORT22_服务器核查与首次出片实测.md` |

---

## 附：一句话给辉哥

**数字人没在干活（只是占着 8.7 G 显存不撒手）；内存那 97% 是 page cache 撑出来的数字，出片时内核当场回收了 14.9 G 证明它随时可用 —— 不用重启。**
**部署已经跑通：5 件官方权重物理独立落盘（顺手把白烧的 3.6 小时换成了 6 分钟），独立实例起在 6011，并且真机出了第一条片子 —— 7 分 11 秒、768×1344、带音轨、手部动作自然。**
**唯一要记的一条新规律：画面动作幅度由首尾帧决定，H3 只是忠实的插值器 —— 想要大动作，得先在图片层做出来。**
