# REPORT15 · 判官团配置规范与崩坏处理总表

> 日期：2026-09-17
> 来源：REPORT11（判官团多采样择优审计）+ REPORT13（抽卡收益边界）+ 部署指南 v2.1
> 性质：**配置规范**——判官按什么形态评判、动作逻辑按什么组合执行、全部崩坏类型怎么分诊
> 配套：`judge_config.yaml`（机器可读配置）、`breakdown_triage.html`（崩坏分诊台）

---

## 0. 一句话总纲

> **判官团的评判方式必须是「逐项独立提问的二值判定」，不是「一次问完的整体打分」。**
> 动作逻辑的解法不是单点最优，而是**组合拳**：分镜定型（预防）+ 混合合成（兜底）+ 判官专项（筛选）。
> 所有崩坏的处理都先问一句：**换 seed 会变吗？** 会变 → 重抽；不会变 → 改方案，重抽一次都不许。

---

## 1. 判官团最擅长的评判形式（配置总纲）

REPORT11 已证伪三类失效：**检查不完整 / 理由不忠实 / 归因纠缠**。下面七条规则就是针对这三失效的直接对策，也是判官团唯一应采用的形态。

### 1.1 七条形态铁律

| # | 规则 | 针对哪个失效 |
|---|---|---|
| **F1** | **原子化**：一项 = 一个「可观察的二值事实」，不含"是否良好""是否自然"这类程度判断 | 检查不完整 |
| **F2** | **一题一次调用**（single-question per call）：N 个判项 = N 次独立调用，永不合并成一次多问 | 检查不完整（合并必漏，且项间互相干扰） |
| **F3** | **先证据后判定**（evidence-first）：先输出观察到的帧号/区域，再给是/否 | 理由不忠实 |
| **F4** | **三值输出**：`是` / `否` / **`N/A`**。画面中不存在该事项时必须给 N/A 出口 | 误杀（缺 N/A 出口 → 被迫判"否"） |
| **F5** | **禁止打分、禁止自由叙述**：不输出总分、不输出"整体感觉" | 三失效全部 |
| **F6** | **关键项双采样一致**：W-01/W-02/W-04/P-01 跑 2 次取一致；不一致 → 升级人工 | 判官自身不可靠 |
| **F7** | **版本锁定 + 标定集独立**：模型 + 权重 + prompt 版本全量入 `runs.jsonl`；标定集独立于调参样本 | 判官漂移（已知未解风险） |

> ⚠️ F2 是最反直觉也最值钱的一条。**一次 prompt 问 20 项 = 平均漏 3–5 项，且漏的往往是后段**。逐项独立调用虽然调用数 ×N，但 L1 已经砍掉 80% 的量，L2 只跑幸存者，总成本可控。

### 1.2 输出契约（唯一合法格式）

```json
{"item_id":"W-03","verdict":"no","evidence":{"frame":142,"region":"bottle-top"},"confidence":"high"}
```

- `verdict` ∈ `yes` / `no` / `na`
- `evidence` **必填**：`frame`（帧号）或 `region`（画面区域）至少一项；L1 数值项填 `value` + `threshold`
- **无总分字段、无 free-text 理由字段**（两条都禁止）

### 1.3 判定聚合规则

| 段 | 聚合 |
|---|---|
| **R 指令遵循** | 任一 `no` → 否决 |
| **W 世界一致性** | 任一 `no` → 否决 |
| **P 感知质量** | 按缺陷计数设阈值（建议 P 段 `no` ≥ 2 → 否决） |
| **N/A** | 全 `na` 的 R/W 项不计入否决，但计入「清单覆盖率」告警 |
| **覆盖率告警** | 单条视频 `na` 比例 > 40% → 疑清单生成有误，需人工看一眼 |

---

## 2. 三级判官配置表

### 2.1 L0 · 技术校验（纯算术，<1s/条，零模型）

| ID | 判项 | 判法 | 否决 |
|---|---|---|---|
| L0-01 | 解码成功 | ffprobe 返回码 | 是 |
| L0-02 | 帧数符合 `17k+5` 网格 | 计算 | 是 |
| L0-03 | 分辨率 = 声明值（0.98MP=1344×768） | 计算 | 是 |
| L0-04 | 时长 = 声明值 ±1 帧 | 计算 | 是 |
| L0-05 | 音轨存在且非全静音 | RMS 阈值 | 是 |
| L0-06 | 无黑屏 / 全白帧段 | 亮度直方图 | 是 |
| L0-07 | 无长静音（> 1.5s） | VAD | 是 |
| L0-08 | 容器完整可播 | ffprobe `-v error` | 是 |

### 2.2 L1 · 专用检测器（数值判定，5–20s/段，本地 CPU）

> 阈值全部**待标定**（字段名以 `τ` 占位，见 `judge_config.yaml`）。原则：**假阴性比假阳性更致命**——误杀好片 = 白烧生成算力，所以阈值取保守侧。

| ID | 判项 | 实现 | 阈值 | 失败类型 | 可重抽 |
|---|---|---|---|---|---|
| L1-01 | 人脸嵌入距离 | ArcFace / InsightFace 余弦 | < τ_face | 随机 | ✅ |
| L1-02 | 产品 ROI 相似度 | ROI 裁剪 + 直方图/嵌入 | > τ_prod | 混合 | ⚠️ |
| L1-03 | 光流幅度（动作是否真发生） | Farnebäck 时序均值 | > τ_flow 且无 >0.5s 平台 | 随机/结构 | ⚠️ |
| L1-04 | 光流断裂点（动作跳变） | 曲线二阶差分尖峰 | 无尖峰 | **结构** | ❌ |
| L1-05 | 跨段直方图/调色板相似 | 相邻段首尾帧比对 | > τ_tone | 随机 | ✅ |
| L1-06 | OCR 逐字比对 | PaddleOCR + 归一化编辑距离 | = 0 | 随机 | ⚠️ |
| L1-07 | 唇音互相关 | 口型 ROI × 音频包络 | r > τ_lip | 随机 | ✅ |
| L1-08 | 状态跟踪（落正确帧号） | `state_track.py` | 状态帧号 ∈ 允许窗口 | **结构** | ❌ |
| L1-09 | 手部关键点计数 | MediaPipe Hands | 5 指且无异常交叠 | 随机 | ✅ |
| L1-10 | 帧间差分周期性（闪烁） | 差分序列 FFT 峰值 | 无周期性峰 | 随机 | ✅ |

### 2.3 L2 · VLM 原子清单（逐项独立调用，只跑幸存者）

**R 段清单按产品生成**（判据 = prompt 原文，**不是**参考视频）；W / P 段为通用项。

#### R 段 · 指令遵循（判据一律来自 prompt 原文）

| ID | 判项 | 判法要点 |
|---|---|---|
| R-01 | 画面主体是否为 prompt 声明的产品 | 只判"是不是这个品类/形态"，不判像不像某条参考片 |
| R-02 | 是否出现 prompt 声明的场景 / 背景 | — |
| R-03 | 是否按 prompt 出现声明的动作 | 判"动作类型对不对"，不判"动作好不好看" |
| R-04 | 画面文字内容是否与 prompt **逐字**一致 | 中文/价格一律 N/A（走后期，见处理表 #4） |
| R-05 | 画幅比例是否与声明一致 | — |
| R-06 | 是否出现 prompt 未声明的多余元素 | 多出来的人/物/文字都算"否" |
| R-07 | 结尾镜是否单一全画幅（无网格/分屏） | 官方硬要求 |

#### W 段 · 世界一致性

| ID | 判项 | 判法要点 |
|---|---|---|
| W-01 | 产品形态/颜色在本段内是否自洽 | **关键项**，双采样 |
| W-02 | 液体/用量等状态是否守恒 | 无该状态 → **N/A**；**关键项**，双采样 |
| W-03 | 接触面是否合理（无穿插/无悬空） | 无接触 → **N/A** |
| W-04 | 动作因果是否成立（先开后倒） | 无因果链 → **N/A**；**关键项**，双采样 |
| W-05 | 光影/阴影方向是否自洽 | — |
| W-06 | 人物身份是否与锚定图一致 | — |
| W-07 | 是否存在未完成 / 中断的动作 | 半截动作单独记为"否" |

#### P 段 · 感知质量（缺陷计数）

| ID | 判项 | 判法要点 |
|---|---|---|
| P-01 | 手部是否有畸形 / 多指 / 缺指 | **关键项**，双采样 |
| P-02 | 面部是否有畸变 | — |
| P-03 | 是否存在纹理闪烁 / 糊化 | 与 L1-10 交叉验证 |
| P-04 | 是否存在边缘撕裂 / 伪影 | — |
| P-05 | 主体是否在画面安全区内 | — |
| P-06 | 画面整体是否可辨识（非严重崩坏） | 唯一允许的"整体"判断，且仅作粗筛 |

---

## 3. 判官团自身的可靠性规则

| 规则 | 内容 |
|---|---|
| **双采样一致** | 关键项跑 2 次；一致 → 采纳；不一致 → **升级人工**（既不自动放行、也不自动否决） |
| **阈值独立标定** | L1 十个检测器各自独立标定，用历史人工判定样本（含正负对照，**一批不是一条**） |
| **保守偏置** | L1 阈值取保守侧（宁可放过，不可误杀） |
| **覆盖率告警** | `na` 比例 > 40% → 人工复核清单 |
| **全量留痕** | 判官模型/权重/prompt 版本 + 每次判定 JSON 全量入 `runs.jsonl` |
| **人工抽检** | 按 5% 抽检判官通过件；发现漏判 → 回标定集，重标阈值 |

> 判官团是**止损器，不是解决器**。它能告诉你不合格，不能把它改合格——**修的动作永远在闸门②和组合拳那侧**。

---

## 4. 动作逻辑的组合方案（保证效果）

### 4.1 按镜头类型配组合拳（这是"效果最佳"的实操形态）

| 镜头类型 | 组合手法 | 参与层 |
|---|---|---|
| **产品静置 / 光影 / 运镜**（无手、无因果） | 纯生成 + 中间锚点 ≤1 个 | 崩坏面最小，L1 主力 = 产品漂移 |
| **单手操作**（有手、**无**可验证物理结果） | 纯生成 + **避免手部特写**（远景 / 过肩 / 只露手腕）+ L1-09 手部检测 + 换 seed 重抽 | 随机型，重抽有效 |
| **使用后状态**（涂开后的肌肤、倒出后的杯） | **状态陈述**：只拍结果态切片，不拍转移过程 | 规避 L3 |
| **多步因果链**（拧→倒→涂） | **拆镜 + 剪辑连接**：每镜 1 个动作，跨段用剪辑点；跨段起点用**派生图**保证状态守恒 | 规避 + 拆解 L3 |
| **必须有精准真人操作 / 卡点** | **混合合成**（拍 2–3 张真实照片合成进 H3 环境）或实拍补 | 转嫁，100% 正确 |

### 4.2 三级保险（层层递减成本、递增确定性）

| 级别 | 手段 | 成本 | 覆盖 |
|---|---|---|---|
| **一级 · 预防** | 分镜定型（每镜动作数 = 1）+ 闸门②（有可验证物理结果 → 否决） | 零 | 结构型崩坏（状态守恒/接触物理/因果链） |
| **二级 · 兜底** | 混合合成：关键镜用真实照片合成 | 低（几张照片） | 全部三层，100% 正确 |
| **三级 · 筛选** | 判官专项项（L1-04/L1-08/W-02/W-04/W-07）+ 换 seed 重抽（N ≤ 3） | 中 | 随机型崩坏 |

### 4.3 三条全局铁律

1. **每镜动作数 = 1**（分镜阶段强制写进规范，闸门③校验）
2. **跨段状态守恒用派生图，不用模型**：第 1 段结束"盖已开" → 第 2 段起点锚定照必须是"已开盖"的派生图
3. **凡"有可验证物理结果"或"需精确手部"的动作，一律不进生成**——这条一执行，崩坏 #7/#8/#9 三类基本清零

---

## 5. 崩坏处理总表（16 类）

### 5.1 四条通用处理原则（比逐条更值钱）

| # | 原则 |
|---|---|
| **P1** | **先分类再处理**：换 seed 会变 → 重抽；不会变 → **改方案**。误把结构型当随机型重抽 = 烧钱买同一份错误 |
| **P2** | **结构型失败的三个出路（重抽不在其中）**：① 规避（不拍它）② 转嫁（混合合成 / 实拍）③ 拆解（拆镜 + 剪辑） |
| **P3** | **凡涉"物理结果"或"精确手部"，一律不进生成**（最大规模的一次性收益） |
| **P4** | **判定必须证据可回溯**（帧号 / 区域），否则判官是黑箱，人工无法复核 |

### 5.2 全类型分诊表

| # | 崩坏类型 | 检测项 | 类型 | 重抽 | 最佳处理 |
|---|---|---|---|---|---|
| 1 | 身份漂移（脸变 / 跨镜不同人） | L1-01 | 随机 | ✅ | **FL2VA 同源编辑链**（底图生成一次后派生）+ 重抽 |
| 2 | 产品漂移（形态/颜色/logo 变） | L1-02 + R-01 | 混合 | ⚠️ | 3 张独立锚定照 + 首尾帧同源派生；有 logo → 混合合成 |
| 3 | 参考不生效（参考图没起作用） | 参考敏感度对照 | **结构** | ❌ | **重抽无效**：查接线（`/history` 反查）+ 确认不是 T2V 模式 |
| 4 | 短英文文字糊 | L1-06 + R-04 | 部分随机 | ⚠️ | 1 中段 + 1 结尾，各 ≤3–5 词；**中文/价格/复杂 logo 一律后期叠加** |
| 5 | 接缝 / 色调漂移（跨段色温跳变） | L1-05 | 随机 | ✅ | 同源派生图 + 统一调色（后期 LUT）+ 重抽 |
| 6 | 音画不对齐（口型不匹配） | L1-07 | 随机 | ✅ | 配音**锚入**（见《音画同步》节）+ 重抽；必要时后置贴轨 |
| 7 | **状态不守恒**（液量不减 / 盖子自合） | L1-08 + W-02 | **结构** | ❌ | 状态陈述替代 / 拆镜 + 派生图 / 混合合成（P2 三出路） |
| 8 | **接触物理**（穿插 / 悬空 / 抓握失败） | L1-03 + W-03 | **结构** | ❌ | 减少手与接触出现（远景/过肩/只露手腕）/ 混合合成 / 实拍 |
| 9 | **多步因果链**（拧→倒→涂） | W-04 + L1-04 | **结构** | ❌ | 拆镜 + 剪辑连接；跨段派生图保状态守恒 |
| 10 | 构图偏移（主体出安全区 / 比例错） | P-05 + R-05 | 随机 | ✅ | 重抽 + 锚定照统一构图 + 闸门③比例校验 |
| 11 | 纹理闪烁 / 时序 flicker | L1-10 + P-03 | 随机 | ✅ | 换 seed 重抽；仍不稳则提步数或改段长 |
| 12 | 手部畸形（多指/缺指） | L1-09 + P-01 | 随机 | ✅ | **重抽（主力）** + 减少手部特写 |
| 13 | 时长 / 节奏不符（动作没走完 / 空转） | L1-03 曲线 | 混合 | ⚠️ | 段长按动作自然时长定（铁律 2）+ 重抽 |
| 14 | 光影 / 阴影方向不自洽 | W-05 | 随机 | ✅ | 重抽 + 锚定照统一光位 |
| 15 | 多余元素（出现未声明的人/物/文字） | R-06 | 随机 | ✅ | 重抽；反复出现 → 检查 prompt 是否有歧义表述 |
| 16 | 合规崩坏（AI 标识缺失 / 功效宣称 / 仿冒名人） | 后期检查表 | **结构（人为）** | ❌ | **不成片**：后期强制叠加显式+隐式标识 + 文案红线（见部署指南 §3.4） |

### 5.3 一张速查图

```
崩坏发生
  ↓
① 有没有可验证的物理结果？（状态守恒 / 接触 / 因果）
   ├─ 有 → 结构型 ⇒ 重抽一次都不许 → 走 P2 三出路（规避 / 转嫁 / 拆解）
   └─ 无 ↓
② 失败位置换 seed 会变吗？
   ├─ 会 → 随机型 ⇒ 换新 seed 重抽，N ≤ 3，到顶走失败出口
   ├─ 不会 → 结构型 ⇒ 同上，走 P2
   └─ 不确定 → 跑一次对照（同 prompt 换 seed）判定后再决定
③ 单点铁律：「选出最好的」≠「达标」
   N 份全不合格 ⇒ 必须走失败出口，不许相对择优
```

---

## 6. 落地清单与验收

| # | 动作 | 验收 |
|---|---|---|
| 1 | 把 §2 三级配置落成 `judge_config.yaml` | 文件存在，字段完整（已在本次交付） |
| 2 | L0 八项实现 | 输入 mp4 → 输出 8 项 `yes/no` + 数值，<1s |
| 3 | L1 十项打包为 `judge_l1.py` | 输出逐项数值 + 阈值比对 + 时间戳证据 |
| 4 | L1 阈值标定（正负对照样本） | τ 全部有值，且假阴性率经人工抽检确认 |
| 5 | L2 逐项独立调用实现 | 调用数 = 判项数（证明未合并） |
| 6 | 输出契约校验 | 无总分字段、无 free-text 理由字段、`evidence` 必填 |
| 7 | 关键项双采样 | 4 个关键项各跑 2 次；不一致路径能走通到人工 |
| 8 | 分镜规范写入「每镜动作数 = 1」 | 闸门③能拦截多动作镜 |
| 9 | 崩坏分诊台接入评审流程 | `breakdown_triage.html` 可用，输出处理动作 |

---

## 7. 诚实边界

- ✅ **已实测**：seed 方差 14.0 / 噪声 1.08 / 同 seed 逐位复现 / 本地参考视频零效果 / FL2VA 与 Ref2VA 互斥
- ❓ **未实测**：L1 各阈值、L2 判官准确率 J、关键项双采样的一致率、混合合成在本品类的实际良率
- ⚠️ **未解决风险**：判官版本漂移（靠 F7 版本锁定缓解，非根治）；VLM 对「因果链」类判断准确率低 → W-04 不能单独作为否决依据，需与 L1-04 交叉
- ⚠️ **本报告不含**：判官模型的选型与部署（属部署指南 Phase 5）
