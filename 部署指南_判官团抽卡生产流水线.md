# H3 带货视频生产流水线 · 部署指南

> 日期：2026-09-17（**v2.8** · 路线已锁定 + 端到端编排 + 模型白名单）
> 来源：REPORT7 / REPORT10 / REPORT11 / REPORT13 / REPORT14 的讨论结论与实测数据
> 性质：**部署执行文档**——按阶段顺序施工，每阶段有验收标准
> 原则：先画边界再动手；实测数据 ✅ / 未测推论 ❓ 分开标注；每步先验证再继续
>
> **v2 变更**：路线决策已锁定（见下方《决策记录》）——放弃参考视频动作迁移，生产主链路固定为 H3 标准链路，质量由判官团筛选 + 不达标重抽兜底。
>
> **v2.1 补充**：① Phase 2 新增《动作逻辑性的三层拆解与解法阶梯》；② Phase 3 前新增《判官基准》——明确「标准视频不能当相似度基准」，只能用于标定集 / few-shot / 规格提取。
>
> **v2.2 补充**：判官团评判形态落成可执行配置——**REPORT15《判官团配置规范与崩坏处理总表》** + `judge_config.yaml`（38 判项）+ `breakdown_triage.html`（崩坏分诊台）。Phase 3/5 改为引用该规范，不再在本指南重复判项清单。
>
> **v2.3 补充（重要）**：服务器换机 + 模型/链路审计 —— 见 **REPORT16**。要点：① 新机是 **RTX 3090**（非 4090），旧耗时基准作废；② 四件核心权重**正确无需替换**；③ 真正错的是**加载链路**（旧脚本含 4 个社区包节点，官方链路从未跑过）；④ 官方 8 步 Turbo LoRA 已下载入库；⑤ 已建隔离目录 `/root/autodl-tmp/h3p/`；⑥ 生产主调用换成 `test_fl2v_official.py`。
>
> **v2.4 补充（辉哥规范）**：**正式部署时，本项目建立自己的文件夹，模型也独立下载使用，不与其他项目共享。** ⇒ 已升级为 §1.1「模型独立性」硬规范 + Phase 0 步骤 ③b：**四件权重与 LoRA 全部落本项目自有真文件（零软链）**，每件用**官方 sha256** 校验（而非文件大小）；执行脚本 `deploy_independent.sh`；产出 `models_manifest.txt` 作为独立资产凭据。
> 　※ 独立性边界：**代码 / 模型 / 输入输出 / 日志全程独立**；**Python 运行时与 CUDA 驱动共用**（同为系统级环境，重装无隔离收益且风险高）。
>
> **v2.5 补充（提速整合）**：见 **REPORT18《极速链路选型与提速整合》** + **`speedup_stack.html` 提速叠加台**。要点：① 新增 **§1.5 极速链路（三档）**，产线默认走 **T0 官方纯净档（零安装，推算 1.4–1.6×）**；② Phase 0 内核目标改为 **0.36.0**；③ Phase 1 资产库新增 **`env_manifest.txt`**（节点包版本 + commit）；④ Phase 6 标定实验新增 **A0–A8 逐层提速 A/B**；⑤ 三条流行说法被纠正：**EasyCache 在 8 步下空转（实测 −0.7%）**、**SGLang 的 3.17× 是 8 卡比 1 卡**、**CUDA13+INT8 前提我们已满足**。
>
> **v2.6 补充（顺序审计 + 文案层）**：见 **REPORT19《部署顺序审计与文案层选型》** + **REPORT20《ClipForge 融入方案》**。**修正了 2 个严重顺序错误**：
> 　🔴 **S1 提速 A/B 前移**：原顺序把「逐层提速 A/B（Phase 6b）」排在「种子资产库（Phase 1）」之后 ⇒ **资产库必然作废**（环境一变，同 seed 复现性立即失效）。现改为 **Phase 0b 跑 A/B → Phase 0.5 环境冻结 → 才建 Phase 1 资产库**。
> 　🔴 **S2 判官执行位置写死**：原 Phase 5 允许「本地 Qwen3-VL-8B」，而 H3 生成要吃 ≈20 GB 显存、3090 只有 24 GB ⇒ **会抢爆显存、拖慢生成**。现新增 **§2.0 判官隔离硬规范（三条红线）**：**判官三级全部出 GPU**。
> 　其余：S3 Phase 4 默认 N=1（机制先行）｜S4 新增 **Phase 3.0 人工标定集**（解鸡生蛋）｜S5 提速 A/B 加**「净耗时」质量闸门**（只看单次速度会误判）｜S6 A/B 只跑一次｜S7 新增 **Phase 0.5 环境冻结**｜S8 判官换版本必须跑回归｜S9 写明 L2 未上线期的盲区。
> 　另新增 **§1.6 文案脚本层（ClipForge）**：文字层外包给 ClipForge（跑在本地 Mac，**零 GPU 依赖**），接口为 **`shotlist_schema.json`**；三条隔离红线确保**不影响生成质量与速度**。
>
> **v2.7 补充（端到端编排 + 模型白名单）**：见 **REPORT21《端到端编排总链路》**。
> 　① 新增 **§1.7 端到端编排总链路**：把「用户上传产品图/模特图/文字 → 自动出片」串成 **S0–S9 状态机 + 四条并行通道 + 四道闸门**；明确 **TT Image2 的四个位置与三条禁区**。
> 　② 🔴 **新增 §2.0b 模型白名单红线（辉哥拍板·最高优先级）**：**生图只准 TT Image2 ｜ 视频禁用一切云端模型 ｜ 文字优先 GPT5.5 / GEM 3.7 ｜ 没有授权的模型坚决不能用**。
> 　③ 新增 **闸门P 图层校验**（S5）：**7 项判据**，把坏图挡在 GPU 之外 —— **用 1 分钟挡 10 分钟**。工具 `gate_p_image_gate.html`。
> 　④ 新增 **S3.5 分组**（相邻镜合并为片内多镜）：把 S 从 N 镜降到 1–2 段，是**免费且指数级**的收益。
> 　⑤ 新增执行件 **`orchestrator.py`**（编排骨架，含模型白名单启动自检 + Gate-P + 并发 + 熔断）。
> 　⑥ 🔒 **新增 §1.8 存储布局硬规范（辉哥要求）**：**所有部署与数据全部落服务器的「数据盘」`/root/autodl-tmp/`，系统盘不留任何本项目产物** —— 含 venv / pip 缓存 / HF 缓存 / torch·triton 编译缓存 / TMPDIR。执行件 `env_datadisk.sh` + `check_datadisk.sh`。
> 　⑦ 🔴 **新增宽高比硬规范（v2.8 · 静默毁片防线）**：**画布由 `aspect` 唯一决定**（9:16@0.98MP → **768×1344**）；**图片必须按同尺寸出图** —— 实测 H3 源码：首帧走 `plain stretch`（比例不符 = **静默拉伸变形**）、尾帧走 `aspect-preserving cover-crop`（**丢边缘构图**），**全程不报错**。已落 **P-07 判据 + 两道硬拦截**（§1.7）。

---

## 决策记录（2026-09-17 · 辉哥拍板 · 已锁定）

| # | 决策 | 状态 | 依据 / 影响 |
|---|---|---|---|
| **D1** | **放弃「真正的参考视频动作迁移」** | 🔒 **关闭** | 本地 `ref_videos` 实测零效果（REPORT6：换人/换场景/时间反转全落进复现噪声 1.08）；官方 API 的 `reference_video` 是否真迁移动作**不再作为生产前提**，只留作未来可选升级项 |
| **D2** | **生产主链路 = H3 标准链路** | ✅ **生效** | FL2VA 参考图生 + 3 张独立锚定照 + **一条片内多镜**（`[Shot 1]…[Shot 2] At 00:03.500`）；不再为「换人 / 换场景动作迁移」预留任何接口 |
| **D3** | **质量兜底 = 判官团筛选 + 不达标重抽** | ✅ **生效** | 不追「一次生成就完美」，改走工程化良率：判官三输出（达标？/ 失败类型？/ 可重抽？）+ 抽卡保底；**闸门②仍是前置硬约束，不能靠重抽弥补** |
| **D4** | 动作表达方式 | ✅ 生效 | 动作一律由**提示词动作白名单**（举起/并排/推近/旋转/开合/滑入/光影流动/静置/特写平移）表达，不依赖任何外部动作源 |
| **D5** | 2K / 15s 长片 | ⚠️ 仍缺 | 本地权重不含 Regenerate-2K；确实需要时按 REPORT14 切官方 API / Comfy Cloud（与本次决策无关，属独立升级项） |
| **D6** | 🚨 **模型白名单（v2.7 新增 · 最高优先级；v2.9 追加判官项）** | 🔒 **硬规范** | **生图只准 TT Image2（灵炫 `tt-image-2`）**；**判官/审片只准 TT-5.6luna（灵炫 `tt-5.6-luna`）**；**灵炫/任何云端的视频模型坚决不能用**（视频只走自持 H3）；**文字优先 GPT5.5 / GEM 3.7**；**没有授权的模型坚决不能用**。执行机制：`orchestrator.py` 启动自检 + `ttimg.py` 常量锁定 + `judge_shot.py` 白名单断言。详见 REPORT21 §11 / REPORT27 |
| **D7** | **端到端编排形态（v2.7 新增）** | ✅ 生效 | **S0–S9 状态机 + 四条并行通道（文字/图片/视频/判官）+ 四道闸门（① 分层 / ② 动作 / P 图层 / ③ 派发）**；图片与判官**全部在 3090 之外**，新增图片环节净时延增量 ≈ 冷启动 3–4 min |

> **一句话总纲**：不追「一条完美生成」，走「**标准链路 + 判官筛选 + 重抽**」的工程化良率路线。
>
> ⚠️ 该决策的边界（必须心里有数）：判官团**能筛不能修**——状态不守恒、接触物理、多步因果链这 3 类结构型失败，换 seed 重抽 **N 次还是同一个错**，必须在闸门②就挡掉（见 Phase 2 / Phase 4 分诊表）。

---

## 0. 方案总览：我们讨论出的完整计划

一句话：**「7 步流水线 + 3 道闸门 + 判官团三级级联 + 抽卡保底」四位一体。**

```
                    ┌────────────── 预防层（闸门）──────────────┐
【Gate】启动问询（素材/款式/时长/比例/风格/文案方式）
   ↓
① 识别产品 + 产品事实摘要 ──── ★闸门① 保真分层（像素级/形态级）
   ↓
② 文案三分流（画面内英文/旁白/后期）
   ↓
③ 11 列精确节拍分镜表 ─────── ★闸门② 动作白名单 + 因果链否决
   ↓                              （一条片内多镜 [Shot 1]…[Shot 2]…）
④ 三张独立锚定照（主视觉/材质结构/结尾构图）
   ↓
⑤ H3 生成 ────────────────── ★闸门③ 派发前 10 项硬校验
   ↓                    ┌────────── 过滤层（判官团）──────────┐
   ↓              L0 技术校验（<1s，纯算术）
   ↓              L1 专用检测器（5–20s/段，本地 CPU）
   ↓              L2 VLM 原子清单判官（贵，只跑幸存者）
   ↓                    ↓
   ↓              判官三输出：达标？/ 失败类型？/ 可重抽？
   ↓              ├─ 随机型失败 → ★闸门④ 可重抽性判定 → 换新 seed 重抽
   ↓              └─ 结构型失败 → 禁止重抽 → 强制换策略（保底）
   ↓
⑥ 配乐与卡点剪辑（按音乐重剪）
   ↓
⑦ 后期合成 + 合规（中文/价格/logo/CTA 叠加 + AI 标识）
```

**三档杠杆的正确排序（钱先花在哪）：**

| 顺位 | 手段 | 收益性质 | 依据 |
|---|---|---|---|
| 1 | **降生成次数**（一条片内多镜，S→1–2） | 指数级（整片良率 68.3%→93.8%，耗时还更短） | ✅ REPORT13 §3 |
| 2 | **提 p**（闸门② + 官方提示词规范） | 决定抽卡是否可负担 | ✅ REPORT10/11 |
| 3 | **抽卡**（增 N） | 边际递减，成本 ∝ N | ✅ REPORT13 |

**核心公式**：`整片良率 = [1 − (1−p)^N]^S`，S = 生成次数（指数惩罚项），N = 每段抽卡次数。

---

## 1. 部署环境与已有资产盘点

### 1.1 硬件与环境

| 项 | 状态 | 说明 |
|---|---|---|
| GPU 服务器 | ⚠️ **换了新机，而且不是 4090** | `connect.nmb2.seetacloud.com:36229`（AutoDL）——**RTX 3090 24 GB**；原 4090 服务器已弃用（审计见 REPORT16） |
| 模型 | ⚠️ 四件核心权重正确，但**链路与 LoRA 不对** | FL2VA / 文本编码器 / 视频 VAE / 音频 VAE 与官方清单逐项一致 ✅；缺官方 8 步 Turbo LoRA（已下载 ✅）；旧脚本实为**社区节点链路** ❌（REPORT16） |
| 项目根目录 | ✅ 已建 `/root/autodl-tmp/h3p/` | 本流水线**自持**：`comfy/`（纯官方节点，`custom_nodes` 留空）+ `models/`（**全部真文件，零软链**）+ `input/output/temp/logs` + `models_manifest.txt` |
| 模型独立性 | 🔒 **硬规范（v2.4）** | **本项目模型完全独立**——四件权重 + Turbo LoRA 全部落在 `$ROOT/models/` 自有副本，逐件校验官方 sha256；**不软链、不依赖共享库**（`/root/autodl-tmp/ComfyUI-models/` 仅作只读来源或弃用）。其他项目改动/删除模型库不影响本项目 |
| 本地工作区 | ✅ `/Users/admin/WorkBuddy/2026-09-11-07-42-55/h3-pipeline/` | 全部脚本、报告、实验数据 |
| 技能包 | ✅ `~/.workbuddy/skills/h3-fl2va-video-pipeline/` | SKILL.md + reference/ + scripts/ 已同步 REPORT7–13 |

### 1.2 已有工具清单（不重复造轮子）

| 工具 | 位置 | 用途 |
|---|---|---|
| `test_fl2v_official.py` | h3-pipeline/ + skill/scripts/ | **官方原生链路生成调用（新的生产主调用）**：只用 ComfyUI 核心节点，零社区依赖 |
| `test_fl2v.py` | h3-pipeline/ | ⚠️ 旧版：含 4 个社区包节点（`MiniMaxH3TurboLoRA` / `H3MemoryOptimization` / `H3SparseAttentionAdvanced` / `MiniMaxH3TurboSampler`），已退出生产链路（REPORT16） |
| `test_r2v.py` | h3-pipeline/ | 仅作历史留档（`--ref-video` 系列开关）；按决策 D1 已退出生产链路 |
| `frames.py` / `align.py` | skill/scripts/ | 派生图自检（背景 MAE<6 / 尺度漂移<2%） |
| `density_roi.py` / `state_track.py` | skill/scripts/ | 动作是否真发生 / 状态落在正确帧号 |
| `asr.py` / `lipsync2.py` / `audio_match.py` | skill/scripts/ | 音画对齐检测（DTW / 口型 r） |
| `refcheck.py` / `v6diff.py` / `v6board.py` / `v6motion.py` | h3-pipeline/ | L1 检测器的现成底料 |
| `gate2_action_whitelist.html` | skill/scripts/ | 闸门② 人工勾选工具（29 动作 + 5 红线问句） |
| `judge_panel_console.html` | skill/scripts/ | 判官团良率/成本模拟器（p/N/段数可调） |
| `gacha_console.html` | skill/scripts/ | 抽卡最优段长计算器 + 保底规则演示 |
| `judge_config.yaml` | skill/scripts/ | **判官团机器可读配置**：38 个判项（L0 8 / L1 10 / L2 20）+ 16 类崩坏分诊 + 运行时规则 |
| `breakdown_triage.html` | skill/scripts/ | **崩坏分诊台**：两问定类（有可验证物理结果？换 seed 会变？）→ 直接输出处理动作 |
| `speedup_stack.html` | h3-pipeline/ + skill/scripts/ | **提速叠加台**（v2.5）：分段 Amdahl 模型算 T0/T1/T2 三档增益；同组互斥项自动取最优不叠乘；附质量风险与装机清单 |
| `REPORT18` | skill/reference/ | **极速链路选型与提速整合**（本指南 §1.5 / Phase 6b 的细则） |
| `REPORT15` | skill/reference/ | **判官团配置规范与崩坏处理总表**（本指南判官部分的执行细则） |
| `shotlist_schema.json` | h3-pipeline/ + skill/scripts/ | **文字层↔画面层的接口契约**（v2.6）：ClipForge 输出 → 11 列节拍表 的映射规则 + 闸门②合并判据 + 端到端验证方法 |
| `REPORT19` / `REPORT20` | skill/reference/ | **部署顺序审计与文案层选型** / **ClipForge 融入方案**（v2.6 的两个依据文档） |
| 官方 SOP/规范 | skill/reference/ | 电商产品广告 SOP、提示词规范 base-en/ref-en、品牌片 SOP |

### 1.3 实测基准（部署时直接引用，不要重新拍脑袋）

| 项 | 数值 |
|---|---|
| FL2VA 耗时 | 2s→81–86s / 3s→100–105s / 4.5s→130–135s |
| 复现噪声基线（同素材改名重跑） | 帧差 1.08/255 |
| **换 seed 的画面差异** | **14.0（真实差异，噪声的 6 倍）** |
| 真模型参数改动（稀疏注意力档位） | 帧差 20–35 |
| 同 seed + 同 prompt | **输出逐位相同（重跑 = 零收益）** |
| 本地分辨率天花板 | 768p（2K 依赖闭源 Regenerate-2K → 需官方 API） |
| 推荐参数 | 8 步 / 段长 2–4.5s / 中间锚点 ≤1 个 |

> ⚠️ **上表耗时数据全部来自已弃用的 4090 机器，在 3090 上不成立**（算力约为 4090 的 50–60%）。换机后必须重跑冒烟重新标定——这正是 Phase 0 的验收内容。
> ⚠️ 另一个前提也已变化：上表所有数据来自**社区节点链路**（旧脚本），官方原生链路的耗时/画质基线同样需要重测（REPORT16）。

### 1.4 单条视频耗时预算（含抽卡，部署前必读）

**耗时模型**：`单次生成(秒) = 20 × 段长(秒) + 43`（拟合自 4090 实测，误差 <1%）
校验：2s→83s（实测 83）｜3s→103s（实测 102）｜4.5s→133s（实测 132）

**换算前提**（都是推算，不是实测）：3090 换算系数取 **1.65–2.00**；官方 8 步 + Sage Attention 的提速**未计入**（即下表的数字是"什么都不额外优化"的保守值）。期望抽次 `E = (1−(1−p)^N)/p`，N=3 封顶：p=0.5→1.75 次／p=0.8→1.24 次。

| 方案 | 段长 | 良率 p=0.5 | 良率 p=0.8 | **GPU 耗时 p=0.5** | **GPU 耗时 p=0.8** |
|---|---|---|---|---|---|
| 微片 15s · S=1 | 15s | 87.5% | 99.2% | 17–20 分钟 | 12–14 分钟 |
| **30s 片 · 片内两镜 S=2** ⭐ | 15s | 76.6% | 98.4% | **33–40 分钟** | **23–28 分钟** |
| 30s 片 · 分四段 S=4 | 7.5s | 58.6% | 96.8% | 37–45 分钟 | 26–32 分钟 |
| 30s 片 · 逐镜 12 段 S=12 ❌ | 2.5s | 20.1% | 90.8% | 54–65 分钟 | 38–46 分钟 |

**端到端交付时间** = GPU 生成 + 判官 + 拼接：
- 判官（L1 CPU + L2 VLM）≈ **3–4 分钟/段，且不占 GPU ⇒ 可与生成并行**
- 拼接 + 音画校验 ≈ 2–5 分钟
- ⇒ 串行 ≈ GPU 时间 + 10 分钟；**并行流水 ≈ GPU 时间 + 5 分钟**（推荐：把判官部署在 CPU/云端，边生成边判）

**单卡日产能**（GPU 满负荷）：30s 片 **约 35–60 条/天**；15s 微片 **约 100 条/天**。

**三条提速杠杆（按收益排序，与 §3 的良率杠杆一致）**：

| 顺位 | 手段 | 收益 |
|---|---|---|
| 1 | **降 S**（一条片内多镜） | 30s 片从 S=12 的 54–65 分钟压到 S=2 的 33–40 分钟，**同时整片良率从 20% 升到 77%** |
| 2 | **提 p**（闸门② + 官方提示词规范） | p 0.5→0.8：30s 片 33–40 → 23–28 分钟，省约 30% |
| 3 | **提速开关**（T0/T1/T2 三档，见 §1.5） | 官方纯净档推算 1.4–1.6×；增量档 1.7–1.9×；激进档 3.3–3.7×（**全部未实测**） |

> ⚠️ **未测项**：3090 换算系数、各提速开关的实际增益、官方链路 vs 社区链路的耗时差。**Phase 0 步骤 ⑦⑧ + Phase 6 的 A0–A8 跑完即可把上表换成实测值**——在此之前，对外报时口径用「30s 片约半小时量级」，不要报精确值。

---

### 1.5 极速链路（速度优先配置）· 详见 REPORT18 + `speedup_stack.html`

**一句话**：最快的方案不是一个节点，而是「**结构性手段（免费、收益最大）+ 官方内核手段（零安装）**」。单卡 3090 的现实极限约是当前基准的 **1/3**；再往上只能上多卡。

#### 三档链路

| 档 | 做了什么 | 装机成本 | 推算提速 | 质量风险 | 用途 |
|---|---|---|---|---|---|
| **T0 官方纯净档** ⭐ 产线默认 | kitchen INT8 注意力 + TorchCompile + 成片走 latent 放大 + **内核升 0.36.0** | **零安装** | **1.4–1.6×** ❓ | 极低 | 成片 |
| **T1 低风险增量档** | T0 + **SageAttention 2.2.0** + **VAE Speedup** | 装 2 个包 | **1.7–1.9×** ❓ | 低–中 | 成片 |
| **T2 激进档** | T1 + **FastH3 4 步** + **FirstBlockCache** | 装节点包 + 换 LoRA | **3.3–3.7×** ❓ | **高** | ⚠️ **仅草稿/试镜**，成片禁用 |
| T3 极限档 | 租 8 卡跑 RunningHub Lightning（开源） | 租 8 卡 + 354GB | **12.2×** 🔶 | 中 | 需预算，非本地 |

> 倍数由 `speedup_stack.html` 的分段 Amdahl 模型算出（去噪 80% / 视频解码 13% / 音频解码 2% / 文本编码 3% / 封装 2%，**同组互斥项取最优、不叠乘**）。**这是排序工具，不是报价依据。**

#### 五条硬规则（照做，别改）

1. **层与层会互相稀释** —— T1 相对 T0 只多约 **12%** 提速（分辨率层已把去噪砍掉近一半，SageAttention 能抓的绝对值变小）。⇒ **先装便宜的，不要为最后 10% 上高风险件。**
2. **分辨率分层的收益在「探索」，不在「成片」** —— 成片仍需 0.98MP，走 latent 放大只算 **1.3×**；它 3–5× 的真正收益是让同样的时间试更多版本，**落在 p 值上**，必须分开记账。
3. **注意力组三选一** —— kitchen INT8 / SageAttention / SLA 互斥，取最优不叠乘；**SLA 必须是最后一个 model patch**，且不得与 Block Sparse 同用。
4. **4 步只给草稿** —— 快速运动在 4 步下会糊、拖影（主要失效模式）；成片永远回到 6–8 步。
5. **每层增益 <10% 就不留**；任何让「同 seed 帧差 > 噪声基线 1.08」的层，标记「变了算」单独决策。

#### 三条被纠正的流行说法（别再照着装）

| 说法 | 真相 |
|---|---|
| 「加 EasyCache 就快一半」 | ❌ **8 步 Turbo 下实测 −0.7% = 空转**（只有 8 步，没有可跳的机会）；且有画质退化报告 ⇒ **不进产线** |
| 「SGLang Pack 3.17× 提速」 | ⚠️ 那是 **8 卡 TP2/Ulysses4 对比单卡原生**，**单卡不适用** |
| 「开 CUDA13 + INT8 提速 3 倍」 | ⚠️ 我们**已经是 torch 2.14+cu130 + INT8 convrot 权重**，前提早已满足；真开关是 **kitchen INT8 注意力后端**（零安装） |

#### 可照抄的装机清单（T1–T2）

| 组件 | 目标 | 说明 |
|---|---|---|
| ComfyUI | **0.36.0**（≥0.35.1） | 含 TAESD H3 快速预览解码 / VAE 优化 / **补齐缺失特殊 token（修生成异常）** / token 级噪声掩码 |
| torch / CUDA | 2.14.0+cu130（**已满足**） | 别动 |
| triton | 3.8.0（**已满足**） | Triton 回退路径前提 |
| sageattention | 2.2.0（cu130 wheel） | ⚠️ **本机当前未安装**（已纠正 REPORT16 记录） |
| comfy-kitchen | 0.2.31（**已满足**） | 若要用 Sol-Attn 需 ≥0.2.33 |
| 节点包（锁 commit） | `ComfyUI-PlagueKind-Nodes @6ca3037`（SLA）<br>`ComfyUI-MiniMax-H3-Turbo @4274783`（Turbo 预设）<br>`ComfyUI-KJNodes @3f20054`（Sage 补丁）<br>`ComfyUI-VideoHelperSuite @4ee72c0`（预览） | ⚠️ **一律克隆到本项目自己的 `custom_nodes/`**，不复制共享实例的目录；装完写进 `env_manifest.txt` |

> ⚠️ **独立性纪律同样适用于节点包**：共享实例 `/root/ComfyUI` 上已有一份"活样本"（0.36.0 + ALLinONE + H3-Motion-Context + VideoHelperSuite），**我们照抄它的配方（版本号 + commit），但不复制它的文件。**

### 1.6 文案脚本层（ClipForge）· v2.6 新增 · 详见 REPORT20

**决策**：带货视频的**文字部分（文案、脚本）外包给 ClipForge**（原「带货剪手」，`xixihhhh/clipforge`，828★，AGPL-3.0，v0.9.7）。
**分工**：**ClipForge 管「说什么」，H3 流水线管「怎么画」**；中间的契约是 **`shotlist_schema.json`**，不是像素。

| 项 | 规格 |
|---|---|
| 部署位置 | 🚨 **本地 Mac / 独立机器** —— **绝不装 3090**（其 FFmpeg 合成与 Node 会吃满 CPU，拖慢 ComfyUI 数据加载） |
| 资源占用 | Node + **SQLite（无需 Postgres）**，CPU ≈ 300–500 MB 内存，**零 GPU 依赖** |
| 部署方式 | Docker 一行：`docker run -d -p 3000:3000 -v clipforge-data:/data ghcr.io/xixihhhh/clipforge:latest`（或 Electron 桌面版 / 源码 `pnpm dev`） |
| 环境要求 | Node ≥ 20、pnpm ≥ 10、FFmpeg（含 `drawtext`） |
| 只取的能力 | `clipforge_generate_script`（**核心**）· `clipforge_ingest_product` · `script-judges` skill · `trends` |
| 不启用 | ❌ 它的出片链路（免费快剪 / AI 成片）—— 素材拼接路线，画质上限低于 H3 |
| LLM Key | 🚨 **必须用远端 API**（≈ ¥0.001/次）—— **不许指向 3090 上的 qwen3:14b**（会抢显存） |

**🚨 三条隔离红线（"不影响质量与速度"的全部依据）**：

| # | 红线 | 为什么 |
|---|---|---|
| 1 | **ClipForge 不装 3090** | 它不占 GPU，但吃 CPU；而 ComfyUI 的数据加载/VAE 解码全依赖 CPU。验收：3090 上 `docker ps` **不得出现 clipforge** |
| 2 | **LLM Key 不用本机 qwen3:14b** | 会占 3090 显存，与 H3 生成（≈20 GB）直接抢卡 |
| 3 | **只用文字产出，不搬九宫格** | 它用 3×3 九宫格，**违背 H3 官方 SOP**（要求 3 张独立锚定照、禁网格/拼贴）。⇒ **九宫格整图绝不进 H3**（裁出的单帧可用作中间锚点，条件是产品为**形态级保真**） |

**⭐ 融入的成败点：文案三分流**（不能跳过）

ClipForge 出的是**中文口播文案**，H3 prompt 要的是**画面内短英文（3–5 词）** ⇒ **必须三分流**：

| 分流 | 去向 |
|---|---|
| **画面内英文** | 重写成 3–5 词英文 → 进 H3 prompt **逐字包含**（闸门③第 1 条校验） |
| **中文旁白** | 走**后期 TTS 配音**（中文口型在 H3 上不稳；若用原生音频须过 `lipsync2.py`） |
| **后期图文**（价格/CTA/合规） | 后期叠加，**绝不交给模型画** |

**它可借鉴进我方规范的资产（两家经验同源互证）**：

| 资产 | 与我们的关系 |
|---|---|
| 动作可生成性**四类禁写**（双主体精确交互 / 内部心理 / 否定式动作 / 一拍三步动作链） | ✅ **并入闸门②**（与动作白名单互补） |
| 爆款复刻：**ffmpeg 解析镜头切点出节奏骨架** | ✅ 与「标准视频只提取规格」结论**完全一致** |
| 废片**六症状单变量重投** | ✅ 与「每次只改一个变量」同源 |
| 钩子库 16 款 / 留人硬规则 / 分镜三硬规则 / 情绪三拍写法 | ✅ 采纳 |

> ⚠️ **AGPL-3.0 边界**：自部署**自用**没问题；**若改造成对外提供的 SaaS，必须按 AGPL 开源修改版**。它生成的**视频内容不受影响**。辉哥的一键视频网站若要内置，这条必须先定。

> 📌 **与 H3 部署顺序解耦**：ClipForge 不参与生成链，**可在 Phase 0 之前或并行部署**，不影响 Phase 0–6 的施工顺序。唯一需对齐的是 **Phase 2 闸门②要并入它的「四类禁写」**。

### 1.7 端到端编排总链路 · v2.7 新增 · 详见 REPORT21

**场景**：用户上传**产品图 + 模特图 + 产品文字信息（可选）→ 系统自动生成完整带货视频**；过程中需要生成图片时，用**灵炫 TT Image2**。

**一句话**：链路不是一条流水线，是**四条并行通道 + 四道闸门**。

| 通道 | 载体 | 与瓶颈的关系 |
|---|---|---|
| 文字通道 | Mac CPU（ClipForge） | 完全解耦 ✅ |
| **图片通道** | **灵炫云 TT Image2** | **完全并行**（37–66 s/张 ≪ 1 段生成 9.4–11.4 min）✅ |
| **视频通道** | **3090 H3** | ⬛ **唯一瓶颈（串行）** |
| 判官通道 | Mac CPU + 远端 VLM | **异步并行**（1–2 min/段）✅ |

| 闸门 | 位置 | 挡什么 |
|---|---|---|
| ① 保真分层 | S0 | 像素级产品被误交给生成模型 |
| ② 动作可行性 | S3 | 结构型失败进门（重抽救不了） |
| **P 图层校验** ⭐新增 | **S5** | **坏锚点/坏首帧进 H3（错误被放大）**；**v2.8 起含 P-07 画布比例一致**（纯算术 · 零成本 · 防静默毁片） |
| ③ 派发硬校验 | S6 前 | 格式 / 接线 / 比例类硬错误 |

**TT Image2 的四个位置**：① 锚定照 ×3 ② 每段首尾镜像帧 ③ 模特补全（无模特图时）④ 中间锚点（可选，按出错频率加）。
**三条禁区**：禁1 像素级产品不得由它重绘 ｜ 禁2 只做「状态切片」不做「状态跃迁」 ｜ 禁3 宫格整图绝不进 H3（禁4 见 §2.0b）。

**🚨 宽高比硬规范（v2.8 新增 · 静默毁片防线 · 详见 REPORT21 §3.5）**

| 项 | 规定 |
|---|---|
| **画布** | **由 `aspect` 唯一决定**：`9:16 + 0.98MP → 768×1344`（**32 对齐后的真实值**，非理论 756×1344）。16:9 → 1344×768 ｜ 1:1 → 1024×1024 ｜ 3:4 → 864×1184 ｜ 2:3 → 832×1248 ｜ 21:9 → 1536×672 |
| **图片** | **TT Image2 的 `size` 直接传画布像素串**（`"768x1344"`）—— ✅ 实测**逐像素命中**，零偏差 |
| **禁令** | 图片比例**不得**与画布不同 —— 哪怕「看起来差不多」（理论 9:16 的 1080×1920 仍有 1.6% 形变） |
| **依据** | 实测服务器源码 `comfy_extras/nodes_minimax_h3.py`：**首帧 `plain stretch`（拉伸变形）｜尾帧 `aspect-preserving cover-crop`（裁掉边缘）｜两者都不报错** |
| **执行件** | ① `test_fl2v_official.py` 提交前比例守卫（偏差 > 2% → **拒绝提交**）② `orchestrator.py` Gate-P **P-07** 逐段校验（不过即**熔断**）③ `img_size` 支持 `auto` 自动推导 —— **不手填两份** |
| **实测** | ✅ 6/6 + 5/5 场景验证：1:1 拦下（偏差 75%）、横版拦下（206%）、竖版放行、路径缺失不误报 |

**执行件**：`orchestrator.py`（S0–S9 状态机 · 模型白名单自检 · Gate-P · 分通道并发 · 超时熔断）
**配套工具**：`gate_p_image_gate.html`（图层闸门校验台）

> 📌 **部署顺序影响**：本编排**不改变** §2.1 的施工顺序（Phase 0b → 0.5 → 1 …）。`orchestrator.py` 属**运行期**产物，可在 Phase 2 之后随时接入；它依赖的 `ttimg.py` 已存在，**无需新增部署**。
>
> ⚠️ **与既有铁律的一致性**：S3.5 分组（相邻镜合并为片内多镜）就是既有「**降 S 是第一杠杆**」的落地；S5 的 Gate-P 是「**防御前移**」原则在新增图片环节上的必然延伸。

### 1.8 🔒 存储布局硬规范（v2.7 新增 · 全部落数据盘）

> **辉哥要求**：**所有部署全部在服务器的数据盘上，不要将文件和数据部署在服务器系统盘。**

**AutoDL 的两块盘（必须先分清楚）**：

| 盘 | 挂载点 | 特征 |
|---|---|---|
| **系统盘** | `/`、`/root`（除 autodl-tmp 外） | 容量小（≈30–50 G）、**不保存镜像就会丢**、扩容困难 |
| **数据盘** ⭐ | **`/root/autodl-tmp/`** | 大容量、持久、按量计费但便宜 |

**逐项落位表（照做，含最容易漏的三处）**：

| 类别 | 必须落在 | 设置方式 |
|---|---|---|
| 项目根 | **`/root/autodl-tmp/h3p/`** | ✅ 已建 |
| 模型权重（4 件 + 8 步 LoRA） | `$ROOT/models/` | `deploy_independent.sh` |
| ComfyUI 实例 + 节点包 | `$ROOT/comfy/` | 独立实例，不复制共享实例文件 |
| 输入 / 输出 / 中间产物 / 日志 | `$ROOT/{input,output,temp,logs}` | 启动参数对齐 `comfy-*` 目录 |
| 🚨 **Python venv** | `$ROOT/venv/` | `python -m venv $ROOT/venv` —— **不要 pip install 进系统 python** |
| 🚨 **pip 缓存** | `$ROOT/cache/pip` | `PIP_CACHE_DIR` |
| 🚨 **HuggingFace 缓存** | `$ROOT/cache/hf` | `HF_HOME` + `HUGGINGFACE_HUB_CACHE`（**用 hf-mirror 下 LoRA 时最易踩**） |
| torch 扩展编译缓存 | `$ROOT/cache/torch` | `TORCH_EXTENSIONS_DIR` |
| Triton 缓存 | `$ROOT/cache/triton` | `TRITON_CACHE_DIR` |
| 临时文件 | `$ROOT/tmp` | `TMPDIR` |
| XDG 缓存 | `$ROOT/cache/xdg` | `XDG_CACHE_HOME` |

**执行件**：
- `env_datadisk.sh` —— **每次开实例第一步就 `source` 它**，之后所有缓存自动落数据盘
- `check_datadisk.sh` —— 自检：列出系统盘上被本项目占用的目录与体积，**非空即报警**

**三条理由（这不是洁癖）**：

| # | 理由 |
|---|---|
| 1 | **系统盘满 = 实例异常**。v2.6 现场就发生过 `/` 跑到 **100%** 导致任务阻塞（后清到 41%）|
| 2 | **系统盘内容不持久**。不保存镜像就丢 —— 重下 2 GB 权重的成本远高于改一个环境变量 |
| 3 | **数据盘空间换得来，系统盘换不来**。系统盘扩容要走工单，数据盘直接扩 |

> ⚠️ **「共享实例」的例外**：`/root/ComfyUI`（他人实例）不在本规范管辖内 —— 我们的纪律是**照抄它的配方（版本号 + commit），但不复制它的文件、也不改动它**。

---

## 2. 分阶段部署计划

### §2.0b 🚨 模型白名单红线（v2.7 新增 · **最高优先级** · 详见 REPORT21 §11）

> 辉哥拍板：**灵炫平台的生图模型只允许用 TT Image2，视频模型坚决不能用；文字模型优先使用 GPT5.5、GEM 3.7；没有授权的模型坚决不能用。**
>
> 🆕 **2026-09-18 追加（判官项）**：**判官/审片模型锁定 `tt-5.6-luna`**，由它替换原先越界使用的 `doubao-seed-2-1-pro-260628`；**除 `tt-5.6-luna` 外，不得再调用任何同类（多模态）模型**。已写成 `judge_shot.py` 的**白名单断言**（违规即退出），不靠自觉。

| 用途 | ✅ 允许 | 🚫 禁止 |
|---|---|---|
| **生图** | 灵炫 **`tt-image-2`**（唯一） | 灵炫其它生图模型；任何未授权模型 |
| **视频** | **自持 MiniMax H3 FL2VA** 本地权重 | 🚫 **灵炫 / 任何云端的视频模型**（seedance / kling / vidu / hailuo / runway / veo …） |
| **文字** | **GPT5.5** / **GEM 3.7** | 未授权模型；3090 上的 qwen3:14b（抢显存） |
| **判官/审片** | 灵炫 **`tt-5.6-luna`**（唯一） | 🚫 **任何同类多模态模型**（`doubao-seed-*` / `gem-*` / `omni-*` / `qwen*-vl` / `kimi*` …）；3090 上的本地 VLM（抢显存） |

**四处执行机制（写进代码，不靠自觉）**：
1. `orchestrator.py` → `_check_model_policy()` **启动自检**（检查 MODEL 常量、H3 权重指向、视频模型关键字）
2. `ttimg.py` → `MODEL = "tt-image-2"` **常量锁定**，改动须先取得授权
3. `judge_shot.py` → `ALLOWED_VLM_MODELS = ("tt-5.6-luna",)` **白名单断言**：即使有人用 `JUDGE_VLM_MODEL` 环境变量改写模型名，不在白名单内立即 `sys.exit`（2026-09-18 实测拦截有效）
4. **违反即中止**（不是警告）：白名单告警出现时不得进入生产跑批

> ⚠️ **能力存在 ≠ 允许使用**：灵炫（`api.lk888.ai`）同时也是多模型网关，`/v1/models` 实测列出 **67 个模型** —— **我们的调用清单里只允许出现两项：`tt-image-2`（生图）+ `tt-5.6-luna`（判官）**。其余 65 个（`doubao-seed-*` / `gem-*` / `omni-*` / `qwen*` / `kimi*` / `glm*` / `deepseek-*` / `MiniMax-*` …）一律不得调用。
>
> ⚠️ **「没有授权」的口径**：社区权重、未购买许可的模型、来源不明的**中转站**模型（常是降智版）均属未授权；H3 商用需 MiniMax 商业许可（见既有商用合规铁律）。

### §2.0 🚨 判官隔离硬规范（v2.6 新增 · 三条红线）

> **这一节回答的正是「判官团会不会拖慢生成」。**
> 结论：**只要守这三条，判官耗时可被完全隐藏；破一条，"不影响"立刻不成立。**

**先看算术（为什么这不是小事）**：

| 组件 | 显存需求 |
|---|---|
| H3 FL2VA 生成（int8 convrot） | **≈ 20 GB** |
| 本地 Qwen3-VL-8B（fp16）判官 | **≈ 16 GB** |
| RTX 3090 总显存 | **24 GB** |

⇒ **装不下**。强行同时驻留会触发显存交换 / OOM 重试，**生成时间可能翻倍甚至失败**。且实测该实例**已跑着 `tts-gateway`**，辉哥另有 qwen3:14b 需求 —— **同机争抢是既成事实，不是假设**。

| # | 红线 | 具体要求 |
|---|---|---|
| **1** | **GPU 机上不得驻留任何判官模型权重** | 3090 上的显存**只留给 H3 生成** |
| **2** | **判官不得占用 3090 的 CUDA** | L1 的人脸嵌入/光流一律用 **CPU 实现**，禁用 GPU 版本 |
| **3** | **判官与生成必须异步解耦** | 生成完立即投递判官、**不等结果**，继续跑下一段；判官结果回来才决定是否重抽 |

**判官三级的执行位置（强制）**：

| 级 | 位置 | 资源边界 | 耗时 |
|---|---|---|---|
| **L0** 技术校验 | 任意 CPU（含 3090 机） | 纯算术，不占 GPU | < 1 s/条 |
| **L1** 专用检测器 | **本地 Mac** ⭐ 首选；或 3090 机 CPU | **限核**（`nice` / `cpulimit`），给 ComfyUI 留 ≥4 核做数据加载 | 5–20 s/段 |
| **L2** VLM 判官 | **远端 VLM API** ⭐ 唯一选项 | 零本地资源 | 需**并发**（见下） |

**"不影响速度"的真正机制 = 延迟隐藏**（前提是判官比"下一段生成"短）：

| 环节 | 耗时 | 与生成并行？ |
|---|---|---|
| 单段生成（15 s 段 @3090） | 9.4–11.4 分钟 ❓ | — |
| L0 + L1 | < 1 s + 5–20 s | ✅ 可并行 |
| L2（20 判项 × 1 次调用/项） | **并发** 5 路 × 4 s ≈ 16 s；若串行则为 80 s | ✅ 可并行 |

⇒ 判官总耗时 ≈ **1–2 分钟/段**，**远小于单段生成时间** ⇒ 可被完全隐藏。

> ⚠️ **唯一的例外：抽卡**。判官否决后的重抽时间是**净增加**、不可隐藏的。所以「判官不影响速度」只在**通过率 p 足够高**时成立 —— 这再次印证三档杠杆的排序：**提 p 比抽卡值钱**。
>
> ⚠️ **若未来被迫共享同一张 GPU**（例如只有一台机器）：则须改成「**分时调度**」（生成空闲时跑判官），且「判官不影响速度」**不再成立**，必须重新评估。

### §2.1 ⭐ 修正后的施工顺序（v2.6 · 照这个走）

```
Phase 0    环境与链路整改
           ├─ 重建项目自持目录（零软链）
           ├─ 重下官方 8 步 LoRA + sha256 校验
           ├─ 装 T0 加速件（零安装项）
           └─ 对齐 comfy-{input,output,placeholder,temp} 目录
Phase 0b ⭐ 逐层提速 A/B（A0–A8）→ 定档       ← 从原 Phase 6b【前移】
Phase 0.5 ⭐ 环境冻结（4 项落文件）             ← 【新增里程碑】
Phase 1    可复现性基建 + 种子资产库            ← 此时环境已冻，建的库才站得住
Phase 2    三道闸门（⭐ 并入 ClipForge 四类禁写）
Phase 3.0 ⭐ 人工标定集（30–50 条）             ← 【新增·解鸡生蛋】
Phase 3    L0 + L1 判官（L1 出 GPU / 限核）
Phase 4    抽卡引擎（默认 N=1，机制先行）
Phase 5    L2 VLM 判官（远端 API + 并发）
Phase 6    判官标定（p / D / J）→ 按 p 开 N
Phase 6b   提速档位复核（仅环境变更时触发）

【并行·与上述解耦】文案脚本层 ClipForge（本地 Mac，零 GPU 依赖）
【运行期·Phase 2 之后随时接入】端到端编排 `orchestrator.py` + 闸门P `gate_p_image_gate.html`
                                （依赖 `ttimg.py`，无需新增部署；模型白名单自检随编排一同生效）
```

> **两条顺序铁律**：
> ① **先冻结环境，再建资产** —— 顺序颠倒 = 全部资产重跑。
> ② **判官永远在 GPU 之外** —— 破这条，"不影响速度"不成立。



### Phase 0 · 环境恢复与链路整改（半天）· 已部分完成

> 按 REPORT16 的审计结论重写。原「重开 4090」的假设已作废：新机是 **3090**，且**旧脚本走的不是官方链路**。

| 步 | 动作 | 状态（2026-09-17 13:40 实测复核） | 验收 |
|---|---|---|---|
| ① | 连通新机 `connect.nmb2.seetacloud.com:36229`，确认 ComfyUI :6006 可用 | ✅ 已确认 | **内核已是 0.36.0**（原计划的 ⑥b 升级**已完成**） |
| ② | 审计四件核心权重 | ✅ **正确，无需替换** | 与官方模板 Model Links 逐项一致；sha256 = 官方值（REPORT17） |
| ③ | 建项目自持目录（零软链） | ❌ **`h3p/` 已不存在，需重建** | 新结构对齐：`/root/autodl-tmp/comfy-{input,output,placeholder,temp}`（今日已建） |
| ④ | 官方 8 步 Turbo LoRA 入库 | ❌ **已丢失，需重下**（现存仅 `..8step..768p..resized_avg_rank_64..` 971 MB 降秩版） | 1,956,193,000 字节，sha256 `2339acdf…b111e`（约 2 分钟 @14.9 MB/s） |
| ⑤ | 生产脚本切到**官方原生链路**（`test_fl2v_official.py`） | ✅ 已就绪 | 只含核心节点，`--dry` 可出图核对 |
| ⑤b | **清除社区污染节点** | ✅ **已完成**（`MiniMaxH3TurboLoRA` / `H3SparseAttentionAdvanced` / `H3MemoryOptimization` 均已不存在） | 环境比计划更干净 |
| ⑥ | 起独立 ComfyUI 实例 | ⚠️ 现 6006 实例已跑（`--port 6006 --enable-manager`），但**未做隔离** | 决定：复用现有实例 + 用启动参数隔离，或新起端口 |
| ⑥c | 装 T0 加速件（§1.5 装机清单），写 `env_manifest.txt` | ⏳ 待执行 | 每件记录版本 + commit |
| ⑦ | 跑官方链路冒烟（0.4 MP / 2s / 8 步），**重标 3090 耗时基准** | ⏳ 待执行（今日已有 5 条测试 mp4，非基准条件） | 得到新基准区间，替换 §1.3 的 4090 数据 |
| ⑨ | 清理系统盘 | ✅ **已完成**（`/` 从 100% → **41%，18 G 可用**） | 阻塞解除 |
| ⑩ | 🔒 **存储布局落位（v2.7）**：`source env_datadisk.sh` + 建 `$ROOT/venv` | ⏳ 待执行 | `check_datadisk.sh` 全绿；**系统盘无本项目产物**；包未装进系统 python |

> ⚠️ **不可跳过**：⑥c⑦ 是「官方链路 + 新版内核 + 提速档位是否真的更好」的唯一证据来源。
> ⚠️ **两条与 v2.4 不同的执行细节**：① 启动参数不再默认加 `--use-sage-attention`（该包**未安装**，加了也不生效）；改由 §1.5 的 T1 档决定用「启动参数」还是「KJNodes 补丁节点」，**二选一，别重复打**。② 升内核后**必须先做同 seed 复现测试**再跑生产。
> ⚠️ **同机多服务已是既成事实**：实测该实例同时跑着 `tts-gateway`（占显存 466 MB）；辉哥另有 qwen3:14b（漫剧分镜）需求。⇒ **显存分配必须在 Phase 0 就定下来**，见 §2.0。

### Phase 0b · ⭐ 逐层提速 A/B（A0–A8）→ 定档（原 Phase 6b，**已前移**）

> **为什么前移**：这是 v2.6 修正的**第一个严重顺序错误**。加速档一旦调整，`custom_nodes` 的 commit 与启动参数就变了，「同 seed 逐位复现」的前提随之失效 ⇒ **先建资产库 = 先建一间要拆的房子**。
>
> **A/B 在这里跑完并定档，然后才冻结环境、才建资产库。** 表格见 §Phase 6b（内容不变，只改执行时机）。

**每层必须同时记三项，缺一不算完成**：

| 记录项 | 判定标准 |
|---|---|
| ① 耗时 | 提速了多少 |
| ② 同 seed 帧差 | 画质有没有变（对照噪声基线 **1.08**） |
| ③ **判官通过率 p 的变化** | **良率有没有掉** |

**⭐ 净耗时闸门（新增，务必算这个账）**：

```
净耗时比 = (单次耗时_new / 单次耗时_base) × (E(p_new) / E(p_base))
其中 E(p) = [1 − (1−p)^N] / p
```

**反例（这就是只看单次速度会误判的原因）**：某层提速 25%，但把 p 从 0.50 压到 0.35：

| | p=0.50 | p=0.35 |
|---|---|---|
| E（N=3） | 1.75 次 | 2.28 次 |
| 相对倍数 | 1.00 | **1.30 ×** |

⇒ 净耗时 = 0.75 × 1.30 = **0.975**，只快 **2.5%**，几乎白忙，却额外承担画质风险与复现性代价。
⇒ **判据：净耗时比 ≥ 0.9 才值得留**（< 0.9 说明收益不足以抵偿风险）。

### Phase 0.5 · ⭐ 环境冻结（Environment Freeze）· 新增里程碑

**没有这一步，「可复现」只是口号。** 冻结四项，写成文件：

- [ ] ComfyUI 内核版本号（`0.36.0`）
- [ ] 加速档位（T0 / T1 / T2 中的哪一个）+ **每个开关的取值**
- [ ] `custom_nodes` 每个包的 **commit SHA** → 写入 `env_manifest.txt`
- [ ] ComfyUI 启动参数（**逐字记录**）

> 之后任何变更 → 走「**重新冻结**」流程，并**评估资产库失效范围**（哪些 `(prompt, seed)` 对需要重跑）。
> 冻结完成后，**才算进入 Phase 1**。


### Phase 1 · 可复现性基建（半天，零 GPU）⭐ 第一交付物 · **环境冻结后执行**

这是比判官本身更重要的收益：**把"一次抽奖"变成"可复现资产"。**

| 步 | 动作 | 验收 |
|---|---|---|
| ① | 建立强制日志：每次生成记录 prompt 全文、seed、全部参数、参考素材 md5、输出路径 | `runs.jsonl` 每行一条完整记录 |
| ② | 重生成逻辑强制**换新 seed**，并记 `attempt_index` | 同 prompt 重跑时 seed 必不同（防陷阱 1 空转） |
| ③ | 判官通过的 `(prompt, seed)` 对标记为资产 | **种子资产库**：可一键复现成片 |
| ④ | 冒烟验证：同 seed 同 prompt 重跑一次，帧差应 ≈1.08（噪声基线） | ✅ 逐位复现成立 |
| ⑤ | **新建 `env_manifest.txt`（v2.5）**：记录内核版本、torch/triton/comfy-kitchen/sageattention 版本、每个 `custom_nodes` 包的 commit SHA | 与 `models_manifest.txt` 同级归档；**"同 seed 逐位复现"依赖它** |

> ⚠️ 铁律：**同 seed + 同 prompt = 逐位相同输出**。判官否决后直接重跑同一任务 = 花 N 倍算力拿 N 份相同的废片。
> ⚠️ **提速会改变资产的有效性**：一旦启用 §1.5 的任何一层加速，**旧的种子资产库要标记"基于旧环境"**，并在新环境下重跑复现测试。加速件换了 commit，历史资产的复现性就不再自动成立。

### Phase 2 · 三道闸门落地（1 天，零 GPU）

**闸门① · 保真分层判断**（装在「识别产品」步）

```
产品有没有 logo / 中文标签 / 价格 / 复杂包装印刷？
  ├─ 有 → 像素级保真 → H3 只画环境，产品走混合合成（真实像素合成）
  └─ 无 → 形态级保真 → 交给 H3，用 3 张锚定照压漂移
```

**闸门② · 动作白名单 + 因果链否决**（装在分镜表步，全场最硬）

每个动作连过**三问**（第三问为 v2.6 并入 ClipForge 经验）：
1. **在白名单内吗**（举起/并排/推近/旋转/开合/滑入/光影流动/静置/特写平移…）？
2. **有没有可验证的物理结果？** —— 有（涂开后皮肤变了/倒了之后瓶里少了/用量下降）→ **否决**，改实拍或后期；无 → 放行。
3. ⭐ **命中「四类禁写」吗？** —— **双主体精确交互 / 内部心理活动 / 否定式动作 / 一拍三步动作链** → **否决**（这类动作模型表达不出来，与物理结果无关，是"可生成性"问题）。

操作工具：`gate2_action_whitelist.html`（29 动作库 + 5 红线问句，自动出绿/黄/红判定）。
> 📌 **两源同源互证**：第 3 问来自 ClipForge 的「动作可生成性硬判据」，与我方第 1、2 问**独立收敛到同一结论**。合并后的完整判据见 `shotlist_schema.json` 的 `gate2_merged_rules`。

更聪明的补充路线——**B 类「不可验证化」**：不拍"涂开"，改拍"产品静置特写 + 光影扫过 + 模特微笑"（画面里不存在可验证的物理结果，B 类崩坏无从发生）。注意：判官清单要同步把这些项判 N/A，不能判"否"。

**闸门②的延伸 · 动作逻辑性的三层拆解与解法阶梯**（2026-09-17 补充）

「动作逻辑性」不是一层，是三层，**三层的可解性完全不同**：

| 层 | 表现 | 类型 | 重抽有效？ |
|---|---|---|---|
| **L1 姿态合理** | 五指齐全 / 不穿模 / 不悬空 | 随机型 | ✅ 有效（判官主力场景） |
| **L2 动作连贯** | 抬手→抓→放 不断裂、不跳变 | 混合 | ⚠️ 部分有效 |
| **L3 因果 / 意图合理** | 拧开→倒出→瓶里变少 | **结构型** | ❌ **零效果** |

> **核心洞察**：动作逻辑性**不是「生成质量问题」，是「叙事结构问题」**。当质量指标交给模型 → 无解；当结构问题交给剪辑 + 合成 → 有解。
> **正解：把因果链从「模型内部」搬到「剪辑外部」**——观众会自动补全因果（库里肖夫效应）。模型不需要懂因果，它只需要把每一个**状态**画好。

**解法阶梯（按性价比排序，与全流水线同序：先规避 → 再合成 → 再锚定 → 再筛 → 最后换工具）**

| 顺位 | 手法 | 治哪层 | 成本 | 效果 |
|---|---|---|---|---|
| 1 | **状态陈述替代状态转移**：不拍「拧开→倒出」，拍「已开盖 + 液体缓流」的单状态切片 | L3 | 免费 | ★★★★★ |
| 2 | **切碎因果链 + 剪辑连接**：3 步因果拆成 3 个单动作镜头，跨段用剪辑点衔接 | L3 | 免费 | ★★★★★ |
| 3 | **混合合成**：真实照片合成进 H3 环境（**只需拍 2–3 张照片，不用拍视频**） | L1–L3 | 低 | ★★★★★ 100% 正确 |
| 4 | **中间锚点钉动作中间态**：`MiniMaxH3AddGuide`，一段 1 个，钉在动作中途 | L1–L2 | 中 | ★★★ |
| 5 | **减少手与接触的出现**：静置 + 光影/推近/旋转；或远景/过肩/只露手腕 | L1 | 免费 | ★★★★ |
| 6 | **判官专项检测**：`density_roi.py` + `state_track.py`，加 L2 原子项「是否存在未完成/中断的动作」 | 只筛不修 | 低 | 止损器 |
| 7 | **换工具**：Wan2.2-Animate 角色替换 / 真实拍摄 | L1–L3 | 高 | 最后手段 |

⚠️ **第 2 条最容易做错的地方**：跨段的**状态守恒必须人工保证，不能靠模型**。
- 第 1 段结束时「盖已开」→ 第 2 段的起点锚定照**必须是「已开盖」的派生图**（这正是「首尾帧同源派生」铁律的真正用途）。
- 跳过这步 → 第 2 段模型从「盖子还拧着」开始画 → 因果链在剪辑点上断裂，观众立刻出戏。

**指标层面的修正（建议写进分镜规范）**：把「动作逻辑性」从**生成质量指标**重新定义为**叙事结构指标**——前者不可控（模型行为，只能靠运气），后者可控（剪辑 + 合成 + 闸门②，可验收）。落法：分镜表阶段强制「**每镜动作数 = 1**」，闸门②做「是否有可验证物理结果」的问询。

**闸门③ · 派发前 10 项硬校验**（装在 H3 生成步之前）

- [ ] prompt **逐字包含**所有画面英文文案（含时间段、前后半、颜色）——不过不许派发
- [ ] 关键帧对齐指令在**第一行**，两位小数，后接空行
- [ ] 每个节拍只有一个主要动作
- [ ] 每个切镜引入新信息（只改景别的已改为运镜）
- [ ] `overall_soundscape` 与 `non_diegetic_music` 都写了且不矛盾
- [ ] 锚定照是 3 张**独立**照片，无网格/拼贴
- [ ] 没把原始商品图传给视频模型
- [ ] 比例与锚定图/简报完全一致
- [ ] 结尾单一全画幅收束，无网格/分屏
- [ ] 文案无箭头/斜杠/加号/连接符

验收：拿一条历史失败 prompt 过三闸门，应能被拦截。

### 判官基准（评价标准从哪来）—— ⭐ 易踩坑，动工前先定清楚

判官的基准只有三种候选：**① 任务规格（prompt / 分镜表）② 人工标定样本 ③ 参考视频**。

**权威基准只有一个：prompt 自身。** 我们生成视频的目标是「实现分镜表声明的画面」，不是「复现别人的片子」。所以 R 段（指令遵循）的判据来自 prompt 原文，标准视频只能作补充。

**「拿一条标准视频当相似度基准」→ ❌ 不要这么做**，三条硬伤：

| 硬伤 | 说明 |
|---|---|
| **目标错位** | 判的是「达标与否」而不是「像不像某条片」。抄了构图但产品崩了 → 高分；构图完全不同但产品/文案全对 → 低分。**判官会把「创新」判成「错误」** |
| **度量不敏感（工程硬伤）** | 全局视频相似度（CLIP / 光流）对我们要抓的失败**几乎不响应**。同类实测：REPORT6 里「换人 / 换场景」这种巨变，配色相似度扰动仅 0.9–1.5，落在复现噪声 1.08 内 ⇒ 此类全局度量分辨不出内容是否变了，更抓不到手部畸形这类局部崩坏 |
| **形态错位** | REPORT11 已定「L2 必须原子是/否清单，禁止整体打分」。相似度分数正是整体打分，必踩三失效（检查不完整 / 理由不忠实 / 归因纠缠） |

**但标准视频有 3 种正确用法：**

| 用法 | 判定 | 要点 |
|---|---|---|
| 作**标定集样本** | ✅ 但必须是「**一批**」而不是「一条」 | 阈值需要正负对照；一条只能说明「这算好」，说不出边界在哪。应含通过样本 + 典型失败样本各若干 |
| 作 **L2 判官 few-shot 示例** | ✅ 建议 **1 合格 + 1 典型失败** | 示例会锚定判官注意力；选与当前产品无关的通用场景，并在 prompt 里声明「示例仅示范判定标准，不要比对内容」 |
| 提取它的**结构化规格**（镜头数 / 节奏 / 动作序列）→ 转成原子清单 | ✅ **最佳用法** | 把「像不像」转成「有没有做到这几件事」——可判定、可归因、可复用 |

> 一句话：**标准视频的价值不在「像素」，在「规格」。** 把它拆成可验证的原子事实，而不是让判官比对像素。

**唯一例外**：客户明确要求 1:1 复刻某条爆款 → 相似度才有意义；但这与决策 D1（不做动作迁移）直接冲突且有版权风险，按 §3.3 归入「必须实拍 / 不进流水线」。

### Phase 3.0 · ⭐ 人工标定集建设（v2.6 新增 · 解"鸡生蛋"依赖）

> **为什么需要这一步**：Phase 3 要求 L1 阈值「用历史人工判定样本标定」，但历史样本来自**已废弃的社区链路**（REPORT16），代表性不足且量少。而标定需要样本、样本需要生成、生成又要判官 —— **这是个环**。

**解法（顺序不可颠倒）**：

```
① 跑 30–50 条段（覆盖 5 类随机型崩坏 + 2 类结构型）
      ↓
② 全部【人工判定】（是/否 + 缺陷位置 + 缺陷类型）
      ↓
③ 形成正负对照标定集（含通过样本 + 典型失败样本各若干）
      ↓
④ 用它标定 L1 阈值、校准 L2 分数映射
      ↓
⑤ 才允许启用自动判官
```

- 覆盖要求：**手部畸形 / 构图偏移 / 纹理闪烁 / 身份漂移 / 短英文糊 / 状态不守恒 / 接触物理**
- 这个集子同时是 **Phase 6 实验 A** 的输入 → 一举两得
- ⚠️ **标定集必须独立于调参样本**（防裁判-运动员冲突）

| 步 | 动作 | 验收 |
|---|---|---|
| ① | 生 30–50 条段样本（覆盖 7 类崩坏） | 样本落盘 + 进 `runs.jsonl` |
| ② | 人工逐条判定，双人交叉 | 每条有「是/否 + 缺陷位置 + 类型」 |
| ③ | 冻结标定集（版本化） | 文件 + 校验值归档 |
| ④ | 用标定集标 L1/L2 阈值 | 阈值写入 `judge_config.yaml` |

### Phase 3 · L0 + L1 判官（1–2 天，本地 CPU，零额外成本）

> 🚨 **执行位置见 §2.0 红线**：L0 可在任意 CPU；**L1 放本地 Mac 首选**，或 3090 机 CPU 且**限核**（给 ComfyUI 留 ≥4 核）。**L1 禁用 GPU 实现。**

> 📋 **判项清单不在本指南展开**——L0 八项 / L1 十项的完整判法、阈值占位、失败归类，见 **REPORT15 §2.1–2.2** 与 `judge_config.yaml`（机器可读版，直接驱动 `judge_l1.py`）。

| 级 | 判什么 | 实现 |
|---|---|---|
| L0 技术校验 | 解码成功/帧数/分辨率/时长/亮度范围/音轨存在/长静音 | 新写（纯算术，<1s/条） |
| L1 专用检测器 | 人脸嵌入距离/光流幅度/调色板相似/OCR 逐字比对/唇音互相关 | **打包现成脚本**成统一 `judge_l1.py`：输入 mp4 + 清单 → 输出逐项 是/否 + 数值 + 时间戳证据 |

**工程要点**：
1. L1 六个检测器阈值**各自独立标定**（用历史人工判定样本）；L1 假阴性比 L2 更致命——误杀好片 = 白烧生成算力。
2. 输出自检报告，可入库可给人看。
3. 早停省钱的实现放在 Phase 4 的抽卡引擎里。

### Phase 4 · 抽卡引擎 + 保底（1 天）⭐ 本阶段即「质量兜底主机制」

> 🚨 **v2.6 修正 S3：本阶段默认 N=1（即不抽卡），只交付「机制」。**
> 理由：p（通过率）要到 Phase 6 才测出来。用拍脑袋的 p 定 N，会烧掉一批算力才发现真实 p 很低。⇒ **机制先上、开关后开**。
>
> 🚨 **v2.6 写明 S9 盲区**：本阶段上线时 L2（Phase 5）尚未就绪，判据只含 L0 + L1。**以下缺陷不会被拦**：构图偏移、纹理闪烁、短英文文字糊、接缝色调。
> ⇒ 因此 Phase 4 期间的产出视为**试产**，不进入正式交付；或显式接受这些缺陷。

> 按决策 D3：**这是全流水线的质量主防线**。前面三道闸门负责「不让结构性失败进门」，判官团负责「把随机性失败挑出来重抽」。两者是分工，不是替代。

判官输出必须**三项**，不是一项：

| 输出 | 取值 | 下游动作 |
|---|---|---|
| ① 是否达标 | 是/否 | 是 → 进种子资产库，**立刻冻结该段** |
| ② 失败类型 | 随机型 / 结构型 | 随机 → 允许重抽 |
| ③ 可重抽性 | 会变 / 不会变 | 不会变 → **禁止重抽**（★闸门④） |

**失败分诊表（判官实现的核心依据）：**

| 失败类型 | 随换 seed 改变？ | 重抽有效？ |
|---|---|---|
| 手部畸形/手指数量、构图偏移、纹理闪烁 | ✅ 会 | ✅ 有效（判官团主力场景） |
| 身份漂移（脸变）、参考不生效、音画不对齐 | ✅ 会 | ✅ 有效 |
| 短英文文字糊 | ⚠️ 部分 | ⚠️ 有限（3–5 词可救，中文/价格走后期） |
| **状态不守恒**（液量不减/盖子自合） | ❌ 不会 | ❌ 零效果 → 回闸门② |
| **接触物理**（穿插/悬空/抓握失败） | ❌ 不会 | ❌ 零效果 → 回闸门② |
| **多步因果链**（拧→倒→涂） | ❌ 不会 | ❌ 零效果 → 回闸门② |

**保底规则（唯一防烧卡装置）：**

```
抽第 1 次 → 判官否决（随机型）→ 换新 seed 重抽
抽第 2 次 → 否决 → 换新 seed 重抽
抽第 3 次 → 否决 → 触发保底 ❌不再抽
   ✅ 强制走：改动作（回闸门②）/ 改提示词 / 改段长 / 换工具 / 实拍补
```

- 硬性封顶 **N = 3**（p≥0.5 时）；p<0.3 时取 5 也别取，直接改方案。
- 到顶**必须报明确失败，绝不静默续跑**（对一键网站尤其重要：无上限重试 = 无上限账单）。
- 判定落在**段**不落在整片；通过的段立刻冻结，绝不整片重跑。
- ⚠️ 单点铁律：**「选出最好的」≠「达标」**。N 份全不合格时必须走失败出口，不许相对择优。
- 抽卡作用在**最小包含失败的单元**上，但不要为省抽卡无限拆段（段数是指数惩罚项）。用 `gacha_console.html` 算最优段长（例：p=0.5、N=5 时 15s×2 次生成 → 整片 93.8%、21.3 分钟，优于 2.5s×12 次）。

### Phase 5 · L2 VLM 判官（2–3 天）

> ⚙️ **评判形态铁律（七条，必须照做）**——见 REPORT15 §1.1：**原子化**（一项 = 一个可观察的二值事实）/ **一题一次调用**（N 项 = N 次独立调用，永不合并）/ **先证据后判定**（帧号或区域，可回溯）/ **三值输出**（是 / 否 / **N/A**）/ **禁止打分与自由叙述** / **关键项双采样**（W-01 W-02 W-04 P-01）/ **版本锁定 + 标定集独立**。
> 输出契约只有一种合法格式（见 REPORT15 §1.2）：`{"item_id","verdict":"yes|no|na","evidence":{...},"confidence"}` —— **无总分字段、无 free-text 理由字段**。
> R/W/P 三段各判项清单见 REPORT15 §2.3 与 `judge_config.yaml`。

| 项 | 规格 |
|---|---|
| 模型 | 🚨 **必须走远端 VLM API**（见 §2.0 红线 1）—— **绝不在 3090 上跑本地 VLM**。锁定 API 的**模型版本号** |
| 调用 | **必须并发**（20 判项约需 5 路 × 4 s ≈ 16 s；串行则 80 s） |
| 判据形态 | **原子是/否清单**，禁止整体打分（防检查不完整/理由不忠实/归因纠缠三失效） |
| 判定规则 | R（指令遵循）/ W（世界一致性）任一"否"→ 否决；P（感知质量）按缺陷计数设阈值；清单里画面不存在的事项判 **N/A 不判否** |
| 标定集 | 必须独立于调参样本（防裁判-运动员冲突）；用 **Phase 3.0** 的标定集 |
| **版本回归** | 🚨 **v2.6 新增 S8**：换 VLM 版本 / 增删判项 / 改阈值 → **必须用标定集跑回归**，测假阳率与假阴率，两个数**都不得恶化**。否则"判官悄悄变严"会让良率莫名下降且极难归因 |

原子清单模板（R/W/P 三段式）直接沿用 REPORT11 §3 改造③的模板，按每个产品的分镜表生成。

**L2 判官对 7 类崩坏的覆盖边界（诚实预期）：**
- ✅ 主力能筛：身份漂移、参考不生效、文字崩、接缝色调、音画不对齐（5 类随机型）
- ❌ 筛得出救不了：状态不守恒、接触物理（2 类结构型）→ 判官只是"昂贵的确认器"，防线仍在闸门②

### Phase 6 · 标定实验（半天 GPU，全部当天可跑完）⭐ 部署后第一件事

搭完判官，**先测 p、D、J 三个数，再定 N**。三者是乘法（抽卡收益 = D × p × J），任一接近 0 抽卡整体归零。

| 实验 | 设计 | 耗时 | 回答的问题 |
|---|---|---|---|
| **A · 抽卡增益 + 失败归因** | 同一镜头（S1 类，无因果链）跑 **8 个不同 seed**，逐次判官判定 + 人工复核 | ≈13 分钟 | 测 p；失败是否落同一处（随机/结构）；D 独立复测 |
| **B · 2 因子方差分解** | **3 个提示词版本 × 3 个 seed = 9 次** | ≈13 分钟 | 组间>组内 → 优化提示词收益大于抽卡（钱该花在哪） |
| **C · 等算力对照** | 8 步×2 seed vs 16 步×1 seed（总耗时对齐） | ≈7 分钟 | 加步数能否 +25pt 单次通过率，否则多抽一次更优 |

**判读规则**：实验 A 有通过且失败位置分散 → 抽卡有效，按良率表定 N；全部失败且失败位置相同 → 结构型，抽卡无效，回闸门②改方案。

### Phase 6b · 提速档位复核（A0–A8）· **已在 Phase 0b 执行**（v2.6 修正）

> 🚨 **v2.6 修正 S1/S6：A/B 实际执行时机是 Phase 0b（环境冻结之前）**，不是这里。
> 原因：A/B 会改环境（commit / 开关）⇒ 必须排在种子资产库（Phase 1）之前，否则资产库作废。
> **本节的表格保留为「复核/扩展清单」**：仅当环境发生变更（换内核、换节点包、增删加速件）时重新触发。

**执行纪律（跑 A/B 时照此做）：一次只开一个变量；同 seed 对照；每层必须过 §Phase 0b 的「净耗时闸门」（净耗时比 ≥ 0.9 才留）；先验证再开下一个。**

| # | 对照 | 变量 | 记录 |
|---|---|---|---|
| A0 | 基线 | 0.36.0 + 8 步 + `res_multistep/simple` | 耗时、帧差基准 |
| A1 | A0 vs 内核升级 | 0.33.0 → 0.36.0 | 耗时差 + **是否修掉生成异常**（特殊 token 修复） |
| A2 | A1 vs kitchen | `comfy kitchen attention` 开/关 | 耗时 + 眼睛/细节检查 |
| A3 | A1 vs TorchCompile | 开/关 | 耗时 + 显存峰值 |
| A4 | A1 vs SageAttention | 装 / 不装 | 耗时 + **眼睛专项检查**（INT8 QK 风险） |
| A5 | A1 vs VAE Speedup | 开/关 | 解码段耗时占比 + 帧差（应 ≈2.4e-4 量级） |
| A6 | A1 vs SLA 6 步 | SLA 配方 vs 8 步基线 | 耗时 + 是否够成片 |
| A7 | A1 vs 分辨率 | 0.4 / 0.5 / 0.98 MP | 耗时的分辨率弹性（验证 3–5× 说法） |
| A8 | A1 vs Sigma Shift | audio 3 vs 6 | 音画同步 |

> 每跑完一层，把实测值回填 `speedup_stack.html` 的作用占比与倍数——**跑完之后那份计算器才从"排序工具"变成"报价工具"**。

---

## 3. 运行期决策规则（投产后的查表逻辑）

### 3.1 按 p 定策略（p = 单次生成通过判官的概率）

| p 区间 | 策略 | 依据 |
|---|---|---|
| p ≥ 0.5 | ✅ 判官团非常划算：N=3~5，一条 30s 从 8.5 分钟涨到 12–17 分钟 | REPORT11 §4 |
| 0.2 ≤ p < 0.5 | ⚠️ 可用但必须分段冻结，坏段单独重跑 | 同上 |
| p < 0.2 | ❌ 止损区：不是运气问题是方案问题 → 回闸门②改动作/提示词/换工具 | 同上 |

（p=0.5、N=5、5 段整片良率 85.3%；p=0.1 时即使 N=8 整片也只有 6%——止损线就在这。）

### 3.2 每次迭代只改一个变量

同时改 prompt 和 seed = 永远不知道哪个起作用。官方规范反复强调，也是实际最常违反的一条。

### 3.3 品类分档（接单时的分流线）

| 档 | 品类 | 做法 |
|---|---|---|
| 直接出片 | 美妆护肤、食品饮品、服饰鞋包、家居香氛、珠宝 | S1 产品动态化为主（无人、无手、无因果链） |
| 生成+后期 | 3C 数码、家电厨具、母婴玩具 | S1 + 局部 S2，重后期 |
| 必须实拍 | 医疗保健、药品酒类、精确 SKU、功效宣称、AI 真人代言 | 不进流水线 |

### 3.4 合规线（成片必检）

- AI 生成音视频必须带**显式 + 隐式**标识（《人工智能生成合成内容标识办法》2025-09-01 施行）；发布时主动选择标签。
- **严禁 AI 换脸仿冒名人带货**（8 平台承诺书明令）。
- 文案红线：写陈述/成分/对比/价格/场景，不写功效宣称、不写"看我怎么用/教你三步"（后者还会把分镜推进多步因果链）。

---

## 4. 验收清单（部署完成的定义）

- [ ] **模型独立已落实（v2.4）**：`deploy_independent.sh` 全绿；`find $ROOT/models -type l` 返回空（零软链）；四件权重 + LoRA 逐件 sha256 = 官方值；`models_manifest.txt` 已生成并归档
- [ ] **D1 落实**：生产脚本与文档中无参考视频依赖；`runs.jsonl` 不出现 `ref_video` 参数
- [ ] **D2 落实**：生产调用只用 `test_fl2v_official.py`（官方原生链路 / FL2VA）；分镜表全部按「一条片内多镜」写法产出
- [ ] **D3 落实**：判官三输出接入生成回路，不达标自动换 seed 重抽；保底封顶 N=3 生效
- [ ] **判官基准已定**：以 prompt 为权威基准；标准视频仅用于「标定集 / few-shot 示例 / 规格提取」，**未用于相似度打分**
- [ ] **判官形态已定**：七条形态铁律全部落实（原子化 / 一题一次调用 / 先证据后判定 / 三值含 N/A / 禁打分 / 关键项双采样 / 版本锁定）；`judge_config.yaml` 可被代码直接加载
- [ ] **动作规范已定**：分镜规范落实「每镜动作数 = 1」；闸门②对含因果链的动作给出否决
- [ ] Phase 0：冒烟片成功，耗时落在实测基准区间
- [ ] Phase 1：`runs.jsonl` 全量记录；同 seed 逐位复现验证通过；种子资产库可一键复现
- [ ] Phase 2：历史失败 prompt 能被闸门拦截；`gate2_action_whitelist.html` 接入分镜评审
- [ ] Phase 3：`judge_l1.py` 输出逐项是/否+数值+时间戳；阈值用历史样本标定
- [ ] Phase 4：重抽必换 seed；保底 N=3 到顶走失败出口；段级冻结生效
- [ ] Phase 5：L2 清单化输出；判官版本锁定；标定集独立
- [ ] Phase 6：p/D/J 三数实测入库；按 §3.1 表定 N 或触发止损
- [ ] **内核版本已锁（v2.5）**：ComfyUI ≥0.35.1（目标 0.36.0）；升完**已重跑同 seed 复现测试**，帧差仍在噪声基线 1.08 内
- [ ] **加速件已锁 commit（v2.5）**：`env_manifest.txt` 记录每个节点包的版本 + commit；`custom_nodes` 中**无共享实例的复制品**
- [ ] **逐层提速已量化（v2.5）**：Phase 6b 的 A0–A8 跑完；每层增益 >10% 才保留；实测值已回填 `speedup_stack.html`
- [ ] **高风险件未进成片（v2.5）**：FastH3 4 步 / FirstBlockCache / TE-Speed **均未出现在成片配置**中（仅草稿档）
- [ ] **EasyCache 未启用（v2.5）**：产线工作流中不含 `EasyCache` / `LazyCache` 节点（8 步下空转，实测 −0.7%）
- [ ] **环境已冻结（v2.6）**：Phase 0.5 四项全部落文件（内核版本 / 加速档位 / 节点包 commit / 启动参数），且**冻结发生在 Phase 1 资产库之前**
- [ ] **提速 A/B 已前移（v2.6）**：A0–A8 在 Phase 0b 跑完；**每层都过了「净耗时闸门」（净耗时比 ≥ 0.9）**，不只看单次速度
- [ ] **判官隔离已落实（v2.6）**：判官三级**全部出 GPU** —— `nvidia-smi` 上无判官进程；L1 在 Mac 或 3090 机 CPU 且**限核**；L2 走**远端 API**
- [ ] **显存分配已定（v2.6）**：3090 的显存只留给 H3 生成（同机的 `tts-gateway` / qwen3:14b 占用已核清）
- [ ] **人工标定集已建（v2.6）**：Phase 3.0 的 30–50 条人工判定样本已冻结并版本化，且**独立于调参样本**
- [ ] **抽卡默认关闭（v2.6）**：Phase 4 上线时 N=1；**p 实测（Phase 6）之前不得开启 N>1**
- [ ] **判官版本回归已过（v2.6）**：换 VLM 版本 / 增删判项 / 改阈值后，已用标定集测假阳率与假阴率，**两数均未恶化**
- [ ] **ClipForge 隔离已验收（v2.6）**：3090 上 `docker ps` **无 clipforge**；其 LLM Key **未指向** 3090 的 qwen3:14b
- [ ] **接口契约已冻结（v2.6）**：`shotlist_schema.json` 两侧解析器互测通过；**端到端连通**（ClipForge 出稿 → adapter → 11 列 → 闸门② → `--dry`）全绿
- [ ] **文案三分流已落实（v2.6）**：画面内英文 / 中文旁白 / 后期图文三条去向明确；**无中文台词直接进 H3 prompt**
- [ ] **闸门②已并入四类禁写（v2.6）**：双主体精确交互 / 内部心理 / 否定式动作 / 一拍三步动作链**已被拦**
- [ ] 🚨 **模型白名单已落实（v2.7 · 最高优先级；v2.9 追加判官项）**：`orchestrator.py` 启动自检**全绿**（生图 = TT Image2 / 视频 = 自持 H3）；`ttimg.py` 的 `MODEL` 恒为 `tt-image-2`；**`judge_shot.py` 的 `VLM_MODEL` 恒为 `tt-5.6-luna`（传入违规模型名必须报错退出，须实测拦截一次）**；**调用清单中不存在任何云端视频模型，也不存在任何非白名单多模态模型**
- [ ] **端到端编排已连通（v2.7）**：`orchestrator.py --demo --dry-run` 跑完 S0→S9；`state.json` + `report/{side_a,shotlist,segments,gate_p}.json` + `runs.jsonl` 全产出
- [ ] **S3.5 分组生效（v2.7）**：30s 片 S ≤ 2；段 prompt 为标准「一条片内多镜」写法（`[Shot 1]…[Shot 2] At 00:NN.NNN`）
- [ ] **闸门P 已落地（v2.7 / v2.8）**：`gate_p_image_gate.html` **七项判据**接入 S5；**人为注入一张坏图（产品位移/异文字）必须被拦下**，且**未消耗 GPU**
- [ ] 🔴 **宽高比一致已验收（v2.8）**：向 `test_fl2v_official.py` 塞一张 1:1 图 → **必须拒绝提交**（退出码 2）；`orchestrator.py` 逐段校验不过 → **熔断**；确认 `img_size="auto"` 解析结果 = **768×1344**
- [ ] **图片通道不构成瓶颈（v2.7）**：生成期间图片通道处于**空闲或领先**状态（R21 §5.1 反压检查）
- [ ] **熔断与降级已验收（v2.7）**：TT Image2 超时熔断、Gate-P 2 轮到顶、视频 N=3 到顶**三条路径各人为触发一次**，均报明确失败而非静默续跑
- [ ] **等待标定（v2.7）**：Gate-P 的 `tau_face_img` / `tau_src` 与图片判项阈值**尚未标定** ⇒ 上线前需并入 Phase 3.0 的标定集
- [ ] 🔒 **存储全部落数据盘（v2.7）**：`env_datadisk.sh` 已 source；`check_datadisk.sh` **全绿**；系统盘上无本项目任何产物（模型 / venv / 缓存 / 中间文件）；`$ROOT/venv` 已建且**未向系统 python 装包**

---

## 5. 诚实边界（部署时心里要有数）

- ✅ **实测**：四件权重 sha256 = 官方值 / 同 seed 逐位复现 / 噪声基线 1.08 / seed 方差 14.0 / 段长耗时曲线（4090 旧链路）
- ✅ **实测（v2.6 现场复核）**：内核已 **0.36.0** ｜ 社区污染节点**已清除** ｜ 系统盘 `/` **41%（18 G 可用）** ｜ GPU **空闲 23.2 GB** ｜ 同实例**跑着 `tts-gateway`**（占显存 466 MB）
- ❓ **未测（Phase 6 补）**：p（通过率）、J（判官准确率）、失败是否随 seed 变化、16 步增益
- ❓ **未测（Phase 6b 补）**：3090 绝对耗时基准、§1.5 三档链路的**每一层实际增益**、kitchen INT8 注意力对 H3 的效果、SageAttention 在 cu130+torch2.14 下的 wheel 兼容性、升内核是否破坏现有复现基线
- ⚠️ **本机不成立的说法（REPORT18 已纠正）**：EasyCache 在 8 步下空转（−0.7%）｜SGLang Pack 的 3.17× 是 8 卡比 1 卡，单卡不适用｜「CUDA13+INT8 提速 3 倍」的前提我们**已满足**
- ⚠️ **速度极限**：单卡 3090 现实极限约是当前基准的 1/3；要真正的 12× 只能租 8 卡（RunningHub Lightning，开源，脚本硬断言 8 卡）
- ⚠️ **t2va_only 才是 FastH3 唯一已验证的 task_family**；FL2VA 走 `t2va_fl2va`（非默认），Ref2VA 明确标 `untrained_exp`
- ⚠️ 本地权重不含 Regenerate-2K ⇒ 2K / 15s 完整形态需**官方 API / Comfy Cloud**（D5 独立升级项）
- ⚠️ 本地 Ref2VA 参考视频 = **零效果**（D1 已关闭，不再作为生产前提）
- ⚠️ **「判官不影响速度」有一个前提（v2.6）**：**判官必须出 GPU**。若被迫与生成共享同一张卡，"不影响"**不成立**，须改分时调度并重新评估
- ⚠️ **「判官不影响质量」的两个失效方向（v2.6）**：① 假阴性（放坏片过）→ 质量直接下降；② 假阳性（误杀好片）→ 烧算力重抽、且可能**越抽越差**。⇒ 故 L1 取保守阈值 + **判官换版本必须跑回归**（S8）
- ❓ **未测（v2.6 新增）**：**ClipForge 的文案质量是否真优于我方自研 prompt** —— 需拿 3–5 个真实商品做**双盲对比**（同 brief 两路出稿）才知
- ⚠️ **ClipForge 的 AGPL-3.0 边界（v2.6）**：自部署自用无碍；**改造成对外 SaaS 必须开源修改版**。它的**生成内容**不受影响
- ⚠️ **TT Image2 的并发行为未测（v2.7）**：37–46 s（文生图）/ 54–66 s（编辑）是**单张串行实测**（✅ REPORT.md）；**并发 N 路时的排队/限流行为未知** ⇒ §5.2 冷启动估算有不确定性，须实测
- ⚠️ **Gate-P 阈值未标定（v2.7）**：**P-01~P-06** 的 `tau_*` 均为占位 ⇒ 与判官阈值一并进 Phase 3.0 标定集（**P-07 无阈值、纯算术，已可上线**）
- ⚠️ **TT Image2 语义控制弱（v2.7 · 实测）**：会「擅自」改变状态（REPORT.md：宫格 p4 把瓶子放回桌面）⇒ **靠闸门② + P-06 兜，不能靠模型自觉**
- ⚠️ **像素级产品的上限受限（v2.7）**：不拍视频的前提下只能「真实像素合成 + 限制动作（静置/光影/推近/旋转）」，**动作幅度显著小于形态级**
- ❓ **未测（v2.7）**：编排引入的**净时延增量**（估算 1–3 min）尚未实测；TT Image2 云端模型版本被供应商静默更新时的漂移量未知（需**金标准样本比对**监控）

---

## 6. 一句话总纲

> **先冻结环境（A/B 定档 + 4 项落文件）→ 再修可复现性（换 seed + 种子库 + 环境清单）→ 装三道闸门（挡结构性失败进门）→ 廉价判官级联（L0/L1 砍量，且全部出 GPU）→ 最后才上抽卡（N≤3 + 保底换策略）。**
> 判官只能筛、不能修；闸门②永远不能卸；抽卡排第三顺位——它前面两件更值钱的事是降生成次数和提 p。
>
> **提速同理，排在最后**：先把「降 S + 提 p」做完（免费、收益最大），再上 §1.5 的 T0 官方纯净档（零安装）。**别为了最后 10% 去上高风险的块缓存**——那些收益会互相稀释，却要拿复现性和画质去换。而且**每层都要过「净耗时」闸门**：只看单次速度会误判（提速 25% 但把 p 从 0.5 压到 0.35 ⇒ 净收益仅 2.5%）。
>
> **文字层单独走线**：文案/脚本由 **ClipForge** 在本地 Mac 出（零 GPU 依赖），契约是 `shotlist_schema.json`，与 H3 的施工顺序**解耦**。**三条隔离红线**（不装 3090 / Key 不指向本机 LLM / 不搬九宫格）是"不影响质量与速度"的全部依据。
