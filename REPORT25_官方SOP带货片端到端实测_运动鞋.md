# REPORT25 · 官方 SOP 带货片端到端实测（运动鞋 / 严格对齐官方提示词 skill）

**日期**：2026-09-18 00:56–02:40
**素材**：产品 `/Users/admin/Desktop/工具/测试产品/K1_服装鞋_运动鞋/主图.png`（1024×1024）
　　　　模特 `/Users/admin/Desktop/工具/测试产品/模特形象/男模特.png`
**产出**：`deliverables/shoe_official/`（两个交付版本 + 规格卡 + 三份过程文档）
**结论**：✅ **按官方《极简产品广告生成器》SOP 端到端跑通。画面内英文文案首次验证成功。**
　　　　　⚠️ 同时纠正了一个我花掉 22 分钟的错误诊断（"推近冲过头" → 其实是"硬切镜"）。

---

## 一、结论速览

| 验证项 | 实测结果 | 判定 |
|---|---|---|
| 官方 SOP 提示词自检（13 项） | A3 与 B 两份**全部通过** | ✅ |
| **H3 能否在画面内生成英文文案** | **能**。两条文案都出现，两段配色正确、卡点淡入、单行 | ✅⭐ 首次证实 |
| **文案可读性** | 初版=半透明幽灵字（**不可读**）；修正版=大号实心深灰/黑字（**可读**） | ✅ 已修 |
| 三张独立锚定照落位 | f0 / f132 / f242 vs 锚1/锚2/锚3：MAD **3.78 / 4.12 / 3.31**；Δ(R−B) −1.25 / −1.53 / −0.79 | ✅ |
| 产品本体颜色保真 | 全片**无暖色漂移**（背景色偏探针全程 < 阈值） | ✅ |
| 锚定照本身零重绘 | 主图→去水印 MAD 2.13（仅 0.37% 像素变化）；去水印→锚1 为纯几何适配（哈希可验） | ✅ |
| **切镜行为** | 多锚点 ⇒ **多镜头 + 硬切**，切点 f133(5.542s) / f213(8.875s) | ✅ 符合 SOP |
| 切点是否卡在故事板位置 | 故事板 5.500 / 8.708 s ⇒ 实测差 **1 帧 / 4 帧** | ✅ |
| 结尾是否稳定画面 | 最后一次切点后近景指数摆幅 **0.67%**；文案动效仍在跑 | ✅ |
| 原生音频 | A mean −14.0 / max −3.5 dB；成片经 loudnorm 后 mean −16.6 / max −3.8 dB，**无削波** | ✅ |
| 音频是否为"音乐"而非噪声 | 频谱呈**周期性脉冲 + 持续低频 + 宽带 pad** ⇒ 有节拍的音乐 | ✅ |
| 无崩坏 / 无漂移 / 无变色 | 全片抽帧扫描 + 逐帧量化，未检出 | ✅ |

---

## 二、交付物清单

| 文件 | 规格 | 用途 |
|---|---|---|
| `SHOE_SOP_FILM.mp4` | 768×1344 / 24fps / 367 帧 / **15.292 s** / 5.16 MB | **纯官方版**：B+A 直拼，保留 H3 原生音轨，**零后期文字**（严格合 SOP） |
| `SHOE_COMMERCE.mp4` | 同上 / 5.46 MB | **带货投放版**：在官方版之上叠中文卖点条 + AIGC 合规标识 |
| `商品规格卡_运动鞋.png` | 768×1344 | 商品详情页 / 主图位（承载价格·尺码·适用场景） |
| `分镜表_SOP官方版.md` | — | 官方 11 列精确节拍分镜表（生成前定稿） |
| `卖点覆盖表.md` | — | 产品信息 → 画面/文案 逐项落点映射 |
| `frames/` `anchors/` `raw/` `post_shop/` | — | 全部证据帧、锚定照、原始成片、后期层 |

**两个版本的区别必须说清楚**：SOP 官方版是 SOP 要求的产物；投放版是**有意扩展**
（SOP 明文允许的例外："只有用户明确要求后期叠字"），不是 SOP 要求的产物。

---

## 三、⭐ 核心发现 1：H3 真的能在画面里画字 —— 但**落点决定成败**

### 3.1 先纠正一个此前的不确定

此前口径是"能不能画字存疑"。**实测结论：能，而且质量可用。**

| 文案 | 出现时段 | 两段配色 | 卡点淡入 | 单行 | 可读 |
|---|---|---|---|---|---|
| `Air Flows Right Through` | 5.500–7.100 s | 前半深灰 / 后半商品黑 | 5.500 + 6.300 | ✅ | 修正后 ✅ |
| `Soft Landing, Light As Air` | 8.700–10.08 s | 同上 | 8.700 + 9.500 | ✅ | ✅ |

### 3.2 但**初版的中段文案彻底废了** —— 根因与处置

- **现象**：初版 f170 的 `Air Flows Right Through` 渲染成**半透明、低对比的幽灵字**，
  压在黑色贾卡网面上，**完全不可读**。⇒ 卖点②「透气」等于没传达。
- **根因**：我在提示词里写的是"放在网面旁的负空间、**垂直居中**"。
  而那一帧画面中部正是**深色网面**——深灰字压深色网面，模型只能渲成半透明。
  **SOP 明文要求"文案可读"，这是硬约束，不是审美问题。**
- **处置**：先确认那一帧的**干净浅色留白带**在哪（本例 = 鞋体上方的浅蓝背景带），
  然后在提示词里**指明位置** + 补三条负向约束：

  > in that **clean pale backdrop band above the shoes**, offset to the left … drawn in
  > **solid fully opaque lettering** at roughly **one tenth of the frame height**, standing
  > directly on the pale backdrop with strong contrast, **staying completely clear of the dark
  > mesh and never overlapping, touching or drifting onto the shoes**

- **结果**：修正版 f168 的文案变成**大号、实心、不透明、深灰/黑**，背景纯净，**清晰可读**，
  两段配色（深灰 → 黑）肉眼可辨。证据：`frames/18_中段文案_旧稿vs修正.png`

> **可复用结论**：画面内文案的成败 = **落点是否落在干净浅色留白带上**。
> 把"干净留白带在哪一帧、在哪一侧"写进提示词，比写"large / legible"有效得多。

---

## 四、⭐ 核心发现 2：多锚点 = 多镜头 + 硬切（**我误诊了一轮**）

### 4.1 错误诊断与代价

初版成片在锚定时刻（5.5 s）之后构图突然变紧。我判成「**相机推近冲过头**」，
于是花 **22 分钟**重跑一轮，把镜 4 改成 `truck`（横移）+「保持距离不变、不得再推近」。

**结果：完全无效。** 修正版的满画幅特写区间反而从 1.5 s 变成 2.5 s。

### 4.2 逐帧量化后的真相

| 帧间 | 近景指数变化 | 耗时 | 判定 |
|---|---|---|---|
| f132 → f136 | **9.5% → 32.2%** | **4 帧 = 0.167 s** | ✂ **硬切镜** |
| f212 → f216 | **34.9% → 13.1%** | **4 帧 = 0.167 s** | ✂ **硬切镜** |

0.167 s 的构图跃变不是运镜能产生的 ⇒ 这是**切镜**。
**提示词里的运镜描述根本约束不到切镜** —— 钱花在了错误的杠杆上。

### 4.3 最终口径（推翻我此前的两条错误认知）

| 我此前的说法 | 实测/官方口径 |
|---|---|
| ❌「本片无切镜，仅运镜接续」 | ❌ 错。**切镜是官方 SOP 预期的**：分镜表表头就有「**转场 / 连贯**」列，原则 5/6 写「元素连续 + 动势连续 + 形态接续」「流畅感 = 缓动 + 不卡时点 + **镜头进出有接缝**」——"接缝"就是切点 |
| ❌「中间镜别写继续推近，否则冲过头」 | ❌ 错。中间镜处的构图跃变是**切镜**。多锚点 ⇒ 多镜头，**锚定照 ≈ 镜头锚** |

### 4.4 但有一个**真实**的偏差要记账

| 项 | 故事板 | 实测 | 差异 |
|---|---|---|---|
| 切点 1 | f132（5.500 s） | **f133（5.542 s）** | 差 1 帧 ✅ |
| 切点 2 | f170（7.083 s） | **未出现** | ❌ 被合并 |
| 切点 3 | f209（8.708 s） | **f213（8.875 s）** | 差 4 帧 ✅ |
| 节拍数 | 6 节拍 / 4 个切点 | **3 个镜头 / 2 个切点** | ⚠️ 镜头粒度变粗 |

⇒ 模型**按锚定时刻切镜，但会合并掉部分切点**。本例镜 2 实际变成一段
5.5–8.9 s（3.3 s）的材质特写（近景指数 ~33–36），比故事板更紧更长。
**想控制镜头数只能改锚定照的数量与时刻，改提示词无效。**

---

## 五、保真链实测（全链路逐像素可验）

### 5.1 锚定照本身：零重绘，可哈希验证

| 环节 | 实测 | 判定 |
|---|---|---|
| 主图 → 去水印（1024×1024） | MAD **2.133**，仅 **0.37%** 像素变化 >20，差异集中在原水印区 | ✅ 纯局部编辑 |
| 去水印 → `anchor_1_hero.png` | **SHA256 完全一致**于 `shoe_real/02_首帧_768x1344.png`（几何适配 768×1344，纯缩放+补边） | ✅ **零重绘** |
| `anchor_2_material.png` | = `c2_last.png`（几何派生） | ✅ |
| `anchor_3_endcopy.png` | = `c1_last.png` + SF Pro Semibold 文案烘焙 | ✅ |

> 走**几何派生**而非图像模型重绘，是因为 REPORT23 实测图像模型会改鞋型/纹理/logo，
> 与 SOP「产品本体颜色是硬保真约束」冲突。

### 5.2 成片对锚定照：三处全部高保真

| 锚定时刻 | vs 锚定照 MAD | Δ(R−B) | 判据 |
|---|---|---|---|
| A3 f0（0.000 s） | **3.78** | −1.25 | MAD<8 ✅ / \|Δ\|<6 ✅ |
| A3 f132（5.500 s） | **4.12** | −1.53 | ✅ |
| A3 f242（10.083 s） | **3.31** | −0.79 | ✅ |
| B f0（真人） | **1.93** | −0.10 | ✅ |
| B f123（真人） | **2.41** | −0.08 | ✅ |

⚠️ B 片的锚定照来自 TT Image2「模特穿鞋」合成（REPORT24），
所以 B 的产品保真是"**同款可辨**"，不是"产品图的数字复刻"；对外商用前需人工过目。
A 片则全程几何派生，是真正的数字复刻。

---

## 六、音频实测

| 项 | A 片 | B 片 | 成片（loudnorm 后） |
|---|---|---|---|
| 采样率 / 声道 | 32 kHz / 2ch（原生） | 32 kHz / 2ch | 44.1 kHz / 2ch |
| mean_volume | −14.0 dB | −14.0 dB | **−16.6 dB** |
| max_volume | −3.5 dB | −0.8 dB | **−3.8 dB** |
| 削波 | 无 | 17 个采样贴 0 dB（可忽略） | **无** |

**频谱核验**（`frames/09_A_音频频谱.png`）：呈**周期性竖直脉冲**（节拍）+ **持续低频带**（sub-bass）
+ **宽带 pad**，与提示词里 `around 100 BPM … light kick with sub-bass sine sweep and wooden
percussion accents` 一致 ⇒ **是有节拍的音乐，不是噪声或纯环境音**。

拼接处用 0.25 s `acrossfade` 消除音乐断点。

---

## 七、成本实测：帧数成本是**超线性**的

| 片 | 帧数 | 时长 | 实测耗时 | 单位帧耗时 |
|---|---|---|---|---|
| B 真人穿着片 | 124 | 5.167 s | **435.3 s** | 3.51 s/帧 |
| A 产品主片（初版） | 243 | 10.125 s | **1325.5 s** | 5.45 s/帧 |
| A3 产品主片（定稿） | 243 | 10.125 s | **1325.9 s** | 5.46 s/帧 |

- 帧数 **×1.96** ⇒ 耗时 **×3.05**，拟合约 **frame^1.7**
- 本次总 GPU 时长：435.3 + 1325.5 + 1325.9 = **3086.7 s ≈ 51.4 min**（含一轮返工）
- 单条定稿成本：435.3 + 1325.9 = **1761.2 s ≈ 29.4 min**

> **排期含义**：一条 10 s 片 ≈ 一条 5 s 片的 **3 倍**，不是 2 倍。
> 「一条片内多镜」省生成次数，但**改动任意一段都要整条重跑** —— 这是"少生成次数"
> 与"可局部返工"的取舍。本次就是吃了这个亏：只改镜 3/4 的文字描述，也要重跑整条 10 s。

---

## 八、官方 SOP 对齐的证据（13 项机器自检）

`check_official.py` 逐项校验，A3 与 B **全部通过**：

```
① 对齐指令=第一行            ② 指令后空行              ③ 三核心字段齐全
④ 首镜含风格声明              ⑤ 每个 [Shot N] 带递增切点时刻
⑥ 运镜=类型+幅度+速度          ⑦ 文案逐字写入 prompt
⑧ 完整文案 3-5 词且≤32 字符    ⑨ 两段式同色规则明写（深灰+商品黑）
⑩ 单行硬约束明写              ⑪ 两段进入时刻明写
⑫ 对齐指令时间点数=参考图数     ⑬ 明确禁止额外文字/图形
```

`qc_final.py` 交付前质检（9 组）：**全部通过**。

| # | 检查 | 结果 |
|---|---|---|
| ① | 规格 768×1344 / yuv420p | ✅ |
| ② | 帧数账平（B+A = 成片 367 帧） | ✅ |
| ③ | 音轨非静音、无削波 | ✅ |
| ④ | 结尾为稳定画面（切点后近景指数摆幅 0.67%） | ✅ |
| ⑤ | 拼接顺序 B 在前（首帧 MAD 0.154） | ✅ |
| ⑥ | 三处锚定保真 | ✅ |
| ⑦ | 文案横带统计（仅记录，见 §3） | — |
| ⑧ | 后期条不与英文文案争纵带（条顶 0.88） | ✅ |
| ⑨ | 切点都落在故事板切点附近 | ✅ |

---

## 九、本轮修正的工具坑（都写进技能包了）

### 9.1 四个"假指标"（花时间最多的地方）

| 假指标 | 为什么无效 | 实测反例 |
|---|---|---|
| 全画幅平均 R−B 当色偏探针 | 背景淡蓝（R−B 负）、产品中性 ⇒ 是**构图代理量** | 旧稿末帧全画幅 Δ(R−B)=+22"像严重偏色"，但与锚定照实测 −0.94 |
| 四角中位色 / 梯度阈值做背景分割 | 背景是蓝→白渐变，角点是最饱和的蓝 | "产品区 R−B"报 −34，纯属误判 |
| 横带 P95−P5 判文案可读性 | 带内有产品时被产品高对比淹没 | 糊字 194.8 vs 清晰字 198.0，零区分度 |
| 降采样"构图稳定量"判定镜 | 分不开定镜与慢推（区间重叠） | 定镜 2.0–4.4 vs 慢推 5.4–7.5 |

**站得住的量**：**深色（明度<50）占比 = 近景指数**（随推近单调，且能干净抓切镜：57–69 vs 运镜 <10）
＋ **成片 vs 锚定照逐像素比对**（MAD / Δ(R−B)）。

### 9.2 两个代码级 bug（都是靠"干跑"抓到的）

| bug | 症状 | 修法 |
|---|---|---|
| `assemble_official.py` 拼接顺序 | `assemble([b, a])` 而 `b` 是第二个参数 ⇒ 实际输出 **A→B**，与"结尾落在文案"相反 | 改**显式命名** `first/second` + 断言帧数（124/243）+ 回显顺序 |
| `ffprobe -show_entries format=…` 取 `size` | `KeyError: 'size'`（该字段不在 format 输出里） | 改用 `os.path.getsize()` |
| 质检脚本只抓 stdout | ffmpeg 的诊断（含 volumedetect 电平）**全在 stderr** ⇒ 误报"无音轨/静音" | 合并 stderr |
| 后期叠字字号写死 46px | 16 字中文 ≈ 736+84 > 768 ⇒ **两端被裁**（少了"两相宜""点击下方链接"），PIL 不报错 | 按可用宽度**反解字号**（降到 38/37pt）+ 生成后断言横向占用 |

### 9.3 中文标签字体

对比图/扫描图的标签用 Arial 会渲染成 **□□□**（无 CJK 字形）。
统一改 `STHeiti Medium.ttc`（退 `PingFang.ttc`）。
另：`￥` 在 Arial Black 里无字形，价格必须中文字体渲染。

---

## 十、未做 / 待定（明确标出，不当作已完成）

| 项 | 状态 | 说明 |
|---|---|---|
| 中文口播版 | ❌ 未做 | 官方 SOP 的适用范围**明确排除 KOC 口播**（"不适合 KOC 口播、普通剪辑或复杂屏幕演示"）。要做需另起一套，且口播文案需辉哥确认（涉及功效表述合规） |
| 2K / 15 s 长片 | ❌ 未做 | 本地权重不含 Regenerate-2K 路径，需切官方 API 或 Comfy Cloud（独立升级项 D5） |
| 镜头粒度对齐故事板 | ⚠️ 未达 | 实测 3 个镜头 vs 故事板 6 节拍。若要 6 节拍，需**增加锚定照**（4–5 张）或**缩短单条时长** |
| 镜 2 时长/紧度 | ⚠️ 超出 | 实际为 5.5–8.9 s 材质特写（比故事板更紧更长）。如要压回"中近景"，需把锚 3 的紧度调近锚 2 |
| 投放前合规标识 | ✅ 已含 | `SHOE_COMMERCE.mp4` 全程带「AIGC 生成」角标；纯官方版无角标，**对外发布前须补标** |

---

## 十一、可复用动作（已并入技能包）

已写入 `~/.workbuddy/skills/h3-fl2va-video-pipeline/SKILL.md` 新增章节
「**官方电商产品广告 SOP 落地：三张锚定照 + 画面内文案**」，
并把「最容易踩的四个坑」扩为「**六个坑**」，新增工具脚本 4 个：

| 工具 | 作用 |
|---|---|
| `scripts/timeline.py` | 近景指数时间轴（含四个"假指标"的踩坑说明） |
| `scripts/cmp_frames.py` | 成片帧 vs 锚定照逐像素比对（MAD / Δ(R−B)） |
| `scripts/scan_clip.py` | 确定性抽帧扫描图（`select=eq(n,N)`，不用 `-ss`） |
| `scripts/ab_scan.py` | 两条成片同帧号上下并排对照 |

---

## 十二、下一步建议（按性价比排序）

1. **看片定夺**：先看 `SHOE_SOP_FILM.mp4` 与 `SHOE_COMMERCE.mp4`，确认风格方向是否要调。
2. **若要镜头粒度更细**：把锚定照从 3 张加到 4–5 张（对应更多切点），**不要改提示词**。
3. **若要镜 2 不那么紧**：把 `anchor_3_endcopy` 的紧度调近 `anchor_2_material`，从源头消除大幅切变。
4. **若要投放**：`SHOE_COMMERCE.mp4` 已带 AIGC 标识；纯官方版若要发布需补标。
5. **口播版**：需先确认口播文案，且要另起链路（超出本 SOP 范围）。
