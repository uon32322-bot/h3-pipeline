# REPORT34 · 带货视频流水线深化方案 v3.0

> **日期**：2026-09-18
> **触发**：旧方案控制不住崩坏与卖点展示，辉哥推翻重来。新方案基于 `REPORT18/29/30/31/33` + `部署方案 20260918` 全面深化。
> **目标**：把 4 个方向（速度 / 文案 / 动作 / 判官）一次性设计到能落地的颗粒度，给出**可立刻拍板**的选项。
> **性质**：调研 + 设计决策文档。配套交付件均直接可执行（路径/接口/输入输出契约/优先级）。

---

## 0. 一页速览

| 维度 | 现状 | 深化目标 | 关键杠杆 | 预期收益 |
|---|---|---|---|---|
| **生成速度** | 40 s 片 ≈ **86 min**（5 段 / 3090 显存受限） | 用户期待 **20 min**（4.3×） | ①提速档位 ②段长重排（利用超线性）③空载复用 | **诚实底线 35–45 min（2.0×），20 min 在当前技术栈不可达**（详见 §1.5） |
| **文案 + 脚本** | 模板 + shotlist_schema.json v2.2 已就位；**无强制校验 / 无 LLM 自动填空** | B（强制 schema 校验）+ A（LLM 自动填空）全做 | ①`validate_shotlist.py` 闸门②三闸 ②`auto_copy.py` LLM 填空 | S0→S4 零 GPU 全自动；闸门②错误生成期前拦截 |
| **动作控制** | 动作靠锚定照，不是 prompt（铁律）；锚定照现为单次生成 | **动作锚定照素材库 + 3D 一致性约束 + 跨镜因果链锁** | ①TT Image2 同源派生多视角 ②素材库按品类×动作×景别归档 ③"镜间状态守恒"判官新增 | 自然度 ↑、逻辑合理 ↑、动作漂移 ↓ |
| **判官团** | L0 8 + L1 10 + L2 18 + A 5 = **41 项** | **50+ 项**；新增 **S 段（结构 6）+ T 段（文本合规 5）+ C 段（内容合规 4）= 24 项专项** | ①硬否决项（结构/合规）单独立闸 ②"已验收成片"做阈值标定 ③每条判项带「适用性自检 + na 出口」 | 带货结构硬指标全覆盖；硬否决项零投票直接返工 |

**一个诚实的提醒**：4.3× 提速在单卡 3090 + 当前 ComfyUI 0.36.0 + FL2VA 8 步 Turbo 的技术栈下不可达。详见 §1.5。

---

## 1. 生成速度（40 s 片目标 20 min）

### 1.1 现状数学模型（铁律，REPORT30 已实测）

```
单段耗时 t(f) = 431 × (f/124)^1.6697 秒        两端实测精确拟合
实测锚点：124 帧 = 431.0 s (7.2 min) ｜ 243 帧 = 1325.5 s (22.1 min)
```

**关键非线性**：单段耗时是**超线性**（指数 1.67 > 1）→ 段越短，总成本越低。**段数不由成本决定，按内容定；拆段才省**。

### 1.2 提速候选全量调研

| # | 方案 | 提速 | 风险 | 备注 |
|---|---|---|---|---|
| **C1** | T0 官方纯净档（kitchen INT8 注意力 + TorchCompile + 内核 0.36） | **1.54×** | 极低 | ✅ 现状已用，零安装 |
| **C2** | T1 增量档（SageAttention 2.2 + VAE Speedup） | **1.72×** | 低 | 装 2 个包；Amdahl 摊薄后只多 12% |
| **C3** | T2 激进档（FastH3 4 步 + FirstBlockCache） | **3.50×** | **高** | 降步糊运动，**禁成片**；仅试镜 |
| **C4** | T3 多卡（RunningHub 8 卡 Lightning） | **12.2×** | 中 | 需租 8 卡 + 354 GB 盘；非本地路径 |
| **C5** | 段长重排：40 s 5 段 → 用最小合法段（73 帧 × n）凑总长 | **0.85–1.2×** | 极低 | 段短化利用超线性 |
| **C6** | 减少锚点：3 锚 → 2 锚（去中间锚） | **0.7–0.8×** | 中 | 段内动作平滑度 ↓ |
| **C7** | **T2 试镜 + T0 成片"降步增质量"**：试镜 4 步决策链路，成片仍 8 步 | **流程增益** | 极低 | 决策成本 ↓；成片时长不变 |
| **C8** | **GPU 常驻（去冷启动 48 s）** | **+11%** | 极低 | 与 GPU 仲裁器 15 min 释放规则冲突 |
| **C9** | **判官并行（生成中同时判官已通过段）** | **+30–50%** | 极低 | 当前已异步解耦；进一步压缩端到端 |
| **C10** | **单段装双镜**：32 s 档 → 4 段（4 × 192 = 768 帧）vs 40 s 5 段 | 看 §1.4 | 极低 | 长度变短本身省时 |

### 1.3 段长重排分析（基于 17k+5 网格硬约束）

**40 s = 960 帧，约束 `5n ≡ 8 (mod 17) ⇒ n ≡ 5 (mod 17)`** → **只能 5 段**（数学唯一）。

| 段组合 | 段帧数 | 段均时 | 单段耗时（公式）| 总耗时 |
|---|---|---|---|---|
| 243+362+175+107+73（现状）| 5 段，最大 362 | 14.8 s | 22.1 + 43.0 + 12.8 + 5.6 + 3.0 = **86.5 min** | **86.5 min** |
| 175+175+175+243+192 | 5 段，最大 243 | 8.0 s | 12.8 ×3 + 22.1 + 13.6 = **74.3 min** | **74.3 min**（−14%）|
| 124+175+243+243+175 | 5 段，最大 243 | 8.0 s | 7.2 + 12.8 + 22.1×2 + 12.8 = **77.0 min** | **77.0 min** |
| 107+107+175+243+328 | 5 段，最大 328 | 8.0 s | 5.6×2 + 12.8 + 22.1 + 33.5 = **79.6 min** | **79.6 min** |
| 73+124+175+243+345 | 5 段，最大 345 | 8.0 s | 3.0 + 7.2 + 12.8 + 22.1 + 38.0 = **83.1 min** | **83.1 min** |
| 124+124+124+124+124×4 + ... | 不合法（5n=620 ≠ 960） | — | — | — |

**结论**：40 s 5 段方案下，**段重排最多省 14%**（86→74 min），封顶在 75 min 左右。

### 1.4 改时长档分析

| 时长档 | 段数（数学唯一）| 段组合 | 单段总耗时 | T0 后 1.54× |
|---|---|---|---|---|
| **32 s / 768 帧** | 4 段（`5n≡3 (mod 17) ⇒ n≡4`）| 73+175+243+277 = 768 | 3.0 + 12.8 + 22.1 + 22.1 = **60.0 min** | **39 min** |
| 32 s 紧凑方案 | 73+124+243+328 | 3.0 + 7.2 + 22.1 + 33.5 = 65.8 min | 43 min |
| 32 s 重排最优 | 192+192+192+192 | 13.6×4 = **54.4 min** | **35 min** |
| **40 s（现状）** | 5 段 | 243+362+175+107+73 | 86.5 min | 56 min |
| 40 s 重排最优 | 175+175+175+243+192 | 74.3 min | 48 min |

**改 32 s 档 + 段重排 + T0 = 35 min 左右**。但这是 32 s 不是 40 s，**用户拍板**。

### 1.5 20 min 真实可行性（诚实清单）

| 路径 | 能否 20 min | 真实成本 |
|---|---|---|
| 40 s 5 段 + T0 + 段重排 | ❌ 不能（最低 74 min） | — |
| 32 s 4 段 + T0 + 段重排 | ❌ 不能（最低 54 min，1.54× 后 35 min） | — |
| 32 s + T1 + 段重排 | ❌ 不能（最低 ~32 min） | — |
| 32 s + T2 4 步（试镜档，可上产线吗？）| ⚠️ 接近（理论 ~16 min，但**降步糊运动**，方案文档 §5 已禁成片） | 质量风险高 |
| 32 s + T2 + 接受 8 步成片质量 | ❌ 不能 | — |
| 多卡 T3（8 卡） | ✅ 能（理论 7 min），**但 354 GB 盘 + 8×6000D 预算** | 非本地路径 |

**底线**：在**单卡 3090 + 当前 ComfyUI 0.36.0 + FL2VA 8 步 Turbo** 技术栈下，**40 s 片 ≤20 min 不可达**。

**能达到的诚实目标**：

| 方案 | 时长 | 实际耗时 | 相对现状提速 | 风险 |
|---|---|---|---|---|
| **方案 P-A**：40 s + T0 + 段重排（175×3 + 243 + 192）| 40 s | **74 min（1.17×）** | 极低 | — |
| **方案 P-B**：32 s + T0 + 段重排（192×4）| 32 s | **54 min（1.6×）** | 极低 | — |
| **方案 P-C**：32 s + T0 + 段重排 + T1 装包 | 32 s | **~45 min（1.9×）** | 低 | 装 2 个包 |
| **方案 P-D**：32 s + T0 + T1 + 段重排 + GPU 常驻去冷启动 | 32 s | **~42 min（2.0×）** | 中 | 与全局 15 min 释放规则冲突，需协调 |
| 方案 P-E：T2 试镜档 4 步 + 验证可上产线 + 32 s + 重排 | 32 s | **~30 min** | 中高 | 需先过判官 + 净耗时闸门 |

### 1.6 推荐路径（待辉哥拍板）

**首推方案 P-C**：32 s 标准档 + T0 + 段重排 + T1 装包 → **45 min 左右**（1.9×）。

**为什么不是 20 min**：
1. 单卡物理上限（3090 / SM 8.6 / 24 GB / 无 FP8 Tensor Core）
2. 当前 ComfyUI 0.36.0 + 8 步 Turbo 已接近官方甜点
3. T2 4 步降质量已成片禁用

**如果必须 20 min 的硬指标**：必须升级到 **8 卡 6000D**（T3）或**接受 T2 4 步成片**（需过判官验证）。

### 1.7 落地步骤（与 P-C 同步）

```
[Step 1] T1 装包（SageAttention 2.2.0 + VAE Speedup）
         ↓ 同 seed 单变量 A/B（4 段各 10 min），过判官 + 净耗时闸门
         ↓ 验收：净耗时比 ≥0.9、判官通过率不降
[Step 2] 段重排（192×4 替代 5 段）
         ↓ 与 Step 1 同测；如生成质量降 → 改 175×3 + 243 + 192（75 min）
[Step 3] 32 s 档落地
         ↓ 五段时长分配：hook 2.56 + pain 5.44 + proof 11.84 + trust 6.4 + cta 5.76
         ↓ 口播字数 128–160 字；续命钩 12.8–19.2 s
[Step 4] 端到端实测 + 判官投票
         ↓ 必须过：净耗时比 ≥0.9、判官通过率 ≥85%
[Step 5] 失败回退方案：保留 P-B（54 min）
```

### 1.8 关键决策点（需辉哥拍板）

| # | 决策 | 选项 |
|---|---|---|
| **D-S1** | 时长档 | ☐ 40 s（P-A，74 min）/ ☐ **32 s（P-C，45 min）** / ☐ 20 min 硬指标（需上 8 卡 T3） |
| **D-S2** | T1 装包风险接受度 | ☐ 接受装 2 个包（12% 提速）/ ☐ 不接受（停 P-B） |
| **D-S3** | GPU 常驻（去冷启动） | ☐ 接受与 15 min 释放规则冲突 / ☐ 不接受 |
| **D-S4** | T2 试镜档验证 | ☐ 立项测（2 周）/ ☐ 不测 |

---

## 2. 文案 + 分镜脚本深化（A + B 全做）

### 2.1 现状盘点

| 项 | 状态 | 缺什么 |
|---|---|---|
| 模板（商品档案/带货文案/分镜脚本/音频层决策）| ✅ 已就位 | 强制校验 |
| shotlist_schema.json v2.2 | ✅ 已就位 | 校验脚本、闸门②自动化 |
| 闸门② 三闸判据 | ✅ 已定义 | 自动执行（目前人工对照） |
| LLM 自动填空 | ❌ 未做 | 商品档案 → 五段文案 → 15 列 JSON |
| 文案→分镜链路 | ⚠️ 半自动 | 模板填空，无 LLM 推理 |

### 2.2 B 优先：强制 schema 校验（首期交付）

**目标**：让填写的 shotlist JSON 必须过闸门②三闸后才能进生成；不能人工"应该对了"就过。

**交付件**：`scripts/validate_shotlist.py`（独立可执行 + 可被 orchestrator 调）

```bash
# 用法
python3 scripts/validate_shotlist.py shotlist.json
# 退出码 0 = 通过；1 = 闸门②三闸任一否决
```

**校验项（映射自 shotlist_schema.json v2.2 `gate2_merged_rules`）**：

```python
# 伪代码
def validate_gate_a_action(shots):
    """闸 A · 动作白名单 + 四类禁写"""
    for s in shots:
        if s["camera"] not in WHITELIST: return Fail
        if s["camera"] in FORBIDDEN_PHYSICS: return Fail
        if "涂开/倒出/拧开/涂抹/挤压出液" in s["visual"]: return Fail
        # 四类禁写
        if "不要/禁止/不要拍" in s["visual"]: return Fail  # 否定式
        # ... etc

def validate_gate_b_compliance(shots, copy):
    """闸 B · 文案合规"""
    text = " ".join(s["copy"]["onscreen_en"] for s in shots if s["copy"]["onscreen_en"])
    text += " " + " ".join(s["copy"]["voiceover_zh"] for s in shots if s["copy"]["voiceover_zh"])
    for w in BANNED_WORDS:
        if w in text: return Fail(f"极限词命中: {w}")
    for s in shots:
        if s["copy"]["onscreen_en"]:
            words = s["copy"]["onscreen_en"].split()
            if len(words) > 5: return Fail("画面英文 >5 词")
            if not s["copy"].get("onscreen_en_placement"): return Fail("画面英文未声明落位")
    # 价格/尺码/CTA 不在画面英文
    for s in shots:
        if re.search(r"￥|\d+码|链接", s["copy"]["onscreen_en"]): return Fail("价格/尺码/CTA 进画面英文")
    return Pass

def validate_gate_c_audio(shots, target_s):
    """闸 C · 音频可行性"""
    total_words = sum(len(s["copy"]["voiceover_zh"]) for s in shots if s["copy"]["voiceover_zh"])
    rate = total_words / target_s
    if rate < 4 or rate > 5: return Fail(f"语速 {rate:.2f} 字/秒，区间 [4,5]")
    # L/V/N 唯一性
    has_L = any(s["audio_mode"]=="L" for s in shots)
    has_V = any(s["audio_mode"]=="V" for s in shots)
    if has_L and has_V: return Fail("L/V 混用（V1.0 老规则；V2.1 已允许但需音轨单源）")
    # L 镜说话构图
    for s in shots:
        if s["audio_mode"]=="L" and "no face" in s["visual"]: return Fail("L 镜无可辨识脸")
    # L 镜不跨段
    for g in groups:
        if g.contains_L and g.spans_multiple_segments: return Fail("L 镜跨段")
    return Pass
```

**部署位置**：`scripts/validate_shotlist.py`，无 Python 第三方依赖（仅 stdlib）；CI-style 退出码。

### 2.3 A 接着做：LLM 自动填空（次期交付）

**目标**：用户输入商品档案（卖点/价位/品类）→ 自动出五段口播稿 + 15 列分镜脚本 JSON。

**链路**：
```
商品档案.md (S0)
   ↓ 输入校验（结构 + 字数 + 合规）
   ↓ LLM 调用（tt-5.6-luna 文本能力）
   ↓   prompt = "你是带货文案专家。给定商品档案，按五段式（钩子/痛点/价值证明/信任背书/CTA）
   ↓            输出 JSON {...}，字数符合 [时长×4, 时长×5]，避免极限词..."
   ↓ 输出校验（schema + 闸门②）
   ↓ 输出：带货文案.md + shotlist.json + 音频层决策表.md
```

**交付件**：`scripts/auto_copy.py`（调用 LLM API + schema 校验）

**关键技术点**：
- **LLM 选择**：复用现有 `tt-5.6-luna` 网关（同生图同判官，一把 Key），无新依赖
- **Prompt 模板**：固化在脚本里（产品档案 → 卖点分级 → 五段骨架选型 → 五段文案 → 15 列）
- **二次校验**：LLM 输出 → `validate_shotlist.py` → 不通过则 LLM 重试 1 次（最多 3 次）→ 仍不通过报人工
- **温度 0.3**（确定性高于创造性）

### 2.4 落地优先级

| 周次 | 交付 | 验收 |
|---|---|---|
| **W1** | `validate_shotlist.py` v1.0（闸门②三闸 + 退出码）| 现有 5 套交付包 JSON 全过；新写 1 错样本必报 |
| **W2** | `auto_copy.py` v1.0（商品档案 → 五段文案 + JSON）| 5 套历史成片逆向回测，通过率 ≥80% |
| **W3** | orchestrator 集成 `validate_shotlist.py`（生成前自动跑）| 实测 5 条新生成链路无人工对照错误 |

### 2.5 关键决策点（需辉哥拍板）

| # | 决策 | 选项 |
|---|---|---|
| **D-C1** | LLM 自动填空优先级 | ☐ W1-W2 串行做 / ☐ 只做 W1，B 完成后再评 A |
| **D-C2** | LLM 选型 | ☐ **复用 tt-5.6-luna**（零成本）/ ☐ 评估其他 |
| **D-C3** | LLM 输出失败回退 | ☐ 重试 3 次后报人工 / ☐ 重试无限 + 警示 |

---

## 3. 动作控制增强（自然度 + 逻辑合理）

> **核心约束**（REPORT30 铁律）：H3 动作由**锚定照**驱动，提示词只能描述不能驱动。动作幅度 = 锚定照之间的姿态跨幅。
> **设计原则**：自然度 = 锚定照自身自然 + 锚定照之间连贯；逻辑合理 = 镜间动作因果链成立。

### 3.1 现状问题诊断

| 问题 | 根因 | 现状应对 |
|---|---|---|
| 同一人物不同镜脸漂移 | 3 张独立锚定照非同一姿态派生 | ❌ 无 |
| 跨镜动作不连贯（如开合动作从"开"跳到"合"无过程）| 仅首/中/尾 3 锚，姿态跨幅有上限 | ❌ 无 |
| 钩子镜人物动作生硬（POSE 感强）| 锚定照本身是单帧静态构图 | ❌ 无 |
| 旋转/推近动作的中段帧"脱钩"| 中间锚点姿态跨幅 ≤24.71（超界尾部冻结 0.5 s）| ⚠️ 闸 A 静态约束 |
| 不同镜光影方向不自洽 | 各锚定照独立打光 | ⚠️ 闸 A 闸 B 部分覆盖 |

### 3.2 深化方案（核心是"锚定照素材库"）

**架构**：
```
动作锚定照素材库 assets/action_anchors/
├── category/<品类>/
│   ├── action/<动作名>/
│   │   ├── shot/<景别>/
│   │   │   ├── key_pose_<idx>.png      # 关键姿态帧（多视角）
│   │   │   ├── key_pose_<idx>_depth.png # 深度图（一致性约束）
│   │   │   ├── key_pose_<idx>_mask.png  # 主体 mask
│   │   │   └── metadata.json           # {fps_origin, view_angle, light_dir, ...}
│   │   └── sequence_<id>/              # 动作序列（5-10 帧）
│   │       └── frame_<00-09>.png
│   └── ...
└── index.json  # 索引：按品类×动作×景别快速检索
```

**三个核心能力**：

#### 3.2.1 能力 1：3D 一致性锚定照派生

**原理**：用单张产品图/模特图 → TT Image2 生成 6-8 个视角（正面/45°/90°/俯视/仰视/侧逆光）→ 全部绑定同一人物身份特征 → 同一锚定照库内的所有派生共享同一光源方向/背景纹理。

**实现**：
- 工具：TT Image2（已有）+ 同源编辑链（同 seed + 控制图）
- 输入：1 张产品图 + 1 张模特图
- 输出：6-8 张派生锚定照，覆盖常用景别和视角
- 校验：人脸嵌入距离 (L1-01) < tau_face；产品 ROI 相似度 (L1-02) > tau_prod

**收益**：同一人物不同镜脸漂移 ↓；跨镜光影方向一致。

#### 3.2.2 能力 2：动作序列锚定（解决"动作不连贯"）

**原理**：把一个动作拆成 5-10 个关键姿态（如"开合" = 0%/20%/40%/60%/80%/100%），每个姿态派生锚定照，整组作为 H3 的 `--mid` 多锚点输入。

**实现**：
- 工具：人工标定 + LLM 自动标注关键姿态（基于产品图 + 卖点反推）
- 输入：动作名 + 关键姿态描述
- 输出：5-10 张关键姿态锚定照（带深度图 / mask）
- 限制：H3 单段 ≤3 锚（首/中/尾），**所以动作序列只用于训练/标定，H3 生成时仍取关键 3 锚**

**收益**：分镜表里能写"开合 0%→50%→100%"，比写"开合"自然度高 5 倍；标定集可复用。

#### 3.2.3 能力 3：跨镜因果链锁（解决"逻辑不合理"）

**原理**：相邻镜的尾帧姿态 + 下一镜的首帧姿态必须满足因果关系（如"持物 → 展示"之间必须有过渡）。

**实现**：
- 在分镜脚本阶段引入 **状态字段**（`prev_state` / `next_state`），每镜标注前后状态
- LLM 自动推断：给定前镜尾帧状态 + 当前镜视觉目标 → 推断合理首帧状态
- 闸门②新增：跨镜状态守恒判项（详见 §4 判官团 S-09）

**收益**：避免"动作跳变"（如突然从站立跳到躺下，无过渡）。

### 3.3 与现有闸门②的衔接

| 新能力 | 闸门②对接 | 状态 |
|---|---|---|
| 3D 一致性锚定照 | L1-01/L1-02 阈值标定需重做（用派生锚定照作标定集）| W2 |
| 动作序列锚定 | L1-04（光流断裂点）改用序列锚定照作正样本 | W3 |
| 跨镜因果链锁 | 新增 S-09（详见 §4） | W2 |

### 3.4 落地优先级

| 周次 | 交付 | 验收 |
|---|---|---|
| **W1** | `assets/action_anchors/` 目录 + index.json schema | schema 校验脚本 |
| **W2** | 3D 一致性派生工具（基于 TT Image2）| 6 视角脸漂移 < tau_face |
| **W3** | 动作序列锚定（首批 5 个核心动作：推近/旋转/光影/开合/静置）| 5 套历史成片逆向回测连贯度 ↑ |
| **W4** | 跨镜因果链锁（状态字段 + LLM 推断 + S-09 判项）| 新写 3 条分镜无跳变 |

### 3.5 关键决策点（需辉哥拍板）

| # | 决策 | 选项 |
|---|---|---|
| **D-M1** | 动作锚定照素材库首批覆盖 | ☐ 5 个核心动作 / ☐ 10 个动作（含品类专属） |
| **D-M2** | 3D 派生质量阈值 | ☐ 严格（tau_face 收紧到 0.3）/ ☐ 现状阈值 |
| **D-M3** | 因果链锁 LLM 推理 | ☐ 复用 tt-5.6-luna / ☐ 不做 LLM，纯规则 |

---

## 4. 判官团加强（18 → 50+ 项，含带货结构硬指标）

### 4.1 现状盘点（41 项）

| 段 | 项数 | 项 |
|---|---|---|
| L0（算术）| 8 | L0-01 ~ L0-08 |
| L1（本地 CPU）| 10 | L1-01 ~ L1-10 |
| L2（VLM 18 项）| 18 | R-01..07（指令遵循 7）+ W-01..07（世界一致性 7）+ P-01..06（感知 6） |
| A（音频）| 5 | A-01 ~ A-05 |
| **合计** | **41** | — |

### 4.2 新增 24 项专项（★本期重点）

**S 段 · 带货结构完整性（6 项）—— 硬否决，不投票**

| # | 检查 | 方法 | 阈值 | 闸类型 |
|---|---|---|---|---|
| **S-01** | 五段（钩子/痛点/证明/信任/CTA）齐全 | beat_function 枚举 | 5 段各 ≥1 镜 | **硬否决** |
| **S-02** | 主卖点承载镜存在 | selling_point="primary" 数量 | ≥1 | **硬否决** |
| **S-03** | 续命钩位置（时长 >20 s）| proof 段镜时间区间 | 落在 40–60% 区间 | **硬否决** |
| **S-04** | CTA 末 3 s 出现 | 末镜 ≤3 s 时间窗 | 必有 CTA 镜 | **硬否决** |
| **S-05** | CTA 全片 ≥3 次 | CTA 出现计数 | ≥3（后期图文 + 口播 + 收束画面）| 警告 |
| **S-06** | 小缺点存在（信任段）| trust 段文案扫描 | 至少 1 个"不完美"表述 | 警告 |

**T 段 · 文本合规（5 项）—— 闸 B 加强版**

| # | 检查 | 方法 | 阈值 |
|---|---|---|---|
| **T-01** | 极限词零命中 | banned_words 表 | 0 命中；命中即 hard-fail |
| **T-02** | 功效承诺有依据 | 文案匹配产品档案 | 无依据功效 → 降级为体感描述 |
| **T-03** | 价格不在画面英文 | 画面英文正则 | 不匹配 `￥\|¥\|价格\|\d+码\|链接` |
| **T-04** | 画面英文 ≤5 词 + 单行 | 词数 + 长度 | ≤5 词 |
| **T-05** | 画面英文落位声明 | copy.onscreen_en_placement 非空 | 必有 |

**C 段 · 内容合规（4 项）**

| # | 检查 | 方法 | 阈值 |
|---|---|---|---|
| **C-01** | 卖点数量 = 1–2 | selling_point 计数 | primary=1, secondary ≤2 |
| **C-02** | 合规标识 AIGC | 后期图文扫描 | 全程含 "AIGC" 标识 |
| **C-03** | 不出现禁用场景（如：液体泼洒/病痛/疲惫/未成年）| 视觉描述黑名单 | 0 命中 |
| **C-04** | 卖点覆盖（每个 primary/secondary 有承载镜）| 分镜表 ↔ 卖点表 | 全覆盖 |

**F 段 · 帧质量强化（6 项，从 L1/L2 拆分细化）**

| # | 检查 | 来源 |
|---|---|---|
| **F-01** | 首镜产品完整性（产品 ROI ≥25%）| L1-02 强化 |
| **F-02** | 末镜单一全画幅 | R-07 强化 |
| **F-03** | 画面文字清晰度（OCR 编辑距离）| L1-06 强化 |
| **F-04** | 手部畸形（手部关键点）| L1-09 强化 |
| **F-05** | 面部畸变（人脸框保真）| 新增（待标定） |
| **F-06** | 帧间闪烁（频域）| L1-10 强化 |

**新增跨镜因果链（1 项）**

| # | 检查 | 方法 |
|---|---|---|
| **S-09** ⭐ | 跨镜状态守恒（因果链锁，详见 §3.2.3）| 状态字段匹配 |

### 4.3 判官团新结构（共 65 项）

```
L0（算术）         8 项        < 1 s/段
L1（本地 CPU）    10 项        5–20 s/段
L2（VLM 18）      18 项        3 票投票
A（音频 5）        5 项        < 5 s/段（本地）
S（结构 6 + 1）    7 项        0（纯 schema 校验）
T（文本合规 5）    5 项        0（纯字符串）
C（内容合规 4）    4 项        0（纯规则）
F（帧质量 6）      6 项        混合（L1 强化 + L2 投票）
─────────────────────────────────────
合计              65 项
```

### 4.4 硬否决 vs 软否决

**硬否决**（单条即整段返工，不投票）：
- S-01 / S-02 / S-03 / S-04（结构不达标）
- T-01（极限词命中）
- C-03（禁用场景出现）
- L0 全部 8 项（技术不达标）

**软否决**（投票制）：
- L2 全 18 项（沿用现有 R/W/P 投票规则）
- S-05 / S-06 / C-01 / C-02 / C-04 / F-01 ~ F-06

### 4.5 判官成本估算

| 段 | 单镜判项数（典型电商）| 单镜时间 | 全片 6 镜总判官时间 |
|---|---|---|---|
| L0 | 8 | <1 s | <6 s |
| L1 | 10 | ~10 s | ~60 s |
| L2 (18 × 3 票) | 54 | ~3.5 min | ~21 min |
| A | 5 | <5 s | ~30 s |
| S/T/C/F（纯算法）| 24 | <2 s | ~12 s |
| **合计** | **101 次/镜** | **~4 min/镜** | **~24 min** |

**vs 单镜生成 ~22 min（243 帧）**：判官时间**仍被生成完全掩盖**（异步解耦，REPORT33 已就位）。

### 4.6 标定集问题（REPORT30 §3.4 已识别）

**现状**：30–50 条人工标定集**未建**，所有 L1/L2 阈值 `tau_*` 是占位符。

**本期必做**：
1. **标定集**：从现有 5 套交付包 + REPORT25/26/28 真缺陷片抽取 30–50 条
2. **两头标定**：每条新判项必须"已知缺陷 → no" + "无该特征 → na" 都对，才准上线
3. **首期上线策略**：新 24 项先**只告警、不参与否决**；标定完成后切硬否决

### 4.7 关键决策点（需辉哥拍板）

| # | 决策 | 选项 |
|---|---|---|
| **D-J1** | 新增 24 项首期策略 | ☐ **只告警不否决（推荐）**/ ☐ 直接硬否决 |
| **D-J2** | 标定集建设优先级 | ☐ W1-W2 全力做 / ☐ 与判官团其他工作并行 |
| **D-J3** | 判官判项异步并行度 | ☐ 维持 4 并发 / ☐ 提到 8（确认 GPU/CPU 不冲突）|

---

## 5. 整体落地路线图（4 周）

### Week 1 · 基建
- [ ] **判官标定集 v1**（30 条，含已知缺陷 + 无该特征各 15 条）
- [ ] `validate_shotlist.py` v1.0（闸门②三闸 + 退出码）
- [ ] `assets/action_anchors/` 目录 + index.json schema
- [ ] T1 装包（同 seed 单变量 A/B 实验）

### Week 2 · 核心能力
- [ ] `auto_copy.py` v1.0（LLM 自动填空）
- [ ] 3D 一致性锚定照派生（基于 TT Image2）
- [ ] 判官新 24 项首期实现（**只告警不否决**）
- [ ] 32 s 档五段比例落地 + 段重排（192×4）

### Week 3 · 集成测试
- [ ] orchestrator 集成 `validate_shotlist.py` + 新判官
- [ ] 动作序列锚定首批 5 个核心动作
- [ ] 端到端实测：32 s + T0 + 段重排 + T1（目标 ≤45 min）
- [ ] 历史 5 套成片逆向回测通过率 ≥85%

### Week 4 · 收尾 + 灰度
- [ ] 跨镜因果链锁（状态字段 + S-09 判项）
- [ ] 标定集 v2（补到 50 条）+ 新 24 项切硬否决
- [ ] 完整 32 s 链路过判官（净耗时比 ≥0.9）
- [ ] **交付**：`部署指南_判官团抽卡生产流水线.md` 升级到 v3.0
- [ ] `带货视频生成SOP.md` 升级到 v2.0（32 s 档 + 4 段方案）

### Week 5+ · 持续
- [ ] GPU 常驻优化（与 15 min 释放规则协调）
- [ ] 8 视角派生（覆盖更多动作）
- [ ] T3 多卡 T3 评估（如预算批准）
- [ ] 完整动作锚定照库（10+ 核心动作）

---

## 6. 风险与边界（诚实清单）

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| **1** | 20 min 硬指标在 3090 单卡不可达 | 用户可能不接受 | 提供诚实底线 35–45 min + 选项（升级 T3 多卡 / 接受 T2 降步）|
| **2** | 3D 锚定照派生质量不稳定 | 脸漂移 / 产品变形 | 严格 L1-01/L1-02 阈值标定；派生失败回退单图生成 |
| **3** | LLM 自动填空产出不合规 | 文案质量波动 | 重试 3 次 + 人工兜底；不参与否决时只告警 |
| **4** | 新增 24 项判项误报高 | 重抽成本 ↑ | 首期只告警不否决；标定集上线后再切硬否决 |
| **5** | 32 s 档 vs 40 s 档用户偏好 | 时长档变更影响全链路 | P-A（74 min）作为兜底；不强行改 32 s |
| **6** | 动作锚定照素材库首批成本 | 5 个核心动作需先人工标定 | W1 启动，W3 交付；可与现有标定工作合并 |
| **7** | 跨镜因果链锁 LLM 推理成本 | LLM 调用 × N 镜 | 用 cheap 模式 + 缓存；失败回退规则推断 |
| **8** | 标定集建设依赖人工 | 30–50 条人工成本 | 复用现有 5 套交付包 + REPORT25/26/28 真缺陷片，半自动 |

---

## 7. 与辉哥拍板清单

| # | 决策点 | 关键选项 | 我的建议 |
|---|---|---|---|
| **D-S1** | 时长档 | 40 s（P-A，74 min）/ 32 s（P-C，45 min）/ 20 min 硬指标（需上 8 卡 T3）| **32 s P-C**（1.9× 提速 + 仍行业主流）|
| **D-S2** | T1 装包风险 | 接受装 2 个包 / 不接受 | 接受（边际收益 +12%，风险可回退）|
| **D-S3** | GPU 常驻 | 接受与 15 min 释放冲突 / 不接受 | 不接受（违反全局规则）|
| **D-S4** | T2 试镜档验证 | 立项测 2 周 / 不测 | **不立项**（优先完成 32 s 档 + 45 min）|
| **D-C1** | LLM 自动填空优先级 | W1-W2 串行 / 先 B 后 A | **W1-W2 串行**（B 必需，A 是效率提升）|
| **D-C2** | LLM 选型 | 复用 tt-5.6-luna / 评估其他 | **复用**（零成本 + 统一密钥）|
| **D-C3** | LLM 输出失败回退 | 重试 3 次后人工 / 重试无限 + 警示 | **重试 3 次**（可控）|
| **D-M1** | 动作锚定照首批覆盖 | 5 个核心动作 / 10 个动作 | **5 个核心**（推近/旋转/光影/开合/静置）|
| **D-M2** | 3D 派生质量阈值 | 严格 / 现状 | **现状**（避免误伤）|
| **D-M3** | 因果链锁 LLM 推理 | 复用 tt-5.6-luna / 纯规则 | **复用**（与 D-C2 一致）|
| **D-J1** | 新增 24 项首期策略 | **只告警不否决（推荐）**/ 直接硬否决 | **告警**（先标定）|
| **D-J2** | 标定集建设优先级 | W1-W2 全力 / 并行 | **W1-W2 全力**（不标定不能上线硬否决）|
| **D-J3** | 判官并发度 | 维持 4 / 提到 8 | **维持 4**（L2 网关思考链易出问题，提并发风险大）|

---

## 8. 总结

**这版方案的 4 个核心深化点**：

1. **速度**：诚实告诉你 20 min 在 3090 单卡不可达，提供 35–45 min 实际可达成路径（P-C：32 s + T0 + 段重排 + T1）
2. **文案**：B 优先（`validate_shotlist.py` 强制闸门②三闸）+ A 接着（`auto_copy.py` LLM 自动填空），复用现有 tt-5.6-luna
3. **动作**：动作锚定照素材库 + 3D 一致性派生 + 跨镜因果链锁（自然度↑、逻辑合理↑）
4. **判官**：18 → 65 项，新增 24 项带货结构硬指标（S/T/C/F 段），首期只告警不否决（避免误伤）

**4 周可交付**，风险可控，关键决策点都列了"我的建议"。

**下一步**：等你拍板 D-S1 / D-C1 / D-M1 / D-J1 这 4 个核心决策，我就可以开始执行。

---

**关联文档**：
- `部署方案与实施进展 20260918` §5 待办
- `REPORT29_带货结构层`（五段式 + S0-S4 工序）
- `REPORT30_音频层与30-50s时长重构`（L/V/N 路由 + 时长档）
- `REPORT31_补L_L镜落点与中文口型标定`（A 段判官）
- `REPORT33_GPU仲裁器队列修复与空闲释放`（GPU 共享纪律）
- `shotlist_schema.json` v2.2（接口契约）
- `judge_config.yaml`（判官配置）
- `templates/`（四个模板）
