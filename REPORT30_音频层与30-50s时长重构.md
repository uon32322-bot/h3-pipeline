# REPORT30 · 音频层（H3 音画同步，两模式自动区分）与 30–50 s 时长重构

> 日期：2026-09-18　｜　触发：辉哥两条要求
> ⚠️ **v2.0 修订（2026-09-18，见 `REPORT31`）**：本文的「**禁 L+V 混用**」已修正为「**音轨单源 + V 镜禁露脸说话**」两条独立约束；
> 运动鞋片由「全 V 片」改为「**V 主体 + 补一镜 L（镜 7）**」。
> 其余结论（三模式 / 三层路由 / 30–50 s 重构 / 五段数学约束 / A 段判官）**不变**。
> ① 「TTS 可以用 H3 自身带的音画同步。两种方式，一种是画外音，一种是口型同步，根据产品以及视频的实际情况进行自动区分。」
> ② 「视频需要与文案内容一致，所以 15s 的视频相对来说较短，正常带货视频应该在 30–50s。」
> 上游依据：`REPORT3`（FL2VA 音画同步实测，5 段 15.2s）｜`REPORT5`（Ref2VA 音频接口实测）｜`REPORT29`（带货结构层）
> 产出：`带货视频生成SOP.md` **v2.0**｜`templates/音频层决策表.md`｜`shotlist_schema.json` **v2.1**｜`scripts/judge_audio.py`

---

## 一、一句话结论

> **H3 在这一环里的角色是「音画同步器」，不是「配音员」。**
> 配音仍由 TTS 出（音色可控）；H3 负责把它**同步进画面**。两种同步方式按「产品需不需要人脸 × 这一镜有没有脸」自动区分；
> 时长从 16.2 s 拉到 30–50 s 后，**五段结构第一次有空间舒展，语速从 5.99 字/秒回到 4–5 字/秒达标区间** ——
> 代价是**单条片期望 GPU 从 ~32 min 涨到 58 / 97 / 123 min（32 / 40 / 50 s）**。

---

## 二、先澄清一个概念（否则方案会走偏）

辉哥说「TTS 用 H3 自身带的音画同步」。这里有两件不同的事，实测把它分得很开：

| 机制 | H3 在干什么 | 台词从哪来 | 音色从哪来 | 实测 |
|---|---|---|---|---|
| **FL2VA `AddGuide(audio)`** | **把已有语音同步进画面**（音频编成 `audio_latent`，每步重新注入、不参与去噪 ⇒ 音轨内容被保留，画面被牵引对齐） | **外部 TTS 配音** | 外部配音，但**音色被重渲染**（波形相关 0.27） | ✅ DTW **0.036** vs 他句 0.20 ⇒ **内容级保留** |
| **Ref2VA `<d>[English]…</d>`** | **自己念台词**（T2VA 路径） | 提示词里的脚本 | 模型自选 | ✅ ASR **5/5 逐字准**；但 `ref_audios` 音色参考**实测零效果**却让算力 **+95~115%** |
| Ref2VA `ref_audios` | 名义上的音色条件 | — | — | ❌ **音轨逐位相同**（两组独立 A/B），源码旁证：只塞了不带数据的占位 |

> ⇒ **结论：生产主链路上，配音必须外部 TTS 出，H3 只做同步。**
> 理由三条：① Ref2VA 链**不锁身份**（REPORT5 §4.3：`ref_image_size=max` 也救不回来）⇒ 已弃为生产主链路；
> ② `ref_audios` 音色条件实测无效；③ 外部 TTS ⇒ **音色 100% 可控**，跨片统一主播声音才有可能。

---

## 三、音频层三模式（不是两种 —— 实测逼出第三种）

| 模式 | 代号 | 画面特征 | 音轨怎么来 | 口型 | 音色 | 成本 |
|---|---|---|---|---|---|---|
| **口型同步** | **L** | 有**可辨识人脸**且在**说话构图**（中近景 / 正侧脸 / 嘴可见） | **H3 锚定**：整条音轨作 `AddGuide(audio)` 锚在生成段首 | 嘴动跟随（实测零时滞 r≈0.44–0.53） | H3 重渲染（波形 0.27） | 锚点单独计价 ≈ 0（100.6 s vs 无锚点 3 s 段同级） |
| **画外音** | **V** | 无脸 / 脸不可辨（**全身远景、运动中**）/ 产品特写 / 空镜 | **后期直贴**：H3 只出环境音，S7 用 ffmpeg 贴 TTS 轨 | 无 | **TTS 音色 100% 保留** | **0**（不锚音频） |
| **纯环境音** | **N** | 需要 ASMR / 拟音 / 氛围的镜（钩子镜、纯视觉镜） | 不贴语音，保留 H3 原生 `overall_soundscape` | 无 | — | 0（提示词声明即可） |

### 3.1 ⭐ 音色矛盾（必须解决的坑，否则整片听感断层）

- H3 锚定会**重渲染音色**（保内容、不保波形）
- ⇒ 若一条片里「部分镜 H3 锚定 + 部分镜后期直贴」，两条音轨音色**不一致** ⇒ 观众听到"换人说话了"
- ⇒ **一致性铁律（全片只走一条路）**：

| 片子类型 | 全片音轨路线 | 得到 | 放弃 |
|---|---|---|---|
| **一旦有任一 L 镜** | **全部含口播的生成段都锚同一条音轨**（同一输入音色 + 同模型 ⇒ 重渲染方向一致，漂移最小） | 音画同步保证 | 1:1 音色（只能"接近"） |
| **全 V 片** | **全片后期直贴**（H3 不锚语音） | 音色完美 + 可随时改文案 + 零生成成本 | 口型（本来也不需要） |

> ⚠️ **禁止混合**（L 镜锚 H3 + V 镜贴后期）—— 除非能接受音色断层。

### 3.2 三层路由（辉哥要的「自动区分」）

```
第一层 · 品类（立项时定，决定"这条片需不需要人脸"）
├─ 产品必须靠「人的脸/身体」才成立？
│   美妆上妆 / 个护上脸 / 服饰上身 / 食品入口 / 健身示范 / 母婴使用
│   ├─ 是 → 片子**必须有至少一镜 L** ⇒ 全片走「H3 锚定」路线
│   └─ 否 → 推荐**全 V**
│       鞋类 / 3C / 家电 / 家居 / 玩具 / 工具 —— 产品本体即主角
↓
第二层 · 逐镜（每一镜单独判）
├─ 这一镜有可辨识人脸吗？
│   ├─ 无（产品特写 / 空镜 / 纯材质） → **V**
│   └─ 有 ↓
│       └─ 处于说话构图吗？（中近景 / 正侧脸 / 嘴可见）
│           ├─ 是 → **L**
│           └─ 否（**全身远景、运动中、背影、侧脸遮挡**） → **V** ⭐
└─ 需要 ASMR / 拟音吗？ → **N**
↓
第三层 · 全片一致性（按 §3.1 铁律收敛到单一路线）
```

### 3.3 ⭐ 为什么「全身远景 + 运动」必须判 V（我们自己的片子正好撞上）

REPORT3 §4.2 实测：口型-音频互相关在**动作幅度小**的段给出**零时滞 r≈0.5** 的干净信号；
在**动作大**的段（手/产品运动与语音时间重叠）r 掉到 0.16–0.36 —— 那是**测量被干扰**，但同时也说明：
**「人在跑 / 在走」的全景镜里，嘴部信号本来就不可辨**。

| 我们的镜 | 判断 | 理由 |
|---|---|---|
| C1 棚拍跑步（全身远景、持续运动） | **V** | 脸小且在动 ⇒ 口型不可辨，做 L 是白付成本 |
| C2 户外步道行走（全身远景） | **V** | 同上 |
| D 片材质特写 / 结构特写 / 收束 | **V** | 无脸 |
| （未来）开箱 / 推荐 / 上脚近景 | **L** | 有脸 + 说话构图 |

> ⇒ **运动鞋这条片天然是「全 V 片」**：零 GPU 增量、音色完美、可随时改文案。
> 这也回答了辉哥"根据产品自动区分"：**鞋不需要人脸，就不该硬做口型同步。**

### 3.4 🚧 两条硬规则（来自实测，不是推测）

| # | 规则 | 依据 |
|---|---|---|
| 1 | **说话时别做别的** —— L 镜不得同时安排大幅手部/产品动作 | REPORT3 §4.2：动作与语音时间重叠 ⇒ 口型测量被干扰（②④⑤ 段 r 掉到 0.16–0.36，静止对照镜恢复到 0.437@0帧） |
| 2 | **L 镜不得跨生成段** —— 一个口型镜必须整镜落在同一生成段内 | 跨段 ⇒ 语音被两次独立重渲染 ⇒ 边界音色 + 口型双断 |

---

## 四、30–50 s 结构重排（五段第一次有空间）

### 4.1 比例表（可缩放到任意目标时长）

| 段 | 带货功能 | 比例 | 下限 | 上限 | **32 s** | **40 s** | **50 s** |
|---|---|---|---|---|---|---|---|
| ① | 钩子 | 8% | 2.5 | 4.0 | 2.56 | 3.20 | 4.00 |
| ② | 痛点 / 场景代入 | 17% | 4.0 | 8.5 | 5.44 | 6.80 | 8.50 |
| ③ | 价值证明 | **37%** | 10.0 | 18.5 | 11.84 | 14.80 | 18.50 |
| ④ | 信任背书 | 20% | 5.0 | 10.0 | 6.40 | 8.00 | 10.00 |
| ⑤ | CTA | 18% | 4.0 | 9.0 | 5.76 | 7.20 | 9.00 |
| | **合计** | 100% | | | **32.00** | **40.00** | **50.00** |

> 对比 v1.0（16.2 s）的旧预算：① 3.04｜② 3.04｜③ 7.10｜④ **1.60**｜⑤ 1.43 ——
> **④ 信任段只有 1.6 s 装不下「证据 + 小缺点」**，那是硬凑出来的；30–50 s 后 ④ 拿到 6.4–10 s，问题消失。

### 4.2 口播字数（≤ 段时长 × 5 字/秒，防语速过快）

| 段 | 32 s | 40 s | 50 s |
|---|---|---|---|
| ① 钩子 | ≤13 | ≤16 | ≤20 |
| ② 痛点 | ≤27 | ≤34 | ≤43 |
| ③ 证明 | ≤59 | ≤74 | ≤93 |
| ④ 信任 | ≤32 | ≤40 | ≤50 |
| ⑤ CTA | ≤29 | ≤36 | ≤45 |
| **合计** | **≤160** | **≤200** | **≤250** |

**语速校验对照**：

| 版本 | 字数 | 时长 | 语速 | 判定 |
|---|---|---|---|---|
| 现状 v2.0 | 97 | 16.208 s | **5.99 字/秒** | ❌ 偏快（行业 4–5） |
| 30 s 档 | 120–150 | 30 | 4.0–5.0 | ✅ |
| **40 s 档（推荐）** | **160–200** | 40 | **4.0–5.0** | ✅ |
| 50 s 档 | 200–250 | 50 | 4.0–5.0 | ✅ |

> ⇒ **辉哥的直觉被数字证实**：16 s 装五段必然超速；40 s 是语速、结构舒展度、成本三者的平衡点。

### 4.3 续命钩（>20 s 必须补，旧 SOP 提过但从未落地）

| 目标时长 | 续命钩位置（40–60%） | 落在 |
|---|---|---|
| 32 s | 12.8–19.2 s | ③ 价值证明中段 |
| **40 s** | **16.0–24.0 s** | ③ 价值证明中段 |
| 50 s | 20.0–30.0 s | ③ 价值证明中段 |

**③ 段内部结构（新）**：前 60% 承接主卖点（慢说明），后 40% 制造第二高潮（换景别 / 换场景 / 揭晓新证据）。

---

## 五、算力账（30–50 s 不是线性外推）

### 5.1 标度

实测锚点（768×1344 / 8 步 / 3090 显存受限 15.4 GB）：**124 帧 = 431 s**，**243 帧 = 1325.5 s** ⇒

```
单段耗时 t(f) ≈ 431 × (f / 124)^1.70  秒     f = 帧数（贴 17k+5 网格）
```

| 帧 | 秒 | 单段耗时 | **每 1 秒成片要多少 GPU** |
|---|---|---|---|
| 73 | 3.04 | 2.9 min | **57.6 s** |
| 124 | 5.17 | 7.2 min | 83.4 s |
| 192 | 8.00 | 15.1 min | 113.3 s |
| 243 | 10.13 | 22.5 min | 133.6 s |
| 362 | 15.08 | 44.4 min | **176.6 s** |

> ⚠️ **两个未测边界**：① >243 帧是**外推**；② 431 s 是**显存受限 15.4 GB** 下的值，独占 24 GB 应更快（未测）。
> ⇒ 上表是**上限估计**，实际可能明显更好。

### 5.2 ⭐ 结论会随成本模型翻转（必须画清边界）

**模型① · 允许逐段重抽**（我们的既有流程）：期望成本 = `Σ t_i / 0.875`（单段过率 `1−(1−0.5)^3`）
**模型② · 要求一次过**（不许重抽）：期望成本 = `Σ t_i / 0.875^S`

| 目标 | 段数 S | 分段（帧） | 时长 | 原始 | **模型①** | 模型② |
|---|---|---|---|---|---|---|
| 32 s | 3 | 243+243+260 | 31.08 s | 70.4 min | 80.4 min | 105.1 min |
| 32 s | **5** | **141+141+158+158+158** | 31.50 s | 50.4 | **57.6** | 98.3 |
| 32 s | 6 | 124×6 | 31.00 s | 43.1 | 49.3 | 96.0 |
| **40 s** | 3 | 311+311+311 | 38.88 s | 102.9 | 117.6 | 153.6 |
| **40 s** | **4** | **226+226+243+243** | 39.08 s | 84.9 | **97.1** | 144.9 |
| **40 s** | 5 | 175+192+192+192+192 | 39.29 s | 73.3 | 83.8 | 143.0 |
| 50 s | 4 | 294×4 | 49.00 s | 124.7 | 142.5 | 212.7 |
| **50 s** | **5** | **226+226+243+243+243** | 49.21 s | 107.5 | **122.8** | 209.6 |

**⇒ 读法**：
- **模型①（可重抽）下，段越短越省**（超线性成本 + 每段独立重抽，没有"全片一次过"的指数惩罚）
- **模型②（一次过）下，段越少越省**（S 是指数项）
- 两个模型**结论相反** ⇒ **分段数不该由成本决定，应由内容决定**（哪几镜必须同段才连贯）
- 唯一确定的是**总量**：16.2 s ≈ 32 min → **32 s ≈ 58 min / 40 s ≈ 97 min / 50 s ≈ 123 min**（模型①口径，约 **2–4 倍**）

### 5.3 五条降本杠杆（按性价比排）

| # | 杠杆 | 收益 | 代价 |
|---|---|---|---|
| 1 | **音频先行**（TTS 先出，段长由音轨定） | 避免"画面生成完才发现口播装不下"的整段返工 | **工序次序改动，零成本** |
| 2 | **分段判官**（一段生成完立即判，不等全片） | 早发现早重来 | 零（判官本就藏在生成后面） |
| 3 | **一条片内多镜**（`[Shot n] At 00:0X.XXX`） | 减 S 不减镜数 | 零 |
| 4 | **只对不通过的段重抽** | 期望成本 ÷ 1.14 而非 × S | 零（既有流程） |
| 5 | 草稿档 0.4 MP 先验结构 | ❓**未测**（耗时是否与像素线性，我们没有数据） | 草稿不能替代正片 |

---

## 六、判官团必须扩展：新增 **A 段（音频）**

**现状缺口（本轮扫描发现）**：`judge_shot.py` 的音频判项只有两个 —— `L0-05` 音轨非静音、`L0-07` 长静音。
**没有任何判项在检查「口播说的是不是我们的文案」**。音频层一旦上线，这就是最大的判官空洞。

| 判项 | 方法 | 阈值 | 成本 | 状态 |
|---|---|---|---|---|
| **A-01** 音轨存在且非静音 | 能量均值 | > −50 dB | <1 s | ✅ 复用既有 L0-05 |
| **A-02** 口播内容与文案一致 | `faster-whisper` **CPU int8** 转写 + 文本相似度 | ≥ 0.90（容同音误差） | 数秒 | 🆕 **需标定** |
| **A-03** 口型-语音同步 | 脸区搜索 + 互相关峰值与时滞（`lipsync2` 算法） | r ≥ 0.30 且滞后 ≤ 2 帧 | 数秒 | 🆕 **需标定**；**仅 L 镜**，V/N 镜判 `na` |
| **A-04** 语音在段内结束 | 能量包络末活跃点 vs 段长 | 尾部空闲 ≤ 1.0 s、无跨段截断 | <1 s | 🆕 纯算术 |
| **A-05** 跨段音色一致 | 各段 F0 中位数 + 频谱质心 | 相邻段 \|ΔF0\| ≤ 15% | <1 s | 🆕 **需标定**；多段才启用 |

### 6.1 三条纪律（不能忘）

1. **能用算术判的不要问 VLM** ⇒ A 段**全部本地 CPU**，一项都不进 L2
2. **ASR 必须 CPU int8** —— `faster-whisper` 在 4090 上会 **OOM**（REPORT5 §6.3 实测）⇒ **GPU 隔离红线延伸：TTS 与 ASR 都不得占 3090 的 CUDA**
3. **新增判项必须两头标定才能上线**（R-08 的教训）⇒ `judge_audio.py` 输出里带 `"uncalibrated": true`，
   标定前**只报数值、不参与否决**；`--calib` 模式可批量出数值用于定阈值

### 6.2 与视觉判项的分工

| 判官 | 覆盖 | 否决权 |
|---|---|---|
| **L0/L1**（算术） | 画面技术 + A-01/A-04 | 有 |
| **L2**（`tt-5.6-luna`，VLM） | R/W/P 段视觉 | 有 |
| **A 段**（新增，CPU） | 音频内容 + 口型 + 音色 | 标定后启用 |

---

## 七、工序次序改动（本次最重要的一条）

**旧次序**：锚定照 → H3 生成 → 判官 → 后期配 TTS
**新次序**：

```
S2 文案定稿 → S3 四分流 → ⭐ TTS 出整条音轨 → S4 分镜（段长由音轨定）
→ S1.5 生成分组 → S6 生成（L 片锚音轨 / V 片不锚）→ 判官（含 A 段）→ S7 装配
```

**为什么必须"音频先行"**：
- 段长由**音轨**决定（不是反过来）⇒ 口播句在哪一秒结束，决定生成段切在哪一帧
- H3 的音频锚点是"**锚在帧索引上、裁到视频剩余时长**"⇒ **音轨时长必须先定**
- 否则：画面生成 97 min → 发现口播装不下 → **整段重跑**

---

## 八、待实测项与风险（诚实边界）

| # | 事项 | 影响 | 处置 |
|---|---|---|---|
| 1 | **中文口型同步未验证** | REPORT3/5 全程只用英文。中文语音 + 中文口型 = **零数据** | 若要上 L：先跑一次标定（中文配音 + 静止脸对照镜 + `lipsync2.py`） |
| 2 | **H3 中文语音质量未验证** | 若走 `<d>` 自生成路线 | 生产主链路**不走** `<d>`（外部 TTS 更可控）⇒ 风险已绕开 |
| 3 | **>243 帧耗时是外推** | 30–50 s 必然用到 294–362 帧 | 上产线前跑一次 362 帧实测校准 |
| 4 | **多音频锚点叠加未验证** | 若想"一条段内多个口播锚点" | 本期方案**规避**：整条音轨作**单锚点**挂段首 |
| 5 | **独占 24 GB 的真实速度未测** | 5.3 表的绝对值可能偏悲观 | 编队独占时复测 124 / 243 / 362 帧三点 |

---

## 九、验收清单（本轮改动）

- [x] 概念澄清：H3 = 音画同步器 ≠ 配音员（`ref_audios` 无效 / Ref2VA 不锁身份 双重依据）
- [x] 音频层**三模式**（L 口型同步 / V 画外音 / N 纯环境音）+ **三层路由决策树**
- [x] **音色一致性铁律**：**音轨单源**（⭐ v2.0 修正：混用的真实危险是「**两条音轨来源拼接**」，不是「L/V 画面驱动方式共存」—— 见 `REPORT31` §五）
- [x] 两条硬规则：说话时别做别的｜L 镜不跨生成段
- [x] 五段**比例表**（含上下限）+ 口播字数表 + 续命钩位置
- [x] 算力账（两模型**结论翻转**已画清边界）+ 五条降本杠杆
- [x] 判官新增 **A 段 5 项**（全 CPU，标定后启用）
- [x] 工序次序改为**音频先行**
- [x] 5 条待实测项显式标注
- [ ] 辉哥拍板：目标时长档（32 / **40** / 50 s）
- [ ] 辉哥拍板：运动鞋走**全 V 片**（推荐）还是补一镜 L

---

## 十、落地验证（2026-09-18 · 运动鞋 40 s 实例全链路走通）

> 交付物目录：`deliverables/shoe_40s/`
> 目的：把上面所有纸面设计**跑一遍真实数据**，看哪些是错的。

### 10.1 真实音轨已产出（不是估算）

| 项 | 实测值 |
|---|---|
| 文件 | `vo/track_full.wav`（32000 Hz 单声道，**40.000 s**，误差 **0.0001 s**） |
| 逐句时间轴 | `vo/track_manifest.json`（6 句，起止全部实测） |
| 音色 / 语速 | Tingting(zh_CN) / `say -r 240` ⇒ **4.30 有效字/秒**（172 字） |
| 响度 | **-16 LUFS / 真峰 -1.5 dBTP**（`loudnorm`） |
| 排版 | 头留白 0.400 + 句间 0.546×5 + 尾留白 0.600 = 40.000 |
| A 段复核（`verify_track.py`） | 6/6 句窗口 RMS ≈ -15.5 dB ✅；7/7 静音窗口 **-180 dB**（数字零）✅ |
| 运行位置 | **本地 Mac**，零 3090 占用 ✅ |

### 10.2 ⚠️ 三处把设计文档写错的地方（本轮修正）

| # | 旧写法 | 错在哪 | 已改为 |
|---|---|---|---|
| 1 | 「帧数必须贴 `17k+5` 网格」并举 **960 帧 = 40.000 s** 为例 | **960 不是合法帧数**（960 mod 17 = **8**，而 `17k+5` 恒 ≡ 5）。合法值应是 957 / 974 | **只有「段」必须贴网格；成片总长 = 各段之和，不必贴网格。** 960 由 5 段凑出：`243+362+175+107+73` |
| 2 | 网格列举 `39/56/73/107/124/141/192/226/243/294/328/362` | **漏了 8 个合法值**（22 / 90 / 158 / 175 / 209 / 260 / 277 / 311 / 345） | 补全 21 个（22 → 362） |
| 3 | 「口播字数 = 时长 × 4–5」 | **没定口径**。含标点计数会虚高 15–20%，实测语速掉到 3.5 字/秒 | 口径钉死 = **有效发音字数（不含标点/空格）** |

### 10.3 ⭐ 分段数是**被数学约束**的，不是拍脑袋

```
Σ(17k_i + 5) = T   ⇒   5n ≡ T (mod 17)   ⇒   n 被 T 唯一约束
40 s / T = 960：5n ≡ 8  ⇒ n ≡ 5 (mod 17) ⇒ 唯一实用解 n = 5，Σk = 55
32 s / T = 768：5n ≡ 3  ⇒ n ≡ 4 (mod 17) ⇒ n = 4
50 s / T = 1200：5n ≡ 10 ⇒ n ≡ 2 (mod 17) ⇒ n = 2 不可行（每段 600 > 362 上限）
                ⇒ 50 s 档让各段之和 ≥ 1200 并装配时裁尾，或改总长 1195（n = 6）
```

**40 s 五段方案**（正好凑满 960，无需裁切）：

| 段 | 帧 | 时长 | 含镜 | 耗时 |
|---|---|---|---|---|
| G1 | 243 | 10.125 s | 镜 1–3（钩子 + 痛点） | **22.1 min**（实测 1325.5 s） |
| G2 | 362 | 15.083 s | 镜 4–6（价值证明 + **续命钩** + 上脚） | 43.0 min（外推） |
| G3 | 175 | 7.292 s | 镜 7（信任背书） | 12.8 min |
| G4 | 107 | 4.458 s | 镜 8（CTA 收束 + 英文） | 5.6 min |
| G5 | 73 | 3.042 s | 镜 9（CTA 定格） | 2.9 min |
| | **960** | **40.000 s** | | **≈ 86.4 min** |

> 4 段方案无解：`17Σk + 20 = 960 ⇒ Σk = 55.29` 非整数。

### 10.4 单段耗时公式重拟合（旧公式高估 2%）

```
旧：t(f) ≈ 431 × (f/124)^1.70      → t(243) = 1353 s（实测 1325.5 s，高估 2.1%）
新：t(f)  = 431 × (f/124)^1.6697   → t(243) = 1325.6 s ✅ 两端精确
     指数由两点反解：P = ln(1325.5/431) / ln(243/124) = 1.6697
```

### 10.5 ⭐ 「有真人出镜」≠「要 L 模式」（本片最关键的判断）

镜 6 有模特上脚，但取景**腰部以下、脸出框** ⇒ **仍判 V**。

| 判断 | 视觉效果 |
|---|---|
| 判 V（本片） | 全片后期直贴 ⇒ **音色 100% 保留 + 零口播生成成本 + 改文案不用重跑** |
| 若判 L | 需**中文口型同步标定**（当前零数据）+ H3 音频锚定 + 音色被重渲染（波形相关仅 **0.27**） |

> ⇒ **一个取景决策，绕掉了整条未验证链路。** 这是「按产品与视频实际情况自动区分」的真正落点。

### 10.6 另外两条设计规则（本轮新增）

| # | 规则 | 依据 |
|---|---|---|
| 1 | **痛点段不拍 Before/After** | AI 无法生成真实旧鞋磨损 ⇒ 镜 2/3 改为「同棚近地低角度 + 结构特写」，**痛点全由口播承担** |
| 2 | **场景变更只允许发生在段边界** | 段内换场景会让「片内多镜」变成视觉跳变；段边界本来就是硬切点，换景免费 |

### 10.7 macOS `say` 语速基准（做占位音轨用）

| 设置 | 有效字/秒 | 结论 |
|---|---|---|
| 默认（`-r` 不指定） | **3.55** | ❌ 比真人带货口播慢约 20% ⇒ 40 s 档会被迫删文案 |
| `-r 240` | **4.42** | ✅ **推荐**，落 4–5 区间 |
| `-r 280` | 5.14 | ⚠️ 偏快 |

> 可用中文语音：Tingting(zh_CN) / Meijia(zh_TW) / Sinji(zh_HK) + Eddy·Flo·Sandy·Shelley 等（zh_CN/zh_TW 各一套）。
> 实测本机**全部能出声**（此前记录的「只有 Tingting/Meijia 有效」不成立）。

### 10.8 本轮新增可复现产物

| 文件 | 作用 |
|---|---|
| `deliverables/shoe_40s/build_vo_track.py` | 音轨先行构建器：合成 → 语速对齐 → 留白求解 → 响度归一 → 出 manifest + 闸C 报告 |
| `deliverables/shoe_40s/verify_track.py` | 音轨 ↔ manifest 一致性复核（A-01 / A-04 实测） |
| `deliverables/shoe_40s/cost_model.py` | 时长档 × 分段方案成本模型（含网格合法性求解） |
| `deliverables/shoe_40s/带货文案_运动鞋40s.md` | 172 有效字 / 4.30 字/秒 / 五段预算 |
| `deliverables/shoe_40s/音频层决策表_运动鞋40s.md` | 9 镜全 V 判定 + 音轨清单 + 闸C |
| `deliverables/shoe_40s/分镜脚本_运动鞋40s.md` | 15 列执行表 + 5 段生成分组 + 锚定照分配 |

