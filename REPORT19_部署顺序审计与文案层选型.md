# REPORT19 · 部署顺序审计 + 文案脚本层选型

> 日期：2026-09-17
> 起因：辉哥三问 —— ① 部署顺序有没有错误？② 要确保部署完后「质量有保证 + 速度快 + 判官团不影响质量与速度」；③ 带货文案/脚本用另一个专门项目做（提到「千川」）
> 性质：**部署前的顺序审计**，含现状复核、9 个问题、修正后的施工顺序、三条硬约束的实现方案、文案层选型与集成架构
> 原则：实测 ✅ / 推算 ❓ / 未测 ⚠️ 分开标注

---

## Part A · 现状复核（2026-09-17 13:40 实测）

**先说结论：指南里有 4 条现状描述已经过期，其中 1 条是阻塞项、2 条是丢失项。**

| 项 | 指南假设 | 实测现状 | 判定 |
|---|---|---|---|
| ComfyUI 内核 | 0.33.0，待升 **0.36.0** | **已是 0.36.0** ✅ | Phase 0 ⑥b **已完成**，可划掉 |
| 项目目录 | `/root/autodl-tmp/h3p/` 已建 | ❌ **不存在**（已删除） | **需重建**（隔离目录成果丢失） |
| 目录结构 | h3p/{comfy,models,input,output} | 新结构：`/root/ComfyUI` + `autodl-tmp/comfy-{input,output,placeholder,temp}` | ⚠️ **结构已变**，指南要对齐 |
| 官方 8 步 Turbo LoRA | 已下载校验（1,956,193,000 B） | ❌ **丢失**（随 h3p 一起没了；现存只有 `..8step..768p..resized_avg_rank_64..` 971 MB 降秩版） | **需重下**（1.83 GB，约 2 分钟） |
| 社区污染节点 | 需清除 | ✅ **已清除** —— `MiniMaxH3TurboLoRA` / `H3SparseAttentionAdvanced` / `H3MemoryOptimization` 全部不存在 | **环境已达标**（比计划更干净） |
| 系统盘 `/`（30 G） | 100% 满，剩 172 MB ⚠️ 阻塞 | **41%，18 G 可用** ✅ | 阻塞**已解除** |
| 数据盘 | 97 GiB 可用 | **144 G 可用** ✅ | 充足 |
| GPU | — | RTX 3090 **空闲 23.2 GB**，利用率 0% | 可用 |
| sageattention | 未装 | **仍未装** | 与指南一致 |
| 现有节点 | — | H3 核心 4 件 ✅ + 官方内核加速节点 6 件 ✅ + 社区包 3 个（ALLinONE / H3-Motion-Context / VideoHelperSuite） | 见 Part B §3 |

**今日产出（说明有人已在跑 H3）**：`comfy-output/video/` 有 5 个 mp4（12:09–12:58），含 `chain_test`（链式）、`va_…_s1/s2_clip1`（**两段 + 音视频**）；`h3_context/` 有 3 个 24 MB latent 存档。

**另注**：同实例还跑着 `tts-gateway`（PID 130844，占显存 466 MB）—— **同机多服务已是既成事实**，这直接关系到 Part B 的判官隔离（问题 S2）。

---

## Part B · 部署顺序审计：9 个问题

### 结论摘要

| # | 级别 | 问题 | 修正方向 |
|---|---|---|---|
| **S1** | 🔴 **严重** | **提速 A/B 排在资产库之后** → 资产库必然作废 | A/B 前移到环境冻结前 |
| **S2** | 🔴 **严重** | **判官执行位置没写死** → 会抢爆 3090 显存，直接拖慢生成 | 判官三级全部**出 GPU** |
| **S3** | 🟠 中等 | 抽卡参数依赖未测的 p → 上线时无参数可用 | Phase 4 只交付机制，默认 **N=1** |
| **S4** | 🟠 中等 | 判官标定有**鸡生蛋依赖**（阈值要样本、样本要生成） | 新增 Phase 3.0 人工标定集 |
| **S5** | 🟠 中等 | 提速 A/B **只测速度不测质量** → 可能"提速却降良率" | 三层联合验收 + 净收益公式 |
| **S6** | 🟡 轻微 | Phase 0 ⑧ 与 Phase 6b **重复定义** A/B | A/B 只在 Phase 0 跑一次 |
| **S7** | 🟡 轻微 | 缺「**环境冻结**」里程碑 | 新增 Phase 0.5 |
| **S8** | 🟡 轻微 | 判官自身**变严无人知**（版本漂移） | 换版本必须跑回归 |
| **S9** | 🟡 轻微 | Phase 4 早于 Phase 5 → **判官不完整时抽卡已上线** | 明确"L2 未上线期间的盲区" |

---

### S1 🔴 提速 A/B 的顺序倒了 → 资产库必然作废

**现状顺序**：
```
Phase 0（装加速件）→ Phase 1（建种子资产库 ⭐）→ Phase 2 → Phase 3 → Phase 4 → Phase 5 → Phase 6b（逐层提速 A/B 定档）
```

**问题**：Phase 1 建资产库时，**环境还没定档**。Phase 6b 一旦调整（换节点包 commit、开关某一层加速、禁用某节点），资产库里所有 `(prompt, seed)` 对的复现性**立即失效** —— 因为「同 seed 逐位复现」这条铁律的前提是**环境逐位相同**。

指南 Phase 1 现有那条注释（"提速会改变资产有效性，旧资产要标记基于旧环境"）是**事后补救**，不是顺序正确。按现顺序走，等于**先建一间要拆的房子**。

**修正后的顺序**：
```
Phase 0  环境与链路整改（含 8 步 LoRA 重下 + 加速件安装）
Phase 0b ⭐ 逐层提速 A/B（A0–A8）→ 定档
Phase 0.5 ⭐ 环境冻结（Environment Freeze）  ← 新增里程碑
Phase 1  可复现性基建 + 种子资产库（此时环境已冻，建的库才站得住）
Phase 2 → 3 → 4 → 5
Phase 6  判官标定（p / D / J）—— 只标判官，不再动环境
```

> **一句话**：**先冻结环境，再建资产。** 顺序颠倒的代价是全部资产重跑。

---

### S2 🔴 判官的执行位置没写死 → 会拖慢生成（辉哥要求 ③ 的核心）

**现状**：Phase 3 写「本地 CPU」（✅ 安全），但 Phase 5 写 L2 模型「**本地 Qwen3-VL-8B** 或 API」—— 这一条是危险源。

**为什么危险（算术）**：

| 组件 | 显存需求 | 说明 |
|---|---|---|
| H3 FL2VA 生成（int8 convrot） | **≈ 20 GB** | 权重 20.97 GB（int8）+ 激活 |
| 本地 Qwen3-VL-8B（fp16） | **≈ 16 GB** | 若跑在 fp16 |
| 3090 总显存 | **24 GB** | — |

⇒ **两者根本装不下**。若强行同时驻留，ComfyUI 会触发显存交换 / OOM 重试，**生成时间可能翻倍甚至失败**。而且这台实例**已经跑着 tts-gateway**（实测占用 466 MB）与另一条线的 qwen3:14b 需求——**同机争抢是既成事实，不是假设**。

**修正（写成硬规范）**：

| 判官级 | 执行位置（强制） | 资源边界 |
|---|---|---|
| **L0** 技术校验 | 任意 CPU（含 3090 机） | <1 s/条，纯算术，不占 GPU |
| **L1** 专用检测器 | **本地 Mac** ⭐ 首选；或 3090 机 CPU | **限核**（`nice` / `cpulimit`），给 ComfyUI 留 ≥4 核做数据加载；**禁用 GPU 版本**（人脸嵌入/光流一律 CPU 实现） |
| **L2** VLM 判官 | **远端 VLM API** ⭐ 唯一选项 | **绝不在 3090 上跑本地 VLM** |

> **红线（三条）**：
> 1. **GPU 机上不得驻留任何判官模型权重**；
> 2. 判官**不得占用 3090 的 CUDA**（含 L1 的 GPU 加速实现）；
> 3. 判官与生成之间必须**异步解耦**（生成完立即投递判官、不等结果，继续跑下一段；判官结果回来才决定是否重抽）。

**"不影响速度"的真正机制**（这条要讲清楚）：

判官不是"零成本"，而是**延迟被隐藏**。前提是它比"下一段的生成时间"短：

| 环节 | 耗时 | 是否与生成并行 |
|---|---|---|
| 单段生成（15 s 段 @3090） | 9.4–11.4 分钟 ❓ | — |
| L0 | <1 s | ✅ 并行 |
| L1 | 5–20 s/段 | ✅ 并行 |
| L2（20 判项 × 1 次调用/项） | **必须并发**：5 路 × 4 s ≈ 16 s；若串行 20×4 s = 80 s | ✅ 并行 |

⇒ **只要判官不出 GPU 且 L2 并发，判官耗时可被完全隐藏。** 反之，若 L2 用本地 VLM，隐藏立刻失效——它会把 10 分钟的生成拖成 20 分钟。

> ⚠️ **唯一的例外：抽卡**。判官否决后的重抽时间是**净增加**的（不可隐藏）。所以"判官不影响速度"只在**通过率 p 足够高**时成立——这再次印证三档杠杆的排序：**提 p 比抽卡值钱**。

---

### S3 🟠 抽卡参数依赖未测的 p → 上线时无参数可用

**现状**：Phase 4（抽卡引擎）上线时，p 尚未测（Phase 6 才测）。§3.1 说"按 p 定策略"，但 p 未知时 N 无据可依。

**修正**：Phase 4 **只交付机制**（N 可配置、保底封顶、段级冻结），**默认 N=1（即不抽卡）**。Phase 6 测出 p 后，按 §3.1 表开 N。

> 理由：用拍脑袋的 p 定 N，会烧掉一批算力才发现真实 p 很低。**默认关，测完再开**是唯一安全的做法。

---

### S4 🟠 判官标定的鸡生蛋依赖

**现状**：Phase 3 说 L1 阈值「用历史人工判定样本标定」—— 但历史样本来自**已废弃的社区链路**，代表性不足；且量少。

**修正**：新增 **Phase 3.0「人工标定集建设」**：
```
跑 30–50 条段（覆盖 5 类随机型崩坏 + 2 类结构型）
  → 全部人工判定（是/否 + 缺陷位置）
  → 形成正负对照标定集（含通过样本 + 典型失败样本）
  → 用它标定 L1 阈值、校准 L2 分数映射
  → 才允许启用自动判官
```

> 这个集子同时是 **Phase 6 实验 A** 的输入，一举两得。顺序：**生成样本 → 人工标注 → 标定判官 → 启用判官**。

---

### S5 🟠 提速 A/B 只测速度不测质量 → 可能"提速却降良率"

**现状**：Phase 6b 表格里 A2/A4 只写"眼睛/细节检查"，A5 写"帧差应 ≈2.4e-4"。没有统一的联合验收。

**修正**：每层 A/B **必须同时记三项**：

| 记录项 | 目的 |
|---|---|
| ① 耗时 | 提速了多少 |
| ② 同 seed 帧差 | 画质有没有变（对照噪声基线 1.08） |
| ③ **判官通过率 p 的变化** | **良率有没有掉** |

**净收益公式（新增，务必算这个账）**：

```
抽卡期望次数  E(p) = [1 − (1−p)^N] / p
净耗时比 = (单次耗时_new / 单次耗时_base) × (E(p_new) / E(p_base))
```

**举个反例**：某层提速 25%，但把 p 从 0.50 压到 0.35：

| | p=0.50 | p=0.35 |
|---|---|---|
| E（N=3） | 1.75 次 | 2.28 次 |
| 相对倍数 | 1.00 | **1.30 ×** |

⇒ 净耗时 = 0.75 × 1.30 = **0.975**，只快 2.5%，**几乎白忙**，却额外承担了画质风险与复现性代价。

> 这条是本报告最实用的判据：**只看单次速度会得出错误结论**。每层加速都必须过"净耗时"这关。

---

### S6 🟡 A/B 重复定义

Phase 0 第 ⑧ 步写「A0–A8（见 Phase 6）」，Phase 6b 又完整定义一遍。**修正**：明确 **A/B 只在 Phase 0b 跑一次**（环境冻结前）；Phase 6b 改名为「提速档位复核（仅当环境变更时触发）」。

---

### S7 🟡 缺「环境冻结」里程碑

**修正**：新增 **Phase 0.5 环境冻结**，冻结内容四项：

- [ ] ComfyUI 内核版本号
- [ ] 加速档（T0/T1/T2 中的哪个）+ 每个开关的取值
- [ ] `custom_nodes` 每个包的 **commit SHA**（写入 `env_manifest.txt`）
- [ ] ComfyUI 启动参数（逐字记录）

> 之后任何变更 → 走「重新冻结」流程，并**评估资产库失效范围**。没有这个里程碑，"可复现"只是口号。

---

### S8 🟡 判官自身变严无人知

**修正**：**判官版本变更（换 VLM 版本 / 增删判项 / 改阈值）必须跑回归** —— 用 Phase 3.0 的标定集测**假阳率与假阴率**，两个数都不得恶化。否则"判官悄悄变严"会让产线良率莫名下降，且极难归因。

---

### S9 🟡 Phase 4 早于 Phase 5 的盲区

**现状**：抽卡引擎（Phase 4）上线时，仅有 L0/L1，L2 还没有。

**修正（写明盲区）**：L2 未上线期间，抽卡判据只含 L0（技术）+ L1（脸/光流/调色/OCR/唇音），**以下缺陷不会被拦**：构图偏移、纹理闪烁、短英文文字糊、接缝色调。

⇒ 因此 Phase 4 期间的产出应视为**试产**，不进入正式交付；或显式接受这些缺陷。

---

### 修正后的完整施工顺序

```
Phase 0    环境与链路整改
           ├─ 重建项目目录（自持，零软链）
           ├─ 重下官方 8 步 LoRA + sha256 校验
           ├─ 装 T0 加速件（零安装项）
           └─ 对齐新目录结构（comfy-input/output/placeholder/temp）
Phase 0b ⭐ 逐层提速 A/B（A0–A8）→ 定档   ← 从 Phase 6b 前移
Phase 0.5 ⭐ 环境冻结（4 项）              ← 新增里程碑
Phase 1    可复现性基建 + 种子资产库        ← 此时环境已冻，库才站得住
Phase 2    三道闸门
Phase 3.0 ⭐ 人工标定集（30–50 条）        ← 新增
Phase 3    L0 + L1 判官
Phase 4    抽卡引擎（默认 N=1，机制先行）
Phase 5    L2 VLM 判官（远端 API）
Phase 6    判官标定（p / D / J）→ 按 p 开 N
Phase 6b   提速档位复核（仅环境变更时触发）
```

---

## Part C · 文案脚本层选型

### C.1 「千川」系的搜索结果（诚实说明）

**搜遍 GitHub，「千川」系项目基本都是投流/素材分析工具，没有专门做「带货文案脚本生成」的。** 完整结果：

| 项目 | ★ | 定位 | 与「文案/脚本」的关系 |
|---|---|---|---|
| `zhujiushequn/juliang-qianchuan-auto-ads-v1` | 50 | 巨量千川自动投流 | ❌ 投放 |
| `fengx8534-tech/qianchuan-ads-automation` | 32 | 千川自动化（Chrome CDP + DeepSeek） | ❌ 直播投放调控 |
| `wu-shaobing/go_qianchuan` | 25 | 千川投放智能助手 | ⚠️ 提到"AI生成视频脚本"，但主体是调价扩量 |
| `liberalwhy/AI-qianchuan-video-agent` | 13 | 千川视频自动化**分析** | ❌ 分析 |
| `bianzigege/qianchuan-material-breakdown-skill` | 9 | 千川素材结构**拆解** Skill | ⚠️ 拆解 ≠ 生成 |
| `3132882323-cyber/douyin-qianchuan-creative-director-agent` | 8 | 千川素材编导侧边栏 | ⚠️ 复盘/矩阵 |

> 若辉哥记忆中的「千川项目」是上述某一个，可直接指认，我再深挖。但就"专门做带货文案脚本生成"这个需求而言，**最匹配的不是千川系**，而是下面这个。

### C.2 最匹配：ClipForge（原「带货剪手」）

| 项 | 值 |
|---|---|
| 仓库 | `xixihhhh/clipforge` |
| Star / Fork | **828 / 179** |
| 语言 / 许可 | TypeScript / **AGPL-3.0** |
| 活跃度 | 创建 2026-03-23，**最后提交 2026-09-16**（昨天） |
| 定位 | 「上传一张商品图 → AI 提炼卖点 · 写种草脚本 · **锁定商品原图不变形** · 配音 + 字幕 + BGM → 几十秒产出抖音小店/快手/小红书/视频号/TikTok Shop 带货视频」 |

**它最值钱的三块（与我们的需求精准对应）**：

| # | 能力 | 对我们的价值 |
|---|---|---|
| 1 | **`clipforge_generate_script`**（MCP 工具）—— 「只生成**去商品化**旁白分镜脚本（含英文素材检索词）」 | ⭐ **这正是我们要的"文案+脚本"接口**：输入带商品信息的 brief，输出可供 H3 用的分镜脚本 |
| 2 | **`script-judges` Skill**（四位判官：节奏 / 口语 / 创意 / 结构，另有第五位画面官） | ⭐ **纯文本、无二进制、任何 agent 宿主都能跑** —— 可直接装进 WorkBuddy，成为我们流水线的「**文案闸门**」 |
| 3 | **`clipforge_ingest_product`** —— 贴商品链接抓标题/价/图（JSON-LD > OG > Twitter > 标题） | 替代我们"识别产品 + 产品事实摘要"的人工步骤 |

**它另外拿得出手的（可借思路）**：
- **钩子库 16 款**（含信任型/社证型：自曝反转、认缺点开场、淘汰式测评、成本拆账…）+ 留人转化硬规则（商品实体前 3 秒入画、超 20 秒必有 40–60% 处续命钩、CTA 双落位、紧迫感分档）
- **爆款复刻**：ffmpeg 真实解析参考视频**镜头切点**出节奏骨架 —— 与我们"标准视频只提取结构化规格"的结论**完全一致**
- **动作可生成性硬判据**：双主体精确交互 / 内部心理 / 否定式动作 / 一拍三步动作链 **四类禁写** —— 与我们**闸门②同源**，可互相印证
- **分镜三硬规则**：商品关键动作唯一主落实镜、相邻两镜至少变一项、景别由必看信息尺度决定
- **情绪写法铁律**：禁终态词，一律「触发 → 身体先反应 → 面部克制」三拍过程
- **废片六症状单变量重投**：脸崩/皮肤蜡感/背景死/光不对/模糊/商品变形 —— 与我们的"每次只改一个变量"铁律一致
- 合规默认开启：GB45438 AIGC 显式+隐式双标识、广告法违禁词扫描

### C.3 ⚠️ 三条必须回避/注意的边界

| # | 边界 | 说明 |
|---|---|---|
| **1** | **AGPL-3.0 商用边界** | 自部署**自用**没问题；**若改造成对外 SaaS 提供，必须按 AGPL 开源修改版**（网络服务条款）。但它**生成的视频内容不受影响**。辉哥的一键视频网站若要内置它，这条必须先定 |
| **2** | **它的「九宫格分镜」与官方 H3 SOP 冲突** | ClipForge 用 3×3 一图多镜 → 自动裁成各分镜关键帧；而 **H3 官方 SOP 明确要求 3 张独立锚定照、禁用网格/拼贴**（我们闸门③第 6 条）。⇒ **只取它的文案层，不要把它的九宫格方案搬给 H3**（它的九宫格是给 Seedance 这条线用的） |
| **3** | **它不是替代品，是分工** | 它自己也能出片（免费素材 + Edge TTS + FFmpeg 合成），但那是**素材拼接/图文混剪**路线，画质上限低于 H3 真实生成。⇒ **文案脚本给它，画面生成留给我们** |

### C.4 集成架构（文字层 ↔ 画面层）

```
┌─── ClipForge（文字层 · 新增）────────────┐
│ ① clipforge_ingest_product  贴链接 → 产品事实  │
│ ② clipforge_generate_script 生成口播脚本+分镜节拍 │
│ ③ script-judges 四判官审稿 → 等长重写          │
│ ④ 输出：分镜表 JSON（镜号/台词/时长/画面描述）    │
└──────────────────┬───────────────────────┘
                   │ 接口物：分镜表 JSON
                   ▼
┌─── H3 流水线（画面层 · 已有）──────────────┐
│ ⑤ 转 11 列节拍分镜表 + ★闸门② 动作审查        │
│ ⑥ 三张独立锚定照                          │
│ ⑦ H3 生成（含抽卡，N 由 p 决定）             │
│ ⑧ 判官团 L0/L1/L2（**出 GPU**）            │
│ ⑨ 拼接 + 卡点 + 后期合规                   │
└──────────────────────────────────────────┘
```

**为什么这样切最合理**：
- 卸掉我们流水线的**文案负担**（不必自己设计文案 prompt 与钩子库）
- 拿到它成熟的**钩子库 / 爆款复刻 / 合规词扫描 / 商品链接入库**
- 它的四判官是「**文案判官**」，我们的判官团是「**画面判官**」—— 互补**不重叠**，不存在判两遍的问题
- 两边的经验已经收敛（动作禁写四类 ≈ 我们的动作白名单；只取规格不比对像素 ≈ 我们的判官基准结论），说明**不是拼凑，是同源**

**部署注意**：
1. 它是 Next.js 应用，要跑 Node 服务 → **放本地 Mac 或独立机器，不要放 3090 服务器**（否则与 Part B S2 的隔离原则冲突）
2. 它需要 **OpenAI 兼容 LLM Key** —— 可复用辉哥本地 qwen3:14b 或 API
3. 安装路径二选一：`npx skills add xixihhhh/clipforge`（只要 Skill）或 Docker 自部署（要完整能力）

---

## Part D · 三条硬约束的落实清单（直接对应辉哥的要求）

| 辉哥要求 | 落实机制 | 落在哪 |
|---|---|---|
| **① 视频生成质量有保证** | 三闸门（预防结构性失败）+ 判官团（筛随机型）+ **标定集**（保证判官判得准）+ **判官版本回归**（防变严） | Phase 2 / 3.0 / 3 / 5 / S8 |
| **② 生成速度快** | 降 S（片内多镜）+ T0 官方纯净档（零安装）+ **每层过"净耗时"关**（不只看单次速度）+ 环境冻结（不返工） | Phase 0b / 1.5 / S1 / S5 |
| **③ 判官不影响质量与速度** | 判官**三级全部出 GPU**（L1 本地 Mac 或限核 CPU、L2 远端 API）+ **异步解耦**（延迟被隐藏）+ 早停（只判幸存者）+ **默认不抽卡**（避免隐藏成本） | Part B S2 / S3 / S9 |

---

## Part E · 诚实边界

- ✅ **实测**：内核 0.36.0 / 社区节点已清 / 系统盘 41% / GPU 空闲 23.2 GB / 数据盘 144 G / tts-gateway 在跑 / 今日 5 条 mp4 产出 / 四件权重 sha256 = 官方值（REPORT17）
- ❓ **推算未测**：3090 单段绝对耗时（9.4–11.4 分钟）、判官耗时（1–2 分钟/段）、提速三层倍数（T0 1.5× / T1 1.7× / T2 3.5×）、净耗时公式里的 p 变化
- ⚠️ **纯未测**：p（通过率）、D（失败稳定性）、J（判官准确率）、每一层加速的**真实**增益与画质代价
- ⚠️ **本报告的判断依赖一个前提**：H3 生成与判官**不能共享同一张 GPU**。若未来必须共享（例如只有一台机器），则需改成"分时调度"（生成空闲时跑判官），此时"判官不影响速度"**不再成立**，必须重新评估
- ⚠️ **ClipForge 的 828★ 与活跃度是实测**，但它"文案质量是否真的优于我们自研 prompt"**未测**——需拿 3–5 个真实商品做盲测对比才知
