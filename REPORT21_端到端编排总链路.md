# REPORT21 · 端到端编排总链路

> 日期：2026-09-17
> 触发：辉哥给出产品化场景 ——「用户上传产品图/模特图/产品文字信息（可选）→ 系统自动生成完整带货视频」，并要求过程中用**灵炫 TT Image2** 生成图片，同时保证**视频质量 / 生成速度 / 判官团准确性**。
> 性质：**运行期编排规范**（与《部署指南》互补：指南管「怎么搭」，本文管「怎么跑」）。
> 上游依据：REPORT.md（TT Image2 实测）/ REPORT13（良率）/ REPORT15（判官配置）/ REPORT16（链路审计）/ REPORT18（提速）/ REPORT19（顺序审计）/ REPORT20（ClipForge）/ `shotlist_schema.json`
> 原则：先画边界再动手；**实测 ✅ / 推算 ❓ / 未测 ⚠️ 分开标注**；每一步都有回退出口。
>
> 🚨 **模型白名单（辉哥拍板 · 最高优先级硬规范，详见 §11）**：
> **生图只准 TT Image2** ｜ **视频禁用一切云端模型** ｜ **文字优先 GPT5.5 / GEM 3.7** ｜ **没有授权的模型坚决不能用**

---

## 0. 一句话总纲

> **链路不是「一条流水线」，是「四条并行通道 + 四道闸门」。**
> 视频通道（3090）是唯一瓶颈；**图片通道（灵炫云）与判官通道（本地 CPU / 远端 VLM）必须全部落在瓶颈之外**，这样新增图片环节的**净时延增量 ≈ 0**。

```
                        ┌──────────────────── 四条并行通道 ────────────────────┐
                        │  文字通道(Mac/ClipForge) · 图片通道(灵炫云)           │
                        │  视频通道(3090/H3)      · 判官通道(Mac CPU + 远端VLM) │
                        └──────────────────────────────────────────────────────┘

【输入】产品图 P[] ＋ 模特图 M[]（可选）＋ 文字信息 T（可选）
   │
   ├─ S0 入库与分层 ──────────── ★闸门① 保真分层（决定 TT Image2 能用到什么程度）
   │      └─ 产出 job.json（fidelity_class = 像素级 / 形态级）
   │
   ├─ S1 文字层（ClipForge·Mac·并行）  中文脚本 + script-judges 审稿
   ├─ S2 适配转换（纯函数）           → 11 列节拍表 + 文案三分流
   ├─ S3 ──────────────────────── ★闸门② 动作白名单 + 四类禁写 + 因果链否决
   │
   ├─ S4 图像层（灵炫 TT Image2·云端·并行）★ 本次新增
   │      S4a 世界观母版 → S4b 锚定照 ×3（独立）→ S4c 镜像帧（同镜首尾同源）
   │      → S4d 中间锚点（可选）→ S4e 模特补全（仅无模特图时）
   │
   ├─ S5 ──────────────────────── ★闸门P 图层校验（本次新增·五+一项）
   │
   ├─ S6 H3 生成（3090·串行·唯一瓶颈）── ★闸门③ 派发前 10 项硬校验
   │
   ├─ S7 判官团（L0/L1 Mac·L2 远端 VLM·异步）
   │      ├─ 随机型 → ★闸门④ 可重抽性 → 换新 seed（N≤3）
   │      └─ 结构型 → 禁止重抽 → 强制走三出路（规避/转嫁/拆解）
   │
   ├─ S8 后期合成（Mac·CPU）      拼接 + 中文 TTS + 图文叠加 + 合规
   └─ S9 交付归档                  成片 + runs.jsonl + 种子资产库
```

**核心公式（沿用并扩展）**：
- 整片良率 `= [1 − (1−p)^N]^S`，S = 生成次数（指数惩罚）
- **新增图层项**：`有效良率 = [1 − (1−p)^N]^S × q`，`q` = 图层闸门通过率
  ⇒ 🔴 **`q` 必须 ≈ 1**，因为图层不过关 = 白烧 GPU（图片便宜、GPU 贵）。这是 Gate-P 存在的全部理由。

---

## 1. 输入契约（用户上传什么）

| 字段 | 必需 | 形态 | 系统怎么处理 |
|---|---|---|---|
| `product_images[]` | ✅ 必需 | 1–N 张（推荐 3–5：正面/侧面/细节/包装） | 入库存 md5；**只作保真基准与派生源，绝不直接喂 H3**（闸门③第 7 条） |
| `model_images[]` | ⬜ 可选 | 0–N 张（推荐 1–3） | 有 → 作**身份锚**锁人脸；无 → 由 TT Image2 文生图补（S4e） |
| `product_text` | ⬜ 可选 | 自由文本：卖点/价格/类目/品牌调性/禁用词 | 有 → 直接喂 ClipForge；无 → ClipForge 从产品图反推卖点（需人工可复核） |
| `params` | ⬜ 可选 | 时长 / 比例 / 风格 / 文案方式 / 目标平台 | 缺省：30s · 9:16 · 白底科技风 · 中文口播（详见 §1.2） |

### 1.1 素材要求（校验前置，不合格早退回，别浪费后面的钱）

| 项 | 门槛 | 不达标怎么办 |
|---|---|---|
| 产品图最小边 | ≥ 1024 px | 提示重传；或用 TT Image2 放大（⚠️ 放大不改保真等级，像素级产品禁止放大后当保真源） |
| 产品图背景 | 任意（白底/场景均可） | 白底 → TT Image2 负责"入景"；场景图 → 可直接进 S4a 母版 |
| 模特图背景 | 任意 | 同上 |
| 模特图人脸占比 | 建议 ≥ 8% 画幅 | 过小 → 人脸嵌入不可靠，P-02 判 N/A 并转人工 |

> ⚠️ **一句硬话**：用户上传的电商白底产品图，**不能直接当锚定照**（背景/构图/光影/比例全不对），必须经 S4 整形 —— 这正是 TT Image2 在链路里的第一个刚需位置。

### 1.2 缺省参数与时长网格（不可随意填秒数）

- 时长必须吸附 **17k+5 帧 @24fps**（官方公式 `max(5,round(a*24)) + (5 − …%17)%17`）
- 分辨率：**0.98MP = 1344×768**（禁用 1.0MP）
- **宽高比由 `aspect` 唯一决定，画布 = aspect + MP 经 32 对齐**（✅ 实测与 ComfyUI `ResolutionSelector` 逐位一致）：
  **9:16 → 768×1344**（带货默认）｜16:9 → 1344×768｜1:1 → 1024×1024｜3:4 → 864×1184｜2:3 → 832×1248｜4:3 → 1184×864｜3:2 → 1248×832｜21:9 → 1536×672
  ⚠️ **图片层必须按同尺寸出图** —— 否则首帧被**静默拉伸**（见 §3.5）
- 段长：**2–4.5s**（实测：5.17s 段里动作只占 2–3.5s，后段空转 ⇒ 压到 2–3s 动作密度立刻上去）
- 一条片优先写**片内多镜**（`[Shot 1]…[Shot 2] At 00:03.500`），把 S 压到 1–2

---

## 2. 九步链路 · 逐节点规格

> 每步给：**载体（跑在哪）· 输入 · 输出 · 闸门 · 耗时 · 失败出口**。照此实现即为编排器。

### S0 入库与分层 · ★闸门①

| 项 | 规格 |
|---|---|
| 载体 | Mac CPU（纯算术 + 目录操作） |
| 输入 | §1 的输入契约 |
| 处理 | ① 素材登记（md5/尺寸/数量）② 闸门① 保真分层 ③ 产品事实摘要（卖点/类目/规格） |
| 输出 | `job.json`（含 `fidelity_class`、素材清单、缺省参数） |
| 耗时 | 秒级 |
| 失败出口 | 素材不达标 → 早退回（§1.1 表） |

**闸门① 保真分层判据（决定整条链路形态，最贵的判断）**：

```
产品上有 logo / 中文标签 / 价格 / 复杂包装印刷 ？
  ├─ 有 → 【像素级】TT Image2 只准画环境/模特/空场；产品本体走真实像素合成
  └─ 无 → 【形态级】TT Image2 可重绘产品（以产品图作参考）→ 整图可直接进 H3
```

> 🔴 **这是引入 TT Image2 后第一处必须写死的边界**：TT Image2 是**不确定的生成模型**，它会把 logo 画歪、把中文写成乱码。像素级产品一旦交给它重绘，保真就再也回不来了（后面 H3 会忠实放大这个错误）。

### S1 文字层（ClipForge）· 与 GPU 链**并行**、互不阻塞

| 项 | 规格 |
|---|---|
| 载体 | **Mac CPU**（🚨 绝不装 3090，绝不指向 3090 上的 qwen3:14b） |
| LLM | 🚨 **优先 GPT5.5 / GEM 3.7**（远端 API）；**未授权模型一律不用**（§11） |
| 输入 | `job.json` + `product_text` |
| 处理 | `clipforge_ingest_product` → `clipforge_generate_script` → `script-judges` 审稿（节奏/口语/创意/结构/画面） |
| 输出 | `side_a_clipforge_output`（`shotlist_schema.json` 结构：product / hook_type / cta / shots[]） |
| 耗时 | 分钟级（取决于 LLM API 往返） |
| 失败出口 | 审稿不过 → 重生成（最多 K=3 轮）→ 仍不过 → 回退到「人工改写」或「模板脚本库」 |

### S2 适配转换（adapter · 无状态纯函数）

| 项 | 规格 |
|---|---|
| 载体 | Mac CPU |
| 输入 | `side_a_clipforge_output` |
| 处理 | ① 时长 → `00:MM.mmm` → **吸附 17k+5 帧网格** ② 相邻只改景别的镜头**合并为运镜** ③ 运镜 → 动作白名单（不在白名单一律降级 + `warning`）④ **文案三分流** |
| 输出 | 11 列节拍表（时间段/镜头/目的/视觉主导/运镜/款式/文案/颜色/文字效果/转场/节奏） |
| 失败出口 | 单行转换失败 → 该行退回文字层；不整表失败 |

**文案三分流（成败点，绝不可省）**：

| 分流 | 去向 | 谁来做 |
|---|---|---|
| 画面内英文（3–5 词） | 逐字写进 H3 prompt（闸门③第 1 条校验） | H3 |
| 中文旁白 | **后期 TTS 配音** | 后期（S8） |
| 后期图文（价格/CTA/合规标识） | **后期叠加，绝不交给模型画** | 后期（S8） |

### S3 闸门② · 动作可行性（全场最硬的一道）

| 项 | 规格 |
|---|---|
| 载体 | Mac CPU（自动）+ 可人工复核（`gate2_action_whitelist.html`） |
| 判据 | ① 在白名单内？（举起/并排/推近/旋转/开合/滑入/光影流动/静置/特写平移）② **有没有可验证的物理结果**？（有 → 否决）③ **命中四类禁写**？（双主体精确交互 / 内部心理 / 否定式动作 / 一拍三步动作链 → 否决）④ **每镜动作数 = 1** |
| 输出 | 定稿分镜表（`status = gated`） |
| 失败出口 | 任一行否决 → **整行退回 S1/S2 重写**（带否决原因），最多 K=3 轮 → 仍不过 → 降级「B 类不可验证化」（改拍静置 + 光影 + 微笑，把可验证物理结果从画面里拿掉） |

> ⚠️ 边界（必须心里有数）：闸门② 挡不住的东西，**判官团也救不了**（状态不守恒 / 接触物理 / 多步因果链换 seed 零效果）。

### S4 图像层（灵炫 TT Image2）· ★本次新增 · 详见 §3

| 项 | 规格 |
|---|---|
| 载体 | **灵炫云（`api.lk888.ai`）**，异步任务 + 轮询 |
| 输入 | 产品图 / 模特图 / 节拍表 / 世界观母版 |
| 输出 | ① 锚定照 ×3（独立）② 每镜首帧 + 尾帧 ③ （可选）中间锚点 ④（无模特图时）虚拟模特 |
| 耗时 | **文生图 37–46s / 次；图片编辑 54–66s / 次**（✅ 实测 2026-09-11）；成本 ¥0.0444/次 |
| 失败出口 | 单张失败 → 重试 1 次；整批失败（网关异常）→ **熔断降级**：形态级可"以原图作弱锚定照"继续，像素级转混合合成 |

### S5 闸门P · 图层校验 · ★本次新增 · 详见 §4

| 项 | 规格 |
|---|---|
| 载体 | Mac CPU（小图，秒级；复用 `align.py` / `frames.py` / `refcheck.py` 思路） |
| 判据 | P-01 产品保真 / P-02 模特一致 / P-03 规格合规 / P-04 异文字水印 / P-05 首尾同源 / P-06 状态单调 |
| 耗时 | < 3 s / 张（⚠️ 未实测，按同尺寸 CPU 检测器推算） |
| 失败出口 | 见 §4.3 回退表（**这是"图片不过关不许进 GPU"的守门人**） |

### S6 H3 生成（3090 · 唯一瓶颈 · 串行）

| 项 | 规格 |
|---|---|
| 载体 | 3090 · ComfyUI 独立实例（:6011）· **官方原生链路** `test_fl2v_official.py` |
| 前置 | ★闸门③ 派发前 10 项硬校验（含"锚定照 3 张独立无网格"、"没把原始商品图传给视频模型"） |
| 参数 | 0.98MP / 8 步 / 段长 2–4.5s / 中间锚点 ≤1 个 / 官方 8 步 Turbo LoRA |
| 耗时 | 15s 段 ≈ **9.4–11.4 min**（❓ 3090 换算推算，未实测） |
| 失败出口 | L0 直接拒收 → 不消耗判官；生成报错 → 重试 1 次 → 仍失败 → 报环境故障 |

### S7 判官团（L0/L1 Mac · L2 远端 VLM · **异步**）

| 项 | 规格 |
|---|---|
| 载体 | 🚨 **三级全部出 GPU**（§2.0 红线）：L0 任意 CPU / L1 Mac CPU（或 3090 机限核）/ L2 **远端 VLM API** |
| 输入 | 段视频 + 判据（R 段来自 prompt 原文；W/P 段通用）+ 锚定照 |
| 输出 | **三项**：① 达标？② 失败类型（随机/结构）③ 可重抽？（★闸门④） |
| 耗时 | ≈ **1–2 min/段**（L0 <1s + L1 5–20s + L2 并发 ≈16s） |
| 失败出口 | 见 §7 回退总表 |

### S8 后期合成（Mac · CPU）

| 项 | 规格 |
|---|---|
| 处理 | ① 按音乐卡点拼接（段序 + 转场）② 中文旁白 TTS 配音 + 音画对齐 ③ 后期图文叠加（价格 / CTA / **AI 生成标识**）④ 合规清单逐项 |
| 输出 | 成片 mp4 |
| 失败出口 | 音画不对齐 → 后置贴轨重排；合规缺失 → 不交付 |

### S9 交付归档

| 项 | 规格 |
|---|---|
| 处理 | 成片 + `runs.jsonl`（prompt/seed/参数/素材 md5/耗时/成本/判官结论）+ 通过的 `(prompt, seed)` 写入**种子资产库** |
| 价值 | 把"一次抽奖"变成**可复现资产** |

---

## 3. ⭐ TT Image2（灵炫）在链路中的定位与使用边界

### 3.1 它的四个位置（且**只有**这四个）

| # | 位置 | 干什么 | 为什么必须是它 |
|---|---|---|---|
| **①** | **S4b 锚定照生产** | 用户产品图/模特图 + 母版 → **3 张独立锚定照**（主视觉 / 材质结构 / 结尾构图） | H3 官方 SOP 要求锚定照，而用户上传的白底图不合格 ⇒ 必须有整形环节 |
| **②** | **S4c 镜像帧生产** | 同镜的**首帧 + 尾帧**，且必须**同一底图同源派生** | REPORT.md 实测：同源派生的首尾帧喂 FL2VA，**落点精度 MAE 3.27→6.95**（首帧近像素级一致）✅ |
| **③** | **S4e 模特补全** | 无模特图时文生图出虚拟模特；有模特图时**锁人脸**跨图一致 | 无模特图是产品化的常态（不能要求每个客户都出镜） |
| **④** | **S4d 中间锚点**（可选） | 按**出错频率**加（不按动作数量加），钉动作中间态 | 一段最多 1 个；`MiniMaxH3AddGuide` 按 `frame_idx` 注入 |

### 3.2 三条**使用禁区**（破一条，质量立刻不保）

| # | 禁区 | 依据 |
|---|---|---|
| **禁1** | **像素级产品不得由 TT Image2 重绘** | 它是生成模型，会把 logo / 中文标签画歪 ⇒ 像素级只准走"真实像素合成" |
| **禁2** | **不做"状态跃迁"，只做"状态切片"** | REPORT.md 实测：宫格路线**擅自把瓶子放回桌面**（状态跃迁不单调）⇒ 一镜只承载**一次**状态变化，且 TT Image2 只负责画**静态状态**，动作过程交给 H3 |
| **禁3** | **宫格整图绝不进 H3** | 违反官方 SOP（要求 3 张独立照、禁网格/拼贴）⇒ 宫格只作**内部母版**，必须裁/派生为独立图才可用 |
| **禁4** | 🚨 **灵炫平台上只准调 `tt-image-2`** | 辉哥拍板硬规范（§11）：灵炫的**视频模型坚决不能用**，其它未授权生图模型同样不得启用 |

### 3.3 两个被实测验证的**有效手法**（直接照抄）

| 手法 | 实测数据 | 用法 |
|---|---|---|
| **同源编辑链**（底图 → 逐张 edit 派生） | 产品尺度漂移 ≤ +2.0% / 位置漂移 ≤ 3.5 px ✅ | 凡"必须保持一致"的一组图（3 锚、同镜首尾），**必须**从同一底图派生 |
| **宫格定世界观** | 纯背景区 MAE **1.11**（编辑链 3.64）—— 宫格背景一致性更优 ✅ | 用**一张**宫格/主视觉定下人物·产品·光照·色调；**只作母版**，再逐张派生 |

> ⭐ **最佳组合（REPORT.md 结论）**：**母版（世界观）用宫格思维做一次定调 → 后续全部走同源编辑链逐张派生**。既拿到跨镜统一，又满足官方"独立照"要求。

### 3.4 并发与串行规则（决定图片通道够不够快）

| 关系 | 规则 |
|---|---|
| 母版 → 3 张锚定照 | **可 3 路并发**（都从母版 edit 派生）⇒ 2 层 ≈ 1.5–2.2 min |
| 同镜 首帧 → 尾帧 | ⚠️ **建议串行**（尾帧从首帧派生，保证同源） |
| 不同镜的图 | **完全并行**（互不依赖） |
| 与 H3 生成 | **完全并行**（云端 vs 3090，零资源竞争） |

### 3.5 ⭐ 宽高比规范（首尾帧必须与画布同尺寸 —— 静默毁片防线）

**实测依据（服务器源码 `comfy_extras/nodes_minimax_h3.py`，非推断）**：

| 帧位 | H3 的实际处理 | 代码注释原文 | 比例不符的后果 |
|---|---|---|---|
| **首帧** | plain stretch 到画布 | `# geometry anchor: plain stretch to canvas` | 🔴 **整体拉伸变形**（产品被压扁/拉长） |
| **尾帧** | 保比例中心裁剪 | `# follower: aspect-preserving cover-crop` | 🟠 **边缘构图被裁掉** |

🚨 **两者都不会报错** —— 这是本链路最隐蔽的坑。

**规则（照做）**：

| # | 规则 |
|---|---|
| 1 | **画布由 `aspect` 唯一决定**：`9:16 + 0.98MP → 768×1344`（32 对齐后的真实值，**不是**理论 756×1344） |
| 2 | **TT Image2 的 `size` 直接传画布像素串**（`"768x1344"`）—— ✅ 实测**逐像素命中**（请求 768x1344 → 返回 768x1344，零偏差） |
| 3 | **禁止**图片比例与画布不同 —— 哪怕「看起来差不多」（如理论 9:16 的 1080×1920 仍有 1.6% 形变） |
| 4 | 切横版/方形时**图片侧同步改**：`orchestrator.py` 的 `img_size` 支持 `auto`（由 aspect + MP 自动推导），**不要手填两份** |

**两道执行件（均已落地并实测）**：

| 执行件 | 机制 | 实测结果 |
|---|---|---|
| `test_fl2v_official.py` → `frame_dev()` | 提交前读首尾帧真实像素；偏差 > 2% → **拒绝提交**（`--allow-ratio-mismatch` 可强跳） | ✅ 6/6 场景：1:1 拦下（偏差 75%）、横版拦下（206%）、竖版放行、路径不存在时不误报 |
| `orchestrator.py` → `_check_segments_ratio()` | S5 Gate-P 的 **P-07**，逐段校验；不过 → **熔断** | ✅ 5/5 场景：全合规放行、单/多违规逐条列出、不存在路径跳过 |

> ⭐ **为什么值得单列一节**：P-01~P-06 都要模型 / 阈值 / 人工，**只有 P-07 是纯算术**（读像素比比例，毫秒级、零成本、零标定），却拦的是**最贵**的一类坑 —— H3 全程不报错，等成片交到客户手里才发现产品变形。

---

## 4. 四道闸门（新增 Gate-P 图层闸门）

### 4.1 闸门全景（引入 TT Image2 后必须补上第 P 道）

| 闸门 | 位置 | 挡什么 | 不过的代价 |
|---|---|---|---|
| **① 保真分层** | S0 | 像素级产品被误交给生成模型 | 白烧 GPU + 产品永久失真 |
| **② 动作可行性** | S3 | 结构型失败进门（重抽救不了） | 白烧 N 倍算力拿 N 份相同废片 |
| **P 图层校验** ⭐新增 | S5 | **坏锚点 / 坏首帧**进 H3 | 🔴 **错误被 H3 忠实放大**（GIGO） |
| **③ 派发硬校验** | S6 前 | 格式 / 接线 / 比例类硬错误 | 直接失败，浪费一次 GPU 排队 |

> 🔴 **为什么必须新增 Gate-P**：图片单张成本 ≈ ¥0.0444 + 1 分钟；视频单段成本 ≈ **10 分钟 GPU**。**用 1 分钟挡住 10 分钟**，这是全链路性价比最高的防御点。

### 4.2 Gate-P 七项判据

| ID | 判据 | 方法 | 分档阈值 |
|---|---|---|---|
| **P-01** | 产品保真 | 产品 ROI 直方图嵌入相似度（`refcheck.py` 思路） | **按 `fidelity_class` 分档**：形态级看形状/色/结构（宽松）；像素级**必须是原图像素合成**（不接受重绘） |
| **P-02** | 模特一致 | 跨图人脸嵌入余弦距离 | `< tau_face_img`（待标定）；同镜组内必须同人 |
| **P-03** | 规格合规 | 比例匹配 / 主体在安全区 / **无网格·无拼贴** / 分辨率达标 / 单主体 | 硬性，不过即拒 |
| **P-04** | 异文字与水印 | 画面内文字必须 ⊆ 白名单（prompt 声明的英文）；OCR 比对 | 出现**未声明**的 logo 字/水印/乱码 → 拒 |
| **P-05** | 首尾同源 | 同镜首尾帧**纯背景区 MAE** | `< tau_src`（实测参考带 1.11–3.64，须按链路类型定档） |
| **P-06** | 状态单调 | 尾帧状态必须是首帧状态的**推进**（人工/半自动确认） | 回转即拒（REPORT.md 的"瓶子被放回桌面"就是此类） |
| **P-07** ⭐ | **画布比例一致** | 逐段读首尾帧实际像素，与画布比比例（**纯算术，100% 可自动化**） | 偏差 **≤ 2%**；画布 = aspect+MP 经 32 对齐（9:16@0.98 → 768×1344） |

> **P-06 是"动作逻辑性"的前哨**：它把结构型失败**挡在花 GPU 之前**。这条比视频端任何判官都便宜。
>
> **P-07 是唯一的"零成本"判据**：无模型、无阈值、无人工，毫秒级 —— 但拦的是**静默毁片**（详见 §3.5）。**它必须真检，不能只勾选。**

### 4.3 Gate-P 回退动作表

| 不过项 | 处置 | 是否重出 |
|---|---|---|
| P-01（形态级） | 强化"保持不变"约束 + 提高参考图权重 → 重出 | ✅ 最多 2 轮 |
| P-01（像素级） | 🚨 **立即转混合合成路线**（产品真实像素后期合成） | ❌ 不重出 |
| P-02 | 把人脸图列为强参考 → 重出；仍不过 → 换回真实模特图 | ✅ |
| P-03 | 修 size / 修提示词 → 重出（这是格式错，必过） | ✅ |
| P-04 | 加负向约束重出；仍不过 → 后期遮挡/裁切 | ✅ 1 轮 |
| P-05 | 🚨 **流程修正**（必须改为同一底图派生），不是重抽 | ❌ 改流程 |
| P-06 | 🚨 **回闸门② 改动作设计**（结构型） | ❌ 改设计 |
| **P-07** | 🔁 **图片层按画布同尺寸重出**（TT Image2 的 size 传画布像素串） | ✅ 重出即修 |

> **熔断**：Gate-P 整体重出上限 **2 轮**；到顶必须报明确失败，**禁止静默续跑**（一键网站场景下 = 无上限账单）。

---

## 5. 速度保证 · 三条并发通道与关键路径

### 5.1 通道分工与反压检查（"不影响速度"的全部依据）

| 通道 | 载体 | 与视频通道关系 | 单件耗时 | 反压结论 |
|---|---|---|---|---|
| 文字通道 | Mac CPU（ClipForge） | 完全解耦（可提前跑完） | 分钟级 | ✅ 不阻塞 |
| **图片通道** | 灵炫云（TT Image2） | **完全并行** | 37–66 s / 张 | ✅ **远快于 1 段生成（9.4–11.4 min）** |
| **视频通道** | 3090（H3） | — 瓶颈本身 | 9.4–11.4 min / 段 | ⬛ 瓶颈 |
| 判官通道 | Mac CPU + 远端 VLM | **异步并行** | 1–2 min / 段 | ✅ 远快于 1 段生成 |

> ⭐ **结论（本次编排的核心论证）**：只要 **TT Image2 与判官都不落在 3090 上**，新增图片环节的**净时延增量 ≈ 冷启动那一次**，其余全部被视频生成时间隐藏。

### 5.2 冷启动关键路径（唯一真实增量）

用户点"开始"到第一段开跑，必须等**最小可用图集**（3 张锚定照 + 第 1 镜首尾帧）：

```
第1层：世界观母版 ×1                    ≈ 37–46 s
第2层：锚定照 ×3（3 路并发，自主视觉锚派生） ≈ 54–66 s
第3层：第 1 镜 首帧 → 尾帧（串行同源）     ≈ 54–66 s ×2 = 108–132 s
──────────────────────────────────────
冷启动 ≈ 3.3–4.1 min
```

**优化（推荐默认开启）**：第 1 镜首帧只依赖**主视觉锚** ⇒ 与另外两张锚定照**并行**；尾帧紧接其后。
⇒ 实际关键路径 = 母版 → 主视觉锚 → 首帧 → 尾帧 ≈ **3 层 ≈ 3.3–4.1 min**（其余锚定照在生成期间补齐）。

### 5.3 30s 片（2 段 × 15s）端到端时序（推算 ❓）

| 时刻 | 图片通道 | 视频通道 | 判官通道 |
|---|---|---|---|
| 0–2 min | 母版 + 锚定 ×3 | — | — |
| 2–4 min | 镜1 首/尾帧 | 闸门③ 校验 | — |
| **4 min** | 镜2 图（后台出） | 🚀 **段1 开始** | — |
| 4–15 min | 镜2 首/尾完成（≈1–2 min，余下空闲） | 段1 生成（9.4–11.4 min） | — |
| 15 min | — | 段2 开始 | 段1 判官（1–2 min）→ 通过即**冻结** |
| 26 min | — | 段2 完成 | 段2 判官 |
| 27 min | — | — | 段2 判定 → 通过 |
| 27–32 min | 后期合成（拼接 / TTS / 图文 / 合规） | — | — |
| **≈32 min** | **交付** | | |

**对比 v2.6 口径**：30s 片 GPU 23–28 min + 后期 5–10 min ≈ 28–38 min
⇒ **引入 TT Image2 后净增量 ≈ 1–3 min**（即冷启动差异，且可与闸门③/后期重叠）。

### 5.4 速度红线（照做）

1. 🚨 **TT Image2 与判官绝不落 3090**（云端 API + Mac CPU）—— 破这条，反压立刻反转
2. 🚨 **图片通道不得成为瓶颈**：生成期间图片通道必须保持"空闲"（说明它跑在生成前面）
3. **超时熔断**：TT Image2 单张 > 120 s（或整批转圈 > 5 min）→ 触发降级（形态级用原图弱锚定；像素级转混合合成）
4. **不重复出图**：Gate-P 通过后**立即冻结该图**（与"段冻结"同规矩），禁止整批重出
5. **图片不入抽卡循环**：图片的重出上限是 2 轮，且**与视频抽卡分开记账**（否则成本失控）

---

## 6. 判官团准确性保证（含新增的图层判官）

### 6.1 分工：两道防线，位置不同

| 防线 | 判什么 | 位置 | 不过的代价 |
|---|---|---|---|
| **图层判官（Gate-P）** | 静态图：产品保真 / 模特一致 / 规格 / 异文字 / 同源 / 状态单调 | **S5（生成之前）** | 省下 10 min GPU ✅ |
| **视频判官（L0/L1/L2）** | 动态视频：指令遵循 / 世界一致 / 感知质量 | **S7（生成之后，异步）** | 走重抽或三出路 |

> ⭐ **准确性增益点**：静态图**确定性高、可判性强** ⇒ 图层判项的假阴/假阳都低于视频判项。**把能前移的判断全部前移**，是提升整体判官准确率最有效的做法。

### 6.2 七条评判形态铁律（沿用 REPORT15，新增部分同样适用）

原子化 · 一题一次调用 · 先证据后判定 · 三值输出（yes/no/**na**）· 禁止打分与自由叙述 · 关键项双采样 · 版本锁定 + 标定集独立

**Gate-P 的关键项双采样**：`P-01`（产品保真）、`P-02`（模特一致）—— 不一致直接升级人工。

### 6.3 防"判官悄悄变严"（版本回归，强制）

| 触发 | 动作 |
|---|---|
| 换 VLM 版本 / 增删判项 / 改阈值 / **换 TT Image2 模型版本或 API** | 🚨 **必须用标定集跑回归**，测**假阳率与假阴率**，两个数**都不得恶化** |

> ⚠️ TT Image2 是**第三方云端模型**，其版本可能被供应商静默更新 ⇒ **每次批量生产前先跑一条金标准样本比对**（同 prompt 同参数，视觉检查是否与上次一致）。这是引入云端生成模型必须付的"监控税"。

### 6.4 覆盖率监控

- 单条 **na 比例 > 40%** → 进人工复核清单
- Gate-P 的 `P-02`/`P-06` 在缺素材时判 **na**（不判否），但计入覆盖率
- **全 na 的 R/W 项不计入否决，但计入覆盖率**（沿用 `judge_config.yaml`）

---

## 7. 失败回退总表（运行期唯一查表入口）

| 失败位置 | 类型 | 处置 | 上限 | 禁止 |
|---|---|---|---|---|
| S0 素材 | 输入 | 早退回重传 | — | 不得带病进入 |
| S1 文案 | 内容 | 重生成 | K=3 | 不得跳过审稿 |
| S3 闸门② | **结构** | 退回重写 / 降级「B 类不可验证化」 | K=3 | 🚫 **不得进入生成后再指望重抽** |
| S4 出图 | 技术 | 重试 | 1 次 | — |
| S4 出图 | 网关异常 | **熔断降级**（形态级弱锚定 / 像素级转合成） | — | 不得无限重试 |
| **S5 Gate-P** | 见 §4.3 | 重出 / 改流程 / 改设计 / 转合成 | 2 轮 | 🚫 **不得带病进 GPU** |
| S6 生成 | 随机型 | **换新 seed** 重抽 | **N ≤ 3** | 🚫 **禁止同 seed 重跑**（逐位相同 = 零收益） |
| S6 生成 | **结构型** | 规避 / 转嫁（合成·实拍）/ 拆解（拆镜+剪辑） | — | 🚫 **禁止重抽**（换 seed 零效果） |
| S6 生成 | 到顶失败 | 报明确失败 | — | 🚫 **禁止静默续跑** |
| S7 判官 | 双采样不一致 | 升级人工 | — | 不得自动放行 |
| S8 后期 | 音画不对齐 | 后置贴轨重排 | — | — |
| S8 后期 | 合规缺失 | 不交付 | — | 🚫 不得漏 AI 标识 |

> **单点铁律（沿用）**：**「选出最好的」≠「达标」**。N 份全不合格必须走失败出口，不许相对择优。

---

## 8. 成本与时延核算（30s 片 · 2 段 · 2 镜例）

### 8.1 成本

| 项 | 数量 | 单价 | 小计 |
|---|---|---|---|
| TT Image2（母版 1 + 锚定 3 + 首尾帧 4 + 中间锚点 0–2） | 8–10 张 | ¥0.0444 / 次 ✅实测 | **¥0.36–0.44** |
| ClipForge 文案（LLM API） | 若干次 | ≈ ¥0.001 / 次（REPORT20） | < ¥0.05 |
| H3 生成（自有 3090） | 2 段 | 自有算力 | GPU 时间成本 |
| 判官 L2（远端 VLM） | 20 判项 × 2 段 | 远端 API | 按 API 计价 |

> ⇒ **图片环节的成本量级是"分"级，GPU 是"十分钟"级** —— 再次印证：**宁可多出几张图，也不要让坏图进 GPU。**

### 8.2 时延（推算 ❓，Phase 0b/6 实测后替换）

| 项 | 数值 |
|---|---|
| 冷启动（图片关键路径） | 3.3–4.1 min |
| 视频生成（2 段，GPU 串行） | 19–23 min |
| 判官（异步，被隐藏） | +0（净增量） |
| 后期合成 | 3–5 min |
| **端到端** | **≈ 26–32 min**（v2.6 基准 28–38 min ⇒ 增量 1–3 min） |
| 图片通道占比 | ≈ 12%（**低于视频通道，不构成瓶颈** ✅） |

---

## 9. 端到端验收清单（"链路串通了"的定义）

**契约层**
- [ ] `shotlist_schema.json` 两侧解析器互测通过（ClipForge 出稿 → adapter → 11 列）
- [ ] 文案三分流落表，画面内英文**逐字**出现在 H3 prompt 里（闸门③ 第 1 条）
- [ ] 时长全部吸附 17k+5 帧网格，无任意秒数

**图层**
- [ ] 3 张锚定照**独立**（非网格/拼贴）、**同源派生**（背景 MAE 达标）
- [ ] Gate-P 六项全绿；`P-05` 同镜首尾同源通过
- [ ] 像素级产品的图中，产品像素**全部来自用户原图**（无重绘）
- [ ] TT Image2 输出无未声明文字/水印（P-04）

**链路连通**
- [ ] `orchestrator.py --dry-run` 能跑完 S0→S7 的状态机，产出全部中间产物清单
- [ ] 三段式并发验证：生成期间图片通道与判官通道**均处于空闲/领先**状态
- [ ] 超时熔断与降级路径各触发一次（人为注入超时）

**质量与成本**
- [ ] 判官三输出齐备（达标？/类型？/可重抽？），无双采样静默放行
- [ ] `runs.jsonl` 每行含 prompt 全文 / seed / 参数 / 素材 md5 / 耗时 / 成本 / 判官结论
- [ ] 同 seed 同 prompt 重跑帧差 ≈ 1.08（复现性成立）
- [ ] 图片重出 ≤ 2 轮、视频重抽 ≤ 3 次，到顶报明确失败

**合规**
- [ ] AI 生成标识已叠加；价格/CTA 为后期图文（非模型绘制）
- [ ] 未出现未授权品牌 logo（P-04）与真人肖像风险

---

## 10. 诚实边界与待确认决策

### 10.1 诚实边界（别指望它做到的事）

| 项 | 说明 |
|---|---|
| **TT Image2 一致性不是 100%** | 实测产品尺度漂移 ≤2.0%、位置漂移 ≤3.5 px ✅，但**语义控制弱**（会擅自改变状态）⇒ 必须靠 Gate-P + 闸门② 兜 |
| **TT Image2 耗时未在并压下实测** | 37–66 s 是单张串行实测；**并发 N 路时的排队/限流行为未测** ⚠️ ⇒ 冷启动估算有不确定性 |
| **3090 全部耗时为推算** | 3090 换算系数 1.65–2.00 ❓ ⇒ 对外报时用"半小时量级"，不报精确值 |
| **判官能筛不能修** | 状态不守恒 / 接触物理 / 多步因果链 **3 类结构型**换 seed 零效果 ⇒ 防线在闸门② |
| **像素级产品上限受限** | 不拍视频的前提下，只能用"真实像素合成 + 限制动作（静置/光影/推近/旋转）"组合，动作幅度显著小于形态级 |
| **AGPL-3.0** | ClipForge 若嵌入对外 SaaS 需按 AGPL 开源修改版；TT Image2 为第三方云服务，服务条款与数据合规需确认 |

### 10.2 待辉哥拍板的三个决策点

| # | 决策 | 选项 | 金刚的建议 |
|---|---|---|---|
| **C1** | TT Image2 的角色定位 | A. 只做"素材整形"（锚定照+镜像帧），保真最好<br>B. 参与"画面创作"（场景/模特/氛围），产品化最好 | **A 为骨架、B 为可选增强**；像素级产品强制 A + 混合合成 |
| **C2** | 引擎缺省档位 | A. 全自动（无人值守，闸门全自动）<br>B. 半自动（闸门②/P 高风险项人工复核） | **先 B 后 A**：跑通 30–50 条样本再切全自动（同时喂 Phase 3.0 标定集） |
| **C3** | 是否接受"图片重出预算" | 2 轮（推荐）/ 1 轮（更省） | **2 轮**：图片便宜、GPU 贵，多出两张图的期望收益为正 |

### 10.3 与既有文档的关系

| 文档 | 关系 |
|---|---|
| 《部署指南_判官团抽卡生产流水线》v2.7 | 本文 §2 的 S6/S7 直接引用其 Phase 2（闸门）/ Phase 4（抽卡）/ §2.0（判官隔离） |
| `shotlist_schema.json` | 本文 S2/S3 的接口契约，未变更 |
| `judge_config.yaml` | 本文 §6 的判项来源；Gate-P 判项为其**新增图层延伸**（建议并入 v1.1） |
| REPORT.md（TT Image2 实测） | 本文 §3 全部实测数据的来源 |

---

## 11. 🚨 模型白名单与黑名单（辉哥拍板 2026-09-17 · 硬规范 · 不可绕过）

> **一句话**：**生图只准 TT Image2**；**视频禁用一切云端模型**；**文字优先 GPT5.5 / GEM 3.7**；**没有授权的模型坚决不能用**。

### 11.1 三类用途的白/黑名单

| 用途 | ✅ 允许 | 🚫 禁止 | 依据 |
|---|---|---|---|
| **生图** | 灵炫 **TT Image2**（`tt-image-2`） | 灵炫平台上的**其它生图模型**；任何**未授权**模型 | 辉哥拍板 |
| **视频** | 本项目**自持的 MiniMax H3 FL2VA** 本地权重 | 🚫 **灵炫平台 / 任何云端的视频模型**（seedance / kling / vidu / hailuo / runway / veo 等一律不用） | 辉哥拍板 + 决策 D2 |
| **文字** | **GPT5.5** ／ **GEM 3.7**（Gemini） | 未授权模型；3090 上的 qwen3:14b（**抢显存**，见 §2.0 红线 2） | 辉哥拍板 |

### 11.2 为什么视频必须禁用云端模型（工程理由，不是偏好）

| # | 理由 |
|---|---|
| 1 | **视频通道是整条链路的瓶颈与质量锚**。锚定保真、片内多镜、判官判项、种子资产库**全部基于 H3 的特性**建立（0.98MP / 17k+5 帧 / 8 步 Turbo / 同 seed 逐位复现）⇒ 换视频模型 = **整条链重建** |
| 2 | **云端视频模型是"黑箱出片"**：拿不到 seed 复现、拿不到中间帧、无法做同源派生与状态守恒校验 ⇒ 判官团与资产库立刻失效 |
| 3 | **授权边界**：灵炫的视频套餐不覆盖我们的商用场景；用未授权模型出片存在**合规风险** |
| 4 | **速度无收益**：我们的瓶颈是**单卡算力**，不是模型慢。换云端只会引入网络排队与不确定时延，还得付两头钱 |

> ⚠️ **特别注意**：灵炫（`api.lk888.ai`）**同时也是一个多模型网关**。它的接口能力上支持非生图模型 —— **能力存在 ≠ 允许使用**。本项目的调用清单里，**只允许出现 `tt-image-2` 这一项**。

### 11.3 三处执行机制（写进代码，不靠自觉）

| # | 机制 | 位置 |
|---|---|---|
| 1 | **启动自检** `_check_model_policy()`：检查生图脚本 MODEL 是否为 `tt-image-2`、H3 调用脚本是否指向自持 H3 权重、是否出现视频模型关键字 | `orchestrator.py`（每次运行开头执行） |
| 2 | **模型常量锁定**：`ttimg.py` 内 `MODEL = "tt-image-2"` 写死，改动须先取得授权 | `ttimg.py` 头部警示块 |
| 3 | **违反即中止**（不是警告）：白名单告警出现时**不得进入生产跑批**，必须先整改 | 编排层 |

### 11.4 "没有授权"的判定口径

| 判定 | 例子 |
|---|---|
| ✅ **已授权** | 我方账号已开通、且服务条款允许商用的 API 模型（如 TT Image2）<br>有明确**商用许可**的权重（H3 需 MiniMax 商业许可 —— 见既有商用合规铁律） |
| 🚫 **未授权** | 社区权重（无商用许可）、未购买许可的模型、来源不明的**中转站**模型（常是"降智版"） |

> 📌 **推论**：若将来要用灵炫跑**文字**模型，必须确认所调端点**确实是 GPT5.5 / GEM 3.7 且已授权** —— 不能接受"看起来像"的替代模型。ClipForge 的 LLM Key 同理，需指向明确的 GPT5.5 / GEM 3.7 端点。

---

## 附：一句话给辉哥

> **串法一句话**：产品图/模特图进来 → **文字层（ClipForge，Mac）** 出脚本 → **适配器**转 11 列节拍表 → **闸门②**挡掉画不出来的动作 → **灵炫 TT Image2（云端）** 出母版/3 张锚定照/每镜首尾帧 → **闸门P** 把坏图挡在 GPU 之外 → **3090 出视频** → **判官团异步筛**（随机型换 seed 重抽、结构型改设计）→ 后期合成交付。
> **速度不减的秘密**：图片与判官都不在 3090 上 —— 唯一增量是开头 3–4 分钟的冷启动，其余全被 10 分钟一段的生成时间藏住了。
> **模型只认四个**：生图 **TT Image2** ｜ 视频 **自持 H3**（云端视频模型一概不用）｜ 文字 **GPT5.5 / GEM 3.7** ｜ 其余未授权模型**一律不碰**。
